# 2026-08-17

> 시간 역순(최신이 위).

## 보안·암호화 전수 조사 (사용자 지시 — "송신자/수신자 외 내용 확인 불가")

**전송 축(원칙 직결) — 전부 건전 · 회귀로 상시화**

| 표면 | 판정 | 근거 |
| --- | --- | --- |
| 세션 와이어(대화·프로필·파일 메타/**내용**) | ✅ 암호문 | Noise XX 위 · 실릴레이 캡처 회귀 **4종 마커**(청크 내용 오늘 추가) |
| 발견 브로드캐스트 | ✅ 설계 평문(존재만) | 프로필 부재 = 타입 보장(DR-22) · 이름 정제(R-19) · 금칙어 스캔(08-15) |
| 그룹 스트림 | ✅ 암호문 | 같은 세션(pairwise) 경유 |
| 핸드셰이크 신원 | ✅ | Noise XX — 정적 키는 암호화 구간에서 교환 |
| 로그·Debug | ✅ | 개인키 hex 부재 전수 단언(08-15) · 알림 파일명 금지 |

**저장 축(로컬 디스크) — 원칙의 범위 밖이지만 정리**

| 표면 | 상태 | 분류 |
| --- | --- | --- |
| identity.key | 평문 · **0600 ✓**(실기 확인) | 설계 확정(D-18 §3 — 래핑 키 원료라 자기 자신 못 감쌈) |
| trust.seg(핀·이름 이력) | ✅ AEAD | M2-5a |
| `.beepq` 격리물 | **평문**(무결성 봉인만 — 실기 확인) | 🔴 ADR-0005 §4~(D-18 잔여) 영역 — **승인 대기 보고** |
| settings.cfg(이메일·전화 PII) | **평문** | 🔴 같은 영역 — M3-17 때 "부적합 후보" 그대로 · **승인 대기 보고** |
| profiles/{peer}.img 캐시 | 평문(08-14 결정 — 이름은 trust.seg) | 위와 동일 묶음 |

**즉시 조치(승인 불요)** — 와이어 회귀 4종 확장(파일 내용 청크 마커). 열린
리스크 재확인 = R-18(브로드캐스트 고정 공개키 — Q-29-2 결정 대기)·R-20·R-4.

## 서버 설계 검토 + 클라 설정 영역(Unmanaged 기본) (main)

**개발한 것(1건)** — 클라이언트 **서버 설정 영역**: CatServer + Entry 4
(`net.server.mode` 기본 **unmanaged** · address · port 47300 · `type` 기본
**auto**). 값만 영속(S-0 그대로) · 네트워킹 배선은 TODO. 실기 = settings.cfg에
`net.server.mode=unmanaged`·`type=auto` 저장 확인. **서버 자체는 개발 제외**
(사용자 확정 "명시 없으면 TODO만 · 큰 작업이 정체성 흔든다").

**설계 검토([32 §12] 신설)**:
- **홀펀칭 실현성**(사용자 우선 질문) — **가능**(STUN 랑데부+동시 열기+TURN
  폴백 = Gateway→Relay 사다리). 도달 = Full/Restricted-cone 대다수 ✅ · 양쪽
  Symmetric = 릴레이 폴백. **⚠️ 선행 관문 = UDP 세션 전송**(현행 TCP는
  홀펀칭 불안정 — 이게 가장 큰 선행 · Q-32-15). "터널링 노드" = L3 터널
  불요, 펀치된 UDP 경로에 기존 Noise+mux를 얹으면 충분.
- **Gateway** = 랑데부+펀치만(양쪽 Symmetric은 못 뚫음 → Relay 폴백 필요).
- **Content** 경계 = 오프라인 큐·팬아웃·알림 봉투까지 · **평문은 절대 못 봄**
  (와이어가 강제).
- **채팅 대상** = Relay/Content는 **LAN∪서버**(증강) · **Registered는 서버
  명부만**(LAN 억제·상호 불가시성 — 사전 가입/승인자만).
- **릴레이 MVP** = Gateway+Relay 폴백(Content/Registered 밖) · 최대 관문은
  UDP 전송 선행 → "서버부터"가 아니라 UDP 전송 먼저.
- TODO = X-1(릴레이 MVP)·**X-UDP(선행 관문)**·X-2·X-2b(Managed 클라) 전부
  **개발 제외**.

## 승격 ② 실격 → 저장 암호화 A 단일 확정 (사용자 확정)

승격 ②(사용자 암호 래핑)도 실격. ②는 ①의 포터블·백업 문제가 없지만 **DR-1
제로컨피그와 정면 충돌**(앱 시작마다 암호 = 로그인 부활) + 분실 영구 유실 ·
막으려는 H-5(오프라인 탈취)는 전체 디스크 암호화+OS 계정 몫. **B·C 둘 다
실격 → 저장 봉인은 A(기기 키 파생 입력 0) 단일**. **재검토 조건 = Managed
Server 모드**(ADR-0013 모드②/③ — 서버가 신원·기록을 알면 위협 모델·키 관리
전제가 달라진다). [17 §3-2] 신설 + 판정 표 C ✗ + DR-17 개정. 코드 무변경.

## 승격 ①(OS 보안 저장소) 실격 — 설계 결정 (사용자 확정)

평문 3면 봉인의 루트(identity.key)를 OS 키체인으로 올리는 승격 ①을 3-OS
동작(mac Keychain·Win DPAPI·Linux Secret Service)까지 검토 후 **기각**:
DR-4 포터블 파괴(기기 종속 → USB 이동 시 봉인 전부 안 열림)가 치명 · 백업
반쪽화 · mac 무서명 반복 프롬프트 · Linux 백엔드 불확정 · 키체인 손상 유실.
대안 = 승격 ②(암호 래핑 — 포터블 유지·유일하게 정체성과 정합). [17 §3-1]
신설 + 판정 표 B 실격 + DR-17 개정. 코드 무변경.

## sealed 봉투 문서화 — 17 §4-a (사용자 요청 "암복호화 문서 정리")

[17 §4-a](../17-adr-0005-history-at-rest.md) 신설 — 봉투 바이트 배치·키 유도
식·봉인/개봉 절차 단계·`profile.sec` 면별 배선 예시·보안 성질 표(위협별
방어 + **한계 명시**: identity.key 탈취 = 루트 탈취 → §3 승격 ①②가 답).
M2-5b 기록 세그먼트가 이 문법을 이어 쓰도록 구현 기록으로 박제.

## 평문 3면 봉인 — 격리물·PII·프로필 캐시 (main · 사용자 승인 "조치하라")

**공용 봉투 `nbeep_store::sealed`** — ChaCha20-Poly1305(trust.seg·Noise 동일
암호군) · `NBSE` ‖ ver ‖ salt16 ‖ nonce12 ‖ ct+tag · 키 = sha256(태그‖
**domain**‖salt‖wrap_secret)(D-18 §3 신원 파생 계층) · **도메인 분리**가 키
유도·AAD 양쪽에 들어가 파일 종류 간 바꿔치기 차단 · 실패 전부 fail-closed
(**평문 폴백 없음** — 봉인 못 하면 저장 안 한다).

| 면 | 종전 노출 | 조치 |
| --- | --- | --- |
| `.beepq` 격리물 | 수신 파일 원본이 디스크 평문(무결성 봉인뿐) | 게이트 저장 시 봉인 · 읽기 관문 `read_beepq_bytes` 단일화(목록·실체화·썸네일·확대·CLI) · 구본 lazy 재봉인 · CLI도 **영속 신원** 키 |
| settings.cfg | 이메일·전화 평문(사람이 읽는 파일) | cfg known 제외(저장 1회로 구본 소거) + 봉인 사이드카 `profile.sec` · 메모리 Entry 무변경(화면·전파 그대로) · 철회 = 파일 삭제 |
| profiles/{peer}.img | 상대가 나에게만 공개한 사진이 평문 캐시 | 수신 시 봉인(실패 = 캐시 포기·화면은 메모리) · 부팅 개봉 + 구본 lazy 재봉인 |

**실증(실기)** — 세 면 모두 디스크 `NBSE` 헤더 · 평문 마커 grep 0건 · CLI
격리 데모 종단 왕복(봉인→개봉→무결성→실체화) 정상 · cfg에서 PII 소거 확인.

**함정 기록** — 게이트 이중 실행이 cargo 락으로 직렬화된 것을 hang으로 오판
+ 감시 until-루프의 `pgrep -f "cargo test"`가 **감시 잡 자신의 명령 문자열과
자기참조 매칭**해 영원히 대기(관측 도구가 관측 대상이 되는 함정 — linkwatch
자기 유발과 같은 무늬). 단일 실행 재확인 = 669 green(최장 4.2s — hang 아님).

## M3-1b StdAccel 계층 (main · M3 잔여 착수)

`nbeep_plat::conventions` 신설 — **매핑은 conventions, 행동은 앱**: StdAccel
(뜻의 열거) · `primary_is_super()`(⌘/Ctrl 한 곳) · `std_accel()`(문자→뜻 ·
대소 무관). 앱 배선 2지점 교체(cfg! 리터럴·문자 리터럴 9종 제거 · 물리 키
우선 규약 불변) + 매핑 전수 회귀. **잔여 판정** — 스크롤 관성: mac은 OS
모멘텀이 휠 이벤트로 이미 들어온다(합성 관성은 Win/Linux 실기 몫) · 창 닫기
mac 차등 = M3-2d 결정지. **M3-1e ①(TextInput 코어)는 전용 세션 슬라이스로**
— 실기로 다져진 입력 스택 두 파일의 공용화라 어중간히 걸치면 위험.

## 2순위 3종 일괄 — R-8 규명 · M1-11 완결 · M1-8y 완결 (main)

### ① mac 유휴 RSS(R-8) — **코드 문제가 아니라 측정 지표 문제였다**

`footprint`(phys_footprint · Apple 공식 지표) 실측:

| 상태 | ps RSS | **footprint(실점유)** |
| --- | --- | --- |
| 활동 후(전송 실기 뒤) | 269.7MB | **23MB** |
| 부팅 직후 유휴 3신원 | 120~151MB | **18~20MB** |

ps RSS가 부푸는 원인 = **MALLOC reclaimable 209MB**(해제된 힙을 malloc이
MADV_FREE로 반납 표시 — 커널이 메모리 압박 전까지 RSS에서 안 뺀다) + 공유
프레임워크 페이지. 실점유 분해(23MB): MALLOC 계열 ~14MB · CoreAnimation
5MB(창 레이어) · DATA ~2MB — 전부 자연 비용. **Windows 17.1MB와 정합 ·
3-OS 예산(≤30MB) 안쪽 → R-8 해소**. [05 §2-1] NFR-B-1에 측정 지표 명시
개정(mac 판정 = phys_footprint).

### ② M1-11 세션 계층 보강 — 실소켓 종단 캡처 회귀

`tests/wire_plaintext.rs` — 실경로(TcpLink→Noise XX→Mux→Chat/Profile/Offer
인코딩) 두 신원을 **투명 TCP 릴레이** 너머로 성립시켜 와이어 전 바이트 캡처
("릴레이는 봉투만 본다"의 테스트판). 마커 3종 캡처 부재 + **수신 도달** 동시
단언(빈 테스트 방지). 발견 패킷의 프로필 부재는 타입 보장(Packet에 필드
없음 — DR-22) — 회귀 자리 주석 박제. ⚠ M1-11 본체(골든·금칙어·tap·Debug 마스킹)는 08-15에 이미 ✅ — 이번 것은 **인코더 출력 검사와 계층이 다른 실소켓 종단 바이트 검사**의 추가다(분석 때 낡은 CLAUDE 문구를 봤다 — 기록 확인 먼저).

### ③ M1-8y TTY 4경로 — 전부 닫힘

① **SIGKILL**(훅 불가·방어 불가 경로) → **진입 직전 kitty pop 선제 방출**
(스택형이라 빈 pop 무해 · **pty 실측: pop(258)<push(262) · pop 2회**) ②
GUI = RawTerm 참조 0건(코드 확정) ③ 같은 TTY 다중 인스턴스 경합 = 선제
청소가 흡수 ④ 비TTY = is_tty 가드 · **파이프 실행 ESC 0건 실측** + cargo
test 자체가 파이프 stdin이라 회귀가 상시 실측(`non_tty_enter_is_a_noop_guard`).

**662 green** · clippy 0.
