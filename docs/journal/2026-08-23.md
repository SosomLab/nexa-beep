# 2026-08-23 (4차 · mac) — LAN 프로필 동기 실기 왕복 + 프레즌스 색 축

> 3신원 실기(맥 relaunch·윈도우 3인스턴스)가 연달아 구멍을 발각(커밋 3 ·
> `79def8a`·`041dff2`·`0a6897d`). **787 green**.

## ① LAN 프로필 즉시 동기 — 2왕복 (`79def8a`→`041dff2`)

- 요구: via server 상대는 공개 카드로 즉시 갱신되는데 **LAN 상대는 서버 없이
  로컬로** 같은 체감을. 프로필 = 세션 경유(DR-22) 불변이라 답 = **발견 즉시
  조용한 세션**(연결이 곧 채널 — 프리페치+변경 push).
- 1차 = Appeared 관측 시 직접 start_connect. ★실기 발각(relaunch 3신원):
  비컨은 이미 새 이름(Super2)인데 목록은 옛 프로필 이름 · 전 행 Idle —
  **동시 재기동이라 상대 리슨 전 1회 실패 → steady 비컨 = observe None =
  재트리거 없음 = 영구 미동기**. 2차 = **재연결 사다리 시딩**(or_insert —
  실패 시 백오프 자동 재시도 · M4-6 문법 · lan_sync_at 스로틀 폐기).
- 교훈: *1회성 트리거 + 실패 = 영구 결손 — 재시도는 트리거가 아니라 사다리에
  태운다*. ※ 08-14 "부팅 자동 연결 비채택"의 의도적 개정 — 발견 = 연결.

## ② 프레즌스 색 축 (`0a6897d` — 사용자 질문 "roster 상대가 온라인으로 안 보인다")

- roster 등재 = 서버에 지금 접속 중(PeerUp/PeerDown 실시간) → 온라인이 맞다.
- 점 부호화 2축 정리(M3-19 문법 연장): **색 = 지금 닿는가**(세션 ∨ LAN 발견 ∨
  roster 생존) · **실루엣 = 세션 유무** — roster 상대 = 초록 빈 링. On 필터
  온라인 판정도 roster 포함. draw_link_badge에 online 인자(호출 3곳).

## ③ 문답·추적

- **keep-alive 질문** = 불필요(RelayClient 20초 Ping/Pong 기존 동작 확인).
- **윈도우 이름 미전파**(3대 전부 연결 주장) = push 경로 전수 추적 결과 배선
  정상 — 스크린샷 실루엣이 전부 빈 링(세션 없음)·"Session ended" 상태바로
  보아 개명 시점 세션 부재 + 스테일 프로필 이름이 방송 이름을 덮은 것.
  ①의 사다리 픽스가 덮는 자리 — 윈도우 pull·재빌드 후 재확인 대기.

## 게이트·남긴 것

- fmt·clippy 0 · 787 green · 릴리스 빌드. 잔여 = 사다리 동기 실기(맥·윈도우) ·
  M3-27 대기 · v0.2.6.
# 2026-08-23 (3차 · mac) — 필터 마감 · 창 최소 크기 · 설정 초기화 확장

> 실기 피드백 4건(커밋 4 · `3a53fe3`~`c9eef39`).

## ① 필터 아이콘 20 확정 (`3a53fe3`)

- 16→**20**(사용자 확정). 동반 조정 = 바 26→**30** — 칩 정사각(h−7)이 아이콘보다
  작으면 넘친다(26이면 19 < 20). 온/오프라인 점(95% 보정)도 비례 동행.

## ② 메인 창 최소 크기 (`fdfceba`)

- 실기 스크린샷 크기 = 논리 **474×689**를 최소로: `with_min_inner_size`(더 못
  줄임) + 저장 크기 복원도 최소 미만이면 올려 잡음 + **첫 실행(저장값 없음) =
  최소 크기**(종전 기본 460×640 대체).

## ③ 설정 초기화 확장 (`c9eef39`)

- registry 기본값 쌍 전체(종전)에 **숨김 확장분** 추가: 목록 필터 3키(전체
  복귀·바 즉시 갱신) + `ui.win_w/h`(현재 창을 최소로 **즉시 리사이즈** · 다음
  실행도 최소). 최소 크기 상수 = `MAIN_MIN_W/H` 모듈 승격(기동·초기화 공용).
- 유지(종전 정책) = 신원·핀·그룹·프로필·창 위치 — 확인 모달 문구 동기.

## 게이트

- fmt·clippy 0 · 테스트 green · 빌드 왕복. 잔여 = 실기 확인 · M3-27 대기 · v0.2.6.
# 2026-08-23 (2차 · mac) — 필터 바 시각 확정 마라톤 + 토글 기본값 구멍

> 실기 피드백 연쇄 7커밋(`d4c4041`~`87efeb2`) — 필터 바가 시각적으로 확정됐다.

## ① 필터 바 시각 확정 (커밋 6)

- **칩 색 통일**(`d4c4041`): 인증·신규의 컬러 RGBA 원색(파랑)이 비선택에서도
  선택 accent와 같은 색으로 오인 → **알파만 취해 상태색 틴트**(비선택 = 회색 ·
  선택 = accent) — 마스크 아이콘과 같은 결로 통일.
- **아이콘 교체 3종**(사용자 지정 SVG → `mkicons.sh`로 구움 · 원본 커밋):
  로컬 = Material `lan`(`icon-lan-96.alpha` — house는 목록 배지 자리 유지) ·
  전체 = 겹층+체크(사용자 제공 · 520×480 → 520×520 정방 보정) ·
  그룹 = Material `group`(원시 두 사람 도형 폐기 — 남은 원시 작도 = 온라인 점 ·
  오프라인 링 둘뿐).
- **크기 확정**(2왕복): 아이콘 18→14(75%)→**16** · 바 높이 32→**26**.
- **정규화·밀착**(`10534fd`): 원시 도형도 자산과 같은 icon_d 상자 채움(크기감
  균일) · **같은 그룹 안 칩 = 여백 0**(세그먼트 버튼 문법 · 그룹 사이 구분선 유지).
- **그룹 이름 3왕복 → `Via · On · Who` 확정**(`87efeb2`): Location/Status/Trust →
  짧게 — Via(3자 · 헤더 "via server/internet" 어휘 정합) · On(2자 · 온라인 준말 —
  Stat은 통계 오독·Up보다 친숙 판정) · Who(3자 · "누구를 보여줄까" — Auth의
  로그인 오독 없음 · 사람 중심 설계 언어). 칩 앞 소형 텍스트 + 툴팁 접두 동기.

## ② ⚠토글 기본값 구멍 (`74dfcf5` — 실기 보고 "둘 다 켜져 있다")

- Toggle 종류는 `TOGGLE_DEFAULT_OFF` 목록에 없으면 **기본 on** — 원격 파일 발신
  2키를 목록에 안 넣어 "기본 끄기" 확정과 반대로 나갔다(전날 단일 키도 같은
  구멍 · fail-closed 방향 정정). 교훈 = **끄기 기본 토글은 등록 지점이 두 곳**
  (Entry + TOGGLE_DEFAULT_OFF) — 한쪽만 하면 정책이 뒤집힌 채 나간다.
- ⚠ 구 빌드를 돌린 기기는 "on"이 **저장값**으로 남는다 — 설정에서 1회 끄면 유지
  (신규 설치는 처음부터 꺼짐). 검증 노트 아래 여백 8→**16**(8은 여전히 붙어 보임).

## ③ 후속 — 툴팁 형식 · 점 크기 (`922b14e` · `7c30560`)

- 필터 툴팁 "Who · All" → **칩 이름만**("All") — 그룹 이름은 바 라벨로 이미
  보이니 접두 중복 제거(사용자 확정).
- 온라인(찬 점)·오프라인(빈 링)만 **95%** — 면이 꽉 찬 도형은 같은 상자에서도
  선 아이콘보다 커 보인다(광학 보정 · 사용자 확정).

## 게이트·남긴 것

- 매 커밋 fmt·clippy 0 · 필터 회귀 5종 green · 빌드 왕복. 남긴 것 = 실기 최종
  확인(라벨·크기·기본값), M3-27 승인 대기, v0.2.6 릴리스 결정.
# 2026-08-23 (1차 · mac) — 필터 바 아이콘 개편 · 툴팁 공용화 · 원격 파일 옵션 2분리 · relaunch --daemon

> 실기 피드백 연쇄(커밋 4 · `e9e9324`~`6996dec`) + beepd 재배포 요청(해시 대조 =
> **비트 동일 · 재시작 생략**) + 41 §9-0 재배포 절차 상세화. **787 green**.

## ① 필터 바 아이콘 개편 (`6996dec` — 사용자 확정 "높이 2배·전체 아이콘")

- 높이 16→**32** · 텍스트 칩 → **전부 아이콘**: 기존 자산 재사용(house/waypoints/
  globe · id 배지 컬러 RGBA(verified/pinned/firstcontact)) + 자산 없는 칩은
  **원시 도형 작도** — 전체 = 점 4개 격자 · 그룹 = 두 사람 · 온라인 = 찬 점 ·
  오프라인 = 빈 링(목록 연결점과 같은 시각 문법) · **자산 증가 0**.
- 틴트(마스크 × 상태색)·RGBA 캐시 — 페인트마다 96² 변환 없음.

## ② 툴팁 공용화 (`6996dec` — "설명이 보이도록" + "기존 툴바에도")

- `nbeep-ctl::draw::draw_tooltip` 신설 — 역상 캡슐(어두운 바탕+밝은 글자 =
  다크/라이트 공용) · 우측 경계 클램프.
- 필터 칩 = "그룹 이름 · 칩 이름" / **툴바 전 항목** = `.tip()` 체이닝(갱신·직접
  등록·격리함·대화함·프로필·서버 표시 — 기존 i18n 재사용 + TipServerOn 1종).
- ★ 페인트는 **팝업 레이어**에서(정렬 팝업 자리) — 툴바 paint 안에서 그리면
  아래 크롬(필터 바)이 덮는다. hover 무효화도 툴팁 띠(+34/40px)까지 확장.

## ③ 원격 파일 옵션 경로별 2분리 (`6996dec` — 사용자 확정)

- "Files on remote paths" 하나 → **"via server 상대에게 파일 전송" / "via
  internet 상대에게 파일 전송"** 둘(기본 둘 다 끄기 · `xfer.remote_files_server`
  / `_internet`). 게이트는 세션 `via_server`로 갈래 — 차단 안내 문구도 옵션명
  동기. (전날 단일 키 `xfer.remote_files`는 반나절 살았다 — 마이그레이션 생략 ·
  미지 키 보존 규약이라 파일에 남아도 무해.)
- **설정 노트 아래 여백 8**(사용자 확정 — 검증 노트와 다음 행이 붙어 보임):
  note_h에 포함 = 전 노트 공통 개선.

## ④ relaunch.sh --daemon + 경계 일치 (`e9e9324`)

- ⚠**발각**: `pkill -f "nexa-beep"` 부분 일치가 **nexa-beepd까지** 죽이고 잔존
  카운트에 세었다(로컬 데몬 실기에서 ①이 서버를 조용히 끊거나 "남은 1개"
  오탈락) → 경계 정규식. Windows Get-Process는 이름 정확 일치라 원래 무해.
- `--daemon`: beepd 동시 빌드 + **클라보다 먼저** 기동(포트 47399 — 공식 서버
  오염 방지) · 키 = `$MULTI/beepd/` 영속(핀 불변) · 핀 출력 + Test 1회 안내.

## ⑤ beepd 재배포 요청 → 해시 대조 = 동일 (41 §9-0 신설)

- musl 재빌드 후 서버와 SHA-256 대조 = **비트 동일**(`42531982…` — 공개 카드
  슬라이스 직후 배포분이 이미 최신 · 서버 축 코드 무변경) → **재시작 생략**
  (연결 중 사용자 무단절). 절차 자체를 [41 §9-0]으로 상세 문서화 — 빌드→대조→
  원자 교체→재시작→사후 확인(클라 자동 재접속 = 핀 보존 증거) + 불변 규칙
  (키 불가침 · 와이어 하위 호환 전제 · 롤링 vs 태그 릴리스 구분).

## 게이트·남긴 것

- fmt·clippy 0 · **787 green** · 빌드 ✓. 남긴 것 = 실기(아이콘 칩 판독성·툴팁·
  옵션 2분리 동작·roster 카드 양방향), M3-27 승인 대기, v0.2.6 릴리스 결정.
