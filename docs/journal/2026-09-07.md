# 2026-09-07

> 시간 역순(최신이 위).

## (Win · main) 09-06~07 ff 4회 수신 · Windows 게이트 · 설치본 3회 교체 · v0.2.15 확인(사용자 "저장소 최신화·변경 분석 → 빌드·재실행(설치 폴더 배포 포함)" 반복)

mac이 하루 사이 밀어낸 4뭉치를 이 PC에서 순차로 받아 **Windows에서 실측**했다(mac은 Win 타깃 `cargo check`까지만 — 테스트 실행은 이 PC 몫).

| ff | 커밋 | Windows 실측 | 설치본(`%LOCALAPPDATA%\Programs\NexaBeep`) |
|---|---|---|---|
| `58db60a`→`0141ac5` | mac 38커밋 = **ADR-0015 S0~S2 병합**(`feat/userid-handle`) + 09-05 0600 `privfile`·매뉴얼 45·`install-local.ps1` | fmt ✓ · `cargo test --workspace`(testkit) **826 passed · 0 failed · 2 ignored**(mac 831 = OS 조건부 스위트 차) | md5 `89424dda`→`564bb9d7` · `--whoami` 지문 5016194e · `user = off` |
| `0141ac5`→`4c6b81e` | `6f74c72` 2-PC 릴레이 결함 2건(`relay_reattach_soft` · 중복 세션 `via_psk` 동기) | nexa-beep 크레이트 75 passed | `564bb9d7`→`19653e78` · `user = super (verified=on)`(직전 기동에서 켠 설정 유지) |
| `4c6b81e`→`f9ea7f2` | `8a3c841` 글꼴 폴백 체인(09-06 이 PC 실기 "□ Verified"가 발단) + `f9ea7f2` CI 정정 | gfx 7 · plat 67 · nexa-beep **60**(체인 회귀 포함) passed · 기호 본 실재 = `seguisym.ttf` 2.5MB · `seguiemj.ttf` 12.4MB | `19653e78`→`527cc84c` |
| `f9ea7f2`→`97bea06` | **v0.2.15**(`d89f91f` 버전 범프만 · 코드 = f9ea7f2와 동일) | GitHub 실측 = release run success · 자산 14종 · 후속 ci success | 미교체 — 설치본 = f9ea7f2 산출물이라 **동작은 v0.2.15와 같고 `--version`만 0.2.14** |

- 절차 = `pwsh tools/install-local.ps1`(09-05 신설분 첫 Windows 완주 — 종료 → release 빌드 → md5 대조 교체 → 기동 → `--whoami`). 백그라운드 `cargo test`와 겹치면 build 디렉터리 락으로 직렬화될 뿐 충돌 없음.
- 설치본 데이터 폴더가 exe 옆 `data\`(NSIS 보존 설계)라 3회 교체에도 신원·사용자 설정이 그대로 따라왔다.
- **관찰(코드 무변경)**: Windows `SYMBOL_CANDIDATES`가 `C:\Windows\Fonts` 리터럴 — 시스템 드라이브가 다른 PC에서는 기호 본을 못 찾고 조용히 종전 동작(두부)으로 돌아간다. 결함은 아니고 `%WINDIR%` 참조 여지.
- 코드 검토 메모: `accept_any`의 psk 선판별은 XX msg1(태그 없음)에서 `read_message` 실패 → XX 재파싱이라 안전 · `relay_reattach_soft`는 기존 경로에서 `relay_hold = true` 한 줄만 뺀 형태 · Outbound 중복 분기의 비 psk 동작은 종전과 동일.
- **잔여 실기(사용자)** = Win↔Mac 서버 경유 형제 판정(30초 내 `keyblob adopt=true` → ID 수렴) · 설정 › 사용자 노트 "✓ Verified" 육안 · mac이 올린 결정 후보 2건(수동 IP 상대 형제 증명 · 툴팁 문구).

## (mac · main) v0.2.15 릴리스 + 문서·위키 최신화(사용자 "새 버전 릴리즈 · choco/winget은 상태 확인 후 · brew 바로")

- **채널 상태 점검(18 §5 규칙)**: winget v0.2.14 PR #427125/#427126 = **OPEN**(수동 리뷰 큐 · 코멘트 0) → 통과 전 새 버전 제출 금지 → `WINGET_PUBLISH=false` 유지(**9회차 제외**) · choco 3종(nexa-beep·portable·beepd) = 페이지 **"Waiting for Maintainer to take corrective action"**(iconUrl 재제출 대기 · 피드 빈 응답) → `CHOCO_PUSH=false` 유지. brew = 자동.
- **릴리스**: `Cargo.toml` 0.2.14 → **0.2.15** + `Cargo.lock`(`d89f91f`) → 태그 `v0.2.15` push → release run **34043100329 전 job success**(package 5타깃 · publish · homebrew/tap · windows-packages switches=false) · Release 공개 **자산 14종** · **brew cask 0.2.15 추종 ✓**(`kiros33/homebrew-tap` Casks/nexa-beep.rb version "0.2.15").
- **탑재**: ADR-0015 S0~S2(핸들+페어링 암호 · XXpsk3 형제 세션·세션 내 증명 · 키 동기·후계 우선 · 신뢰 해제 12곳 · 보라 "내 기기" 배지 · UserHello · Succession 사슬 · 무장 UI) · 2-PC 릴레이 결함 수정(9차) · 글꼴 폴백 체인 · 목록 증발 수정(09-03) · 비밀 파일 0600(09-05) · 설정 노트 배치.
- **문서·위키**: [45 매뉴얼](../45-user-manual.md) v0.2.15 — **7-1 다중 기기 신원** 절 신설(켜기·암호·내 기기 배지·서버 관계·키 교체·핸들 ⚠) · 5-1/0600 "예정" → 확정 · 위키 **Home/Install/Features/Release-Notes(v0.2.15 절)/User-Guide(7-1 거울)** 갱신 · README 현재 상태·배포 행 · CLAUDE.md §1 공개 = v0.2.15.
- ⚠ 사용자 2-PC 재실기(서버 경유 형제 판정)는 이 릴리스 빌드로 진행 — 결과 대기.

## (mac · main) 글꼴 폴백 체인 — 두부(□) 예방(사용자 "nexa-clip을 참고해서 tofu가 생기지 않도록")

실기 09-06 Windows 캡처: 설정 › 사용자 노트가 "□ Verified · super"로 — 맑은 고딕에 ✓(U+2713)가 없다. 종전 beep의 폴백은 **슬롯 얼굴 → 기본 얼굴** 한 단(08-10)뿐이라 기본 얼굴에도 없는 기호는 두부였다.

- **nbeep-gfx `Font` = 얼굴 체인**(clip 09-01 이식): `faces: Vec<FontRef>` · `push_fallback`/`push_fallback_font` · 글자마다 글리프가 있는 첫 얼굴(`face_for`) · 측정과 그리기가 같은 얼굴(캐럿 일관) · 기준선·줄 높이는 주 폰트 · 제어 문자 규칙(탭 = 공백 4칸 · 그 외 폭 0 — clip 09-03 탭 두부).
- **nbeep-plat `symbol_fallback_fonts()`**(clip 09-04 이식): mac Apple Symbols → Arial Unicode MS · Windows Segoe UI Symbol → Segoe UI Emoji(흑백 윤곽) · Linux DejaVu Sans → Noto Sans Symbols2/Symbols. 컬러 이모지 본(sbix/CBDT)은 넣지 않는다(빈칸 = 두부보다 못함).
- **앱 `load_ui_font(family)`**: 주 글꼴(시스템 UI 본 또는 사용자 지정 `font.base.family`) → 시스템 본 → 기호 본 순으로 잇고 진단 1줄(`글꼴 폴백: Apple Symbols → Arial Unicode MS · ✓=true ⚠=true 🎉=false`). 슬롯 얼굴(목록·메시지·상태·고정폭)은 체인 없이 1벌 — 래스터가 기본 얼굴로 폴백(08-10 그대로)하고 기본 얼굴이 체인을 품는다.
- 회귀: 앱 `ui_font_chain_covers_ui_symbols`(✓·⚠·→·· ·가·A — 3-OS CI가 각자 본으로 실측) · plat 후보 존재. 게이트 4종 ✓ · **835 green** · Windows·Linux cross check ✓.

