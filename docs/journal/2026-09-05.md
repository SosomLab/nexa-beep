# 2026-09-05

> 시간 역순(최신이 위).

## (mac) 실기 확인(0600 스윕 ✓) · 로컬 갱신 스크립트 clip 동등화(`install-local.sh` 개정 + `install-local.ps1` 신설) · 채널 3종 실측(choco = 검수자 Requirement)

사용자: *"실기 중 직접 확인이 필요한 대상은 확인해줘 · beep도 nexa-clip처럼 각 OS별 최신 릴리스 직접 설치 없이 로컬에서 Release로 컴파일된 파일로 갱신하는 스크립트 + 문서 최신화 · brew·winget·choco 진행 상태 확인"*.

### ① 실기 — 0600 부팅 스윕(1차 잔여)

개정한 `tools/install-local.sh`로 설치본(brew cask `.app` · v0.2.14 08-30 빌드 → 이 트리 산출물)을 갱신·재기동한 뒤 데이터 폴더를 대조했다:

| 폴더 | 전 | 후 |
|---|---|---|
| `~/Library/Application Support/nexa-beep` | `identity.key`만 600 · `settings.cfg`·`profile.sec`·`server.pin`·`trust.seg`·`keys.seg`·`history/*.seg` **644** | **전부 600** ✓ |
| `~/.nexa-beep-multi/A/data`(미기동) | 644 | 644(불변 — 스윕은 **기동한 인스턴스**만) |

`--whoami` = 지문 `be140131` · exe = `.app` · data = Application Support(설치본 신원 보존 ✓). 스크립트 완주 43s(증분 빌드) · md5 `dd1da59d` → `194b0218` 교체 · adhoc 재서명 · 프로세스 1개.

### ② 로컬 갱신 스크립트 — nexa-clip `scripts/dev-install-*`와 동등화

beep에는 이미 `tools/install-local.sh`(08-29)가 있었다. clip의 세 스크립트(linux.sh/mac.sh/win.ps1 · 09-04~05)를 대조해 빠진 것을 옮겼다:

| 항목 | 종전 | 09-05 |
|---|---|---|
| 프로필 | release 고정 | `--debug` |
| Linux 설치 자리 | `/usr/bin` 고정 | `NEXA_INSTALL_DIR` → `command -v nexa-beep`(readlink) → `/usr/bin` · **개발 트리 오인 차단** |
| 권한 | `sudo` | 쓰기 가능하면 없음 · TTY 없으면 **pkexec**(에디터 통합 셸에서 sudo가 "terminal is required"로 죽는 것) · pkexec는 cwd를 안 물려줘 **절대 경로** |
| Linux 기동 | `gtk-launch nexa-beep`(ID) | **`gio launch <설치 .desktop 경로>`** — ID 탐색은 앱이 제 손으로 쓴 사용자 런처(자동 실행 슬롯 · `~/.local/share/applications`)가 `/usr/share`를 가리면 **개발 빌드가 대신 뜬다**(clip 09-05 실측) · `/proc/<pid>/exe`로 설치본인지 대조 |
| 자산 | — | `--assets`(.desktop·아이콘 · `update-desktop-database`) |
| 로그 | — | 설치본 stdout → `target/installed-nexa-beep.log` |
| Windows | Git Bash에서 `powershell.exe` 호출 | **`tools/install-local.ps1` 네이티브 신설**(`-Debug/-NoBuild/-NoRun` · 자리 = `$env:NEXA_INSTALL_DIR` → HKCU `Software\SosomLab\NexaBeep\InstallDir`(NSIS가 남김) → `%LOCALAPPDATA%\Programs\NexaBeep` · `Get-FileHash` md5 대조 · 버전 = `Cargo.toml`) · sh의 win 분기는 **위임** |

clip의 `systemd-run --scope`(포털 전역 단축키용 앱 스코프)는 옮기지 않았다 — beep은 포털 단축키를 쓰지 않고 창 `app_id`를 스스로 박는다. SSOT = [18 §0-2](../18-build-and-test.md) · 스킬 `install-local` 갱신.

### ③ 채널 실측(brew · winget · choco)

| 채널 | 상태 |
|---|---|
| **brew**(kiros33/homebrew-tap) | `nexa-beep.rb` **0.2.14** · `nexa-beep-portable.rb` 0.2.14 · `nexa-beepd.rb` 0.2.5 — 최신 추종 ✓(로컬 `brew info` = 0.2.13 → 0.2.14 업그레이드 가능) |
| **winget** | v0.2.14 #427125(설치본)/#427126(포터블) **OPEN** · 라벨 `Azure-Pipeline-Passed`+`Validation-Executable-Error`+`New-Package` · **코멘트 0**(09-01 재제출 후 4일 · 수동 리뷰 큐) · beepd #422579 OPEN `Validation-Completed`(모더레이터 승인 대기 · 08-22 봇 코멘트 1) · `manifests/s/SosomLab/`에 NexaBeep 미등재(= 미병합) |
| **choco** | ★ **셋 다 "Waiting for Maintainer to take corrective action"** — nexa-beep·nexa-beep-portable 0.2.2(검수 09-04) · nexa-beepd 0.2.5(09-02). 검수자 virtualex: *"install/portable split·checksums·Install-ChocolateyZipPackage·uninstall 전부 정상 — Requirement 하나: `iconUrl`이 raw.githubusercontent.com(허용 CDN 아님) → jsDelivr `cdn.jsdelivr.net/gh/SosomLab/nexa-beep@<tag>/packaging/branding/nexa-beep-256.png` · main 아닌 **태그에 핀** · 세 패키지가 같은 아이콘이라 한 번에 · **같은 버전으로 재제출**"*. 부수 = VirusTotal 1~5건 Note(승인 차단 아님). OData 피드는 대기 중 패키지를 숨기고 페이지는 UA 없는 요청에 403 — 조회 명령 [18 §5] 갱신 |

**조치** = nuspec 3종 `iconUrl` → `https://cdn.jsdelivr.net/gh/SosomLab/nexa-beep@v@VERSION@/…`(beepd = `@beepd-v@VERSION@`) · CDN 도달 v0.2.14·beepd-v0.2.5 **200** 확인. **재제출은 하지 않았다** — 사용자 결정(스위치 `CHOCO_PUSH` false · "같은 버전" 재제출이면 0.2.2 nupkg를 다시 밀어야 하고, 다음 태그에 얹으면 0.2.15가 첫 승인 대상이 된다 — 18 §5 규칙과 함께 판단).

---

## (mac) [44 §7] nexa-clip 전달문 32 수신·처리 — 비밀 파일 0600 한 벌 · 설정 중복 줄 · 와이어 공유 고지 + beepd 연결 정리 기준 점검

사용자: *"nexa-clip 프로젝트 폴더에 오늘 발견된 beep로 전달할 결함을 문서로 만들었어. 32번 문서를 찾아서 반영해줘.
beepd에서 일정 시간 이상 연결이 되지 않는 대상은 제외되고 있는지 관리 기준 확인해줘."*

원격 6커밋(09-03 Win — 목록 증발 수정 `528453c`·TODO 파손 복구) fast-forward 후 작업. 코드 커밋 **`92c92ab`**.

### ① 전달문 = clip `docs/32-beep-handover.md`(대조 판 beep v0.2.13 `5537f5c`) — 8항목

| # | 판단 | 처리 |
|:--:|---|---|
| A-1 `nexa-conf` umask 권한 | 이식 + **실측 확대** | `92c92ab` |
| A-2 미지 키 두 줄 | 이식 | `92c92ab` |
| B-1 RID 도메인 = 앱 식별자 | **이미 반영**(08-26 `78a4035`) | 기존 |
| B-2 glare 타이브레이크 | X-11 채택 검토 대기(사용자 결정) — 실코드 현황을 [44 §7-2] 박제 | ⏸ |
| B-3 relay 사본 동기 고지 | 수용 — 머리말 고지 + 절차 [44 §7-4] | `92c92ab` |
| C-1~3 | 정보 공유(요청 아님) | — |

### ② A-1 — beep 실측: `settings.cfg`만이 아니었다

clip은 *"`profile.sec`·`server.pin`·`trust.seg`·`keys.seg`가 664인데 `write_atomic`을 타는지는 beep이 확인"* 이라 했다.
확인 결과 **하나도 안 탄다** — 파일마다 temp+rename을 손으로 짰고(`trustfile`·`groupfile`·`keytable`·`app.rs` 기록/대기/그룹 기록),
`profile.sec`는 `fs::write` 직접(**비원자**), `server.pin`은 `nbeep-relay::pinfile`이 `fs::write`. 이 PC 실측(mac umask 022):

| 폴더 | 결과 |
|---|---|
| `~/.nexa-beep-multi/A/data` · `B/data` | `identity.key`만 600 · `groups.seg`·`keys.seg`·`server.pin`·`settings.cfg`·`trust.seg`·`profile.sec`·`history/*.seg` **전부 644** |
| `~/Library/Application Support/nexa-beep`(설치본) | 동일 — `identity.key` 600 · 나머지 644 |

위험 판정: 내용은 AEAD 봉인(신원 키 파생)이라 노출은 **암호문**에 그친다. 그러나 Linux 664는 **같은 그룹 계정의 쓰기 = 훼손·삭제**를
허용하고(fail-closed라 결과는 기록·핀 소실), **`server.pin`은 평문**이라 훼손 = 서버 바꿔치기(핀 불일치 모달은 *바뀐 뒤*에 뜬다).
크기·갱신 시각 노출도 남는다. → **한 벌로 정리**:

- **`nexa-conf`**: `write_atomic` temp를 `OpenOptions::mode(0o600)`(rename이 모드를 나른다) · `Store::open`이 기존 파일을 0600으로 죈다(`tighten` — clip은 안 넣었다 · beep은 부팅마다 idempotent).
- **`nbeep_store::privfile` 신설**: `write_atomic(path, bytes)`(0600 · sync_all · rename · 실패 시 temp 제거 · 부모 생성) · `tighten` · `tighten_data_dir`(폴더 바로 아래 `*.seg|sec|pin|key` + `history/`·`pending/`의 `*.seg` — 격리물·이미지 캐시는 목록 밖 = 불변). `trustfile`·`groupfile` 인라인 블록 교체(−20줄) · `keytable` · `app.rs` 봉인 쓰기 4곳(`profile.sec` 원자화 포함) · `run()` 부팅 1회 스윕.
- **`nbeep-relay::pinfile::store`**: 의존 추가 없이 인라인 0600(nbeep-store를 끌면 clip 사본이 갈라진다 — B-3 정신).
- Windows = 전부 no-op(프로필 ACL이 같은 역할). `.locked` 보관본(`groups.seg.locked`)은 확장자가 달라 스윕 밖 — 옛 잠긴 세그먼트라 그대로 둔다.

### ③ A-2 — `serialize` known 우선

`unknown` 재방출 앞에 `known.iter().any(|(kk,_)| *kk == k)` 건너뛰기. beep은 키를 계속 등재하므로(08-30 `ui.tray_hide_taskbar` 등) 잠복 중이던 증상 — 구버전 파일을 신버전이 열면 두 줄이 된다. 파서는 마지막 줄 채택이라 값은 무해했다.

### ④ B-2 glare — beep 실코드 현황(채택은 사용자 결정)

릴레이 계층에는 규칙이 없다(clip 진단 = 맞다). 앱 계층 `AppEvent::Inbound`에 `conversations.contains_key(&peer) → return` 가드 하나 —
**진짜 동시 열기면 둘 다 상대 인바운드를 드롭 → 둘 다 Closed → 백오프 재시도**(수렴은 운). clip 규칙(PeerId 사전순 작은 쪽 = initiator)은
이 자리 `peer < me` 한 줄 — 서버 무변경 · LAN에도 동일 적용 가능. [44 §7-2] 표로 박제 · TODO X-11 현황 갱신.

### ⑤ 질문 2 — beepd "일정 시간 이상 연결 안 되는 대상 제외" 기준 → [41 §3-2](../41-beepd-ops-guide.md) 신설

결론: **서버에 시간 기준 명부가 없다.** 대상 = 살아 있는 TCP 제어 연결. 연결 죽음 = 즉시 회수(RID·토큰·채널·공개 목록·`PeerDown`).

| 단계 | 기준 |
|---|---|
| 미등록 | 10s 안에 `Register` 없으면 절단(`HANDSHAKE_TIMEOUT`) |
| 등록 후 유휴 | **앱 계층 타임아웃 없음** — 클라 `Ping` 20s는 NAT 유지용 · 서버는 `Pong`만(수신 시각 미기록) |
| 죽음 감지 | **TCP keepalive**(`TcpLink::new` — 10s/5s/재시도 3 = Linux·mac ≈25s · Win ≈1분) + 쓰기 30s → `Err` → 스레드 종료 |
| 느린 소비자 | 큐 8MiB 초과 = kill |
| 재접속 잔재 | `Register` 최신 우선 덮어쓰기 — 죽어가는 옛 연결이 자리 못 막음 |
| 감지 전 창 | ≈25s 동안 죽은 conn에 `Open` = `status 0`+큐 적재 → 감지 시 `ChClosed` · 클라 사다리 12s 상한으로 먼저 포기 |

유휴 타임아웃을 **추가하지 않은 이유**도 적었다 — keepalive가 커널에서 같은 일을 더 정확히 한다(추정 금지). 필요하면 `Ping` 수신 시각 필드 1개 · 와이어 무변경.

### ⑥ 게이트·부수

- fmt ✓ · clippy `-D warnings` 0(1차 = `doc_lazy_continuation` 1건 정정) · **807 green**(mac — 09-03 Win 797+mac 조건부 스위트 +회귀 7) · rustdoc ✓.
- ⚠ **DEVLOG 모지바케 줄 1개 제거** — 09-03 Win 2차 항목이 UTF-8 깨진 사본으로 한 줄 더 들어가 있었다(정상 줄 바로 위에 존재 · 내용 동일). 같은 세션의 다른 파일은 무사(`grep â` 전수 = DEVLOG만).
- 잔여 = **실기**: 다음 기동 후 `ls -l` 로 세 폴더 0600 확인(스윕) · Win/Linux 교차 = CI.

### 판단 메모

- **"한 곳 고치면 끝"이 아니었다** — 같은 결함이 7경로에 복제돼 있었다(파일마다 temp+rename을 새로 짠 08-11~08-21 누적). 헬퍼 한 벌로 모은 것이 이번의 실질 — 다음 봉인 파일은 `privfile::write_atomic`만 부른다.
- **relay에 store 의존을 넣지 않은 것**은 B-3의 첫 적용 — 공유 크레이트의 의존 트리가 곧 사본의 동기 부담이다.
- clip 원장 22 상태 전환(I-1·5·7·8 [반영] · I-4 [전달])은 **clip 쪽이 적는다**(다른 프로젝트 · [44 §7-3]에 회신 표 준비).
