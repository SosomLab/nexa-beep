# 2026-08-21 — 클라이언트 공백 3종 연속 구현(오프라인 큐 · 클립보드 이미지 · 등급/공지)

> 사용자 절차 확정: "조사 → 결정 질문 1회 → **중단 없이 순차 개발**". 결정 4건 =
> ② 재시작 유지 · ③ 3-OS 동시 · ④ 배지+명령 · 공지 = 발견된 전체.
> 배경 = 경쟁 비교(03 §4 대조)에서 추린 공백의 2·3·4순위(1순위 폴더 전송은 다음).

## ② 오프라인 큐(M4-6 · C-3 · FR-M-3) — `221bde6`

**발각된 사전 구멍** — 세션 없는 발신은 풍선만 남고 **조용히 유실**됐다(send
합류점의 `if let Some(conv)` fall-through — 전송도 기록도 없음). 큐가 이 구멍을
메운다.

- 세션 없음 = 상대별 보관(상한 200·오래된 것부터) + 풍선 **대기 막대**(점 계열과
  모양 분리 — 색맹 안전) + 상태바에 **한계 명시**("내 PC가 켜져 있고 상대가
  나타나면") + 즉시 연결 시도.
- 영속 = `data/pending/{short}.seg`(**SEAL_PENDING** 봉인 · 원자적 쓰기) · 부팅
  복원 = 핀 매핑 + 대기 풍선 재생 + 재연결 후보 등록.
- flush = 세션 성립 합류점(Outbound·Inbound — `flush_group_sends` 옆): fresh seq
  발신 · `resolve_queued`로 풍선 해소 · **세션 급사 시 잔여 도로 대기**(유실 금지).
- 발견 Appeared 훅 = `reconnect.entry().or_insert((0,0))` — **or_insert가 요점**
  (Appeared는 비컨마다 온다 · 덮으면 1.5초 재시도 폭주).
- 회귀: 코덱 왕복+꼬리 손상 fail-soft · resolve_queued.

## ③ 클립보드 이미지 전송(3-OS) — `a0f4633`·`bc00b2e`

- **plat::clipboard::get_image**: Windows = CF_DIB 직접 + **순수 DIB→RGBA 파서**
  (24/32bpp·BI_RGB/BITFIELDS·bottom-up/top-down·stride·상한 8192/16.7M · 알파
  255 고정 — 스크린샷 DIB 알파 관행 불신) · mac = AppleScript가 클립보드 PNG를
  임시 파일로(pngpaste 무의존) · Linux = wl-paste→xclip 사다리.
- **imgdec `--encode-raw`**: 원시 RGBA→원본 크기 PNG(R-5 유지 — 본체 인코더
  무링크 · lockdown 그대로 · raw 상한 64MiB).
- 앱 = 붙여넣기 3경로(StdAccel Ctrl+V + 1:1·그룹 컨텍스트 메뉴)의 **텍스트 없음
  폴백** → 워커 스레드(스폰 1~2s 얼음 방지) → `AppEvent::ClipImage` →
  `data/clipboard/clip-{ms}.png` → `offer_file`(승인·격리·미리보기 전부 기존).
- **실측**: imgdec raw 왕복 **비트 동일**(2x2 → PNG 86B → 재디코드 픽셀 일치) ·
  **실물 클립보드**(.NET SetImage 3x2 → CF_DIB→RGBA 정확) · DIB 회귀 3종.
- 교훈 재확인: mac 타깃 check가 dead fn(`read_bytes_from` Linux 전용)을 발각 —
  반대편 타깃 게이트가 두 번째로 값을 했다.

## ④ 메시지 등급 + 공지(D-1 잔여 · FR-M-6) — `5fcab51`

docs/24 확정 매핑 준수 — 발신자는 **요청**만(Importance 니블 · N-1 자리 그대로 ·
와이어 개정 0), 강도 판정은 수신자.

- 발신 2경로: **입력줄 오른쪽 배지**(일반→알림→긴급 순환 · **다음 1회 적용 후
  복귀** = Urgent 마찰 원칙 · 긴급 진입 = 상태바 경고) + **/notice·/urgent**
  (한글 별칭 · 빈 본문·멀티라인 = 사용법 안내 fail-closed · 그룹 방 = 미지원 안내).
- 표시 = **풍선 등급 링**(알림 warn·긴급 danger — 색+형태 이중 부호화 · 송수신 대칭).
- 강도: 미검증 = 종전 무음 게이트 유지(자동 강등) · 핀/검증 **Urgent = 앱 전면에도
  알림**(notify_user `force`). Alarm(지속 최상위 창·소리 축)은 후속 — 근사임을 명시.
- **공지** = 메뉴 ▸ 공지 보내기 → 본문 프롬프트(DisplayName 제약 밖 — 본문이다) →
  **발견된 전체** Notice 팬아웃 · 미연결 = **②의 큐로 편입**+자동 연결 · "즉시 N ·
  대기 M". Urgent 공지는 발신부터 만들지 않는다(§3-3 팬아웃 강등의 정신).

**게이트(3종 공통)** — 최종 **725 green**(+7) · clippy 0 · fmt 0 · rustdoc 0 ·
linux/mac 타깃 check 0. 실기 절차 = **[26 §3-4](../26-run-and-manual-test.md) 신설**
(3종 각각 단계·확인 포인트).

**★ 사용자 실기 확인(같은 날)** — **3종 모두 정상 동작 확인** ✓(오프라인 큐 ·
클립보드 이미지 · 등급/공지). Windows 실기 잔여 없음 — 남은 것은 mac/Linux
클립보드 실기(해당 OS 몫)뿐.

---

## (2차) CI red 진단 → 잠복 결함 2건 종결 — dir_writable 경합 · 클립보드 use-after-free (T-flake 정체)

**발단** — 3종 push(`b489274`)의 CI가 test(windows)에서 red. 실패 테스트 =
`part::tests::partial_roundtrip_and_size_mismatch_discards`(저장 직후 조회가 None).
part 자체는 결백했다.

### ① dir_writable PID 프로브 경합 (`ea96094` · CI red의 범인)

- 프로브 파일명이 `.probe.{pid}` 하나 → **같은 프로세스의 병렬 테스트 스레드가
  한 파일을 공유**. Windows에서 한쪽 remove가 만든 delete-pending 순간에 다른 쪽
  `File::create`가 ACCESS_DENIED → **false 오판** → `data_dir()`가 그 호출만
  %APPDATA% 폴백으로 튐 → `save_partial`/`load_partial`이 **다른 루트**를 봄.
- 제품에서도 다중 스레드 `data_dir()` 호출 시 같은 순간 이동 가능(격리물이 순간
  다른 루트로 — 신원 durable 계열 구멍).
- 수정 = AtomicU64 시퀀스 접미(호출마다 유일) · 회귀 = 8스레드×50회 전부 true.

### ② 클립보드 use-after-free (`35a6160` · ★T-flake 08-20 2회 관측의 정체)

- 전량 테스트가 간헐 nbeep-plat **0xc0000374(STATUS_HEAP_CORRUPTION)**·드물게 AV로
  중도 사망. 60개 중 31개 시점 abort = T-flake의 "정확히 29개 모자람"과 일치.
- **재현 확립이 반이었다**: 직접 exe 루프는 희귀(2/171) · **cargo 문맥 4/20**.
  모듈 이분 탐색: 전체 → 초반 5모듈(3/20) → autostart+clipboard(5/25) →
  **clipboard 단독 2/25**.
- 원인 = **`OpenClipboard(NULL)`의 거짓 성공**: 내부 오픈 상태를 "연 창 핸들"로
  대조하는데 NULL==NULL → 둘째 호출자(스레드·프로세스 불문)도 성공 → 한쪽
  `EmptyClipboard`가 상대가 `GlobalLock` 중인 HGLOBAL을 해제 = use-after-free.
  **제품 실버그**(이미지 워커 x UI 복사 · 3-신원 다중 인스턴스 간).
- 수정 3겹 = **호출마다 메시지 전용 창(진짜 HWND)으로 열기**(둘째 = 정직한 실패) ·
  프로세스 내 뮤텍스 직렬화 · `get_text` 널 스캔 GlobalSize 상한.
- 왕복 테스트 환경 플레이크 2종 skip 흡수(set 직후 클립보드 히스토리 서비스 잠금
  = 300회 루프 실측 2% · 타 프로세스 덮어씀). 왕복 검증은 조용한 CI가 지킨다.
- **실측 = 수정 전 4/20·2/25 크래시 → 수정 후 0/70**(clipboard 40 + 전체 30).

**게이트** — **726 green**(+1 dir_writable 동시성 회귀) · clippy 0 · fmt 0 ·
rustdoc 0 · linux/mac 타깃 check 0. **T-flake 항목 ✅ 해소**(TODO 대조 완료).

**교훈** — *"간헐"은 난수가 아니라 재현 조건을 못 찾은 것* — 실행 문맥(cargo vs
직접·병렬 폭)을 축으로 잡자 4/20까지 올라왔고, 그때부터는 이분 탐색이 기계적으로
범인을 냈다. 그리고 Win32 "성공"은 성공이 아닐 수 있다(OpenClipboard(NULL) ==
NULL 대조) — **핸들 없는 열기부터 의심한다**.

### 부수 확정 — 폴더 전송 목표 제외(사용자 확정 08-21)

- C-2/M4-2c = **보류 재확정**("차후 안정성 충분히 확보된 이후 재검토") —
  [25](../25-feature-scope.md) C-2 행·[TODO](../TODO.md) M4-2c 행·CLAUDE.md 정합.
  v1.0 커트라인 실질 공백 = 스팸 방어(M5-3)로 압축.

---

## (3차) v0.2.1 공개 + 네트워크 계측 모드 netmon (사용자 요청 2건)

### ★ v0.2.1 공개 (`33a0260` + 태그 — "brew 배포까지 진행해줘")

- 패치 승격(기능 3종은 기존 축의 채움 · 규약 변화 없음). release.yml **success** ·
  **자산 14종**(5타깃×2채널·SHA256SUMS·manifests) · ★**brew 탭 자동 추종 확인**
  (`kiros33/tap` cask `version "0.2.1"`) · choco = 보류 그대로(CHOCO_PUSH='').
- 탑재 = 3종(오프라인 큐·클립보드 이미지·등급/공지) + CLI 단말 축 + WNET-1 +
  **안정성 2건**(클립보드 UAF·dir_writable 경합 — 2차 참조).

### ★ netmon — 과도 송수신 모니터링 (`d363473` · "의도적으로 켜는 경우에만")

- **구조 = 세는 것/쓰는 것 분리**(설계 = [06 §7-1] 신설): 카운터는 이음새 4곳
  상시(relaxed 원자 · ns·할당 0 · **횟수·바이트만** — 봉투 원리) / 기록은
  `netmon.enabled`(기본 off · 설정 고급 › 네트워크 점검) 옵트인 —
  주기(기본 10s) 델타 한 줄 = `data/logs/netmon-YYYYMMDD.log`.
- M3-22 StatusLog **재사용**(prefix 파라미터화 · sweep 자기 프리픽스만 — beep와
  netmon이 서로 안 지운다) · 종료 flush 훅 동참.
- **과다 판정** = 발견 발신 25pps·수신 100pps·연결 분당 30회 → `warn=` 태그 +
  **상태바 경고**. 세션 바이트는 기록만(파일 전송 = 정당한 대량).
- 사용 절차·점검 시나리오 3종 = [26 §3-5] · 줄 포맷 안정 계약 회귀 4종 박제.
- 정직한 한계: 상대별 분해 없음(봉투 원리와 상충 — 필요 시 별도 결정) · CLI 모드
  배선은 후속(카운터는 공유).

**게이트** — **730 green**(+4) · clippy 0 · fmt 0 · rustdoc 0(§ `[무시]` 링크 오인
1건 즉수정) · linux/mac 타깃 check 0.

---

## (4차) 10분 자동 모니터링 실측 + 공지 제동 2건 + 차단 4축 설계 확정

### ★ netmon 10분 자동 시나리오 (사용자 요청 — NETMON-1 ⓓ 완료)

`NEXA_NETMON` 환경변수 스위치(`28ee019` — NETMON-1 ⓐ · NEXA_IME_TRACE 선례)를
신설해 CLI 모드에서도 수집 가능하게 한 뒤, 격리 신원 2개(포터블 exe 복사 ·
같은 PC)로 5초 해상도 수집:

- **P1 발견 대기 210s**(chat-live × 2): 광고 발신 **3.7pps**(주기 0.8s × 경로
  ~3 — 기본 멀티캐스트+브로드캐스트+IF별) · 수신 7.4pps 중 **자기 에코 50%**
  (same-PC 멀티캐스트 루프백 — 다중 인스턴스 발견에 의도된 동작) · 드리프트 0 ·
  세션/연결 축 0 · **경고 0**(임계 25pps의 15% 수준).
- **P2 연결→채팅→전송→종료 315s**(chat-serve/connect): 연결 시도 **정확히 1회**
  (co=1·cok=1) · 세션 성립 시 1회성 70KB 양방향 = 프로필 교환(설계된 비용) ·
  **메시지 1건 ≈ 85B 프레임 1개**+ack 62B(과다 없음 · A/B 프레임 수 대칭 정확) ·
  **유휴 세션 앱 프레임 0**(keepalive는 OS TCP 계층) · **64MiB 전송 ≈ 12초**
  (~5.6MB/s Auto 페이싱 · 2,049프레임 = **32KiB 청크 그대로** · 총 오버헤드
  168KB = **0.25%**, 프로필 제외 0.15% · 전송 중 발견·연결 축 침묵) · 종단 ack
  39B 1건 · **/quit 후 재연결 시도 0**(백오프 홍수 무재발). **4개 로그 전체 warn 0.**
- 판정: **과다 송수신 없음** — 발견·채팅·파일·종료 전 축이 설계 기대치와 일치.
  기준선은 [26 §3-5 6항]에 박제.

### ★ 공지(브로드캐스트) 제동 2건 (`8da3bce` · 사용자 확정)

- **발신 3초 1회 제한** — 연타는 상태바 안내(조용한 생략 금지) · 프롬프트 제목에
  제한 명시(4개 국어).
- **받지 않기 설정**(`notify.broadcast_mute` 기본 off) — 와이어 공지 표식 신설
  (kind 상위 니블 bit2 · 구버전 = Normal 강등 표시 = 전방 호환 · 마스킹 디코드
  회귀 박제) · on = 표시·알림·기록·전달 ack 없이 폐기(그림자 — 발신자 쪽은
  오프라인 수신자와 구분 불가) · 대기 편입 공지도 표식 유지(PendingDirect bit2 ·
  저장 포맷 불변) · CLI = `[공지] ` 접두. 검증 절차 = **[26 §3-6] 신설**.

### ★ 수신 제한(차단) 4축 설계 확정 — **[38](../38-receive-restriction-design.md) 신설** (M5-3 1차 · 개발은 요청 시)

사용자 4문항 확정: **그림자 차단** · **IP = accept 즉시 끊기**(핸드셰이크 비용
차단) · **자동 임시 차단 포함**(기본 10분 · 영구는 사람만) · **기간부+정확 일치**
(IP 기본 24h·신원 축 영구). 뼈대 = 네 축은 등가가 아니다(PeerId/UserId = 신원 ·
IP = 경로 · 이름 = 표시 — DR-20 "차단은 사람 단위"·DR-28 두 축 원리 정합) ·
판정 = 비용 순(IP 소켓 → PeerId 핸드셰이크 후 → 이름 파싱 후) · 저장 =
trust.seg 봉인 확장 · 구현 슬라이스 BL-1~5 = TODO 등재.

**게이트** — **731 green**(+1) · clippy 0 · fmt 0 · rustdoc 0 · linux/mac check 0.

---

## (5차) 공지 후 대화창 진입 수정 — 스트레이 Enter 가드

**실기(사용자)** — 공지 발송 뒤 메인이 "보낸 사람 중 1명"의 대화창으로 들어간다
(기준 불명). 추적: 뷰를 여는 경로는 `activate` 5곳뿐 — 공지 경로는 전부
auto(조용한 연결)라 결백 · 메뉴 픽은 MouseDown 소비+목록은 더블클릭 활성화라
마우스 경로도 결백. 남은 것 = **목록의 Enter = 캐럿 행 활성화**: 공지 프롬프트를
Enter로 제출하면 모달 창이 파괴되고 메인이 포커스를 돌려받는데, 같은 keypress의
잔향·키 반복 Enter가 메인 목록에 도달 → **캐럿 행이 열렸다**("기준 모름" = 캐럿).

**수정** — 메인 `Focused(true)` 시각 기록 + **300ms 동안 목록행 Enter만 무시**
(`ENTER_GUARD_MS` · 마우스·다른 키·다른 위젯 무영향 · Enter 제출 모달 8종 공통
해소). 사람이 포커스 전환 후 300ms 안에 의도적 Enter를 치는 것은 사실상 불가능.
검증 = [26 §3-6] 5항 추가. 게이트 전량 **731 green**.

---

## (6차) 공지 창 위치 + Enter 잔향 방어 3겹 (사용자 실기 2차)

- **창 위치** — 공지(프롬프트 공통) 창이 OS 기본 위치로 목록과 멀리 뜨던 것 →
  **메인 창 중앙 부근 배치**(경고 모달과 같은 문법 08-20 · outer_position+크기
  중앙 계산 · 메인 소유는 그대로).
- **Enter 잔향 2차 보강** — 5차 가드(Focused(true) 기준)만으로는 잔향 Enter가
  포커스 이벤트보다 먼저 메인에 도달하는 순서에서 늦는다 → 방어 3겹으로 확장:
  ① **모달 닫힘(제출/취소) 시점**에도 가드 ② 포커스 복귀 시점 가드(기존)
  ③ **활성화 드레인 최종 폐기**(모달 열림 중이거나 가드 창 안의 목록 활성화 =
  정의상 잔향 — 이벤트 순서와 무관하게 여기서 막힌다). 검증 = [26 §3-6] 5·6항.

게이트 전량 **731 green**.

**★ 사용자 실기 확인(6차 직후)** — ① 3초 내 재전송 = 상태바 제한 안내 표시 ✓
② 공지 창 = 메인 중앙 배치 ✓. 잔여 육안 = Enter 제출 후 목록 잔류(잔향 3겹) ·
공지 받지 않기 설정 · 수신측 [공지] 표식/등급 링 · 대기 편입 경로([26 §3-6] 3·4·5항).

---

## (7차) 공지·등급 UI 다국어 승격 + 프롬프트 word-wrap (사용자 실기 3연)

- **등급 배지 i18n**(GradeNormal/Notice/Urgent 4개 국어) — 한글 하드코딩이던
  입력줄 칩 · **폭 = paint 실측**(Cell — "Normal"이 고정 36px을 넘쳐 잘릴 것 ·
  자당 근사는 메뉴를 잘랐던 전례라 실측).
- **프롬프트 i18n**(WinBroadcast·PhBroadcastBody·PhGroupName) — 공지인데 창
  제목이 "Group"·플레이스홀더 "공지 내용" 한글 고정이던 것(스크린샷 실기).
- **프롬프트 제목 word-wrap + 창 높이 자동**(360→420 논리 폭) — 긴 영어 제목이
  창 폭에 잘리던 것: paint가 실측 단어 접기(무공백 CJK = 글자 폴백)로 여러 줄
  렌더 + `desired_height`(Cell) → 호스트가 첫 프레임 뒤 창 높이 1회 보정 ·
  relayout은 실측 title_h로 입력줄 y 산정(미실측 = 종전 한 줄과 동일).

게이트 전량 **731 green**.

---

## (8차) 저장 암호화 전수 실측 감사 — "지금 암호화 저장되고 있는가" (사용자 질문)

**방법** — 실기 GUI 데이터 2신원(`~/.nexa-beep-multi/A·B` — 실제 대화·파일 왕래
이력)+CLI 실기 산출물 전수: 파일 매직 검사 + `strings` 가독 평문 탐색.

**결과 = 수신·기록 축 전면 암호문** ✓
| 저장물 | 형식 | 판정 |
| --- | --- | --- |
| 대화 기록 1:1·그룹(`history/*.seg`) | NBSE | 암호문 ✓ |
| 오프라인 대기(`pending/*.seg`) | NBSE | 암호문 ✓ |
| 격리물+사이드카(`.beepq`/`.meta` — 64MiB 실파일) | NBSE | 암호문 ✓ |
| 핀·이름 이력(`trust.seg`) | NBTS v2 AEAD | 암호문 ✓ |
| 그룹 roster(`groups.seg`) | NBGS | 암호문 ✓ |
| PII 사이드카(`profile.sec`)·상대 사진 캐시(`.img`) | NBSE | 암호문 ✓ |
| strings 전수 | — | 가독 평문 0 ✓ |

**평문(의도된 경계)** — `settings.cfg`(설정값만 — PII 분리) · `identity.key`
(NBK1 — 계층의 루트·자기 봉인 불가) · `profiles/*.meta`(내장 아바타 키+보더색 —
비PII) · 로그(상태 문구·계수 — 봉투 원리).

**관찰 → TODO SEAL-2** — 본인 축 평문 3종(me.wire.png·custom 사본·클립보드
스테이징 clip-*.png 전송 후 잔존) = 수신 위협 모델 밖이나 정리/봉인 검토 가치.

**상태 갱신** — M2-5b 행 ☐→🚧(저장 암호화 실측 ✓ · 잔여 = **D-18 §4~ 문서 확정 ·
동기화 폴더 감지 경고 · 크립토 셰레딩** 3건) · [17 §4-a]에 감사 결과 박제.

**★ 사용자 실기 확인(8차 직후 — 26 §3-6 완주)** — ① **Enter 제출 후 목록 잔류 ✓**
(잔향 3겹 종결 — "전송+대화창 열기 동시" 재발 없음) ② 창 높이 자동 = 확인 불가
(한국어 제목은 한 줄이라 재현 안 됨) → **사용자 확정 완료 처리**(영어 UI 재현은
필요 시) ③ **등급 배지 정상 표시 ✓** ④ **공지 받지 않기 동작 ✓** ⑤ **대기 중
메시지 자동 전달 ✓**(오프라인 큐 합류점). ⇒ **공지 축 실기 잔여 없음** — 26 §3-6
1~6항 전부 확인 종료.

---

> ⚠️ **날짜 정정** — 이하 (9차)~(14차)는 오후 연속 세션분(원문 1차~6차). 당초 별도 파일(2026-08-22.md)로 오기됐다가 병합했다(실제 작업일 = 08-21 — 시스템 시계 실측).


## (9차) 잔여 업무 검증 — 유령 3건 발각·정정 + 범위 확정 2건 (사용자)

**유령 정정(낡은 기록이 잔여 업무 보고에 재등장)** — "문서 헤더·상세 행이 진실"의
재확인. 상위 표·요약이 뒤처져 있었다:
- **D-12(ADR-0004)** = 08-17 Accepted 완료 — TODO 결정 표만 🚧로 낡음(정정).
- ★**M4-5ⓒ 권한 강등 = 08-15 완료·R-5 해소** — 사용자 지적("별도 프로세스+권한
  제한으로 마무리했던 것 같은데")으로 코드 재확인: imgdec가 입력 전량 읽은 뒤
  파싱 전 자기 잠금(mac Seatbelt pure-computation · Linux seccomp-bpf 허용
  목록+no_new_privs · Win SetProcessMitigationPolicy 동적코드/원격이미지 금지 ·
  실패 = exit 4 fail-closed · CI 3-OS `tests/lockdown.rs` 실측). TODO 상세 행은
  ✅였고 리스크 표(R-5 ☐)·CLAUDE.md 요약 2곳("잔여 = 권한 강등 하나")이 낡음 —
  전부 정정. ⚠ Win 강등은 mac/Linux보다 얕다(완화 정책 2종 — win32k 락아웃은
  실기 후 강화 예정 · 코드 주석 명기).
- **M4-6 오프라인 큐** = 08-21 구현+실기 완료 — TODO 행 ☐ 낡음(정정).

**범위 확정(사용자)** — ① **M3-9 수동 "확인" 버튼 = 목표 제외**(설계 [24 §5]만
유지 · 요청 시 착수 — 전달/읽음 점 마크는 기구현·유지) ② **M5-4a 코드 서명 =
차후 보완**(인증서 이슈로 지금 불가 · 요청 시 진행) ③ (08-21 재확인) BL 차단
4축 = 나중에 추가 구현.

**교훈** — 완료 표기가 상세 행에만 남고 상위 표·요약이 낡는 유형이 사흘 새
3건(D-12·R-5·M4-6). 완료 커밋에 "상위 표·CLAUDE.md 동기"를 붙이는 규약(08-20
"색인 3곳 동기"의 일반화)을 지킬 것.

---

## (10차) 완전성 마감 1회전 — AMSI 실물·zip 파서·SEAL-2 (사용자 "완전성 마감 우선 구현")

### ★ AMSI 검사 어댑터 실물화 (`3fb34e0` · FR-S-15 — 수신 게이트 4단계 완성)

- 게이트의 검사 단계가 3-OS `Unavailable` 고정이던 것 → **Windows AMSI 실물**.
  `FileScanner` 포트 뒤라 **게이트 호출부는 한 줄도 안 바뀌었다**(DR-21 증명).
- `amsi.dll` **동적 로드**(정적 링크 대신 — 미설치/비활성 = 정직한 Unavailable
  폴백) · 문맥 1회 초기화(다중 스레드 허용) · 4GiB 초과 = 절단 검사 금지 ·
  관리자 차단 대역 = Detected 편입.
- ★**실측**(이 PC Defender): 무해 텍스트 = `Clean` · **EICAR = `Detected`**
  (문자열은 런타임 조립 — 소스/바이너리에 연속으로 실으면 백신이 저장소를
  격리하는 사고 · 표준 관행). CI 러너는 제공자 변동 허용(Unavailable = skip).
- 표기(사실 3종 · NFR-S-5): 격리함 행 라벨(탐지 = 위험색) · QMeta 꼬리 1바이트
  (전방 호환 회귀) · 수신 즉시 Detected = 상태바 "승인하지 마세요" · CLI 표기.

### ★ M4-4 zip 파서 어댑터 (`24781b5` — 해제 없는 중앙 디렉터리 점검)

- 08-09 정책 계층의 첫 실물: EOCD+중앙 디렉터리만 파싱(**inflate 부재 = 압축
  폭탄이 이 경로로 못 터진다**) → `check_archive`. fail-closed 전면(잘림·서명·
  ZIP64 v1 미지원·EOCD 선언 불일치·주석 속 미끼 서명) · 유닉스 심링크 인지.
- 게이트: Archive+zip이면 격리 시 점검 → 위반·판정 불가 사유 동봉 → 격리함
  "! 아카이브 위반"(4개 국어)·CLI 사유("해제 금지 권고"). 자동 해제는 원래 없다.
- 잔여 = 내용 목록 UI·zip64·실체화 시 로컬 대조(TODO 갱신).

### SEAL-2 종결 (`c5529ca`) + 유령 1건 추가 발각

- 클립보드 스테이징 = 부팅 24h 정리 · 내 사진 사본/축소본 = 의도 경계 확정.
- **M1-11(평문 회귀 CI 상시)도 이미 ✅(08-15)**였다 — 잔여 보고의 낡은 줄 4건째.

**게이트(3슬라이스 공통)** — **739 green**(+8: AMSI 3·zip 4·sweep 1) · clippy 0 ·
fmt 0 · rustdoc 0 · linux/mac check 0.

**남은 완전성 대상(후속 회전)** — M2-5b 잔여 2건(동기화 폴더 경고·크립토 셰레딩)
· M1-2/M1-9 발견 완결 · M3-19/14/20 배지·아이콘 배선 · M3-1e ① TextInput 코어 ·
Win 강등 강화(win32k) · M5-5 24h·M5-6 문서.

---

## (11차) 완전성 2회전 — 동기화 폴더 경고·M3-20 헤더 아이콘 (+유령 5·6호)

- ★**동기화 폴더 감지 경고**(`cc8b4bf` · M2-5b ①): 데이터 폴더가 OneDrive·
  Dropbox·Google Drive·iCloud·Nextcloud 안이면 부팅 1회 상태바 경고(4개 국어 —
  충돌·구버전 복원 위험 · 막지 않는다 DR-1) · 경로 구성요소+Win OneDrive env ·
  오탐 금지 회귀. **셰레딩은 결정 선행 보류** — 대화별 데이터 키 계층(17 §3·§7)
  = 저장 구조 변경이라 D-18 §4~ 확정 후가 맞다.
- ★**M3-20 큰 자리 연결 아이콘**(`5d255b3`): "붙일 자리가 없다"던 2층 —
  **대화 헤더 상태 줄 신설**(제목 왼쪽 20px · plug/plug-zap/cable/unplug ·
  상태색 틴트+캐시) · `peer_link_state` 단일 산출점 · 합류점 5곳(시도·성립
  아웃/인바운드·끊김·실패) 배선. 잔여 = 실기 육안 · 피어 카드/툴팁 확장.
- **유령 5·6호** — M3-19(배지 파냄)·M3-14(인증 배지 M3-14b Material) 둘 다
  **08-15 완료**였다(TODO 상세 행 ✅ · CLAUDE.md §5 요약만 낡음 — 정정은 마감 때).

**게이트** — 전량 green · clippy/fmt/rustdoc/타깃 check 0.

**남은 완전성 대상** — M3-1e ① TextInput 공용 코어 · M1-2/M1-9/M1-13 발견 완결 ·
Win 강등 강화(win32k) · M5-5 24h·M5-6 문서 · (D-18 §4~ 확정 후) 셰레딩.

---

## (12차) 완전성 3회전 — M1-9 Windows netif 실물 + imgdec win32k 락아웃

- ★**M1-9 Windows 인터페이스 열거**(`101b319`): 빈 목록 폴백이던 것 →
  `GetAdaptersAddresses` 직접(iphlpapi · WNET-1 문법 — 읽는 필드까지만 repr(C)
  부분 선언+`offset_of` 컴파일 타임 단언 · 실패 = 종전 폴백). **Windows도
  인터페이스별 멀티캐스트 지정 발신·S3 지향 브로드캐스트·S4 서브넷 자격 판정이
  실물**이 됐다(다중 NIC·링크로컬 직결 FR-D-7). 실측 = 실 IF 4개 열거·마스크/
  브로드캐스트 정확·루프백 자격 제외 ✓ · Win 친화 가상 이름 7종(vEthernet·
  OpenVPN·WireGuard 등) 추가 · 한계(정직) = "이더넷 N" 표기 VM host-only는 이름
  휴리스틱 밖(로컬 VM 브리지 한정 — 위협 낮음).
- ★**imgdec win32k 락아웃**(`badc5db` — 08-15 "실기 검증 후 강화" 예고분):
  SystemCallDisable(bit0) 추가 — 콘솔 I/O(condrv)+순수 연산뿐이라 win32k 불요 ·
  파서 관통 시 GUI 커널 표면(폰트/GDI 역사) 통째 폐쇄 · 베스트 에포트(실패 =
  기본 2종 유지 · 모드 문자열 가시화). **실측 = `…win32k-lockout` 성립 + 디코드
  정상** · 3-OS lockdown CI green. Windows 강등이 mac/Linux와 격차를 좁혔다.

**게이트** — **741 green**(+1) · clippy/fmt/rustdoc/타깃 check 0.

**남은 완전성 대상** — M3-1e ① TextInput 공용 코어(대형 리팩터) · M1-2 링크
구독 완결·M1-13 · M5-5 24h·M5-6 문서 · (D-18 §4~ 후) 셰레딩.

---

## (13차) 완전성 4회전 — 유령 7호 + M5-6 "알려진 한계" 문서 신설

- **유령 7호** — M3-1e ①(TextInput 공용 코어)도 **08-17 종료**였다(상태·로직
  공용화 완료 · 선택 페인트 미공용은 기하 상이 근거로 정당 종료 명시). **M1-2
  linkwatch도 3-OS 완비 확인**(Windows NotifyIpInterfaceChange 포함) — M1 코드
  축 잔여는 실기(ⓓ 매트릭스·E-3)와 M5-3c(D-19 후)뿐.
- ⇒ **완전성 마감의 코드 슬라이스 소진** — 남은 것은 실기(24h·2대)·결정
  (D-18/19/24)·그 후속(셰레딩·경로 축)뿐.
- ★**[40 알려진 한계](../40-known-limitations.md) 신설**(M5-6 골격 — D-24 검토
  자료 겸): "무엇을 막지 못하는가" 6축 SSOT — 신원(TOFU 첫 접촉·이름≠신원·
  R-18 재식별·차단 우회) · 저장(기기 키 한 점·물리 삭제 비보장·동기화 폴더) ·
  전달(서버리스 오프라인·점 없음≠미수신·발견 확률성) · 수신 안전(검사≠안전·
  zip64·실체화 후 OS 몫) · 노출(존재 평문 방송·휴리스틱·트래픽 패턴) · 플랫폼
  (무서명·강등 격차·IME). UI·위키·릴리스 노트가 여기서 가져다 쓴다.

---

## (14차) 세 결정 확정 + 크립토 셰레딩 구현 + 기본 아바타 무작위 (사용자)

**결정 3건 확정**(AskUserQuestion — 전부 권고안):
- **D-24**(커트라인) = 확정 · **스팸 방어(M5-3) = v1.0 필수**(BL 최소층 = 수동
  차단이라도 출시 전 · 착수 시점은 별도 지시) — docs/25 확정 표기.
- **D-18 §4~ 전체** = Accepted(ADR-0005 헤더) + **§7 셰레딩 구현**.
- **D-19** = Accepted(ADR-0006 헤더) + M5-3b/c 구현 착수(다음 회전).
- 색인 동기(08-20 교훈): README ADR 표·집계(✅13/📐1)·25행 · TODO 결정 표·
  크리티컬 패스.

**★ 크립토 셰레딩**(`b04af4a` · D-18 §7): 기록 봉인 키를 신원 파생 단일에서
**대화별 무작위 데이터 키**로 승격(`keytable.rs` — `keys.seg` 단일 원천 ·
신원 키로 봉인). **대화 삭제 = 키 폐기** → 세그 잔존 바이트가 복호 불가 난수.
개봉 = 데이터 키 우선 + 레거시(신원 파생) 폴백(마이그레이션은 쓰기 경로가
자연히 승격 · 구세그 삭제는 셰레딩 미보장 = 정직 한계). 백업 = keys.seg 동반 ·
복원 = 키 병합(백업 키 우선). 회귀 4종. **M2-5b 잔여 소진 = 저장 축 종결.**

**★ 기본 아바타 무작위**(`0a47c6b` · 사용자 요청): 12간지 중 CSPRNG 배정 →
즉시 저장(이후 실행 안정 · 08-14 우려는 저장 없는 랜덤 대상). 보더도 같은 시드.

**게이트** — **745 green**(+셰레딩 4) · clippy/fmt/rustdoc/타깃 check 0.

---

## (15차) M5-3b/c 1차 — 경로 2축 실물(PathClass·원격 파일 게이트·P-3) (`fea0355`)

D-19 확정의 예고분("확정과 함께 원격 정책 구현 착수") — DR-28 "신원 신뢰는
키에, 경로 등급은 경로에"의 실물 절반.

- **`PathClass{Local,Remote}`**(core `path.rs` 신설) — 등급은 광고가 아니라
  **성립한 세션의 실소켓 주소**가 정한다(§5-1-5 그대로): `Link::remote_ip`
  기본 메서드(+TcpLink `peer_addr`)를 핸드셰이크 **직전**에 읽어 세션과 동행
  (`InboundSession.path` → `Conversation.path`). 판정은 주소 대역뿐 —
  사설·링크로컬·ULA·v4-mapped = Local, 그 외 전부 Remote(fail-closed).
  트레이트 문서에 용도 제한 명문(등급 판정 전용 — 주소를 위로 흘리지 않는다).
- **§5-1-3 곱 행렬 = `file_allowed` 단일 함수**(core — 행렬 전수 회귀):
  원격은 **지문 대조만** 통과. TOFU 핀은 원격에서 "그 주소의 누군가"일 뿐.
  발신(offer_file — 모달 안내 "/verify 가 먼저")·수신(judge_offer 앞 즉시
  거절)이 **같은 판정 하나**를 부른다 — FR-S-24 ✅.
- **미등록 원격 인바운드 = 세션 드롭**(§6 — FR-S-25의 fail-closed 절반):
  LAN 밖 미지 키를 TOFU 자동 등록하면 공인망 스캐너가 목록에 스스로를
  심는다. 아는 상대(핀·대조)의 원격 인바운드는 통과. 요청 대기 승인 UX는
  후속 슬라이스.
- **P-3(R-20 핵심 축) ✅**: 수동 주소가 **다른 키**로 성립 = 그 주소 매핑
  즉시 삭제+상태바 고지 — 낡은 주소로 남에게 붙는 것을 막는다(08-13 래치
  버그의 정책판). P-1/P-2 = 기존 백오프 중단이 자동 시도 축을 이미 충족 ·
  P-6 = `manual_addrs` 비영속(v1)이라 해당 없음.
- 원격 성립 = 스레드 1줄 고지(NoticeRemotePath — *경로가 바뀌면 조용히*).
  i18n 6종 4개 언어. 잔여 = 헤더 상시 배지(M3-14 합류)·CLI 축 게이트·요청
  대기 UX.

**게이트** — **748 green**(+3: 곱 행렬·주소 판정 2) · clippy/fmt/rustdoc/
linux·mac check 0.

**부수 정정** — 오후 세션 전체가 날짜를 08-22로 오기(시스템 시계 실측 =
08-21) → 저널 병합 + 코드 주석·문서 20파일 일괄 치환.

---

## (16차) M5-3b 2차 — 원격 인바운드 요청 대기 UX(FR-S-25 완결) (`d5c3789`)

15차의 fail-closed 드롭을 §6 본안으로 승격 — **버리는 대신 사람이 결정한다**.

- 핸드셰이크 완료 세션을 `pending_remote` 슬롯(1개 — 모달 1개 규칙)에 보류 →
  기존 2택 모달(open_choice·AlertCtx 라우팅 재사용 — 새 UI 0)로 수락/거절.
  본문에 "수락해도 지문 대조 전에는 파일 차단" 명시(§5-1-3과 한 몸).
- 수락 = **`accept_inbound` 합류점 신설**(즉시 수락 경로와 승인 경로가 같은
  함수 — 정책 두 벌 금지) · 거절/X 닫기/키 불일치 = 드롭 = 소켓 닫힘(상대는
  Closed만 관측 — §2 규칙 4 "포트 스캔에 정보를 주지 않는다").
- 슬롯 점유 중 추가 원격 인바운드 = 드롭 유지 · X 닫기 = 거절 동치(응답 없는
  대기를 남기지 않는다 — Approve 창 관례와 동일).

**게이트** — 748 green · clippy 0(`PendingRemote` alias) · fmt/rustdoc/타깃 0.
**M5-3b 잔여** = CLI 축 게이트 · 실기(공인망 2대 — 대행 불가). **M5-3c 잔여** =
헤더 상시 배지(M3-14 합류).

---

## (17차) M5-3b 3차 — CLI 축 원격 파일 게이트 = 경로 2축 코드 완결 (`00a3463`)

- CLI는 SAS 대조 축이 없다 → 신뢰 상한 = Pinned → §5-1-3 곱에 따라 **인터넷
  경유 채널은 파일 양방향 차단**(메시지 허용). 판정은 GUI와 **같은 함수**
  (`file_allowed`) — 정책 두 벌 금지 유지.
- 성립 지점 2곳(--chat·chat-live /connect)에서 핸드셰이크 전 실주소 판정 →
  `run_interactive(… path)` 동행 · 시작 1줄 고지 · 수신 Offer 즉시 Reject ·
  /send 시도 0건+안내 · 수신 전용 채널은 원래 전면 거절(무변경).
- localhost 스모크 = Local 판정이라 기존 왕복 무회귀.

**게이트** — 748 green · clippy/fmt/타깃 check 0.
**M5-3b/c 코드 축 완결** — 잔여 = 공인망 2대 실기(대행 불가) · 헤더 상시 배지
(M3-14 합류) · 홀펀칭·릴레이는 X-UDP/X-1 별개 축.
