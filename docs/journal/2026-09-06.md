# 2026-09-06

> 시간 역순(최신이 위).

## (mac · 브랜치) X-13 S1 구현 완료 — 형제 세션(UserHint · psk 랑데부 · XXpsk3 배선 · 세션 내 증명 승격 · 재래핑)(3커밋)

같은 핸들+페어링 암호를 가진 내 다른 PC를 **수동 검증 없이** 형제로 판정한다(ADR-0015 §3-3~§3-5). 실기 = mac 2인스턴스(idE 47221 / idF 47222 · 핸들 `kiros33` · 같은 암호) + 설치본(미인증 · 대조군).

| 커밋 | 내용 | 실측 |
|---|---|---|
| `6e4dc71` S1-a | `NoiseSession::initiate_psk` / `accept_any`(msg1을 psk 상태로 파싱 → AEAD 태그+`NBPSK1` 마커로 XX/XXpsk3 판별 · 왕복 0) | psk 회귀 5(불일치 = 응답자 msg3 실패) |
| `a31a735` S1-e 재료 | `Session::handshake_binding()`(hh · 개시자) · `session_proof = HMAC(PSK, "nbeep-user-proof-v1"‖hh‖role)` · `proof_eq` 상수 시간 · `UserProof` Control 태그 11 | 회귀 2(역할·해시·PSK 어느 하나 달라도 다르다) |
| `0b1a324` S1-b~e | net `PacketKind::UserHint`(LAN_tag hex · Announce마다 ≈70B · `set_user_tag` 즉시 1발) · relay `attach(.., extra_rids)` 페어 RID 3개·`connect_via(.., psk)`·`connect_via_rids_first` · app `psk_shared`+IP 백오프(5회/60s → 10분) · `psk_for` → XXpsk3(실패 = XX 1회 폴백) · 힌트 **원문 보관**+조회 시 일치 · `seed_reconnect` 힌트 유예 1.5s · `siblings`(세션 수명) · `StfConnectedOwnOpen` · 세션 내 증명 3지점 송신/대조/회신 · `derive_and_load(.., held)` 재래핑 · `user_boot` 사이드카 뒤 · `NEXA_USER_TRACE` | 아래 3경로 |

**실기 3경로(`NEXA_USER_TRACE=1` · 지문 앞자리·참/거짓만)**
1. **첫 접촉 승격** — E 기동 → F 기동. E는 F 광고 직후 연결(F의 PBKDF2 워커가 **debug 빌드에서 ~6s** — 힌트 1.5s 유예로는 부족)하여 XX 성립 → F 워커 종료 후 E 힌트 일치 → F가 `proof sent`(force=false) → E `proof from 2ef6 verified` → `session via_psk=true` → E 회신 → F `verified`. **재접속 0회로 양쪽 형제 확정.**
2. **재기동 재연결** — F 재기동 → F가 E 힌트를 쥔 채 `connect ba25 psk=true` → `session via_psk=true`(핸드셰이크에서 곧장 XXpsk3). 설치본(미인증)은 전 구간 `psk=false`(대조군 — 빈 사용자 묶임 없음).
3. 상태 문구 = "내 기기 연결됨(인증): Bob — 목록에서 열기"(종전엔 `StfConnectedOpen`이 덮어 확인 불가 → `connected_msg` 단일 지점).

**발견·판단**
- **부팅 경합의 정체**: 광고(Announce)와 힌트는 별 패킷(≈1ms 차)이고, 힌트는 인증 워커가 끝나야 나간다. 종전 `on_user_hint`가 "재료 없으면 버림"이라 E가 F보다 먼저 떴을 때 F의 힌트를 전부 놓쳤다 → **힌트는 원문 보관·일치는 조회 시점**. 유예만으로는 워커 지연(debug 6s · release ~0.1s)을 못 덮는다 → **세션 내 증명**이 근본 처방(재접속 없음 · 기능을 나중에 켜도 즉시).
- **증명의 노출 등급** = LAN_tag와 동일(둘 다 KP 파생 HMAC · 힌트 후보에게만 송신) — 오프라인 추측 면은 태그가 이미 지는 것과 같다(강도 등급·60k이 방어선). 회신은 검증된 상대에게만이라 무조건.
- `Closed` 핸들러는 peer 단위라 **세션 교체(재다이얼 승격)는 새 세션을 죽인다** → 채택 안 함(증명 프레임으로 우회). S2 이후에도 같은 이유로 "승격 = 세션 안에서".
- clippy `large_enum_variant`: NoiseSession +33B로 CLI `LiveConn::Via`가 걸림 → Box.

게이트 = fmt·clippy(-D warnings)·test·rustdoc 4종 ✓. 다음 = **S2**(형제 = 신뢰 해제 `TrustStore::level/on_session`·`judge_offer`·`file_allowed`·FR-S-25 · 서명 UserHello 기기 목록·Succession·정직한 충돌 · UserKey 블롭 동기 · "내 기기" 배지(사용자 요청: 구조 유지·색으로 식별) · UserId 변경 = 별도 PC 강등 · 핸들 충돌 표시).

## (mac) [46 ADR-0015] UserId 기반 사용자 관리 설계 — Handle + Passphrase · 브랜치 `feat/userid-handle`

사용자: *"브랜치 추가 · UserId 기반 사용자 관리 · 개발 전에 방향 정리 · clip의 Handle/Passphrase처럼 두 값이 같으면 같은 사용자 · 같은 사용자면 승인/Verify 없이 · 메시지 동기화 · 릴레이까지는 Peer 단위 · 컨텐츠 모드는 일반 메신저처럼 · 난이도 평가"*.

### 조사(에이전트 2 · 요약)
- **clip**: `RID = PBKDF2-HMAC-SHA256(passphrase, "nclip-rid-v1"‖handle‖day, 60k)[..16]`(`nclip-sync/src/rid.rs:78`) — 패스프레이즈는 **만남 지점 파생만**, Noise에 안 들어감(PSK 0건), "같은 사용자" 판정 = **기기별 수동 승인**(6자리 육안 · `devices.rs:102`). DeviceList 서명은 문서만(T-24 ☐). `sync.passphrase`는 `settings.cfg` **평문**. 동기화 = 오프라인 큐 없음 · 도착순 LWW · 밀린 항목 최신 1개.
- **beep**: `UserId`는 타입 자리만(사용처 0) · **V1-1 미반영**(문서 32 §8 P-1이 "반영"이라 오기 — 스레드·기록·차단 전부 PeerId) · V1-2·3·4 실물 · `trust.level(peer)` 직접 호출 12곳 → `TrustStore::level` 한 곳에서 10곳 흡수 가능 · beepd RID 1:1(공유 URID 불가 · clip과 같은 우회) · Control 태그 4~9 비어 있음 · `ProfileMsg::Info` flags bit7 여유 · `snow 0.9.6` psk 수식자 지원.

### 설계 요지([46](../46-adr-0015-userid-handle-passphrase.md))
- 만남은 clip 식 그대로(도메인 `nbeep-user-*` 분리) · **신뢰는 Noise XXpsk3**(PSK = KDF(passphrase)) — 승인 0회의 근거를 사람의 습관이 아니라 핸드셰이크 성립에 둔다. 오프라인 사전 공격 불가(온라인 추측만) + 인바운드 실패 백오프.
- `UserId = SHA-256("nbeep-user-id-v1"‖handle‖KP)` — 두 값이 같으면 같은 사용자. ADR-0007 서명 체인은 v2에 `user_tag`로 얹는다.
- 제약 해제 = "사람을 믿느냐"만(TOFU·SAS·파일 승인·원격 대기·알림) · "파일을 믿느냐"(격리·무해화·기기별 실체화)는 유지(DR-13).
- 동기화 = sender copy(V1-2 집합 확장) + **따라잡기**(같은 사용자 기기 간 `SyncPull` 200줄 · append-only · 저장 키는 각 기기 것) · 컨텐츠 모드 = [32 §3-8] 동기 저장소 + 사용자 마스터 키(래핑 재검토 조건 발동 · 봉투 ①) · 파일 1회 업로드 + 보관함 온라인.
- 난이도: S0 하 · S1~S4 중(약 2주 · 서버 무변경) · S5 대(2주+). 🔴 **D-32 7문항**(권고안 굵게).
- 회신 예정(clip): 패스프레이즈 평문 저장 · 도메인 등재 · XXpsk3 채택.

### 후속 검토(사용자 질문 — 선점·변경·연속성) → [47 검토 보고서](../47-adr-0015-review-userid-continuity.md)
- 원안 `UserId = H(handle‖KP)`의 결함: 암호 변경(정상 행위)이 신원을 파괴 · UserId가 공개값이라 서버 가명 이름공간 오염(P-3) 가능.
- 개정 = **3층**: PeerId 앵커 / 무작위 **UserKey**(전 기기 복제 · K_wrap_user 봉인) → `UserId = H(pub)` 불변 / 핸들+암호 = 문 열쇠(PSK·RID·래핑). 선점 = 구조적 불가(등록소 없음 · 서버 쓰기 = 서명 챌린지) · 변경 = **Succession**(옛 키 서명 + 핀된 PeerId 제시 + version) · 폐기 경쟁(전 기기 복제의 대가) = **정직한 충돌 표시**(자동 승자 없음 · v2 주 기기 승격 경로 유지). 의존 +1(`ed25519-dalek`) · S0/S2 +1일씩 · D-32-8·9 추가.

### 결정 상세·영향·흐름도 → [48](../48-adr-0015-decisions-impact-flows.md)
- 추가 조사: snow `Builder::psk` · psk 패턴은 `e` 뒤 MixKey(e.pub) → **msg1 payload AEAD 태그로 XX/XXpsk3 판별(왕복 0)** · `curve25519-dalek 4.1.3` 기존 → `ed25519-dalek 2` 공유(3.0은 중복) · 발견 패킷 길이 엄격(`wire.rs:138`) → LAN 힌트는 별도 패킷 · 힌트 없으면 XX→UserHello 주장→XXpsk3 재세션 폴백.
- Mermaid 9(부트스트랩·판별·제3자 접기·폴백·sender copy·따라잡기·암호 변경 vs 분실·컨텐츠 모드·상태도) · 결정 9 영향 표 · 성능(예산 대조) · 보안(불변식·새 공격면 T-1~T-10·약/강) · 실측 T-1~T-5 · 사용자 질문 4(D-32-3·4/5·6/7·8).

### ✅ D-32 확정(사용자 답변 4 · 전부 권고안) → 46 ✅ Accepted · DR-29
- D-32-8 전 기기 복제 · D-32-3 사람 판정 전부 해제 · D-32-4/5 copy+따라잡기 200줄·컨텐츠는 실기 후 · D-32-6/7 봉인 저장+경고만. 46 본문에 3층 개정 반영(§3-1 파생식·§3-5 후계·§3-6 `user.key`·§5-3 서명 UserHello/Succession/UserKeyBlob·§6-4 서명 챌린지·§7 S0 2일/S2 3일) · [10 DR-29] 등재 · X-13 착수 가능.
- 부수(main): 설정 행 노트 배치 수정(`97674b8` — 노트가 다음 설정에 가까워 보이던 것 · clip 09-03 동일) → 브랜치에 병합.

## (mac · 브랜치) X-13 S0 구현 완료 — 신원 재료·UserKey·설정 › 사용자(3커밋)

사용자 지시 *"설계/기능 단위로 commit 진행하면서 개발"* + 진행 중 확정 규칙 6건(필수 값 없으면 기능·설정 불가 · 테스트 버튼은 값이 비거나 바뀌면 비활성 · 성공 = 즉시 인증/실패 = 기능 중지 · 성공 정보 비노출 보관 · 순차 입력·정확도 게이트 · 자동 인증 시각 식별은 S2) + 설계 질문 답(미입력이 LAN을 막지 않는다 · **스위치 + 기본값 자동 배정** · 빈 사용자로 묶임 = KP 파생이 빈 입력을 거부해 구조적 차단 · 핸들 선점 불가 · 동일 UserKey = 자동 검증/UserId 변경 = 별도 PC 강등은 S2).

| 커밋 | 내용 | 실측 |
|---|---|---|
| `073e4b1` S0-a | `nbeep-crypto::userkey` — KP(PBKDF2 60k · sha2 직접 · clip 이식) → PSK·RID_pair·LAN_tag·K_wrap_user · UserKey(Ed25519 · `ed25519-dalek 2` · dalek 4 공유 트리 중복 0) · `UserId=H(pub)` · 빈 입력 = None | 회귀 7 · RFC 벡터 |
| `7f4a8b2` S0-b | 설정 › 사용자(스위치 기본 off · 핸들 · 암호 · 인증) · `userident.rs` 순수 상태기계(검증·강도·추천·Unconfigured/Untested/Testing/Verified/Failed · derive_and_load 봉인 열기/생성) · 워커(60k) · 노트·잠금 깔때기 · PII 사이드카 · `user.key` 0600 · `--whoami` | 스위치 on → Alice · 자동 인증 ✓ · cfg에 암호 0건 |
| (S0-c) | 비밀 행 = `SettingKind::Text{secret}` · `•` 마스킹 · [생성][눈] 아이콘(clip 자산) · 2초 무장 · 생성 = 자동 표시 + 값 변경 규칙 | 눈 → 평문(accent) · 무장 빨강 → 새 암호 · "값이 바뀌었습니다" 노트 |

부수 = `SettingsWidget::set_value`가 Face(Text) 역반영 · `settings_view_set` 헬퍼(프로그램 변경 → 설정 창). 다음 = **S1**(XXpsk3 세션 · msg1 AEAD 판별 · 힌트 패킷 · RID_pair 등록 · 폴백 · 실패 백오프).
