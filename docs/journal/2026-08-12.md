# 2026-08-12 · 작업 기록 (시간 역순)

> 일자 단위 기록. **최신이 위**. 요약 [DEVLOG](../DEVLOG.md)·[MILESTONES](../MILESTONES.md).

## Windows 전체 빌드·실기 기동 점검 — GUI+테스트단말 · RSS 관측 (이 Windows PC)

코드 변경 없는 점검 트랜잭션. 동기(fetch — **신규 없음**, local == origin/main ==
`163572c` · clean) → 전체 빌드 → [26 §2 시나리오 A](../26-run-and-manual-test.md)
기동까지.

**결과(실측)**

| 항목 | 결과 |
| --- | --- |
| 빌드 | 워크스페이스 디버그 green(exit 0) · 릴리스 `nexa-beep`+`nbeep-imgdec` green(캐시 히트 5.5s) |
| 바이너리 | `nexa-beep.exe` 1,356,288B · `nbeep-imgdec.exe` 362,496B(v0.1.4 CI 동봉분과 동일 크기) |
| 기동 | GUI `--window --live`(PID 19300) + 테스트단말 `--chat-live 테스트단말`(PID 16068) — 둘 다 별도 창·생존 유지 |
| ★ RSS 관측 | 기동 십수 분 후 WorkingSet — **GUI 17.1MB · 테스트단말 8.5MB**(Private 4.1/1.7MB · 핸들 226/145) |

RSS 관측치는 **대화·전송 부하 없이 발견만 도는 상태**의 값 — NFR-B-1(유휴 ≤30MB)의
정식 유휴 프로토콜 실측은 아니지만, **Windows 릴리스 빌드가 예산 안쪽**임을 처음
관측했다(mac 85.7MB 관측과 대비 — R-8의 남은 축은 mac 재실측).

⏸ 실기 계속(사용자 육안 몫): 이미지 `/send` 미리보기 · 프로필 왕복 · 2-PC 핀 유지.

## v0.1.4 릴리스 — 프로필·아바타·imgdec·영속 일습 · imgdec 동봉 첫 CI 검증 (main 직커밋 + 태그)

08-11 하루치(STATUS 9~18차 + 08-12 1차)를 내보냈다. **imgdec 5타깃 동봉의 첫 실전
검증을 겸한다**(M4-5ⓐ는 로컬 NSIS 부재로 CI에서만 검증 가능했다).

**실린 것** — M3-15 설정 영속(D-25) · M2-5a 신원·핀 영속+백업/복원(D-18 §3 ·
R-17 해소) · M1-10 표시 이름 정제(R-19 해소) · M3-17 프로필 일습(화면·공개 토글·
교환 와이어·목록 아바타·프로필 보기 카드) · M4-5 imgdec(아바타 실사진·수신 이미지
미리보기·5타깃 동봉) · M4-3 Win MotW · Switch·컨트롤 크기 옵션 · desc 워드랩.

**결과(실측)**

| 항목 | 결과 |
| --- | --- |
| 파이프라인 | run 31505303269 — 5타깃 패키지·publish·homebrew 성공(자산 14종) |
| ★ **imgdec 동봉** | windows-x64 포터블 zip에 **`nbeep-imgdec.exe` 362,496B 실재** — M4-5ⓐ CI 검증 통과 |
| 체크섬 | 포터블 zip SHA-256 = SHA256SUMS.txt 일치 |
| 로컬 | 472 green · `--version` 0.1.4 |

winget/choco는 여전히 변수 잠금(사용자 보류 결정 유지). brew는 자동 반영(맥 재검증은
mac PC 몫).

---

(이전 항목: [2026-08-11](2026-08-11.md) — M4-5ⓑ 수신 이미지 미리보기가 자정에 걸쳐
08-11 파일 최상단에 있다.)
