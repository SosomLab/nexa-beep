# 2026-08-22 (3차 · mac) — ★ X-2b GUI Managed 배선 완결 + musl 정적 전환 + 채널 판정

> "relay 서버 개발 계속 진행" — 서버 축 잔여 중 X-2b(GUI Managed 배선)가 다음
> 슬라이스였다(X-UDP-e LAN 통합과 한 몸). 커밋 7개(`bd1ae07`~`c9a7cdb`) + 선행
> 정리(`9cd7671` — 타 세션 미커밋 문서 최신화의 충돌 병합·stash 정리).

## ① X-2b GUI Managed 배선 — 4슬라이스 (설정이 처음으로 네트워킹을 움직인다)

- **접속 정책 공용화**(`bd1ae07` · nbeep-relay): CLI에 있던 주소 해석·핀 TOFU·등록을
  `nbeep_relay::attach`로 올렸다(정책 두 벌 금지) + `RelayClient::is_alive`(액터 명령
  채널 생존 = 서버 세션 사망 감지 · `Cmd::Nop`). CLI `attach_server`는 프린터로.
  e2e `attach_pin_tofu_and_liveness`(첫 핀 → 재접속 일치 → **변조 = PinMismatch 중단**
  → 서버 종료 = is_alive false). ⚠ 부수 발각: e2e `test_server` 폴더가 (pid,
  subsec_nanos)로 유일하지 않아 8병렬에서 AlreadyExists 충돌 — pid+원자 카운터로
  (08-21 dir_writable 교훈과 같은 유형).
- **접속 수명 관리**(`b31454f` · app): `server_tick`(about_to_wait · 2s 페이스) —
  목표(설정 `net.server.*`) 대비 수렴 **단일 경로**. 백오프 5s/15s/60s/**300s 상한
  반복**(상대 재연결 ⓑ와 달리 중단 없음 — 서버는 명시 등록 인프라). ★핀 불일치 =
  경고 모달 + 자동 재시도 정지(재개 = 핀 줄 삭제 후 설정 재저장 —
  `server_settings_changed`가 푼다). 설정 hot-swap = 접속 내려놓고 틱이 새 목표로 ·
  세대(gen) 가드가 낡은 워커 성공을 폐기. i18n 10종 · 회귀 2종.
- **인바운드 수락 루프**(`2bd6e05`): `accept_incoming` → `accept_via` 사다리 →
  **`AppEvent::Inbound` 합류**(TOFU 판정·원격 요청 대기 FR-S-25·중복 가드·경로 등급
  전부 기존 경로 그대로 = 새 정책 0 · DR-21). 수명 = 클라 Arc(Weak 승격 실패 =
  루프 종료 — 해제·재접속 시 스레드가 스스로 끝난다).
- **아웃바운드 사다리 + 지문 연결**(`d9711df`): `start_connect` 3단 — 발견 → 수동
  주소 → ★서버 랑데부(`connect_via` 펀치→릴레이). 주소 모달(⌘/Ctrl+K)이 **64자리
  지문**을 받는다(위젯 = 어휘 판정·포트 미부착 · 해석 = 호스트 `parse_peer_hex` ·
  CLI `/connect <지문>` 미러). 자동 재연결 reachable에 서버 사다리 포함.

## ② 스모크가 발각한 결함 2건 (둘 다 즉시 수정)

- ★**자유 입력형 설정이 재시작마다 증발**(`3bf946d` · 08-17부터 잠복):
  `net.server.address`(RadioInput 무옵션)는 `default_values()`가 빈 목록 → 값 맵에
  키 자체가 없음 → 파일 로드(set_by_name)가 **미지 키로 흘림** = 화면에서 저장한
  주소가 부팅마다 빈 값으로. 스모크 1차(붙지 않음 · 트레이스 `addr=""`)가 발각.
  옵션 없는 RadioInput도 빈 기본값으로 키 등록 + 회귀. **키 등록과 값 저장은
  언제나 쌍**(08-14 HIDDEN_KEYS 영속 구멍과 같은 유형 — 세 번째 발병이다).
- **--whoami에 전체 지문 없음**(`6ecf4a1`): 서버 랑데부 대상 값은 64hex 전체인데
  짧은 8자리만 출력 — `full =` 줄 추가(X-2c P0의 진단 축 누락).

## ③ 로컬 3프로세스 스모크 — GUI가 설정만으로 서버에 붙는다 (실측)

beepd(127.0.0.1:47391) + GUI A(포터블 · settings.cfg에 managed 시드) + CLI B.
- A: 부팅만으로 접속 — `server.pin` TOFU 저장 ✓(서버 실키 일치) · beepd에 RID 3개 등록 ✓.
- B: `--chat-connect-via <A 전체지문>` → **"경로: UDP 직결(홀펀칭)"** 성립 → 메시지 발신.
- A 상태 로그(`log.enabled=on`): `First contact with server — pinned` →
  `Connected: beep-4adf5788` → **`New message (unread 1)`** — 설정→attach→핀→랑데부→
  펀치→종단 Noise→GUI 대화 도달 **전 구간 관통**. beepd 로그 = 봉투만(conn#·ch# 성립).
- 잔여 실기 = GUI 쪽 발신 육안(모달 지문 입력·사다리 폴백) · 실 NAT(대행 불가).

## ④ musl 정적 전환 (`c9a7cdb` · 사용자 확정 "musl 정적 빌드로 전환")

- beepd는 외부 의존 0이라 **rust-lld만으로** 어느 호스트에서든 musl 교차 링크
  (mac 실측: x64 **static-pie 651KB** · arm64 static — C 툴체인·musl-gcc 불요).
- `.cargo/config.toml` 신설(musl 2타깃 linker=rust-lld — 규칙 한 곳) ·
  release-server.yml linux 2타깃 gnu→musl(+정적 링크 검증 단계 — `file` 판정,
  동적이면 실패 · ARM 러너 불요) · beepd-cloud 킷 = **바이너리 업로드 기본**(VM
  셋업 수 초 · rustup 소멸 · `-FromSource` 폴백 유지).

## ⑤ winget·choco 진행 판정 → 이번 릴리스 제외 (사용자 지시)

- winget PR #421961/#421967 = **OPEN + `Validation-Executable-Error` 라벨**(파이프라인
  통과했으나 실행 파일 검증 플래그 · New-Package 수동 검수 대기) · choco = 승인
  목록(OData)에 없음 = **모더레이션 미통과**. → 저장소 변수 `WINGET_PUBLISH`·
  `CHOCO_PUSH` **false**(설계된 스위치 — 코드 무변경 · 첫 제출 검수가 끝나기 전
  새 버전을 얹지 않는다).

## 게이트·남긴 것

- **781 green**(+4) · fmt·clippy -D·rustdoc 0 · relay e2e 8종.
- X-2b 잔여 = 서버 타입 협상(auto — 서버 타입 통지 와이어 후속 · MVP는 relay
  단일이라 실질 auto=relay) · Registered LAN 억제(타입 축과 함께).
- 다음 = v0.2.3 릴리스(winget/choco 제외 · brew/Releases만) — 사용자 지시.

---
# 2026-08-22 (2차) — 클라우드 단발 실측 킷(tools/beepd-cloud) — 실 NAT 실기의 발판

> 사용자 문의 연쇄: "Cloud Run 배포 가능?" → 부적합 판정(원시 TCP·UDP 미지원 · 다중
> 인스턴스가 메모리 상태를 가름) → "VM은 IPv4 유료라 유지 불가" → **단발 세션**으로
> 방향 확정(실 NAT 실측은 상시 서비스가 아니라 단발 측정 · 세션 몇 시간 = 수백 원 미만
> · 상시 무료가 필요해지면 Oracle Always Free = 공인 IPv4 무료·linux-arm64 자산 그대로).

- **킷 구성**: `deploy-gcp.ps1`(방화벽 tcp/udp 47300 → VM(서울 e2-small 기본) → 소스
  tarball 업로드 → 셋업 → 핀 출력 → `-Teardown` 철거까지) + `vm-setup.sh`(저메모리
  스왑 · rustup 최소 · `-p nexa-beepd`만 빌드 · systemd 비루트 상주) + README(체크리스트
  6항 — 같은 NAT 헤어핀 / 다른 NAT 펀치 / Symmetric 폴백 / 봉투 로그 / 재배포 핀 경고 /
  원격 파일 게이트).
- **소스 빌드인 이유(정직하게)**: 이 PC에 gcloud·Docker·WSL 전무 → 직접 배포 불가 +
  리눅스 교차 빌드 수단 없음. 검증 전 브랜치라 CI 자산도 없다(push 전) → `git archive
  HEAD`(2.3MB · Cargo.lock 포함 재현 빌드)를 VM에서 빌드가 가장 곧은 길. 병합·태그
  후엔 release-server.yml 자산 다운로드로 대체.
- **함정 선처리**: rust-toolchain.toml이 5타깃을 강제 설치(CI 재현용) → VM에선 제거 후
  기본 stable(호스트 타깃만). e2-micro(1GB) 대비 스왑 2G 자동.
- 검증: PS1/sh 구문 파스 ✓ · tarball 드라이런 ✓(2.3MB·필요 크레이트 포함). **실 실행은
  gcloud 인증이 있는 자리에서**(사용자) — 킷은 그 절차를 스크립트 1개로 줄인 것.

---
# 2026-08-22 — ★ 사다리(X-UDP-e 원격 구간) + CLI Managed(X-2c P0·P1·P2) — 서버 축 2차

> "다음 진행" — 08-21 축 개통의 잔여 중 설계 순서([32 §13-4])상 첫 슬라이스.
> 같은 브랜치(`feat/udp-relay-server`) 커밋 3(`6597fd7`·`bbc665a`+docs).

## ① X-UDP-e 원격 사다리 — `connect_via`/`accept_via` (nbeep-relay)

- **여는 쪽** `connect_via`: 랑데부(에폭 RID 오늘→어제→내일) → **홀펀칭 시도** →
  실패 = **릴레이 폴백**. 각 단이 독립 핸드셰이크라 단 사이 오염이 없다.
  인증 키 ≠ 지정 지문 = `WrongPeer` 폐기(RID는 힌트 — 근거는 암호학적 증거).
- **받는 쪽** `accept_via`: 펀치를 병행하며 **첫 프레임이 온 링크**로 종단 수락 —
  여는 쪽이 어느 단을 골랐든 따라간다(`PrefixedLink`가 첫 프레임을 재생).
- **구조 변경 = 지연 수락** — 액터의 자동 Accept를 소비자 호출로 미뤘다. 이유:
  수락 **전에** UDP 프로브를 마쳐야 서버 OpenResult에 내 신선한 관측 엔드포인트가
  실린다(프로토콜 순서가 자연히 보장 — Open 전 프로브/수락 전 프로브). **펀치
  소켓 = 관측 소켓, 시도마다 신선하게** — 소켓 공유의 recv 경합(두 링크가 서로의
  데이터그램을 훔침)을 원천 차단. 서버는 미수락 CloseCh를 열기 거절(2)로 즉시
  알린다(여는 쪽 타임아웃 대기 제거).
- 부수: `pinfile`(서버 TOFU 핀 영속 — v1 텍스트·원자적 교체·손상 줄 = 핀 부재) ·
  `parse_peer_hex`/`peer_hex`(지문 교환 단위 = 64hex 전체 — 짧은 지문으론 키 복원
  불가) · `incoming_rx` Mutex화(수락 스레드 공유 — Receiver !Sync 실측).
- 검증: 사다리 e2e 2종 — 루프백 = **UDP 단**(양쪽 `taken` 일치) · 받는 쪽 무펀치 =
  관측 부재 → 여는 쪽 조용히 **릴레이 단**.

## ② X-2c CLI Managed — `--server`·`--chat-connect-via`·`/connect <지문>` (P0+P1+P2)

- **P0 신원 영속**: `--identity <파일>`(기본 `data/identity.key` — GUI와 같은 노드
  DR-18). 서버 사용 = 영속 강제(RID·핀이 키에 묶임) · 시작 시 **내 지문 64hex**
  출력. 서버 없인 기존 임시 신원 그대로(3-신원 실기 격리 불변).
- **P1 서버 등록**: `--chat-live --server <주소>` — 핀 규약(`data/server.pin` · 첫
  접속 TOFU 저장 · **불일치 = 접속 중단** + 파일 경로 안내 = 재핀은 사람의 결정) ·
  접속 실패 = LAN만으로 계속(S-2). 인바운드 랑데부 = 기존 **수신 전용 채널** 합류
  (`accept_via` — 08-20 구조 재사용 · 파일 거절·대화 미진입 그대로).
- **P1+P2 발신**: `/connect <64hex지문>`(대기 중) · `--chat-connect-via`(독립 단말)
  — `connect_via` 사다리 → `run_interactive` **무변경 합류**(경로 등급 = 성립 링크
  실주소 → CLI 원격 파일 게이트 곱 판정 그대로).
- 호스트명 서버 주소 = DNS 해석(DDNS 경로 — 정규화 실패 시 폴백).

## ③ 실물 스모크(3프로세스 자동 · 파이프 stdin) — 전 구간 실측

서버 기동 → A(`--chat-live --server`) **첫 접속 핀 저장** → B(`--chat-connect-via`)
**핀 일치** → ★**UDP 직결(홀펀칭) 성립**("경로: UDP 직결" 1줄) → A에 `[릴레이]
인바운드 성립(UDP 직결)` 수신 전용 → 메시지 왕복 + **전달/읽음 확인 + 프로필
프리페치까지 기존 스택 무변경 동작**(DR-21 — mux·ack·프로필이 사다리 위에서 그대로)
→ 서버 로그 = 봉투만(conn#·rid 앞 4B·ch#). 절차 = [26 §3-7](../26-run-and-manual-test.md).

## 게이트·남긴 것

- **777 green**(+3) · clippy·rustdoc·fmt ✓ · 교차 3타깃 check ✓.
- 잔여: **X-2b GUI Managed 배선**(`net.server.*` 설정 소비·발견 목록 증강·GUI 핀
  UX) · 실 NAT 홀펀칭 실기(공유기 2대 — 대행 불가) · 인바운드 랑데부 처리 직렬
  1스레드(동시 다수 인바운드는 순차 — 개선 여지) · UDP 관측 프로브 평문(토큰
  스푸핑 = 가용성 축 — 08-21 기록 유지).

---
