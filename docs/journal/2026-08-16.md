# 2026-08-16

> 시간 역순(최신이 위).

## 대화창 명령 `/…` — GUI 도입 + 판정 단일화 (main · 직전 세션 완성분 정리 커밋)

> 직전 세션이 완성해 두고 커밋하지 못한 작업분을 검증 후 정리 커밋한다
> (651 green · clippy 0 · rustdoc 0 · TODO 스텁 없음 — 08-12 전례).

**무엇** — CLI 대화 모드에만 있던 `/명령`을 GUI 대화창(1:1·그룹)에 도입하되,
**판정을 `nbeep_core::command` 한 곳**에 모았다(부르는 쪽이 각자 문자열을 뜯으면
"어디서는 명령·어디서는 메시지"가 된다).

- **규칙 3종**: ① 원본 첫 글자 `/` + **한 줄** + 아는 이름 = 명령(상대에게 안
  나감 · 앞 공백 있으면 메시지 · 줄바꿈 있으면 메시지 — 멀티라인 뒷줄 증발 방지)
  ② `//` = escape(한 꺼풀 벗겨 메시지로 — 이게 없으면 `/`로 시작하는 문장을 영영
  못 보낸다) ③ **미지 `/이름` = 오류**(오타를 상대에게 쏘지 않는다 — fail-closed).
- **명령은 로컬** — 어떤 것도 와이어로 나가지 않는다.
- **GUI 명령 4종**: `/help`(안내) · `/trust`(신뢰 상태 — 미검증/고정/지문 대조
  라벨) · `/verify`(SAS 안전 번호 카드 열기 — 승격은 사람 몫) · `/close`(뷰 닫기).
  결과는 로컬 공지 줄(`push_chat_notice`)로 스레드에 표시.

**검증** — 651 green(명령 규칙 테스트 포함) · clippy 0 · rustdoc 0.
linkwatch 문서 링크 수정 동반(조건부 의존이라 intra-doc 링크 금지 — Windows
rustdoc red 실측의 처방).
