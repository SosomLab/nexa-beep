# 2026-08-16

> 시간 역순(최신이 위).

## 수신측 취소 무반응 — 등록 지점이 갈라져 있었다 (main · 실기 후속)

**실기(사용자)** — 발신측 취소는 동작(34% 중단 · 양쪽 "취소함"/"상대가 취소"
기록 왕복 확인), **수신측 취소 버튼은 무반응**.

**원인** — 수락 경로가 둘이다: ⌘Y(`answer_offer`)와 **승인 창 Approve**
(`run_offer_choice`). `active_recv` 등록을 answer_offer에만 넣어, 승인 창으로
수락하면 xid 미등록 → `cancel_active_xfer`가 (설상가상 **조용히**) return.

**수정** — ① 등록을 두 경로의 합류점 `send_xfer_decision` **한 곳**으로 이관
(M3-21 push 깔때기와 같은 교훈 — *호출부 배선은 곧 빠뜨린 호출부가 된다*)
② 조용한 무반응 제거 — 활성 xid 없으면 상태바 안내(08-10 "실패도 말한다").

## 수락 후 취소 UX — 재개형 발신 펌프가 선행이었다 (main · M4-2 잔여)

**구조 발견** — 발신 청크 펌프가 Accept 수신 처리 안의 **블로킹 루프**였다:
펌프가 도는 동안 액터가 out_rx(내 취소 명령)도 recv_any(상대의 Cancel)도 못
본다. 즉 "취소 버튼"을 달기 전에 **전송 중 취소가 구조적으로 불가능**했다.
수락되는 순간 발신 대기 창이 닫혀(awaiting_accept 가드) 취소 경로 자체도
사라졌다 — TODO 박제(같은 날 검토)가 그대로 확인됐다.

**수정** — ① **재개형 펌프**: `ActiveSend{id,data,next,total,pacer}` 상태로
들고 액터 루프 틱마다 최대 4청크(128KiB)씩 보내며 명령·수신과 교대(Pacer
예약-대기 문법·쌍방 협상·진행 이벤트 비트 동일 · **Pacer/RateMeter 시계는
`xfer_now_ms` 한 곳으로 통일** — 기준이 갈리면 대역 계산이 깨진다). ② 취소
3경로: 내 발신 취소(sending 즉시 중단+Cancel 프레임) · 상대 Cancel 수신(펌프
중단 — **수신측이 발신자를 멈출 수 있게 됨**) · 수신측 취소(inbox 부분 조립
폐기). ③ 호스트 `active_send/active_recv` 추적 + `cancel_active_xfer`(발신
취소는 대기 큐도 비움 — 취소는 "이 작업 그만"). ④ 진행 배너 우측 **"취소"
버튼**(페인트가 히트 영역 기록 · 1회성 take → 호스트 라우팅) · 스레드 기록 =
"취소함".

**검증** — 656 green · clippy 0 · 3신원 재기동·상호 발견 ✓. **실기 잔여 =
대용량 전송 중 취소 왕복 육안**(발신 중 취소 · 수신 중 취소 · 배치 취소).

## 잔여 백로그 일괄 검토 + 6슬라이스 (main · 사용자 지시 "진행분 검토 후 순차 적용")

**검토(탐색 2기 병렬)** — 프로필·전송·입력·발견 영역 전수. 발견: **연락처 상세
UI는 이미 구현돼 있었다**(이메일·전화 = 카드 상세 행 · "(비공개)" 명시 — 낡은
주석 "M3-17 잔여"가 착시). 나머지는 전부 미구현 확인 + 붙일 자리 실측.

**적용 6슬라이스(각 1커밋)**

1. **docs/37 신설** — M4-10 전송 동기·재개 설계 📐(`.beepq.part` 부분 격리물 ·
   Accept 확장 resume_offset+prefix_sha(불일치 = 처음부터 fail-closed) · 재-Offer
   1회+2택 · 🔴 **D-31** 결정지 3건).
2. **M3-21 ③ 수신 시각** — `PeerProfile.received_ms` + 카드 "이미지 · 캐시됨 ·
   N분 전 수신"(부팅 복원 = 캐시 mtime 근사 · .meta 규약 불침).
3. **M1-2b ⓐ 능동 생사 촉진** — LinkChanged 시 활성 세션 전부에 기존
   `ProfileMsg::Request` 1발(새 와이어 프레임 0 · 죽은 소켓 = write 즉시 실패 →
   Closed → 리셋된 백오프가 즉시 재연결 · 22초 keepalive 감지 단축).
4. **FileScanner 포트**(M4 §6) — `Unavailable` 하드코딩 2곳을 이음새 뒤로:
   `nbeep_core::FileScanner`+`NoScanner` · `nbeep_plat::scan::platform_scanner()`
   (3-OS 지금 전부 NoScanner — **Windows AMSI가 들어오면 이 함수 하나만 갈린다**) ·
   gate는 디스크 기록 전 버퍼 단계에서 포트 호출.
5. **관리 복사(custom 이관)** — 사진 선택 시 `data/profiles/custom/`으로 복사,
   사본이 정본(이름 = 내용 해시 8자리+정제 원본명 — 재선택 = 재사용). 원본
   이동·삭제에도 프로필·최근 목록 불괴. 관문 2곳(피커+take_changes).
6. **TODO 박제** — 미착수 4건(M3-18·취소 UX·확대 미리보기·M3-1b)의 탐색 결과를
   구현 계획으로 등록(다음 세션이 탐색 없이 착수).

**실측** — M1-8y 유휴 축: GUI 3신원 8.7분 간격 2회 lsof/ps = fd 49/48/49·스레드
13 **완전 동일(핸들 증가 0)**. TTY 경로(①~④)는 raw 터미널 세션 몫.

**판단(적용 안 함)** — objc2 0.5/0.6 이중 트리 정렬: 0.6 통일 = `declare_class!`
→`define_class!` 전면 개편으로 **실기 검증 unsafe 3곳**(트레이·정식 알림·keytap)
재작성 필요. 방금 사용자 실증까지 끝난 알림을 건드리는 위험 대비 이득 = 산출물
수백 KB(예산 1.7/10MB 여유)뿐 — 보류가 옳다.

**보류(대형 — 별도 세션 슬라이스)** — M3-18(즉시 적용 2경로 이관+프리뷰 의존) ·
수락 후 취소 UX(3층 배선) · 확대 미리보기(Role 신설) · M3-1e ① TextInput 코어 ·
폴더 전송 · M4-4 zip · TextInput 코어. **환경 불가** — Linux SNI 실기 · M5-4a
서명(인증서) · M1-2b ⓓ 실기 매트릭스(네트워크 끊는 실기 = 사용자와 함께).

## 초과 사진 선택 시 2단계 깜빡임 제거 — push 보류 + 세대 번호 (main)

**실기(사용자)** — 큰 사진 선택 시 상대가 "옛 내장 그림(원숭이) → 실사진"
2단계를 봤다. 원인 = 깔때기 push가 축소본 워커보다 **먼저** 나가 사진 없는
프레임(내장 키만)이 1단계로 실렸다(직전 커밋의 "자기 치유 순서"의 대가).

- **push 보류** — `wire_pending` 동안 `push_profile` 전체 보류, 완료 이벤트가
  Full을 **한 번에** 민다(그 사이 바뀐 텍스트 필드도 동승). 실패해도 push는
  나간다(사진만 빠지고 텍스트·키 폴백은 닿아야 한다 + 경고 유지).
- **세대 번호(`wire_gen`)** — 워커는 파일을 안 쓰고 **바이트로 복귀**, 메인이
  세대 일치 확인 후에만 `me.wire.png`를 쓴다. 사진 A 워커 도는 중 B로 바꾸면
  A 결과는 조용히 폐기 — **낡은 축소본이 새 선택을 덮는 경합 자체가 없다**
  (파일 쓰기·판정 = 메인 단일 지점). 중복 스폰 가드(13 §12-1) 동반.

## 프로필 사진 256KiB 초과 "조용한 생략" 종결 — 와이어 축소본 + 두 필드 동시 송수신 (main)

**증상(실기·스크린샷)** — 본인 프로필은 실사진인데 **상대에게는 직전에 선택했던
내장 아바타(뱀)** 가 보였다. 사용자 추가 관측: "PNG 스티커는 전파되는데 JPG
사진만 안 된다" — **포맷은 무관, 범인은 크기다**(실측: 스티커 PNG 66,745B ≤
상한 / 사진 JPG **574,963B > `PROFILE_IMAGE_MAX` 262,144B**).

**원인 사슬** — ① `my_profile_frames_scoped`가 상한 초과 사진을 **조용히 생략**
(`filter(b.len() <= MAX)` — 경고 없음) → ② `image_shared=false`가 되고, 내장
아바타 키 조건 `share_basic && !image_shared`가 참이 돼 **옛 내장 키를 대신
광고** → ③ 상대는 뱀. 본인 미리보기는 로컬 디코드(상한 무관)라 정상으로 보여
발견이 늦었다.

**수정 3겹(사용자 확정 — "두 필드 동시 송수신 + Custom 우선 + NULL 엄격")**

1. **와이어 축소본(근본)** — imgdec에 `--encode-png` 모드 신설(인코딩도 imgdec
   몫 — 본체는 이미지 인코더도 링크하지 않는다 · R-5 결): 원본이 상한 초과면
   워커에서 256px PNG(`data/profiles/me.wire.png`)를 굽고 프레임은 그 파일을
   싣는다. 완료 = `AppEvent::WireAvatar` → **Full push 재전파**(자기 치유 순서 —
   변경 직후 깔때기 push엔 키만 나가고, 축소본이 준비되면 실사진이 따라간다).
   부팅에서도 보장(기존 초과 사진 사용자 자기 치유). **실측: 문제의 574KB JPG →
   256×256 PNG 102,922B**(상한 이내). encode 입력 상한 16MiB(원본 폰 사진 대비 ·
   픽셀 상한 4096²는 동일 — 할당 폭탄 방어 유지) · 왕복 회귀(비트 동일).
2. **두 필드 동시 송신** — 내장 키 조건에서 `!image_shared` 제거: 사진과 키가
   **항상 함께** 실린다. 사진이 어떤 이유로든 빠져도 수신측 폴백이 명확하고,
   "옛 키가 사진을 대변하는" 통로 자체가 사라진다.
3. **수신 사진 우선** — `image_keep`+내장 키 동시 도착 시 내장이 사진을 덮던
   승계 구멍 수정(공유 사진이 살아 있으면 그 그림이 이긴다 · 키는 폴백).

**NULL 엄격** — `image_path` 변경·비움 시 옛 축소본 즉시 삭제(안 지우면 철회가
안 나간다). 축소 실패는 **조용히 넘기지 않는다** — 상태바 경고("사진이 256KiB
초과인데 축소 생성 실패 — 전송되지 않습니다").

**교훈** — *조용한 생략은 언젠가 "왜 상대에게 옛 그림이 보이지?"로 돌아온다.*
빠뜨릴 수 있는 경로에는 ① 폴백을 명시적으로 싣고(두 필드 동시) ② 실패를 소리
내게 한다. 그리고 **상관관계(PNG는 되고 JPG는 안 됨)는 원인(크기)이 아니다** —
실측 두 파일의 바이트 수가 판정을 끝냈다.

**검증** — 655 green(imgdec 왕복 회귀 포함) · clippy 0 · rustdoc 0 · 실물 JPG
인코드 실측 통과.

## 대화창 명령 2차 확정 — 첫 글자 `/`는 무조건 비전송 (main)

**사용자 확정(08-16 2차)** — "trim 필요 없고 무조건 첫 글자가 `/`로 시작하는
경우만. **첫 글자가 `/`로 시작하는 모든 입력은 전달 대상이 아님**." 종전 규칙의
예외 3종(`//` escape · 한 줄 한정 · `/` 단독 통과)을 **전부 폐지**했다.

- 판정 = trim 전 원본의 **첫 글자 하나**(앞 공백 = 메시지 — 종전과 동일).
- 멀티라인도 첫 줄 첫 단어로 명령 판정만 하고 **전체 비전송** — "뒷줄 증발"
  경로가 아예 없다(안 보냈음이 안내로 보인다).
- `/` 단독·`/   ` = Unknown 안내(비전송) · 미지 이름 = fail-closed 그대로.
- 회귀: `any_leading_slash_is_never_sent`(어떤 형태든 Text/Empty로 새면 실패) ·
  멀티라인 비전송. app.rs의 구 escape 주석 2곳 정리.

## 대화창 명령 `/…` — GUI 도입 + 판정 단일화 (main · 직전 세션 완성분 정리 커밋)

> 직전 세션이 완성해 두고 커밋하지 못한 작업분을 검증 후 정리 커밋한다
> (651 green · clippy 0 · rustdoc 0 · TODO 스텁 없음 — 08-12 전례).

**무엇** — CLI 대화 모드에만 있던 `/명령`을 GUI 대화창(1:1·그룹)에 도입하되,
**판정을 `nbeep_core::command` 한 곳**에 모았다(부르는 쪽이 각자 문자열을 뜯으면
"어디서는 명령·어디서는 메시지"가 된다).

- **규칙 3종**: ① 원본 첫 글자 `/` + **한 줄** + 아는 이름 = 명령(상대에게 안
  나감 · 앞 공백 있으면 메시지 · 줄바꿈 있으면 메시지 — 멀티라인 뒷줄 증발 방지)
  ② `//` = escape(한 꺼풀 벗겨 메시지로 — 이게 없으면 `/`로 시작하는 문장을 영영
  못 보낸다) ③ **미지 `/이름` = 오류**(오타를 상대에게 쏘지 않는다 — fail-closed).
- **명령은 로컬** — 어떤 것도 와이어로 나가지 않는다.
- **GUI 명령 4종**: `/help`(안내) · `/trust`(신뢰 상태 — 미검증/고정/지문 대조
  라벨) · `/verify`(SAS 안전 번호 카드 열기 — 승격은 사람 몫) · `/close`(뷰 닫기).
  결과는 로컬 공지 줄(`push_chat_notice`)로 스레드에 표시.

**검증** — 651 green(명령 규칙 테스트 포함) · clippy 0 · rustdoc 0.
linkwatch 문서 링크 수정 동반(조건부 의존이라 intra-doc 링크 금지 — Windows
rustdoc red 실측의 처방).
