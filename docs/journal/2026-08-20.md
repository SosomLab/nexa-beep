# 2026-08-20 — 자동 취소 가시화 마감 + 콤보 팝업 잘림 + 검증 원복 실기

## (Windows 세션 3) 시스템 시작 시 자동 실행(M3-25) + 대화함 풀다운 메뉴 (`5b0c9ef`)

**요청(사용자)** — ① 3-OS 자동 실행 기능 · **기본 on**(끄기 = 옵트아웃) · 설정
고급의 "닫기 버튼 = 트레이" 항목 **위**에 배치 ② 대화함을 풀다운 메뉴에도 추가.

**구현** —
- **`nbeep-plat::autostart` 신설** — 전부 사용자 수준 등록(**T0 무권한** —
  관리자 권한을 요구하는 순간 DR-1·DR-4가 깨진다):
  - Windows = `HKCU\…\CurrentVersion\Run` 값 `NexaBeep`(**advapi32 직접
    링크** — launch.rs kernel32 `#[link]`와 같은 규약 · 외부 크레이트 0).
    경로는 따옴표로 감싼다(공백 대비).
  - macOS = `~/Library/LaunchAgents/com.sosomlab.nexa-beep.plist`(RunAtLoad).
  - Linux = `${XDG_CONFIG_HOME:-~/.config}/autostart/nexa-beep.desktop`.
  - 콘텐츠 빌더(plist·desktop)는 순수 함수로 분리 — **계약 테스트가 전 OS에서
    돈다**(XML 이스케이프 · Exec 상시 따옴표+예약 문자 `\"` `\\` `$` 백틱).
    반대편 타깃 dead_code는 `cfg(any(<타깃>, test))`로 봉합.
- **부팅마다 재동기화**(`apply_boot_settings`) — 포터블(DR-4)은 실행 파일
  위치가 옮겨질 수 있어 등록된 옛 경로가 조용히 죽는다: on = 현재
  `current_exe()`로 덮어쓰기 · off = 등록 제거(**멱등 치유** — 없으면 no-op).
- 설정 = `app.autostart` Toggle(고급 · 트레이 항목 위 · 기본 on — 미등록
  토글 기본 on 규약 그대로라 `TOGGLE_DEFAULT_OFF` 무변경) · 토글 즉시
  적용(`apply_settings` arm) · **실패 = 상태바 고지**(조용한 실패 금지 —
  설정값은 유지돼 다음 부팅·토글에서 재시도). i18n 4개국 5종.
- **대화함 풀다운 메뉴** — `build_menus()`에 항목 한 줄(격리함 다음).
  `take_picked`의 `"convbox"` 팔은 이미 있었고 **메뉴 항목만 없었다**
  (진입점이 툴바뿐이던 것).

**실측(Windows · 격리 포터블 인스턴스)** — 설치본이 실행 중이라 죽이지 않고,
포터블 규약(영속물 = exe 옆)으로 디버그 exe를 스크래치 폴더에 복사해 격리:
① on 부팅 = Run 값 등록 `"…\astest\nexa-beep.exe"`(따옴표 경로 정확) ②
settings.cfg에 `app.autostart=off` 후 재부팅 = **값 제거 확인**(치유 경로) ③
실측 전후 레지스트리 원상(NexaBeep 값 부재) 확인. 첫 실행이 settings.cfg에
`app.autostart=on`을 시드하는 것도 확인(Entry 레지스트리 영속 정상).

**설계 메모** — [04 T-2](../04-safe-transfer.md)의 "시작 프로그램 경로 쓰기
금지"는 **수신 파일**의 실체화 위치 얘기다 — 앱이 **자기 자신**을 등록하는
이 축과 무관(수신물은 여전히 시작 경로 근처에 못 간다). **715 green**(+10) ·
clippy 0.

⚠️ 잔여 = mac·Linux 실기(LaunchAgent 로그인 실효·GNOME/KDE autostart 실효 —
Windows만 실측했다) · 설치/포터블 두 채널에서 로그인 재부팅 육안.

## ★ v0.1.8 공개(릴리스 + brew — 사용자 지시 "brew까지만")

`443e92f` + 태그 `v0.1.8` push → release.yml **success(4m33s) · 자산 14종**
(5타깃×2채널 · SHA256SUMS · package-manifests). **homebrew 자동 추종 ✓** —
탭 저장소(kiros33/homebrew-tap) 최신 커밋 "nexa-beep 0.1.8"(Cask 설치본 +
Formula 포터블). **winget/choco = 게시 변수 꺼짐 그대로**(사용자 보류 —
"brew까지만"과 일치). 탑재 = v0.1.7 이후 08-16~20 누적(스트리밍·이어받기·
상한·인챗 제어·요청 단위 승인·대화함·그룹 영속·지문 검증·봉인·i18n·durable).

## (Windows 세션 2) 대화함(M3-23) 기본 기능 전체 구현

**절차(사용자 지시)** — 결정 필요 사항을 먼저 질문으로 수집(AskUserQuestion) 후 일괄 구현.
**수집된 확정**: ① 백업 형식 = **sealed 그대로**(기록 암호화 철학 유지 — 같은 신원만
복원) ② 행 표시·동작 = 등재분(아바타·이름·마지막 시각) + **마지막 메시지 1줄
미리보기 · 파일 크기 · 이름 필터 검색 · 행 클릭 = 대화 열기**(전부 채택 — 타
메신저 차용) ③ 백업 범위 = **전체만** ④ retention(자동 정리) = **비채택**(v1).

**구현** —
- **서랍 아이콘 자체 작도**: `tools`의 rsvg 파이프라인 없이 순수 파이썬 SDF
  4x 슈퍼샘플로 96px 알파 마스크를 구움(`icon-drawer-96.alpha` + 원본
  `assets/icons-src/drawer.svg` · **16px ASCII 판독 실측** — 상자·뚜껑 선·손잡이).
- **`ConvboxWidget`**(nbeep-ui 신설 · 격리함 문법): 상단 이름 필터(TextBox —
  프리에딧·클립보드·우클릭 편집 메뉴 전부 배관) · 행 = 아바타(이니셜 폴백)·
  이름(+그룹 태그)·마지막 시각·미리보기 1줄·크기·**휴지통 2단계**(무장 =
  위험색 + 안내 문구 · 본문 클릭 = 확인 취소) · 하단 = 건수/결과 문장 +
  전체 백업·복원·**전체 삭제 2단계** · 휠 스크롤 · **회귀 6종**(열기/2단계/
  취소/필터 정합/1회성/Esc 규약).
- **앱 배선**: Role::Convbox + 툴바 서랍(격리함 옆) + 프리에딧/클립보드/편집
  메뉴 라우팅 7곳 + `build_convbox_rows`(**메모리 스레드 우선, 없으면 세그
  개봉** — 다른 신원 봉인은 크기만 · 최신순) · 행 클릭 = `activate`/
  `open_group_thread`(목록 클릭과 동일 경로).
- **삭제 = 파일 + 메모리 동시**: sealed 삭제 + parked/conv/group_threads 비움 +
  **열린 대화창 즉시 반영**(`ChatWidget::clear_lines` 신설) — 안 지우면 다음
  record_history가 파일을 되살린다.
- **백업/복원 피커 2목적**: `HistoryBackupDir`(하위 폴더 `nexa-beep-history-{지문}`
  생성 후 세그 복사) · `HistoryRestoreDir`(**[이 폴더에서 복원]** + 개별 .seg
  클릭 = 그 파일만) · 복원 = **중복 덮어쓰기·없으면 추가·기존 유지**(사용자
  기확정) + `apply_restored_history`(**복원이 이긴다** — 메모리에도 반영해야
  활성 대화의 다음 저장이 복원본을 되덮지 않는다).

**711 green** · clippy 0 · rustdoc 0. ★ **사용자 실기 확인(같은 날)** — 아이콘·
목록·행 표시·마지막 메시지 미리보기·파일 크기·이름 필터·행 클릭 = 대화 열기·
대화방별 삭제·백업·복원 **10항 ✓**. 잔여 육안 = 전체 삭제 2단계 · 삭제 시
열린 대화창 즉시 비움 · 그룹 행 케이스.

## (Windows 세션) 요청 단위 승인 완결 + 요청당 상한 + 경고 모달 진원 창 + M5-4e·I18N-2

**실기 왕복 연쇄(사용자 확정 4차까지) — 코드 커밋 11건(`3ea8d2f`~`a1fb215`).**

**1. 승인 창 = 요청 단위 정보(`3ea8d2f`)** — 파일 수 행(시도 전체) · 콤마 구분
순수 파일명 목록(폭 실측 word-wrap · 3줄+…) · 크기 = 비제외 총합.

**2. 요청 단위 거절 완결(`9cb7d30`+`d07592f`)** — 배치 1회 거절 = 전체 마감.
1차 수정 후 실기 재보고("2번째 파일 승인 대기 잔존")의 진범 = **발신측
XferFailed가 큐만 버려** 남은 라인이 영구 잔존(오퍼가 안 나가 수신 자동거절
장부가 발화 기회조차 없음) → `fail_pending_xfer_lines(why)` 로 발신측도 잔여
라인 동일 사유 종결(쌍방 Declined). 승인·거절이 같은 순수 헬퍼
(`batch_remainder`/`batch_take`) 공유 + 자동화 회귀 박제.

**3. 제외 파일 표시(`c253aec`)** — 파일 수 = 시도 전체 포함 · "제외됨" 행
신설(용량 초과·폴더). **4. 승인 창 = 대화창 중앙(`86f5ea9`)** — 실기 ✓.

**5. 요청당 파일 수 상한 일습(`b375e29`→`571a1e7`→`3fc5acd`→`0494549` — 확정 4차 연쇄)**
— 최종 의미론: **제외 포함 합산**이 기준 · **폴더 = 전송 불가(제외 편입)** ·
설정 `xfer.batch_max`(Files · 1~5 · 기본 5 · 설명문에 자동 제외 안내) ·
**초과 드롭 = 전송 시도 자체 없음**(winit DnD가 파일당 이벤트라 판정 지점이
없었다 → `pending_drops` 수집 버퍼 + about_to_wait **묶음 선판정**) + 닫기
버튼 하나짜리 안내 모달(현재 설정값·최대 5개 지정 가능 안내 · 4개국).

**6. 경고 모달 소유·위치 = 진원 창(`28dfc8c`)** — 실기: 경고가 뜨며 **메인
목록 창이 대화창 위로 부상**(z순서 역전). 원인 = open_alert 메인 소유 고정 →
`pending_alert`에 진원 창 동봉 · 소유 = `modal_attrs_from(진원)` · 위치 =
진원 창 중앙(발생처 4곳 전부).

**7. M5-4e 해소 + I18N-2 종결(`a1fb215` — 사용자 확정)** — `data_dir()` 최후
폴백 = 임시 폴더 → **홈 숨김 폴더 `~/.nexa-beep`**(ⓑ 채택 — tmp_cleaner 신원
상실 함정의 제품 축 제거 · 홈조차 없는 극단만 temp 잔존) · 하드코딩 한국어
**16곳 Msg 승격**(액터 fail 5종 · 이어받기/수락/거절/대기 제안/타임아웃 거절 ·
그룹 오퍼 · 목록 삭제 · 로그 없음 · 피커 타이틀 6종+[폴더] 접두 · 4개국).

**실기 확인(사용자 · 08-20)** — 08-19 잔여 실기 3건 완료 판정: **큐 단계 정지
수신 라인 동기화 ✓** · **재시작 후 Invited 방 클릭 수락 ✓** · **재시작 후
발신자 라벨 잔존(그룹·1:1) ✓**. 이어 **T-macxfer ①~⑥ 전항목 완료 보고**(10GB
DnD 무정지 · 송신 전 상한 확인 · 상한 설정 hot-swap · 끊김 이어받기 ·
평균 속도 관측 · Win↔mac 교차 — 세부 실측값은 미제출). 요청 단위 거절도 실기 스크린샷(5줄 전부
Declined)으로 확인.

705 green · clippy 0 · 3신원 재기동 반복. 잔여 = 제외 표시·묶음 선판정·모달
z순서 육안 · 경고 모달 잔여 일반화(설정 초기화 선택 모달 등 9곳은 메인 소유
유지 — 실해 있는 자리만 후속).

> 앞선 심야 작업(08-19 journal 6·7차 — ⓓ·①ⓐ·별도 창 제거·카운트다운·직전값
> 규약)의 실기 확인과 후속 피드백 4건. 코드 커밋 = `ebc19bd`.

## 실기 확인(사용자)

- **다중 파일 배치 합산 배너** — 발신 누적·수신 누적·Progressbar "1, 2, 3 확인 완료".
- **검증 원복** — "15분 입력시 이전으로 원복됨"(경고 모달 + 직전값).
- **자동 취소** — "2분에 정상 취소됨"(기본값 2분 · 양쪽 정리).

## 후속 피드백 4건(`ebc19bd`)

1. ★ **콤보 팝업 잘림**(스크린샷 — 설정 창 아래 끝의 "자동 취소"·"대기
   타임아웃" 드롭다운이 창 밖으로 나가 선택 불가) → **`ComboCore.viewport_bottom`**
   신설: `popup_rect`가 하한을 넘으면 **시작 위치를 위로 이동**해 전체 목록
   노출. 히트·페인트·무효화가 전부 `popup_rect` 단일 원천이라 지오메트리
   수정 한 곳으로 끝(설계 배당). 호스트 배선 4곳 = 설정 Combo·Font size·
   갤러리·수신 승인 기간 콤보.
2. **카운트다운 위치** — 배너 줄 안(막대 왼쪽) → **전체취소 버튼 바로 위**
   (헤더 줄 · 버튼 오른끝 정렬 · 사용자 확정 2차).
3. **상태줄 i18n 2건** — "전송을 취소했습니다"(StXferCanceled) · "{}초 동안
   응답이 없어…"(StfXferTimeoutCanceled) 4개국. 스윕에서 나온 잔여 하드코딩
   (액터 fail 사유·수락/거절/이어받기 상태줄 등)은 **TODO I18N-2** 등재.
4. **"(DR-19)" 제거** — 주소 연결 모달 제목의 결정 번호는 개발 문서 표기라
   사용자 화면 비노출(사용자 질문 "구현이 필요하다는 의미인가?" — 아니다,
   이미 구현된 결정의 번호였다).

707 green · 3신원 재기동.
