# 2026-08-29

> 시간 역순(최신이 위).

## (Linux) 리눅스 환경 첫 관통 — 게이트 전량 · 3신원 기동·상호 발견 · relaunch.sh cargo PATH 폴백

리눅스 PC(7.0.0-30-generic · zsh)에서 이 저장소를 처음 돌렸다. 코드 변경 없이 **빌드·테스트·포맷·린트
4게이트 전량 통과**, `relaunch.sh`로 **3신원 기동 + 상호 발견**까지 성립. 발각된 것은 툴링 1건 —
사용자 터미널에서 `cargo`를 못 찾아 재기동 스크립트가 ②에서 즉사.

### ① 게이트 실측 (docs/18 절차 그대로 · cargo 1.98.0 stable)

| 게이트 | 결과 |
|---|---|
| `cargo build --workspace` | ✅ |
| `cargo test --workspace --features …testkit` | ✅ **790 passed · 0 failed · 2 ignored** |
| `cargo fmt --all --check` | ✅ 차이 0 |
| `cargo clippy … -D warnings` | ✅ 경고 0 |

ignored 2 = `udp::tests::{rename_reannounces_immediately, two_instances_discover_each_other_via_real_multicast}`
— 실네트워크 멀티캐스트 필요(D-8a · `--ignored` 전용). mac "792 green"과의 차 = 이 2건(정합 · 회귀 0).

### ② 3신원 기동·발견 (`./tools/relaunch.sh` · 신원 보존)

릴리스 빌드 3m 05s(콜드). 창 3개 · 8초 프로브에서 셋이 서로 발견.

| 신원 | 지문 | 이름 | 실행 경로 |
|---|---|---|---|
| 기본 | `de829962` | beep-de829962 | `target/release/nexa-beep` |
| A | `4a7325f3` | beep-4a7325f3 | `~/.nexa-beep-multi/A/nexa-beep` |
| B | `3f493eef` | beep-3f493eef | `~/.nexa-beep-multi/B/nexa-beep` |

이 PC의 첫 기동이라 지문 비교 기준 없음(이후 재기동에서 위 값과 대조).

### ③ 발각·수정 — `cargo: 명령을 찾을 수 없음`

- **원인**: rustup을 당일 13:57에 설치했는데 `~/.zshrc`에 `~/.cargo/env` 로드 줄이 없었다 →
  사용자 터미널 PATH에 `~/.cargo/bin` 부재. 세션(Claude)은 PATH를 직접 넣어 통과했고 스크립트 자체는 정상.
- **처방 2겹**: ⓐ `~/.zshrc`에 `. "$HOME/.cargo/env"` 추가(PC 로컬) ⓑ ★ **`tools/relaunch.sh`가
  PATH에 cargo가 없으면 `$HOME/.cargo/bin`을 스스로 붙인다**(`command -v cargo` 실패 && 실행 파일 존재 시만 —
  사용자 rc에 의존하지 않는다 · 다른 PC clone·rustup 직후 셸 미재시작 재발 방지). 빈 PATH(`env -i`)에서 cargo 발견 실측 ✓ · `bash -n` ✓.

### 잔여 (Linux 실기 후보)

- Linux 고유 축 미실기: **SNI 트레이(M3-2 Linux 구현 🚧)** · **XDG autostart(M3-25)** · 클립보드 이미지 `wl/xclip` · seccomp lockdown은 CI `tests/lockdown.rs`로만 확인.
- ignored 2건 `--ignored` 실행(실 멀티캐스트).
