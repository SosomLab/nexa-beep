# 2026-08-15

> 시간 역순(최신이 위).

## 전체 문서 현행화 + 08-14 미기록분 회수 (main · 소스 무변경)

사용자 요청: *"소스는 수정하지 않는 수준에서 변경된 내용을 확인하고 전체 문서에 대한
최신화 진행."*

### ① 기록 트랜잭션이 한 번 끊겼다

08-14 커밋 2건(`1b8b427` 상태 표시 시각 규약 + Lucide 자산 4종 · `bd47a1b` 물음표
재작도 + 파생본 규약)이 **STATUS에 없었다.** 원인은 병렬 세션 — 같은 작업 트리를 만지던
세션이 커밋할 때 내 journal 편집까지 함께 스테이징해 가면서(`ccdcdd5`), journal 본문은
살아남았는데 **STATUS 차수만 비었다.** [16 §3] "내가 고친 것만 add"가 지켜지지 않으면
**남의 커밋 사유가 유실되는 게 아니라 기록 순서가 어긋난다**는 사례가 하나 더 늘었다.
→ **08-15 2차로 회수**(M3-19·M3-20·M3-14 설계 · `tools/mkicons.sh` 승격 · 파생본 규약).

### ② 뒤처진 문서 6종 — 무엇이 얼마나 뒤처졌나

| 문서 | 뒤처진 값 | 고친 값 |
| --- | --- | --- |
| `README.md` | 08-13 · **v0.1.5** · 586 green · 산출물 **0.76MB** | 08-15 · **v0.1.6** · **603 green** · **약 1.7MB** + 실증 행 신설(2-PC·3-신원) |
| `docs/README.md` | 34번 "**H-1~21** · 교훈 5" · 14번에 §12·§13 없음 | **H-1~27 · 교훈 8** · 14번에 상태 배지 2절 추가 |
| `MILESTONES.md` | 표 갱신선 586 · M3 행 08-12 · ★**그룹 2행이 아직 "📐 설계"** · 배포 v0.1.4 | 603 · M3 행 08-15(08-14~15 추가분) · **그룹 전송 ✅ / 그룹 관리 🚧** · **수동 엔드포인트 📐→🚧** · v0.1.6 |
| `CLAUDE.md` | §1 586 green · §5 헤더 "08-13 21차" · M3-1e 미완 표기 | §1 603 + M3-21/H-27/상태 규약 · §5 "08-15 2차" + 요약 블록 · M3-1e ② 완료 반영 |
| `TODO.md` | 크리티컬 패스 "08-12 현행" | 08-15 현행(그룹 개통·IME 완주 취소선 + M3-19/14 추가) |
| `STATUS.md` | 08-14 상태 규약 누락 | 08-15 2차로 회수 |

★ **가장 큰 괴리는 그룹이었다** — 08-13에 G1~G3가 개통되고 3-신원 실기까지 마쳤는데
MILESTONES 기능표에는 **"📐 설계"** 로 남아 있었다. 마일스톤 개요(M5 행)에는 개통이
적혀 있어서 **같은 문서 안에서 두 표가 서로 다른 말**을 하고 있었다.
교훈: **개요 표를 고칠 때 기능 표도 같이 본다** — 기능 현황은 개요보다 늦게 늙는다.

### ③ 검증 — 읽기 전용, 소스 무변경

- `cargo test --workspace --features …` = **604 passed / 0 failed / 0 ignored**.
  다만 작업 트리에 **다른 세션 미커밋 변경 4파일**(`i18n.rs`·`settings.rs`·`app.rs`·
  `ime_gate.rs` · +491행)이 있다 → **문서에는 HEAD 기준 603**을 썼다(08-15 1차 기록과
  일치 · 초과 1건은 그 세션의 진행 중 작업). **남의 트리를 stash·정리하지 않았다.**
- 산출물 = `target/release/nexa-beep` **1,796,632B ≈ 1.7MB**(게이트 10MB의 17%).
  빌드 시각 00:32 < HEAD 01:42라 근사치로 표기했다.
- 스테이징은 내가 고친 문서만(`git add <파일>`).

## IME 기준값 설정화 — 하드코딩 9종 + 토글 2종 = 설정 "한글 입력(IME)" (main)

**요청(사용자)** — 틱 폴백 250ms·신선도 40ms 등 하드코딩 기준값을 설정에서 조정
+ 켜고 끌 수 있는 것은 토글로.

- **`ImeTuning`**(게이트 공개 타입 · Default = 실측값): `stale_ms` 250 ·
  `same_key_ms` 40 · `pending_ms` 150 · `echo_ms` 120 · `stash_ms` 300 ·
  `owed_ms` 800 · `pre_clear_ms` 300 · `swallow_ms` 2000 · `selfcommit_ms` 1000
  — **게이트 내 시간 상수 전수**(인라인 250/2000/1000까지 포함)를 필드로.
- **토글 2종**: `ime.inject`(삼킨 키 보충 주입 — 끄면 관측 잔재도 청소) ·
  `ime.leak`(유출 자모 조합기 — 끄면 낱자 직결).
- 설정 카테고리 **"한글 입력(IME)"** 11항목(RadioInput 프리셋+직접 입력 · 4언어) ·
  설명에 **플랫폼 경계 명기**(macOS 전용 실측 — Windows 무관 · 사용자 질문 반영).
- 배선 = `apply_ime_tuning` 한 깔때기(부팅 + `ime.*` 어느 키든 hot-swap).
- 회귀: stale 단축이 더 빨리 정산 · inject 끄면 보충 없음.

**Linux는?**(사용자 질문) — 미지. IM 프레임워크마다 이벤트 순서가 달라 비슷한
경합이 있을 수 있으나 실측 전엔 모른다 → [TODO LIME-1](../TODO.md) 등록 ·
[34 §2-10](../34-hangul-input-issues.md) 플랫폼 절 신설. **604 green**.

## H-27 — 같은 문자 반복이 판정을 무너뜨린다 ("ㅇㅇㅇ222" · 대화창/갤러리/프로필 공통)

**실기 보고(사용자, "제어 없이 대기")** — "ㅇㅇㅇ222" 순서대로 타이핑 →
"ㅇㅇ2ㅇ22". 자동 실기(osascript key code + `NEXA_IME_TRACE` + 스크린샷 픽셀
판독)로 합성 재현 = "ㅇㅇ22"(ㅇ 1·2 하나씩 유실). 트레이스 + 재생으로 원인 3중
확정 — 공통 뿌리는 **문자 "내용" 대조가 같은 문자 반복을 못 가른다**:

① 유출 첫 ㅇ(보류)이 다음 키의 preedit "ㅇ"에 starts_with로 중복 오폐기 →
   `SAME_KEY_MS`(40ms) 신선도 조건(같은 keypress의 keydown→Ime는 수 ms — 시각이
   가른다) ② 삼켜진 '2'가 다음 '2'의 정상 도달로 오인 소진 + 배달 증거 링이 잔여
   주입까지 차단 → **증거 링 폐기, 선배달 장부(owed)로 교체**(프록시 지연 짝을
   관측 도착 시점에 상쇄 — 큐 잔여 = 전부 진짜 삼킴. 불변식: 관측 수 = 도달 수 +
   삼켜진 수) ③ 조합 중 stale 틱 주입이 Commit보다 앞에 박힘(실타의 "2ㅇ") →
   조합 중엔 대기 + 주입 경로도 leak 선확정(`inject_char`) + 조합 중 keydown 관측
   짝 소진 + `PRE_CLEAR_SLACK`(300ms).

**검증** — 재생 3종 박제 + 기존 G1 2종("나!@#"·"다123") 새 의미론에서 통과(회귀 0)
+ 합성 자동 실기 재검: 삼켜진 셋째 '2'의 틱 폴백 주입 트레이스 실측 + 입력창
**"ㅇㅇㅇ222" 픽셀 확인**(이번 세션은 스크린샷 Read 가능 — 판독 루프가 살았다).
**603 green** · [34 §2-10](../34-hangul-input-issues.md) H-27 등록(✅ 22건 · 재생 17종).

**교훈** — 반복 입력에서 진실은 내용이 아니라 **수와 시각**이다. 내용 대조로 짝을
맞추는 판정 전부가 후보 함정(§2-10에 각론 박제).
