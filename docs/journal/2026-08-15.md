# 2026-08-15

> 시간 역순(최신이 위).

## 목록 선택 = 탐색기 표준 문법 (main · f84336d)

사용자 확정 규칙 3 + "탐색기 방식 전부" + **2차 확정("일반 좌클릭은 Selection 없이
대상만" · 74b88c1)**: 일반 클릭/무수식 이동 = **캐럿·anchor만**(선택 표시 없음 ·
기존 선택 해제 — 파란 표시는 Shift/⌘ 제스처에서만) ·
Shift 계열 = **anchor부터 범위 교체**(역방향 = 축소) · ⌘클릭 = 토글 · ⌘이동 = 캐럿만 ·
⌘Space = 골라 담기(목록 모드 한정 — 무수식 Space는 타입어헤드 유지) · ★우클릭 =
선택 밖이면 **전부 해제 + 미선택**(메뉴 재료는 ctx_peer 폴백) / 선택 안이면 유지(그룹
기능 진입 — 규칙 2). select_single은 바뀐 행만 국소 무효화(FR-U-13 회귀에 잡혀 고침 —
전체 무효화 금지 계약이 살아 있었다). 회귀 3종 신설 · 639 green.

## 설정 초기화 점검 + 잔재 정리 (main · 49f21e6 · c8ad0c4)

- **초기화 구멍**(사용자 요청 점검에서 발견): Reset이 항목당 `e.key` 하나만 되돌려
  **FontSection의 짝 키(size)가 샜다**(글꼴 크기를 바꾼 뒤 초기화해도 크기 잔존).
  원천을 `Entry::default_values`(기본값 쌍 전체 · pub 승격)로 — 시드와 같은
  원천이라 **초기화 = 첫 실행**이 구조로 보장. 회귀 2종: 전개의 짝 키 포함+시드
  일치 · **확정 기본값 고정**(close_to_tray=off 공통·프로필 공개 off·배지 on·
  정렬 chat — 오늘 겪은 "기본값 오염" 유형의 재발 감시이기도 하다).
- **잔재 정리**(사용자 요청 전수): 폐기 방향(Lucide badge) SVG 원본 6종 제거 +
  14 §13 폐기 표기 · /tmp 실험 인스턴스·로그 삭제 · 임시 진단 코드 제거 재확인
  (마커 0). 유지 판정 명시: `badge()`(격리함 실사용)·`icons::link`(M3-20 대기)·
  `icons::path`(M5-3c 자리). untracked 0 · 641 green.

## M1-12·M3-2d 확정·트레이 실기·Quit 메뉴 — 승인 배치 (main)

사용자 승인("진행해줘") 배치 + 진행 중 지시 3건(고급 맨 밑 · 기본 꺼짐 · Quit 메뉴).

- **M1-12 ✅**(0b77fd3): `tools/wirecap.sh` — 실소켓 캡처 + 고정 구간 자동 요약.
  **첫 실측 통과**(3신원): ASCII = `NXBP`+표시 이름만 · 새 재식별자 0 · 같은 seq
  5부 = M1-9 팬아웃 사본. 29 §4-2를 스크립트 기준으로 개정(트리거에 발신 경로
  증가 커밋 추가).
- **M3-2d**: ① 기본값 — mac 차등 권고(475cdb0)를 **사용자 확정 = 3-OS 공통 off**
  (15ada84)로 뒤집음 ② 수신 배지 = M3-8 이관 ③ **고급 카테고리 맨 밑**(첫 등장
  순서 규칙 — close_to_tray 항목을 등록부 끝으로 · 실화면 확인).
- ★ **기본값 오염 사고**: 잠깐의 mac-on 빌드에서 전체 저장이 `ui.close_to_tray=on`을
  주 신원 settings.cfg에 박제 — "새로 실행하면 자꾸 켜져 있다"(사용자)의 원인.
  3신원 설정에서 키 제거로 청소. **교훈: 기본값 실험은 저장을 통해 오염된다.**
- **트레이 mac 실기**: 숨김 = ✅(빨간 버튼 → CloseRequested·tray=true·on 진단 확인 ·
  프로세스 생존) · 트레이 메뉴 열림·툴팁·**Quit 왕복 ✅**(합성 클릭은 press-hold-
  release 필요 — cliclick 단타는 NSMenu 트래킹에 안 잡힘) · Open 복원만 육안 1클릭
  잔여. 앞선 "숨김 실패·즉사"들은 전부 **합성 클릭 오조준**이었다(코드 무결).
- **Quit 메뉴 ✅**(0add145 · 사용자 요청): Menu ▸ 구분선+종료 — close_to_tray on이면
  X로 못 끝내던 구멍. 트레이 종료와 같은 확정 경로(즉시 exit).
- ★ **M1-2 종단 실기 ✅**: Wi-Fi off→on 토글 후 3신원 전원 재발견 + **Hello 관측**
  (주기 광고는 Announce뿐 — Hello = kick 전용) = 감지→디바운스→kick 발화 직증.
  ⚠ 교훈: **네트워크 끊는 실기는 내 작업 채널도 끊는다** — 앞으로 사용자에게
  부탁하거나 대체 경로 확보 후(사용자 지시 · 메모리 박제).
- 부수: relaunch.sh 종료 단계가 git ssh(`git-upload-pack …nexa-beep.git`)를 앱으로
  오인해 중단 — 패턴 과광역(T-relaunch에 합류할 소재).

## M3-14b — 신뢰 배지 개편: Material Symbols 컬러 · Pinned 숨김 (main · 55b5253)

1차(Lucide badge 14px 틴트)가 실기에서 **유령 원**으로 읽혔다(사용자 스크린샷) —
핀 고정(정상 기본값)의 "흐린 빈 배지"가 전 행에 반복되며 정보 0. **ASCII 실측
통과 ≠ 실화면 문맥 통과**가 교훈(원장 10 §4에 박제).

- 사용자 확정 연쇄: 직관 아이콘 재탐색 → 색 충분히 → 웹 후보 조사(Tabler/Material
  몽타주 실렌더 제시) → **Material Symbols 채택**(Apache-2.0) · **Pinned 목록 숨김**.
- 기호 = 보편 관용: 미검증 `psychology-alt`(머리+? — `help`는 도움말 오독 위험) ·
  대조 `verified`(SNS 파란 실) · 차단 `block` · 충돌 `warning` · 핀 `lock`(카드
  전용) · 첫접촉 `person-add`. 이름 실존은 iconify API로 전수 확인.
- ★ **컬러 RGBA 파이프라인 신설**(`tools/mkicons-rgba.sh`) — 색이 정보인 자산은
  알파 마스크(모양만·틴트)가 아니라 96×96×4 원시 RGBA로 굽는다(`from_rgba` —
  brand 선례 · 런타임 SVG 파서 0 유지). 구 badge-*.alpha 6종 삭제.
- `trust_icon` → **Option**(Pinned=None) · 18px("조금 크게" 확정) · 툴팁도 그려진
  표식만. 검증 = 매핑·자산 계약 회귀 + 렌더 프로브(호박 픽셀 254) + 실기
  스크린샷(핀 상대 전 행 유령 원 소거 · 상태바에 M1-2 "네트워크 변경 감지"
  실동작도 함께 관측). 637 green.

## 배지 2종 + G4 잔여 + M1 잔여 일괄 — 9커밋 배치 (main · 사용자 지시 "모두 진행")

사용자 지시(상태 배지·그룹 구성원 관리 잔여·M1 잔여) 순서대로 9슬라이스.
M1-12(와이어 수동 캡처)만 설명 후 **승인 대기**로 보류. 637 green · clippy 0 ·
rustdoc 0 · 조건부 컴파일 슬라이스는 win/linux 크로스 check 0.

### M3-19 ✅ — 세션 배지 색+실루엣 2중 부호화 (3f946c2)

- [14 §12] 규격 그대로: `Idle`=빈 링 · `Connecting`=갭 링(90° · **캐럿 530ms 틱
  재사용** 4스텝 회전 — 새 타이머 0) · `Active`=꽉 찬 원 · `Lost`=가로 막대.
  기하 = 지름 D 비율 고정(구멍 0.53D · 막대 0.56×0.19D) — 전부 수식 렌더(자산 0).
- **`DrawCtx::fill_pie` 신설**(부채꼴 AA — 타원 SDF ∧ 반평면 2장 · sweep ≤180°)
  + RasterCtx 구현. 갭은 배경 테두리 반경까지 파서 AA 잔흔 없이 테두리 밖 불침범.
- `draw_link_badge` 단일 함수 — 목록 행 + **그룹 구성원 모달 공용**(link_color를
  한 곳에 뒀던 것과 같은 결). `ui.link_badge_shape`(Toggle · 기본 on) 부팅+핫
  스왑+쌍방 동기. 회전 위상은 Cell(paint가 `&self`).
- **픽셀 회귀 6종**(`tests/link_badge.rs`): 상태별 파냄 단언 · spin 0→1 회전 ·
  D=12/22 비율 불변 · off=현행. 파이 SDF가 첫 실행에 전부 통과.
- 설정 첫 토글에 순서 결합돼 있던 기존 테스트는 새 첫 토글로 갱신(주석 명기).

### M3-14 ✅ — 신뢰 배지 아이콘 + 화면에 없던 3상태 가시화 (ddd5448)

- 글자 칩 → Lucide `badge-*` 14px 윤곽선([14 §13]): Unverified=`-nb` 파생 물음표 ·
  Pinned=빈 배지 **흐리게**(정상 기본값은 조용히) · Verified=check ·
  ★`Blocked`=x(등급을 **덮는다**) · ★**이름 충돌**=alert 덧붙는 표식(같은 이름·
  다른 키 — **v1 사칭 유일 가시 신호**) · FirstContact=plus(토스트 전용 — 기존
  상태바 문구가 그 자리 · 자산만).
- 자산 9종 굽기(경로 축 house/globe/waypoints 자리 포함) — **굽기 전 14px/12px
  ASCII 실측**([13-4] 추정 금지): 파생 물음표 살아남음 ✓ · waypoints 12px 점 뭉침
  경향(v2 전용 — 재판독 주석).
- `PeerRow.blocked/conflict` 신설(`is_blocked`·`name_conflict`는 도메인에 있는데
  화면에 안 나오던 상태). hover **툴팁**(등급명+한 줄 · 4개 언어) — 페인트·히트
  같은 기하 함수. 틴트 캐시 = RefCell(툴바 문법).
- 함정 하나: `include_bytes!` 상수는 **사용 지점마다 주소가 갈린다** — 포인터
  비교 테스트가 실패해 내용 비교로(런타임 캐시 키는 지점 고정이라 무해).

### M5-1 G4 잔여 ✅ — 재동기 확장 · 로컬 승격 (a58c188)

- 발신자 보관 모델의 구멍 2개: ① **인바운드 세션에서 flush_group_sends 미호출**
  ("접속하면 밀어준다"에 상대가 걸어온 경우가 빠짐) ② 자동 연결 소진으로 초대가
  폐기된 상대는 **다음 편집까지** 낡은 채 남음.
- 처방 = **세션 성립마다 소유 방 Invite 재송**: 소유자는 수락·수신 여부를 모르므로
  Invite 하나가 세 경우를 닫는다(유실 상대=카드 · 낡은 구성원=명부 갱신 · 최신=
  같은 버전 무시). 수신측은 이미 있는 방(비 Invited)에 온 Invite를 **명부 갱신으로만**
  취급(카드 재노출 금지). 와이어 불변(Q-32-7 무충돌).
- **로컬 그룹 승격**(부팅 1회): 동보 UI 숨김(08-13) 후 보이지도 지워지지도 않던
  구버전 로컬 그룹 → 같은 이름·구성원의 공유 그룹(내 소유·v1) + 로컬 삭제.
  초대는 부팅에 안 쏜다(주소가 없어 전부 실패) — 위 재동기 push가 세션 성립
  순간 전달. 회귀 = 승격 재오픈 생존(반쪽 영속이면 방이 두 개).

### M1 잔여 5종 — 발견·연결 견고화

- **M1-13ⓔ** (b23356b): 발견 포트 47100 배타 점유 = 기동 패닉 → **발신 전용
  강등** + 상태바 고지("상대 목록에서 나를 선택"). 수신 소켓 실패만 분리(송신
  소켓 실패는 여전히 Err). 회귀 = unix 점유 시뮬레이션(Windows는 SO_REUSEADDR
  의미가 달라 성립 안 함 — R-7 결).
- **M1-14** (f6df511): 주소 정규화를 `nbeep_core::endpoint`로 내려 CLI
  `--connect`/`--chat-connect`가 GUI 모달과 **같은 규칙**(포트 생략=47200).
  ★ 숫자·점뿐인 호스트는 IPv4 리터럴 강제 — 옥텟 오타(`10.60.218.517`)가 DNS로
  흘러 오해의 Unreachable이 되던 실기 함정 차단(호스트명·DDNS는 통과).
- **M1-15** (2ed8735): `--discover-probe`에 `tcp=` 필드 — 실측: `from=…:62197
  tcp=47200`(UDP 발신 포트 ≠ 광고 세션 포트 실물 확인). 26 §6 미해결 표기 해소.
- **M1-9** (c448211): `nbeep-net::netif`(getifaddrs · unix) — **인터페이스별**
  멀티캐스트 발신(IP_MULTICAST_IF)+서브넷 지향 브로드캐스트+그룹 조인. 링크로컬
  직결(169.254 · FR-D-7)·다중 NIC에서 기본 경로 밖 링크로도 나간다. ★ 가상·터널
  제외(utun/wg/docker/awdl… — VPN으로 존재 방송 금지 · R-18 결). Windows = 빈
  목록 폴백(어댑터 = WNET-1).
- **M1-8** (6c369c8): S4 이웃 유니캐스트 — `nbeep-net::neigh`(Linux /proc/net/arp ·
  mac `arp -an` 스폰 = pbcopy 선례). 기동 1회 + 광고 16주기(≈12.8s)마다 서브넷
  안 이웃(상한 64)에 HELLO · ★ **수신측이 발신 주소로 유니캐스트 Announce 응답**
  (멀티캐스트 차단 망에서 왕복이 서는 나머지 절반). 대상 선별 `s4_targets` 순수
  함수 회귀(서브넷 밖 잔존 ARP 항목 유출 금지 — FR-S-49 결).
- **M1-2** (3ed37e6): L1 링크 구독 3-OS — `nbeep-plat::linkwatch`(mac PF_ROUTE ·
  Linux NETLINK_ROUTE · Win NotifyIpInterfaceChange raw extern 의존 0). 이벤트
  **내용은 파싱 안 함**(봉투: "변했다"뿐) → core Debouncer(quiet 1000ms 잠정 —
  D-8b) → `Transport::link_changed` 포트(기본 no-op) → LocalDirect =
  `UdpDiscovery::kick`(그룹 재조인+즉시 HELLO+S4). ⏸ 실기 = Wi-Fi 토글 3-OS.

### 기타

- **M3-22 신설**(사용자 스크린샷): 포터블·개발 바이너리 직접 실행 시 macOS Dock이
  기본 `exec` 아이콘 — mac=부팅 1회 `setApplicationIconImage` · Win=exe 리소스
  확인 · Linux=설치본 .desktop 기존 해결 명기.
- **M1-2b 신설**(사용자 요청 — "유선↔무선·유선→유선·무선→무선 전환에도 상태
  유지"): 있는 조각(L1 감지·재발견·keepalive 22s·자동 재연결ⓑ·스레드 대피·핀=키)
  위에 **설계 잔여** = LinkChanged 시 활성 세션 능동 생사 확인(22초 안 기다림) ·
  직전 활성·핀 상대 우선 즉시 재연결 · 같은 LAN 전환 = 경로 등급 불변(조용히 —
  DR-28) · 실기 매트릭스 4종. TODO 등록.
- **실기 스모크**: relaunch(신원 보존) — 새 빌드로 3신원 기동·상호 발견
  ✅(kiros33·호랭이2·용용용).
- M3-2b 메뉴바 메뉴 동작 **사용자 육안 확인 ✅**(a25d690) — 잔여 육안 =
  close_to_tray on 숨김/복원 하나.

## M3-2b ✅ — macOS 메뉴바 상주(NSStatusItem) (main · mac 세션)

Windows(M3-2a)·Linux(M3-2c) 뒤를 이어 mac 어댑터 완성 — **트레이 3-OS 개통**.

- `tray.rs::mac`: NSStatusItem(가변 길이) + **NSMenu 좌/우클릭 공통**(맥 관례 —
  분석 표 그대로): 이름 헤더(비활성)·구분선·열기·종료. 액션 = 선언 클래스
  `NbeepTrayTarget`(objc2 `declare_class!`)의 셀렉터 → 호스트 프록시 콜백.
- 아이콘 = 호스트 합성 아바타 RGBA → `NSBitmapImageRep`(32px) → `NSImage`
  18×18pt — **레티나에서 2x 원본이라 선명**.
- **AppKit 메인 스레드 강제** = `MainThreadMarker`로 증명(밖이면 spawn None ·
  update 무시 — fail-soft · Windows "워처 부재"와 같은 강도). 상태(NSStatusItem
  비-`Send`)는 thread_local — `TrayHandle`은 표식.
- 의존 추가 0(기존 keytap의 objc2 0.5/0.2 판 재사용 — feature만 상속 사슬 포함
  확장). 크로스(linux·win) check 0.
- ★ **실측**: 앱 실행/종료 스크린샷 diff — 메뉴바에서 **폭 35px 아이콘 3개**
  (신원별 아바타 · x=840/908/976 · 68px 간격)가 정확히 등장·소거.
  ⏸ 육안 = 메뉴 열기/종료 클릭 · `ui.close_to_tray` on에서 빨간 버튼 = 숨김·복원.

618 green · clippy 0 · rustdoc 0.

## M3-2c 🚧 — Linux 트레이(SNI · D-Bus) 구현 — 실기는 Linux 몫 (main)

**사용자 확정**("리눅스 직접 확인이 어려우니 여기서 구현") — 권고했던 폴백 대신
**구현**으로. ⚠ **이 PC에서 실기 불가** — 검증 = 크로스 컴파일(check·clippy 0 ·
x86_64-unknown-linux-gnu 전 워크스페이스)까지. 데스크톱 실기(KDE·GNOME+확장)는
Linux 환경이 생기면.

- **SNI 서빙**: `org.kde.StatusNotifierItem`(zbus `#[interface]`) — Category/Id/
  Title(이름)/Status/IconPixmap(**ARGB32 네트워크 바이트 순서** — RGBA에서 채널
  재배열)/ToolTip/Menu(`/MenuBar`)/ItemIsMenu=false · `Activate`(좌클릭)=Open.
- **dbusmenu 최소 구현**: `com.canonical.dbusmenu` — GetLayout(루트 0 → 4항목:
  이름 헤더(비활성)·구분선·열기·종료 · `(ia{sv}av)` 구조를 OwnedValue로 조립) ·
  GetGroupProperties/GetProperty · **Event("clicked")** → Open/Quit ·
  AboutToShow(false). 갱신 = NewIcon/NewToolTip/NewTitle + LayoutUpdated(리비전).
- **fail-soft**: 세션 버스·워처(StatusNotifierWatcher) 부재 = `spawn` None —
  호스트의 `close_to_tray` 가드("트레이 없으면 종전 종료")가 그대로 지킨다
  (GNOME 확장 미설치 환경에서 앱이 조용히 정상 동작).
- **의존 = zbus 5**(MIT · 순수 Rust — 시스템 런타임 요구 없음 · **Linux 타깃
  한정**이라 Win/mac 산출물 크기 불변 · 원장 10 §3).

## M3-2a ✅ — Windows 트레이 상주(아바타 아이콘·close_to_tray) (main)

**구현**(분석은 같은 날 앞 기록) —

- **`nbeep-plat::tray`**(TrayPort · DR-21): `TrayContent{rgba·tooltip·name·라벨}` ·
  `TrayEvent{Open,Quit}` · `spawn/update`. Windows 어댑터 = **raw extern**(의존 0):
  전용 스레드 + **보이지 않는 일반 창**(메시지 전용 창은 TaskbarCreated 브로드캐스트를
  못 받아 explorer 재시작 시 재등록 불가 — 일반 창 + 재등록 배선) · `Shell_NotifyIconW`
  (레거시 콜백 시맨틱) · **RGBA→BGRA→CreateIconIndirect**(알파 존중 · 갱신 시 이전
  HICON 파괴) · 우클릭 = **TrackPopupMenu**(이름 헤더(비활성)+열기+종료 ·
  `SetForegroundWindow` 선행) · 좌클릭 = Open. 비-Windows = None 스텁(mac은 mac PC ·
  Linux SNI는 다음 슬라이스).
- **아이콘 = 내 아바타**: `my_avatar_rgba(32)` 합성(투명 → 시드 원 1px AA →
  사진>내장 **박스 평균 축소** over 블렌드 → 보더 링). 이니셜 글자는 생략(32px
  판독성 — 툴팁·메뉴 헤더가 이름을 보인다 · 기록). 갱신은
  `refresh_toolbar_avatar` 깔때기 합류(아바타·보더·사진·표시 이름 변경 전부).
- **설정 `ui.close_to_tray`**(Toggle · 기본 off — M3-2d 결정지): on이면 메인 X =
  숨김(전송·세션 유지 · 복귀 = 트레이). ⚠ 배치 삽질 1회 — CatAppearance 앞자리
  삽입이 **순서 결합 테스트 2건**(첫 행=테마 콤보 · 첫 토글=typeahead)과 충돌 +
  VS Code식 정렬(직속 먼저)이라 텍스트 이동으로도 안 풀림 → **CatAdvanced**로
  (동작 설정이라 의미도 맞다).
- 트레이 "종료" = 즉시 `el.exit()`(명시적 종료 의도 — 전송 가드는 X 버튼 UX).

**검증** — 617 green · clippy 0 · 재기동 3신원 + **오버플로 플라이아웃 스크린샷:
아이콘 3개가 각자의 아바타**(실사진·b:ox·기본)로 표시 확인. ⏸ 실기 육안 =
우클릭 메뉴(이름·열기·종료)·좌클릭 복원·스위치 on 후 X=숨김.

## M3-2 트레이 — 요구 확정·OS 분석·세분화 (docs · 구현은 M3-2a부터)

**사용자 확정(08-15)** — 트레이 요구가 이번에 구체화됐다: 아이콘 = **사용자
프로필 아바타** · 툴팁/우클릭에 표시 이름 · 우클릭 메뉴에 **종료** ·
**`ui.close_to_tray` 스위치**(닫기 = 종료 대신 트레이 상주) · 3-OS 동일 패턴.
"짧으면 바로, 크면 정리" 지침 — **크다**(3-OS 네이티브 + winit 펌프 통합)로
판정하고 분석·세분화로 정리했다.

**종전 🔴 "아이콘 의존 판단" 해소** — 아이콘이 사용자 아바타(RGBA 자체 렌더)로
확정되면서 외부 에셋도 `tray-icon` 크레이트도 불필요해졌다. **직접 어댑터 판정**:
Win `Shell_NotifyIcon`·mac `NSStatusItem`은 시스템 API 직접 호출(런타임 의존 0 ·
MotW/NSSound와 같은 선례 문법). 트레이 **메뉴는 OS 셸 영역이라 네이티브**
(TrackPopupMenu/NSMenu — ADR-0014 "OS 네임스페이스를 여는 문은 OS 것"과 같은
경계 획정. DR-6은 앱 창 안 렌더링 원칙이라 충돌 없음).

**OS별 차이 핵심**:

| | Windows | macOS | Linux |
|---|---|---|---|
| 자리 | 알림 영역(트레이) | **메뉴바** NSStatusItem | SNI(D-Bus) — 파편화 |
| 콜백 | uCallbackMessage → **창 프로시저 필요**(winit wndproc 불가침 → **숨은 메시지 창 전용 스레드** + EventLoopProxy 복귀 — 워커 문법) | AppKit **메인 스레드 강제**(winit 메인 루프에서 생성·갱신) | zbus 의존 체인 |
| 아이콘 | RGBA→HICON(NIM_MODIFY 동기) | NSImage(18~22pt@2x) | — |
| 메뉴 관례 | **우클릭**=메뉴 · 좌클릭=복원 · `SetForegroundWindow` 선행(안 하면 안 닫히는 고전 버그) | **좌/우 모두 메뉴** | — |
| 닫기 관례 | X=종료가 기본(메신저는 트레이 관례) | **빨간 버튼=창만 닫힘이 OS 관례** — 스위치 기본값 차등 결정지 | — |

**세분화**(TODO 재작성) — **M3-2a Windows**(TrayPort+어댑터+설정+CloseRequested
인터셉트 — 이 PC 실기 · 착수 1순위) · M3-2b macOS · M3-2c 🔴 Linux 범위(권고 =
v1 폴백 — 트레이 없음·스위치 숨김) · M3-2d 결정지 2건(스위치 기본값 공통/차등 ·
숨김 중 수신 배지 = M3-8 접점).

## ★ Windows 실기 배치 완주 — 26 §7-1 W-A~W-H 8건 중 7건 통과 (빌드 `2512beb`)

08-15 핸드오프([26 §7-1](../26-run-and-manual-test.md))를 Windows에서 소화했다. **헤드리스로
증명 가능한 것과 눈으로만 되는 것을 나눠** 앞엣것을 먼저 끝내고, 나머지를 사용자에게 넘겼다.

### 헤드리스 실측 4건 — W-A · W-B · W-H(+W-C/D 영속)

| 항목 | 근거(추정 아님) |
| --- | --- |
| **W-A** 마이그레이션 | 파일 헤더 직접 판독 — `NBGS 03`(groups v2→**v3**) · `NBTS 02`(trust v1→**v2**) **3신원 전부** · `identity.key` mtime 08-11/08-13 **무변경**이고 파일에서 읽은 PeerId가 발견 결과와 일치(`b39633e2`=sybae0057 · `f313de9e`=이제는2 · `02f70af6`=가나다123) · trust.seg **828→1179B**(초기화면 줄었을 값 = fail-closed 잠김 아님) |
| **W-B** 권한 강등 | **실바이너리** 4갈래 — stderr `lockdown = windows-mitigation(dynamic-code·image-load)` · PNG→NIMG 256²(262,144B)·64²(16,384B) **바이트 정합** · 쓰레기 5KB·빈 입력 = **exit 2 · 출력 0B**(fail-closed) |
| **W-H** 설정 신설 | `settings.cfg` 판독 — `ime.*` 9종+`ime.inject/leak` · `ui.tooltip_ms=2000` · `ui.carousel_scroll=auto` · `ui.list_refresh_scroll=keep` · **키 68개 중복 0** |
| (부수) | `ui.list_sort=chat`·`ui.win_x/y/w/h` 저장 확인 — ★ **08-14에 잡은 HIDDEN_KEYS 구멍이 실제로 막혔다**(그때는 로드에서 미지 키로 버려졌다) · `profile.avatar=b:ox`·`avatar_border=#B57C1E` = 아바타 모델 종단 동작 |

★ **W-B의 의미** — 권한 강등은 "걸렸는가"만으로 부족하다. **걸린 채로 정상 입력이 통과하는가**가
같이 성립해야 한다(과하면 이미지가 조용히 죽는다 — imgdec 동봉 누락과 같은 실패 양식). 두 방향을
같은 배터리에서 확인했다.

### 사용자 육안 4건 — W-C · W-D · W-F · W-G

팝업 렌더·선택 즉시 재정렬 ✅ · 순서 변경 + **깜빡임 없음** ✅ · 캐러셀 Windows 방향 기획대로 ✅ ·
**3신원 그룹 생성·메시지 전달** ✅.

### 잔여 — W-E 하나

2-PC(Win↔Mac) 프로필 전파. **Windows 단독으로 불가**하고 Mac이 같은 망에 있어야 한다.

> ⚠️ **실기 후 3커밋**(`8d1b046`·`ac7f83f`·`86b045a` — 모달 = 마우스 위치 + 메인 소유)이 들어왔다.
> 실기는 `2512beb` 빌드 기준이므로 **모달 z순서는 재확인 대상**으로 남긴다(무엇을 언제 봤는지가
> 흐려지면 실기 기록은 값을 잃는다).

**부수 — `tools/relaunch.sh`가 Windows에서 조용히 실패한다.** ① 종료가 `pkill` 기반이라 Git Bash에서
Windows 프로세스를 못 보고 **0개로 보고** → 구 인스턴스가 exe를 잠가 **② 빌드가 `os error 5`로 실패** →
그런데 다음 단계로 진행해 ④가 **3개를 더 띄우고**(총 6개 = 각 신원 2벌) ⑤가 구 인스턴스를 발견해
**"성공"으로 오판**, **exit 0**으로 끝났다. 08-13 "빌드 조용한 실패"와 같은 계열 —
**실패가 exit 0으로 덮이면 검증이 거짓말을 한다.** 최소 처방 = ② 실패 시 즉시 exit 1
(등록됨 · Windows 경로 분기는 별도).

## 모달 창 = 마우스 위치 + 메인 창 소유(owned) — 2-앱 실기 z순서 (main · Windows)

**실기 보고 2건**(사용자 · 스크린샷 2장) — ① 프로필이 기본 위치(멀리)에 열려
커서 왕복이 번거롭다 ② **2-앱(A·B) 각각 프로필을 연 상태에서 B의 모달을
선택해도 B 메인이 A 창들 뒤에 남는다** — "모달을 선택하면 그 앱의 메인을 먼저
올리고 그 위에 모달"이 기대 동작.

**처방 — `modal_attrs(attrs, at_cursor)` 헬퍼**:
- ① `at_cursor` = **현재 마우스 위치(메인 창 inner_position + 클라이언트 커서)를
  창 좌상단**으로. 프로필에 적용(요청 명시 범위).
- ② **Windows = 메인 창 소유(owned window)** — `with_owner_window`(winit
  플랫폼 확장). 소유 창을 클릭하면 **OS가 소유자(메인)까지 함께 앞으로**
  올린다(창 묶음 부상 = 정확히 기대 동작 · AlwaysOnTop 없이 "모달은 메인 위"도
  성립 · 다른 앱을 가리지 않음 — 08-14 "AlwaysOnTop 금지" 표준과 정합).
  모달 8종 전부 적용: About·알림·구성원·확인(Alert 계열)·파일 선택(Picker)·
  그룹 이름(NamePrompt)·주소로 연결(AddEndpoint)·프로필.
- ⚠ 첫 패치에서 제네릭 타이틀 패턴이 **별도 대화 창**(open_separate_window)에
  잘못 걸렸다 — 대화 창은 모달이 아니다(메인 종속 금지) → 원복하고 주석 명문.

**검증** — 617 green · clippy 0 · relaunch 3-신원 실기(마우스 위치 열림·B 모달
클릭 시 B 메인 동반 부상). mac은 소유 미적용(비Windows 분기 — 관례 상이 ·
후속 판단).

## mac 유휴 메모리 재실측 — R-8 mac 축 해소(지표 오선택이 원인) (main · 실측)

**방법** — 신선 인스턴스(별도 data/ · 무조작 유휴)를 10분 방치하며 `ps RSS`와
`footprint`(phys_footprint)를 병행 실측. 장기 가동(8h45m) 3인스턴스도 동일 측정.

**결과** —
- `ps RSS`: 신선 유휴 140~176MB 요동 · 장기 141~175MB — **예산(30MB) 판정에
  부적합한 지표였다**(macOS RSS는 공유 프레임워크 페이지 포함 · 요동도 큼).
- **`phys_footprint`(Activity Monitor "메모리"와 같은 지표): 신선 유휴 18.8MB
  (peak 37.4 — 기동 순간) · 장기 8h45m 20~21MB.**

**판정** — ★ **mac 유휴 실메모리 = 18.8~21MB ≤ 예산 30MB — R-8 mac 축 해소.**
기존 "85.7MB"는 RSS 지표를 예산과 직비교한 오판정이었다(Windows 17.1MB는 Working
Set — 대응 지표 비교로도 mac이 예산 안쪽). 부수 관측: 8h45m 증가분 +1~2MB(활동
포함 — 순수 24h 유휴 누수 측정은 M5-5 몫). **교훈: 예산 게이트는 수치보다
지표 정의가 먼저다** — 05 NFR-B에 측정 지표(mac = phys_footprint)를 명시할 것.

## M5-1 G4 — 구성원 편집(제외) 모달화 · 와이어 불변 (main)

Q-32-7(콘텐츠 키 봉투) 🔴 대기 중이라 **와이어를 건드리지 않는 범위**로 한정:
roster 편집·재배포는 기존 `SGroupMsg::Roster` 경로 그대로다.

- **구성원 모달의 행 클릭 = 제외 진입**(소유자만 — 제목에 "행 클릭 = 제외" 힌트):
  알림 모달 상태 목록에 `rows_clickable`/`take_row_clicked` 채널 신설(행 클릭은
  모달을 닫지 않는다 · 페인트·히트 행 높이 공유 `row_h` — 어긋나면 클릭이 빗나간다).
- 행 해석 문맥 = `members_ctx`(방·**모달과 같은 정렬의 구성원 순서**·소유 여부 —
  모달 닫힘 시 소거) → 나 자신은 제외 불가.
- 확인 모달(`AlertCtx::GroupKick`) → 긍정 = **기존 `GA::RemoveMembers` 경로 재사용**
  (소유자 검증·제외자에게 마지막 명부 1회 배포 포함) → 구성원 모달을 새 명부로
  재오픈(이어서 편집). `apply_alert_choice`에 `el` 전달(시그니처 확장).
- 초대는 기존 흐름 유지(목록 다중 선택 → 그룹 우클릭 "선택 N명 초대" · Suggest).
- 회귀: 행 클릭 채널(허용 시에만 · 1회성 · 모달 유지).

## M4-5ⓒ ✅ — imgdec 권한 강등 = R-5 종결 (main)

**설계** — 강등 적합점이 프로토콜에서 자명했다: stdin→stdout 순수 계산이라
**입력을 전부 읽은 직후 · 파싱 전에** 자신을 잠근다. 이후 필요한 것은 메모리
할당·stdout/stderr 쓰기·종료뿐 — 파서가 뚫려도(RCE) 열 수 있는 것이 없다.

- **macOS** = Seatbelt `pure-computation`(`sandbox_init` — 이미 열린 fd 읽기/
  쓰기는 허용 · **로컬 실측: 강등 상태 디코드 생존 ✅**).
- **Linux** = seccomp-bpf 허용 목록(read/write/할당 계열/futex/종료/시그널 복귀 ·
  arch 검증 선행 · 기본 KILL_PROCESS) + `no_new_privs`. syscall 번호는 libc
  상수(x86_64/aarch64 — 손 번호 금지).
- **Windows** = `SetProcessMitigationPolicy`(동적 코드 금지 + 원격 이미지 로드
  금지). win32k 락아웃은 콘솔 경로 호환 실기(WGUI) 후 강화.
- **fail-closed**: 강등 실패 = 디코드 포기(exit 4) — imgdec 부재와 같은 강도
  (본체는 이니셜 폴백 · 파서를 무방비로 돌리지 않는다).
- ★ **실측 경로 = 통합 테스트**(`tests/lockdown.rs` — `CARGO_BIN_EXE`로 실제
  바이너리 스폰 · 정상 PNG 생존 + 쓰레기 fail-closed): CI가 3-OS에서 돌린다.
  크로스 3타깃 check ✅(조건부 컴파일 병합 = 크로스까지가 게이트).
- 새 의존 = `libc`(Linux 한정 · MIT/Apache — 이미 링크되는 시스템 라이브러리
  바인딩이라 런타임 의존 0 불변 · 원장 10 §3).

**R-5(이미지 파서 RCE) 종결** — 격리(별도 프로세스·본체 미링크) + 상한 +
권한 강등 3중이 완성됐다.

## M1-11 ✅ — 와이어 평문 자동 회귀(CI 상시) (main)

- ① 골든 레이아웃(기존) + ★**허용 목록의 구조적 강제** — 인코딩 전체가 허용
  필드의 연접과 **바이트 단위 동일**(전 패킷 종류). 몰래 필드를 추가하면 즉사.
- ② 발견 금칙어 스캔(`@`·`profile.`·전화 패턴 등 — 구조상 입력이 없지만 회귀 감시).
- ③ 세션 tap 평문 부재를 **금칙어 세트**(이메일·전화·한글 본문·프로필 설정 키)로
  일반화 — 여러 프레임 왕복 후 링크 바이트 전체에서 부재 단언.
- ④ Identity `Debug` 마스킹 — 개인키의 **4바이트 연속 hex 전수** 부재(2바이트는
  공개 지문과 우연 일치 확률이 있어 4바이트 판정).
- 전부 일반 `cargo test` → 3-OS CI에 상시. **SEC-1 잔여 절반 해소.**

## 상대 프로필 카드 — 아바타 보더 누락 보완 (main)

**실기 보고(사용자)** — 목록에는 보더 색이 그려지는데 **프로필 보기 카드에는
안 보인다**. 원인 = `PeerInfo`에 보더 필드 자체가 없었다(카드가 그릴 재료 부재).
`border` 필드 추가 + 큰 프리뷰 **3px** 링(프로필 화면과 같은 확정 규약 · 목록
소형 2px) + 목록 행과 같은 원 배경 문법(내장 아바타 투명 배경 대비). 611 green.

## 정렬 아이콘 다듬기 일습 — 사용자 제공본 교체 + 크기·굵기 정합 (main)

- **교체 2종(사용자 제공)**: `sort-name`(A/Z+↓ — 자체 작도 1차본보다 가독 우수) ·
  `sort-online`(사람 노드 3 연결망 — 의미 직관). SVGRepo 계열 wrapper 정리 후 반입 ·
  원장 [10 §4](../10-decision-record.md) 출처 갱신.
- **크기 정합**: `sort-online` 75% 축소+중앙(각변 여백 12.5%) — 알파 바운딩 실측
  name 71% vs online 74%(교체 직후 ~100%).
- **굵기 통일**: `sort-recent` 외곽·`sort-seen` 전체 획을 1.5/24로 — name(2/32)과
  같은 상대 두께 6.25%. 실측: 96px 획 단면 중앙값 셋 다 6px.
- 전 과정 = 굽기(mkicons) → **20px 축소 판독·수치 실측**(아이콘도 추정 금지) →
  재기동 확인. IconDropdown ▼는 우하단 오버레이 + `theme.warn`(호박색 — 픽셀
  실측 #AC7D37)로 확정, 툴바↔설정 **쌍방 동기화**(apply_settings → 열린 설정
  화면 `set_value` 역반영 · 전 키 공통)와 함께 직전 커밋들에 정리.

## IconDropdown 다듬기 — ▼ 오버레이(호박색) + 설정 쌍방 동기화 (main)

**사용자 정정·요청 3건**:

- **▼ 표식 = 우하단 오버레이**(오피스 툴바 관례 · 샘플 이미지 첨부): 아이콘 옆이
  아니라 **아이콘 위에** 작은 배경판 + 아래 삼각형(라인 프리미티브 없이 좁아지는
  가로 막대 계단). 색 = **`theme.warn`(호박색)** — 파랑(accent)은 선택/포커스가
  쓰고 녹색/빨강은 상태 점 의미가 있어 피했다(모양이 1차 신호, 색은 보조 —
  색맹 규약 정합). 픽셀 검증: 툴바 영역 `#AC7D37` 실측.
- **쌍방 동기화**: 설정→툴바는 됐는데 툴바→(열려 있는) 설정 화면이 안 됐다 —
  설정 창 컨트롤이 열 때 상태로 고정되는 구조. `apply_settings`가 **모든 키에
  대해** 열려 있는 설정 화면에 `set_value` 역반영(기존 API 재사용 · 토글 행
  동기화도 보강). 자기 자신이 낸 변경은 같은 값이라 무해. 이 배선은 목록 정렬만이
  아니라 **다른 경로發 설정 변경 전부**(프로필 화면·복원·초기화)에 적용된다.

611 green · clippy 0 · 3신원 재기동.

## 정렬 4모드 + IconDropdown(19번째 컨트롤) — 툴바 정렬 선택 (main)

**요청(사용자)** — 정렬 4가지(기본 = 정보 기반 최근 판단 · 이름순 · 최근
접속순(온라인 무관) · 온라인순(최근 접속 우선)) + **툴바 한 칸의 이미지
드롭다운**으로 선택·영속 + 갤러리 편입 + SVG 추천.

- **정렬 4모드**(`ui.list_sort` — 기존 키 그대로 영속·hot-swap):
  `chat`(기본 — 계층→①최근 대화→②최근 접속) · `name` · `seen`(**온라인 여부
  무관** 최근 접속) · `online`(계층→최근 접속). `peer_order_key` 회귀 갱신.
- **IconDropdown 신설**(19번째 · `nbeep-ctl/controls/icondrop.rs`): 툴바 슬롯
  크기 버튼(현재 선택 아이콘 + ▾) → 클릭 = 아이콘+라벨 팝업. 마스크 틴트
  캐시(툴바와 같은 문법) · `paint_popup` 최상위 레이어 · Esc/밖 클릭 닫기 ·
  `take_changed` 1회성 · `set_value` 외부 동기화(설정 화면 Radio와 상호 동기).
  테스트 4종(열기-선택-1회성 · 밖/Esc 취소 · 동일 재선택 무보고 · 외부 동기화).
- **툴바 통합**: `Toolbar::left_items_end`/`slot_px` 신설 — 좌측 아이콘 끝에
  이어 붙는 한 칸. 팝업 열림 = 모달 캡처(메뉴와 같은 규약) · 선택 =
  `apply_settings` 깔때기(영속+재조립) · 설정 쪽 변경도 버튼에 역동기화 ·
  언어 변경 시 라벨 재구성.
- **SVG 4종 자체 작도**(Lucide 시각 문법 참고 · 원장 [10 §4](../10-decision-record.md)):
  기본 = 말풍선+점 3 · 이름순 = A/Z+↓ · 최근 접속 = 시계+↓ · 온라인 = 상승
  신호 막대. `mkicons.sh`로 96px 알파 구움 + **20px 축소 판독 실측**(아이콘도
  추정 금지). 갤러리 25번째 항목으로 편입(팝업 z순서 포함).

**611 green** · clippy 0 · rustdoc 0 · 3신원 재기동 · 툴바 슬롯 표시 캡처 확인.

## 정렬 사슬 확정 — 구획 안은 ① 최근 대화 ② 최근 접속 (main · 사용자 확정)

**사용자 확정** — "접속된 사람 중 ① 최근 대화한 대상이 더 위 ② 최근 접속한
대상은 그다음. **비접속 대상도 동일 기준**."

기본 사슬을 **고정 → 접속 계층(세션>발견>오프라인) → ① 최근 대화 → ② 최근
접속(분 버킷)**으로 확정(기본 모드 = 최근 대화순 · 미지 저장값 관용 폴백).
시드돼 있던 구 기본값("seen")은 사용자가 고른 값이 아니라서 세 신원의
settings.cfg를 종료 상태에서 이관(seen→chat). 회귀: 비접속 구획 동일 기준 ·
대화 동률 = 접속순. 607 green.

## 정렬 플리커 해소 + 온라인 우선 구조화 + 설정 스크롤 (main)

**실기 보고(사용자 2건)** — ① "온라인 대상이 먼저 보였다가 처음 목록처럼 보였다가
반복" ② IME 설정 카테고리에서 "내용이 더 있는데 스크롤이 안 된다".

**① 플리커 — 원인은 "갱신 때 정렬 누락"이 아니라 정렬 키 자체의 요동**:
최근 접속(last_seen)이 발견 비컨(800ms)마다 ms 단위로 갱신돼, **마지막 비컨의
주인이 매 갱신 바뀌며 동률 상대의 순서가 뒤집혔다**. 처방 2겹 —
접속을 **3계층**(0 세션 중 · 1 발견됨 · 2 오프라인 — 발견만 된 상대와 대화 중
상대를 같은 층에 두지 않는다)으로 가르고, 최근 접속은 **분 단위 버킷**으로
비교(버킷 동률 = 이름순 → 안정). "온라인이 더 위" 요청은 계층을 전 모드 공통
선두(이름순 제외)로 구조화 — 설정의 "접속 우선" 옵션은 중복이라 제거(저장값
관용 폴백). 그룹 섹션은 원래 인덱스 공간에서 피어보다 먼저라 항상 최상단.
**실측**: 재기동 후 3초 간격 스냅샷 3장 픽셀 해시 동일(플리커 0) · 순서 =
그룹 → 발견 상대(이름순) → 오프라인(beep-*).

**② 설정 스크롤** — 우측 패널 총높이 합산이 **설명 워드랩 예약분(2~3줄)을 빼고**
계산돼, IME처럼 긴 설명이 많은 카테고리에서 스크롤 상한이 실제 끝보다 작았다.
배치 루프와 같은 추정식으로 합산(회귀: 상한 스크롤에서 마지막 행이 뷰포트 안).

**607 green** · clippy 0 · 3신원 재기동 ✅.

## 목록 속성·정렬·고정(핀) + 구성원 모달 상태 색 + 설정 고급 (main · 자율 배치)

**사용자 요청 3건**(자리 비움 — "순서대로 진행하고 직접 테스트"):

**① 그룹 구성원 모달 — 접속 상태 + 목록과 같은 색**: `link_color(theme, LinkState)`
를 peer_list에서 추출(색 뜻의 단일 지점 — 여기와 다르게 칠하면 그게 버그)하고,
알림 모달에 **상태 목록 모드**(`set_status_list` — 줄 앞에 같은 팔레트 점)를 신설.
구성원 판정은 목록 행과 같은 4상태(Active/Connecting/Lost/Idle · refresh_rows
동일 기준) · 모달 내용은 **여는 순간** 계산(`pending_members` → about_to_wait).

**② 설정 "고급" — 백업/복원/초기화**: 신원 백업과 같은 행위 항목(Action) 3종.
백업 = settings.cfg를 선택 폴더로(플러시 후 복사 · 파일명에 지문). 복원 = 백업
파일의 **아는 키 전부를 정식 깔때기(apply_settings)로 즉시 적용**(hot-swap 일습 —
테마·IME·목록·프로필 전파까지 · 모르는 키 보존 · 행위 키는 손편집이어도 제외).
초기화 = **확인 모달**(파괴적 — AlertCtx::SettingsReset) 후 레지스트리 표시 항목
전부 기본값(숨김 키·신원·핀·그룹은 유지). 선택 모달 창 제목을 "확인"으로 일반화.

**③ 목록 속성·정렬·고정(핀)** — 상대별 관리 속성에 **최근 접속·최근 대화·현재
접속 여부**를 추가(기존 = PeerId(=v1 UserId)·표시이름·이메일·전화·아바타):

- **영속 = trust.seg v2**(레코드 꼬리: fav u8 · last_seen/last_chat u64) — 핀
  목록과 같은 민감도(인간관계 메타)라 같은 암호화 세그먼트. **v1 파일 관용**
  (꼬리 없음 = 기본값 · 마이그레이션 테스트). 최근 접속은 발견 비컨마다 갱신되므로
  **60초 스로틀 저장**(핀·차단·이름은 여전히 즉시). 기록은 **아는 상대만**(발견만
  된 상대에 기록을 만들면 "핀 = 연결됐던 기록" 부팅 시드 의미가 깨진다).
- **기록 지점**: 발견 Appeared·세션 성립 = note_seen · 메시지 수신/발신 = note_chat.
- **정렬**(`ui.list_sort` 설정 · hot-swap): **고정(★) 구획이 항상 먼저**, 각 구획은
  모드별 속성 사슬 — 최근 접속순(기본)/최근 대화순/접속 우선/이름순. 순수 함수
  `peer_order_key` + 회귀 테스트.
- **고정 토글**: 우클릭 메뉴 "목록 상단에 고정/해제"(상대·그룹 공용 문구) ·
  ★ 글리프 표시(색만으로 가르지 않기). **그룹도 핀 가능** — groups.seg **v3**
  (공유 레코드 pinned 꼬리 · v2 관용) · 그룹 구획도 고정 먼저 + 이름순.
- **프로필 카드에 "최근 접속/최근 대화" 행 추가**(상대 시각 표기 "N분 전" ·
  기록 없음과 미공개를 구분).

**검증** — 606 green(신규: trustfile v1 관용 · peer_order_key 사슬) · clippy 0 ·
rustdoc 0 · 3신원 재기동 ✅. 화면 잠금으로 캡처 불가 — 육안(정렬·★·모달 색·고급
설정)은 복귀 후 확인 대상.

## 전체 문서 현행화 + 08-14 미기록분 회수 (main · 소스 무변경)

사용자 요청: *"소스는 수정하지 않는 수준에서 변경된 내용을 확인하고 전체 문서에 대한
최신화 진행."*

### ① 기록 트랜잭션이 한 번 끊겼다

08-14 커밋 2건(`1b8b427` 상태 표시 시각 규약 + Lucide 자산 4종 · `bd47a1b` 물음표
재작도 + 파생본 규약)이 **STATUS에 없었다.** 원인은 병렬 세션 — 같은 작업 트리를 만지던
세션이 커밋할 때 내 journal 편집까지 함께 스테이징해 가면서(`ccdcdd5`), journal 본문은
살아남았는데 **STATUS 차수만 비었다.** [16 §3] "내가 고친 것만 add"가 지켜지지 않으면
**남의 커밋 사유가 유실되는 게 아니라 기록 순서가 어긋난다**는 사례가 하나 더 늘었다.
→ **08-15 2차로 회수**(M3-19·M3-20·M3-14 설계 · `tools/mkicons.sh` 승격 · 파생본 규약).

### ② 뒤처진 문서 6종 — 무엇이 얼마나 뒤처졌나

| 문서 | 뒤처진 값 | 고친 값 |
| --- | --- | --- |
| `README.md` | 08-13 · **v0.1.5** · 586 green · 산출물 **0.76MB** | 08-15 · **v0.1.6** · **603 green** · **약 1.7MB** + 실증 행 신설(2-PC·3-신원) |
| `docs/README.md` | 34번 "**H-1~21** · 교훈 5" · 14번에 §12·§13 없음 | **H-1~27 · 교훈 8** · 14번에 상태 배지 2절 추가 |
| `MILESTONES.md` | 표 갱신선 586 · M3 행 08-12 · ★**그룹 2행이 아직 "📐 설계"** · 배포 v0.1.4 | 603 · M3 행 08-15(08-14~15 추가분) · **그룹 전송 ✅ / 그룹 관리 🚧** · **수동 엔드포인트 📐→🚧** · v0.1.6 |
| `CLAUDE.md` | §1 586 green · §5 헤더 "08-13 21차" · M3-1e 미완 표기 | §1 603 + M3-21/H-27/상태 규약 · §5 "08-15 2차" + 요약 블록 · M3-1e ② 완료 반영 |
| `TODO.md` | 크리티컬 패스 "08-12 현행" | 08-15 현행(그룹 개통·IME 완주 취소선 + M3-19/14 추가) |
| `STATUS.md` | 08-14 상태 규약 누락 | 08-15 2차로 회수 |

★ **가장 큰 괴리는 그룹이었다** — 08-13에 G1~G3가 개통되고 3-신원 실기까지 마쳤는데
MILESTONES 기능표에는 **"📐 설계"** 로 남아 있었다. 마일스톤 개요(M5 행)에는 개통이
적혀 있어서 **같은 문서 안에서 두 표가 서로 다른 말**을 하고 있었다.
교훈: **개요 표를 고칠 때 기능 표도 같이 본다** — 기능 현황은 개요보다 늦게 늙는다.

### ③ 검증 — 읽기 전용, 소스 무변경

- `cargo test --workspace --features …` = **604 passed / 0 failed / 0 ignored**.
  다만 작업 트리에 **다른 세션 미커밋 변경 4파일**(`i18n.rs`·`settings.rs`·`app.rs`·
  `ime_gate.rs` · +491행)이 있다 → **문서에는 HEAD 기준 603**을 썼다(08-15 1차 기록과
  일치 · 초과 1건은 그 세션의 진행 중 작업). **남의 트리를 stash·정리하지 않았다.**
- 산출물 = `target/release/nexa-beep` **1,796,632B ≈ 1.7MB**(게이트 10MB의 17%).
  빌드 시각 00:32 < HEAD 01:42라 근사치로 표기했다.
- 스테이징은 내가 고친 문서만(`git add <파일>`).

## IME 기준값 설정화 — 하드코딩 9종 + 토글 2종 = 설정 "한글 입력(IME)" (main)

**요청(사용자)** — 틱 폴백 250ms·신선도 40ms 등 하드코딩 기준값을 설정에서 조정
+ 켜고 끌 수 있는 것은 토글로.

- **`ImeTuning`**(게이트 공개 타입 · Default = 실측값): `stale_ms` 250 ·
  `same_key_ms` 40 · `pending_ms` 150 · `echo_ms` 120 · `stash_ms` 300 ·
  `owed_ms` 800 · `pre_clear_ms` 300 · `swallow_ms` 2000 · `selfcommit_ms` 1000
  — **게이트 내 시간 상수 전수**(인라인 250/2000/1000까지 포함)를 필드로.
- **토글 2종**: `ime.inject`(삼킨 키 보충 주입 — 끄면 관측 잔재도 청소) ·
  `ime.leak`(유출 자모 조합기 — 끄면 낱자 직결).
- 설정 카테고리 **"한글 입력(IME)"** 11항목(RadioInput 프리셋+직접 입력 · 4언어) ·
  설명에 **플랫폼 경계 명기**(macOS 전용 실측 — Windows 무관 · 사용자 질문 반영).
- 배선 = `apply_ime_tuning` 한 깔때기(부팅 + `ime.*` 어느 키든 hot-swap).
- 회귀: stale 단축이 더 빨리 정산 · inject 끄면 보충 없음.

**Linux는?**(사용자 질문) — 미지. IM 프레임워크마다 이벤트 순서가 달라 비슷한
경합이 있을 수 있으나 실측 전엔 모른다 → [TODO LIME-1](../TODO.md) 등록 ·
[34 §2-10](../34-hangul-input-issues.md) 플랫폼 절 신설. **604 green**.

## H-27 — 같은 문자 반복이 판정을 무너뜨린다 ("ㅇㅇㅇ222" · 대화창/갤러리/프로필 공통)

**실기 보고(사용자, "제어 없이 대기")** — "ㅇㅇㅇ222" 순서대로 타이핑 →
"ㅇㅇ2ㅇ22". 자동 실기(osascript key code + `NEXA_IME_TRACE` + 스크린샷 픽셀
판독)로 합성 재현 = "ㅇㅇ22"(ㅇ 1·2 하나씩 유실). 트레이스 + 재생으로 원인 3중
확정 — 공통 뿌리는 **문자 "내용" 대조가 같은 문자 반복을 못 가른다**:

① 유출 첫 ㅇ(보류)이 다음 키의 preedit "ㅇ"에 starts_with로 중복 오폐기 →
   `SAME_KEY_MS`(40ms) 신선도 조건(같은 keypress의 keydown→Ime는 수 ms — 시각이
   가른다) ② 삼켜진 '2'가 다음 '2'의 정상 도달로 오인 소진 + 배달 증거 링이 잔여
   주입까지 차단 → **증거 링 폐기, 선배달 장부(owed)로 교체**(프록시 지연 짝을
   관측 도착 시점에 상쇄 — 큐 잔여 = 전부 진짜 삼킴. 불변식: 관측 수 = 도달 수 +
   삼켜진 수) ③ 조합 중 stale 틱 주입이 Commit보다 앞에 박힘(실타의 "2ㅇ") →
   조합 중엔 대기 + 주입 경로도 leak 선확정(`inject_char`) + 조합 중 keydown 관측
   짝 소진 + `PRE_CLEAR_SLACK`(300ms).

**검증** — 재생 3종 박제 + 기존 G1 2종("나!@#"·"다123") 새 의미론에서 통과(회귀 0)
+ 합성 자동 실기 재검: 삼켜진 셋째 '2'의 틱 폴백 주입 트레이스 실측 + 입력창
**"ㅇㅇㅇ222" 픽셀 확인**(이번 세션은 스크린샷 Read 가능 — 판독 루프가 살았다).
**603 green** · [34 §2-10](../34-hangul-input-issues.md) H-27 등록(✅ 22건 · 재생 17종).

**교훈** — 반복 입력에서 진실은 내용이 아니라 **수와 시각**이다. 내용 대조로 짝을
맞추는 판정 전부가 후보 함정(§2-10에 각론 박제).
