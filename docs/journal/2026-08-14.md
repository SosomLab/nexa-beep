# 2026-08-14

> 시간 역순(최신이 위).

## 툴바 기본 32px + 48px 옵션 추가 (main · 사용자 요청)

**왜 지금인가** — 08-14에 툴바 오른쪽 프로필 버튼이 **내 아바타**를 아이콘으로 쓰게
되면서(`refresh_toolbar_avatar`), 그 자리가 **12간지 그림이 가장 작게 축소되는 지점**이
됐다. 24px에서는 160px 자산이 1/6로 줄어 형태가 뭉개진다. 08-09에 32→24로 내렸던
결정을 **아이콘이 글리프에서 그림으로 바뀐 사정**으로 되돌린다.

**고친 곳 — 기본값이 두 군데였다**

| 파일 | 변경 |
| --- | --- |
| `nbeep-ctl/controls/toolbar.rs` | `DEFAULT_ICON` 24 → **32** |
| `nbeep-ui/settings.rs` | `RADIO_DEFAULTS`의 `ui.toolbar_size` `"24"` → `"32"` · 옵션에 `"48"` 삽입 |
| `nbeep-core/i18n.rs` | `Msg::Tb48` = `48×48` 신설(4개 언어 공통 표기) |

★ **한 곳만 고치면 화면과 실제가 갈린다** — 컨트롤의 `DEFAULT_ICON`은 "설정을 아직
안 읽었을 때"의 크기이고, `RADIO_DEFAULTS`는 "설정 화면이 어느 항목을 선택 상태로
그릴지"다. 둘이 어긋나면 화면은 24를 가리키는데 툴바는 32로 서는 상태가 된다.

**결과 치수**(논리 px · 호스트가 DPI 배율을 곱한다)

| 아이콘 | 툴바 높이 = icon+16 | 슬롯 폭 = icon+8 |
| --- | --- | --- |
| 16 / 24 | 32 / 40 | 24 / 32 |
| **32(새 기본)** | **48** | **40** |
| **48(신규)** | **64** | **56** |
| 64 | 80 | 72 |

**⚠️ 기존 사용자는 24가 유지된다** — 설정 영속(ADR-0011)이 저장된 값을 그대로 읽는다.
새 기본은 `settings.cfg`에 이 키가 없는 **신규 프로필에만** 적용된다(의도 — 저장된
사용자 선택을 업그레이드가 덮지 않는다는 기존 계약).

**검증** — 워크스페이스 **564 green** · clippy 0 · 3인스턴스 재기동(main 47200 · A 29701 ·
B 29702 · 셋 다 exe mtime 동일 확인). 옛 값을 말하던 주석 4곳도 정정.

**부수 함정(기록)** — 앞선 `sed` 실행의 `cd`가 셸에 남아 상대 경로 빌드·복사가 하위
폴더를 가리켰고, **구 exe로 인스턴스가 잘못 떴다**. [[nexa-beep-run-cycle]]의 "빌드 →
전체 종료 → mtime 확인" 절차가 이걸 잡았다 — **재기동 때 exe mtime 대조는 생략하지 말 것**.

## 컨트롤 라이브러리화 R1~R4 — `nbeep-ctl` 분리 · 측정 O(n)화 (main · 기능 불변)

**요청** — 컨트롤을 별도 라이브러리로 분리 가능하게 모듈화·추상화(Interface/
Abstract/상속) · 일반 함수 성능·모듈화 최적화 · 커버리지 관점 조건 정리 ·
**기능 불변이 최우선**.

- **R1 — i18n 결합 역전**(`f7d59ab`): 분리 후보 9모듈의 유일한 앱 도메인 결합
  (TextBox 메뉴 라벨의 `nbeep_core::i18n` 직접 호출)을 `CtlMsg`+`set_ctl_labels`
  주입(OnceLock — `set_control_size_mult`와 같은 전역 설정 문법)으로 끊음.
  공급자가 `t()`를 부르므로 언어 전환 자동 반영 = 표시 문자열 불변.
- **R2 — `nbeep-ctl` 크레이트 신설**(`16d928b` · 12번째 크레이트): geom·event·
  draw·theme·widget·edit·avatar·raster·controls(17종) 이동. **불변식 = 앱 도메인
  의존 0**(Cargo.toml에 명문). 추상화 구조를 lib.rs에 명문화 — Interface =
  `Widget`·`DrawCtx`(계약·백엔드 교체 지점) / Abstract = `Control`+`ControlBase`
  (공통 상태+기본 메서드 — 구체 컨트롤은 `base()/base_mut()` 둘만) / 상속 =
  조합+위임(Rust 문법). 기존 경로는 **모듈 재수출**로 전부 유지 → 사용부 코드
  무변경 = 기능 불변. `ProbeCtx`는 공개 승격(#[cfg(test)]는 크레이트 경계를
  못 넘는다 — 다운스트림 위젯 테스트용 정식 테스트 백엔드).
- **R3 — 측정 O(n²)→O(n)**(`28f4539`): 캐럿 깜빡임 상시 리페인트로 TextBox·
  ChatView의 접두사 재측정이 상시 비용이 됐다. `DrawCtx::text_prefix_widths`
  신설 — **계약 = out[i] == 접두사 text_width 동일 값**(기본 구현 = 종전 로직
  그대로 · Raster 오버라이드 = 두 경로(기본·폴백 런)의 f32 접기 순서·ceil 자리를
  복제해 **비트 동일**). 비선형 폭 목업 계약 테스트(폭=n²×3 — 선형 가정이면 실패).
- **R4 — 조건 정리 실측**: ctl 전체 pedantic 스캔 — 분기 단순화 후보는
  `FontSlot::Mono→Status` 1건뿐(의도 규칙·주석 명문) = **조건은 이미 깔끔**
  (수치 캐스트 계열은 래스터라이저 본질 — 기존 allow 정책 유지).

**검증** — 564 green(전 스위트 0 failed) · clippy 0 · rustdoc 0 · 표준 사이클
재기동. 산출물 1.67MB(예산 10MB 안). 워크스페이스 12크레이트.

## 아바타 보더 + 툴바 프로필 버튼 = 내 얼굴 (main · 사용자 요청 3건 연속)

**요청** — ① 아바타 원에 3px 보더 · 기본색은 r/g/b 각각 랜덤 · 사용자가 직접 색
지정 가능 ② 작은 아바타에선 2px ③ 프로필 버튼을 툴바 **우측 끝**으로 옮기고
버튼 이미지를 **설정된 프로필 그대로**(내장/이니셜/사진 + 보더)로.

**구현**

- **렌더 원자**: `DrawCtx::stroke_ellipse`(타원 링 AA — 가장자리 **안쪽** width 밴드
  SDF · rect 밖으로 안 나감) 신설 + Raster 구현.
- **색 모델**(core::avatar): `parse_border("#RRGGBB")`(관용 파싱 · 무효 = None) ·
  `default_border_for_seed`(채널별 다른 해시 솔트로 **r/g/b 각각 시드 유도** —
  "랜덤"을 12간지 기본값과 같은 이유로 **키 지문 안정**으로 해석: 실행 난수면
  상대 화면에서 테두리가 요동친다).
- **설정** `profile.avatar_border`(HIDDEN) — 부팅 미설정 시 시드 기본값 저장.
  프로필 화면에 **ColorPicker 행**(기존 컨트롤 재사용 · 라벨 i18n 4언어) —
  변경 = 즉시 보고·재전송. 프로필 창 610→650.
- **와이어**: Info **bit4 = border**(avatar와 같은 꼬리 확장 문법 — image_len 뒤
  순서대로 · 구버전 꼬리 무시 전방 호환 · 수신 `parse_border` 검증 · 미래 확장
  꼬리 관용 테스트).
- **표시**: 프로필 프리뷰 **3px** · 목록 행(상대) **2px** · 툴바 미니 **2px**
  (사용자 확정 "작은 프로필은 2px").
- **툴바 프로필 버튼**: `ToolItem::align_right()`(우측 끝 배치 — slot_rect가 좌/우
  분리 계산) + `ToolIcon::Avatar{img·initials·seed·border}`(사진>내장>이니셜/빈 원 +
  링 — 목록 행과 같은 시각 문법) + `set_item_icon`(호스트 갱신 통로).
  `refresh_toolbar_avatar()` 훅 = 부팅·아바타/보더/사진/표시 이름 변경·사진 디코드
  복귀(MyAvatar를 앱 보관으로 승격 — 프로필 창 없어도 버튼이 내 얼굴).

**검증** — 563 green · clippy 0 · 표준 사이클 재기동 + 무간섭 스크린샷(좌 3아이콘 +
우측 끝 미니 아바타 확인). ⏸ 실기 육안 = 보더 색 변경 왕복·상대 목록 2px 링.

**후속(같은 날) — 원형 클립 굽기**(사용자: "동그라미 밖은 잘라내라 · 사진도") —
사진은 imgdec `circle_mask`가 이미 하고 있었고, 구멍은 **내장 12간지**: 0.92
정사각 안착이라 정사각형에 가까운 그림의 모서리(거리 0.65)가 내접원(반지름 0.5)을
넘었다. mkavatars에 imgdec와 같은 공식의 `circle_mask`를 넣어 **자산에 굽는다**
(런타임 비용 0 · 스와치·프리뷰·툴바 어디서든 일관). 자산 163KB(AA 경계 +3KB).

## 기본 아바타 일습 — 다른 세션 WIP 인계 완주 (main · M3-17 확장)

**경위** — 다른 세션("네트워크 포트·프로토콜 정리")이 기반(12간지 내장 자산 NBAV1 ·
`AvatarChoice` 모델 · mkavatars 굽기 도구)까지 만들고 API 오류로 중단 — 인계받아
남은 전부(용량 최적화·선택 UI·렌더·와이어)를 배선했다. 커밋 4개:

1. **기반 인계 커밋**(`604e6b1`) — 원형 유지(clippy 1건만 손질). 모델의 핵심 설계:
   선택은 **문자열 하나**(`initials`/`none`/`b:키`/`c:파일` — 두 키로 쪼개면 어긋난
   상태가 저장된다) · 미지 값 = 이니셜 폴백(전방 호환) · **시드 안정 기본값**
   ("랜덤"을 문자 그대로 하면 상대 화면에서 내 얼굴이 요동친다 — 키 지문 해시로
   사람마다 다르고 나는 늘 같게).
2. **내장 용량 305KB → 160KB**(`a46d5a8` · 사용자 확정 "필요한 수준으로 축소") —
   변 256→**160**(카드 120논리px가 Windows 기본 125% 배율=150px까지 원본 해상도) +
   `clean()`(투명 픽셀 색 0 통일 · 하위 3비트 절사 — RLE 런을 잇는다). 실측:
   256=275K·192=198K·**160=160K**·128=125K.
3. **선택 UI + 렌더** — 프로필 화면 **스와치 행 14개**([이니셜][없음][12간지…] ·
   현재 선택 링 · 클릭 = 즉시 보고) · **사진과 상호 배타**(스와치 선택 = image_path
   소거 · 사진 선택 = 사진 우선) · 프리뷰 우선순위 = 사진 > 내장 > 빈 원 > 이니셜 ·
   `draw_builtin`(시드 색 원 배경 + 투명 일러스트 — 이니셜·사진과 같은 시각 문법) ·
   목록 행도 원 배경 통일 · 레이아웃 +40px 시프트(창 570→610).
4. **와이어 — 내장은 키만 나른다**(상대도 같은 자산 내장 — 그림 바이트를 나를 이유
   없음): `ProfileMsg::Info`에 `avatar` 필드(flags bit3 · **image_len 뒤** — 구버전
   decode가 꼬리를 안 보는 성질로 전방 호환. 사이에 끼우면 구버전 오프셋이 틀어진다) ·
   수신측 **ZODIAC 검증** 후 즉시 반영(디코드 없음 — imgdec·R-5 무관) · 사진 도착
   시 사진이 이김 · **아바타 변경 = 연결 상대 전원에 능동 재전송**(Request 불요) ·
   **부팅 기본값**: 미설정이면 시드 배정을 저장(사용자 변경 후엔 불변).

**검증** — 561 green(+스와치 배타 · 와이어 왕복 · 전방 호환 꼬리 테스트) · clippy 0.
⏸ 실기 육안(스와치 클릭·상대 화면 반영·2-인스턴스 왕복)은 사용자 확인 대기 —
데스크톱 실사용 중이라 자동 실기는 생략.

## 선택 하이라이트 2차 — 글자와 반전의 클립이 달랐다 (main)

**재보고** — 뷰포트 클립(`bfe324e`) 후 이번엔 반대 증상: *"선택 반전이 오른쪽
2글자만큼 빠져 보인다. 왼쪽도 마찬가지."*

**원인** — 클립 불일치. 글자는 `clip = b`(상자 전체)로 그려 **우측 여백(×·배지
자리 24px)·좌측 패딩(10px) 아래까지 보이는데**, 하이라이트만 뷰포트
(`[b.x+10, b.right()-24]`)에서 멈췄다. 둘의 클립이 다르면 어느 쪽이 맞아도
어긋나 보인다.

**처방** — 글자(placeholder 포함)도 **같은 뷰포트 rect로** 잘라 그린다. 부수
효과 = 텍스트가 × 버튼 자리 아래로 흐르던 것도 정리.

**검증** — ① 회귀 테스트 강화: 전체 선택(스크롤 중) 하이라이트가 **뷰포트
전폭(`[x+10, right-24]`)과 정확히 일치** 단언. ② **자동 실기 픽셀 포렌식**:
합성 타이핑이 긴 문자열을 흘려(216자 중 39자만 도달 — 스크롤 자체가 안 걸림 ·
사용자 코칭 "3배 넣어야 보인다"가 이걸 가리켰다) **클립보드 붙여넣기 288자**로
재현 후, 선택색(D8E8FF)과 포커스 링(D4E6FF)을 픽셀 단위 구분 측정 —
전체 선택·Home+⇧End·역방향 드래그 3상태 모두 **선택 [36..407] ⊂ 뷰포트
[34..408] ⊂ 링 [21..434]** 확인(이탈 0·누락 0). 워크스페이스 green.

**교훈** — ① 같은 영역을 그리는 두 페인트(글자·반전)는 **클립을 공유**해야
한다 — 한쪽만 자르면 "잘림"이 아니라 "어긋남"으로 보인다. ② 합성 입력 검증은
**입력이 실제로 다 들어갔는지부터** 확인한다(안 들어가면 재현 조건 자체가
성립 안 함 — 클립보드 붙여넣기가 안정적).

## 프로필 Choose… 묵은 포커스 — Enter 이중 처리 · 포커스 링 잔존 (main)

**실기 보고(사용자 · 2건이 한 원인)** — ① Display name에서 Enter를 치면 **위의
Choose… 버튼이 눌린다**(피커 재오픈) ② 텍스트박스로 포커스를 옮겨도 Choose…의
**포커스 링이 남는다**. 사용자 자가 진단 그대로: *"Choose 누른 뒤 편집해도
Choose가 포커스를 가진 게 문제"* — 정확했다.

**원인** — `ProfileWidget`은 이벤트를 모든 자식에게 **브로드캐스트**하는데,
`MouseDown` 배타 포커스 갱신이 **텍스트박스 3개만** 대상이었다. Button·Switch는
클릭 시 스스로 포커스를 갖고(`base.focused = true`), **포커스 상태에서
Enter/Space = 클릭/토글**로 처리한다. 그래서 Choose…를 한 번 누르면 그 뒤
텍스트박스를 클릭해도 버튼 포커스가 해제되지 않아 — 같은 Enter가 텍스트박스
확정(정상)과 **버튼 클릭(이중)** 양쪽에서 처리됐다. 참고: 갤러리는 처음부터
**전 컨트롤 배타 포커스**였다 — 프로필만 배선이 부분 복사된 "두 번째 구현" 계열.

**Q(사용자): 텍스트박스가 Enter를 직접 안 잡는 건가?** — 잡는다(`committed` →
`take_committed` 확정 보고). 문제는 전파가 아니라 **브로드캐스트 + 묵은 포커스**로
같은 이벤트가 형제에게도 배달된 것.

**처방** — `MouseDown` 배타 포커스 갱신에 **Choose… 버튼 + 공개 토글 3종 포함**
(클릭된 컨트롤만 포커스 보유). Switch도 같은 구멍이었다(토글 클릭 → 필드 Enter =
재토글 가능성).

**검증** — 회귀 테스트: Choose… 클릭(피커 요청 소비) → 이름 필드 클릭 → **버튼
포커스 해제 단언** → Enter → **피커 재요청 없음 + `profile.display_name` 확정
보고** 단언. 워크스페이스 green.

**교훈** — 브로드캐스트 전달 구조에서는 **포커스 단일성(배타)**이 이중 처리를
막는 유일한 문이다. 컨테이너마다 손으로 배선하는 한 같은 구멍이 반복된다 —
M3-1e(TextInput 공용 코어 + 포커스 관리 일원화) 근거 추가.
