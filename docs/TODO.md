# TODO — 할 일 백로그 (목표 순차 · living)

> **목표를 향한 순차 진행 순서.** 새 항목은 해당 단계 섹션에 append.
> **규모**: 소=반나절/1커밋 · 중=1~2일/1~3슬라이스 · 대=3일+/설계(ADR) 동반.
> **상태**: ☐ 대기 · 🚧 진행 · ✅ 완료(커밋) · ⏸ 보류. **우선**: P0(정체성·게이트) · P1 · P2.
> 단계별 원안·게이트 근거는 [02 로드맵](02-roadmap.md), 기능 관점은 [MILESTONES](MILESTONES.md).

---

## 🗺️ 개발 전체 흐름 (한눈에)

```
M-1 설계 ──► M0 기반 ──► M1 발견 ──► M2 대화 ──► M3 셸·UI ──► M4 전송·무해화 ──► M5 그룹·마감·배포 ──► v1 출시
 (거의 완료)   예산검증     서로 보임    암호화 대화    매일 쓸 만함    파일 안전 송수신     그룹·스팸방어·배포        │
                 │                                                                                        ▼
                 └── SP-1 실패 시 스택/예산 재검토(R-8)                                            v2: 릴레이·다중기기·백업
```

| 단계 | 목표(끝나면 되는 것) | 병합 게이트 | 해소 리스크 | 진행 |
|---|---|---|:--:|:--:|
| **M-1 설계** | 무엇을·어떻게 만들지 확정 | **ADR 11종** + 요구/아키텍처 확정 | — | 🚧 **거의 완료** |
| **M0 기반** | "이 예산이 가능한가"에 답 | **SP-1 통과** 또는 NFR-B 정정 | **R-8** | **🚧** |
| **M1 발견** | 두 대 켜면 서로 목록에 뜬다 | 발견 ≤3s(P95) · **격리 AP에서 발견**(S-3) | **R-2·R-7** | **🚧** |
| **M2 대화** | 암호화된 1:1 대화 + 기록 | 경로 변경에도 스레드 연속 · InMemory 테스트 green | R-1 완화 | **🚧** |
| **M3 셸·UI** | 매일 켜 둘 만한 프로그램 | **OS 구분 불가**(S-7) · 60fps · 유휴 RSS | **R-3·R-9** | **🚧** |
| **M4 전송·무해화** | 파일을 안전하게 | 안전 회귀 전항목 · 전송 링크 70% | **R-5** | ☐ |
| **M5 그룹·마감·배포** | **v1 출시** | **성공기준 S-1~S-8** · 예산 전항목 | R-4 | ☐ |
| **v2** | 릴레이 · 다중 기기 · 백업 | 별도 계획 | — | ⏸ |

**크리티컬 패스(08-15 현행)** — ~~M0-1 → M0-1b → SP-1(크기 축)~~ ✅ → ~~D-22 확정~~ ✅ → ~~M1-3 와이어 포맷~~ ✅ → ~~M2-7 비동기 펌프~~ ✅ → ~~M4-9 종단 ack~~ ✅ → ~~D-25 → M3-15 설정 영속~~ ✅ → ~~D-18 §3 → M2-5a 신원·핀 영속~~ ✅ → ~~M5-4 배포 2채널~~ ✅(v0.1.4 공개) → ~~M5-1 그룹 개통~~ ✅(08-13 G1~G3) → ~~M3-1e ② IME 상태기계~~ ✅(08-14 · H-25/26/27 완주) → ~~M4-5ⓒ 권한 강등~~ ✅(08-15 · R-5 종결) → ~~M1 잔여(M1-2·M1-8·M1-9)~~ ✅(08-15 — 발견 폴백 사다리 S1~S4 개통·L1 구독 3-OS) → ~~M5-1 G4~~ ✅(08-15 재동기·승격) → ~~M3-19/14 상태 배지~~ ✅(08-15) → ~~D-23~~ ✅(08-17) → ~~D-24~~ ✅(**08-21 확정 — 스팸 방어 = v1.0 필수**) → ~~D-18 §4~·D-19~~ ✅(08-21) → **지금 = §7 셰레딩 → M5-3b/c(원격 정책·경로 2축 — R-20) → BL 차단 4축(착수 지시 대기) → M5-5 24h → M5-6 문서 마감([40] 골격 ✅) → v1.0**.
> ⚠️ **배포가 앞서 나갔다** — M5-4는 끝났지만 **M5-1 그룹 전송이 미착수**다. v1 약속("발견된 사용자를 그룹으로 묶어 전송")의 절반이 아직 없다.
**신규 항목 의존** — `D-23 ✅ → M2-4b(N-1 ✅) → {M3-8 알림 강도, M3-9 수신확인}` · ~~M3-10 릴레이·SP-2 = v2(개발 제외)~~ · `M0-2b(SP-2) → M3-10` · `ADR-0009 → M4-7 → M4-8` · `D-24 → v1.0/v1.1 재정렬`.
**병렬 가능** — D-13(L1 구독)·D-14(소켓 검증표)는 M0 직후 M1과 나란히. M3 UI 시안(M3-1c)은 M2 중 착수 가능.
🔐 **보안은 마일스톤이 아니라 상시다** — 열린 보안 항목과 점검 트리거는 **[§9 보안 상시 관찰](#9--보안-상시-관찰-sec--끝나지-않는-항목)**. 기준은 [29](29-wire-security-audit.md) W-1~W-12.
**막힌 것** — **D-8 발견 스파이크는 실기 2대+ 필요**(대행 불가). ADR-0002 타이밍 6종이 여기 묶여 M1 완결을 잠근다.

### 설계 확정 현황 (ADR)

| ADR | 주제 | 상태 |
|---|---|:--:|
| [0001](07-adr-0001-stack.md) 스택 | Rust·자체 CPU 래스터라이저·P2·Wayland+X11·시스템폰트 | ✅ Accepted |
| [0002](08-adr-0002-discovery-transport.md) 디스커버리/전송/암호 | 자체 UDP·TCP·Noise_XX·TOFU | ✅ Accepted |
| [0007](20-adr-0007-multi-device-identity.md) 다중 기기 신원 | UserId 1:M PeerId·주 기기+복구 시드 | ✅ Accepted(구현 v2) |
| [0003](09-adr-0003-transport-abstraction.md) 전송 추상화 | 4계층 경계·InMemory | ✅ Accepted(구현으로 비준) |
| [0004](11-adr-0004-quarantine.md) 수신 무해화 | `.beepq`·등급·상태기계 | ✅ **Accepted**(08-17 · D-12 — 도메인은 M4-1로 선구현돼 있었다) |
| [0005](17-adr-0005-history-at-rest.md) 기록 저장 암호화 | 블라인드 인덱스·크립토 셰레딩 | 📐 **부분 확정** — §3 키 관리 ✅(08-11 · M2-5a 구현) / **§4~ 확정 대기**(M2-5b 선행) |
| [0006](19-adr-0006-manual-endpoint.md) 수동 엔드포인트 | `Locator::Manual`·원격 신뢰등급 | 📐 **Proposed — 확정 대기** |
| [0008](22-adr-0008-profile-disclosure.md) 프로필 옵트인 노출 | 브로드캐스트 미포함·세션 경유 프리페치 | ✅ Accepted |
| [0009](23-adr-0009-shared-folders.md) 공유 폴더 | 가상경로·pull·fail-closed | ✅ Accepted |
| [0010](24-adr-0010-message-priority-notification.md) 등급·알림·수신확인·수신자 릴레이 | 등급=요청 / 강도=수신자 판정 · 릴레이 팬아웃 | ✅ **Accepted**(08-17 · D-23 — 핵심 5판단 승인) |
| [0011](28-adr-0011-settings-persistence.md) 설정 직렬화·영속 | Entry 레지스트리=저장 스키마 · coalescing 저장 · 미지 키 보존 | ✅ **Accepted**(08-11 · D-25 — `crates/nexa-conf` 구현) |
| [0012](31-adr-0012-shared-group-chat.md) 공유 그룹 채팅 | 소유자 roster 서명 · 초대 수락제 · pairwise 팬아웃 | ✅ **Accepted**(08-13 · D-28 — G1~G4 구현) |
| [0013](32-adr-0013-server-modes.md) 서버 모드(릴레이·컨텐츠·관리) | 서버가 **아는 것**의 3단계 · S-0~S-3 불변식 · **§5 그룹 결합**(P-9~P-11 선행 제약) | 📐 **Proposed — 확정 대기**(D-29) · **방향성만**(구현은 별도 저장소·v1 이후 — DR-9) |
| [0014](35-adr-0014-native-file-dialog.md) 파일 선택 다이얼로그 | DR-6 경계 획정 · `FileDialogPort` · OS 네임스페이스는 OS 것 | ✅ **Accepted**(08-18 · D-30 — Win/mac 시스템 다이얼로그 · Linux 자체 피커 유지) |

---

## §1. M-1 — 설계

| ID | 항목 | 우선 | 규모 | 의존 | 상태 |
|---|---|---|---|---|---|
| D-1 | 문서 표준 차용 + 4층 골격 생성([16](16-doc-git-conventions.md) §6) | P0 | 소 | — | ✅ (08-08) |
| D-2 | **경쟁 프로그램 전수 조사** — 국내/국외 목록 + 기능 매트릭스 → [03](03-competitive-landscape.md) | P0 | 중 | D-1 | ✅ (08-08 — 37종·A~D 4계층·매트릭스 23기능×7제품) |
| D-3 | 경쟁 프로그램 **장단점** + 차별화 지점 도출 → [03 §5·§6](03-competitive-landscape.md) | P0 | 중 | D-2 | ✅ (08-08 — 시장 공백·보안 충돌·발견 제약·크기 축 4건) |
| D-4 | [00 비전](00-vision.md) — 왜 만드나·차별화·성공 기준 | P0 | 소 | D-3 | ✅ (08-08 — 경쟁 좌표표·성공기준 S-1~8·긴장 V-1~7) |
| D-5 | [05 요구사항](05-requirements.md) — FR/NFR(**예산 수치 확정**)/제약/리스크 | P0 | 중 | D-4 | ✅ (08-08 — FR·NFR-B 예산 게이트·제약 C-1~6·리스크 R-1~15) |
| D-6 | **ADR-0001 스택** → [07](07-adr-0001-stack.md) | P0 | 대 | D-5 | ✅ **Accepted** (Rust·자체 CPU 래스터라이저·P2·Wayland+X11·시스템폰트) |
| D-7 | **ADR-0002 디스커버리·전송·암호** → [08](08-adr-0002-discovery-transport.md) | P0 | 대 | D-6 | ✅ **Accepted** (자체 UDP·TCP·Noise_XX·TOFU·발견은 미검증 힌트) |
| D-9 | **ADR-0003 전송 추상화** → [09](09-adr-0003-transport-abstraction.md) | P0 | 중 | D-7 | ✅ **Accepted** (08-08 · M1-1에서 구현) |
| D-10 | [01 아키텍처](01-architecture.md) · [02 로드맵](02-roadmap.md) | P0 | 중 | D-9 | ✅ (08-08 — 9크레이트·단방향 의존·스레딩·데이터흐름·M0~M5) |
| D-11 | **차용 자산 실측 평가** → [12](12-asset-reuse.md) | P0 | 중 | D-6 | ✅ (08-08 — **`ctl` 재사용 불가**, 실제 자산 `nexa-gui` 1,187 LOC. R-3·M3 정정) |
| D-12 | **ADR-0004 수신 무해화** → [11](11-adr-0004-quarantine.md) | P0 | 대 | D-6 | ✅ **Accepted**(08-17 — 문서 헤더가 진실 · 이 행이 낡아 08-21 정정) |
| D-16 | **[13 코드 설계 표준](13-code-design-standards.md)** — 모듈화·포트&어댑터·`ActionKind`·인터셉터·계측 | P0 | 대 | D-10 | ✅ (08-08 — 충돌 2건 절충·리뷰 체크리스트 13항) |
| D-17 | **[14 컨트롤·UX](14-control-ux-architecture.md)** — 시각=macOS/동작=OS·3단계 이벤트 | P0 | 대 | D-16 | ✅ (08-08 — DR-16 · FR-U-9~14) |
| D-18 | **ADR-0005 기록 저장 암호화** → [17](17-adr-0005-history-at-rest.md) | P0 | 대 | D-16 | ✅ **Accepted 전체(08-21)** — §3은 08-11 선확정·구현(M2-5a) · §4~ 원안 확정 · **§7 셰레딩 = 확정과 함께 구현 착수**(키 테이블·삭제=키 폐기·lazy 마이그레이션·백업 키 동반) |
| D-19 | **ADR-0006 수동 엔드포인트 등록** → [19](19-adr-0006-manual-endpoint.md) | P1 | 중 | D-9 | ✅ **Accepted(08-21)** — 골격 기구현·실기 · 확정과 함께 M5-3b/c(원격 정책·경로 2축) 구현 착수 |
| D-20 | **ADR-0007 다중 기기 신원** → [20](20-adr-0007-multi-device-identity.md) | P1 | 대 | D-9 · D-18 | ✅ **Accepted** (Q-1: 주 기기 + 복구 시드 / 기록 키 분리) |
| D-21 | **v1 제약 4건 반영**([20 §7](20-adr-0007-multi-device-identity.md)) — `UserId` 타입 자리 · 발신 경로 `PeerId` 집합 일반화 · `sender_device` 필드 · **저장 마스터 키 한 겹 래핑** | P0 | 소 | D-20 | ✅ (08-08 — M0-1b UserId·Recipients 타입 / **M2-4 `sender_device`+`fanout` 배선 완료** / 저장 키 래핑은 store doc 명시 → M2-5 구현) |

| D-22 | **신원 키 파일 복제 대응 확정**([21 §5](21-identity-spec.md) · R-12) — U-P1 `instance` 16B + U-P2 자기 `PeerId` 세션 거부 | P0 | 소 | D-7 | ✅ (08-08 — **사용자 승인(추천안 수용) · 둘 다 채택 + 구현 완료**: U-P1 = `wire.rs` `instance` 필드+`CloneWatch`(창 내 공존 탐지·재시작 무오탐), U-P2 = `NoiseSession` `SelfPeer` 거부. **탐지이지 방지 아님** 명시) |
| D-23 | ✅ **확정(08-17) — ADR-0010 Accepted** · 핵심 5판단 승인 · **수신자 릴레이(외부 채널)는 v2**(TLS 예산·Linux 소리 · **설계만·개발 제외**) · 웹훅 양방향도 v2 검토 · N-1 등급 자리 구현 ✅. 종전:([24](24-adr-0010-message-priority-notification.md) · DR-25) — ⓐ 등급 3종 + **신뢰 게이트 강등표**([24 §3-3](24-adr-0010-message-priority-notification.md)) 승인 ⓑ **수신자 릴레이를 v1에 넣을지**(R-14 TLS 크기 · SP-2 결과에 종속) — 규칙 `조건→채널 집합` · **다중 어댑터 동시 팬아웃** · 채널별 노출 수준 포함 ⓒ **N-1~N-4 프로토콜 자리**(등급 1바이트·ack 제어 타입·기록 상태 필드·ActionKind 5종)를 **M2-3/M2-4에 같이 넣는다** — 나중이면 포맷 변경 | P0 | 중 | D-7 | ✅ **확정 08-17** |
| D-24 | 🔴 **기능 범위 확정**([25](25-feature-scope.md)) — 경쟁 매트릭스 × FR 119건 대조로 **🅐입장권/🅑정체성/🅒경쟁채택/🅓사용자확장/🅔제외** 5등급 분류. **v1.0 커트라인 = 🅐+🅑+🅒4+🅓4 (94 → 약 65건)** · v1.1로 미룸 5건(검색·브로드캐스트·상태/타이핑·**공유 폴더·수신자 릴레이**) — ⚠️ **미루는 건 구현이지 설계가 아니다**(모델·프로토콜 자리는 v1.0에 함께). 서브넷 너머는 오히려 **앞당김**(S6과 같은 코드) | P0 | 중 | D-2 · D-5 | ✅ **확정(08-21)** — 권고안+개정분(C-2·M3-9 제외) 승인 · ★**스팸 방어(M5-3) = v1.0 필수**(BL 최소층 = 수동 차단이라도 출시 전 — 착수는 별도 지시) |
| ~~D-25~~ | **ADR-0011 설정 직렬화·영속 표준 확정**([28](28-adr-0011-settings-persistence.md) · DR-27) — ⓐ 설계 승인 ⓑ Q-28-1 크레이트 위치 ⓒ 기본값 | P0 | 중 | D-17 | ✅ **확정(08-11)** — ⓐ Accepted ⓑ **저장소 안 `crates/nexa-conf`**(안정화 후 분리) ⓒ quiet 1s·max 10s 유지(Q-28-2에서 실사용 조정). **M3-15 구현 완료** |

> **설계에 남은 것**: **ADR-0004·0005(§4~)·0006·0010 사용자 확정** → `Accepted`. ⚠️ **지금 실제로 잠겨 있는 것은 D-23(→ M2-4b → M3-8/9/10 알림·수신확인·릴레이 연쇄)과 D-18 §4~(→ M2-5b 기록 저장)** 둘이다. **D-12·D-19는 구현이 먼저 끝나 확정만 남았고**(M4-1 도메인 · `add_endpoint`), D-22·D-25·D-18 §3는 ✅ 확정·구현 완료.
> ⚠️ **표기 규약(08-12 정정)** — 상태 칸은 **확정 여부**, 본문은 **구현 여부**를 적는다. 둘을 섞어 적어 `Proposed`인 ADR이 `Accepted`로, 해소된 리스크가 미해결로 남아 있었다.
> **실기·코드 필요 항목**(D-8 발견 스파이크 · D-13 L1 구독 · D-14 소켓 검증표 · D-15 SP-1)은 아래 M0/M1에서 실행한다.

---

## §2. M0 — 기반 · 예산 검증

> **목표: "이 예산이 가능한가"에 답한다.** 게이트 — SP-1 통과 또는 [05 NFR-B](05-requirements.md) 정정 확정(**R-8 해소**).

| ID | 항목 | 우선 | 규모 | 의존 | 상태 |
|---|---|---|---|---|---|
| M0-1 | 프로젝트 스캐폴딩 — Cargo 워크스페이스·`rust-toolchain.toml`(stable 고정)·release 프로파일·9크레이트 골격·4타깃·린트/테스트 러너. **최소 지원 OS·MSRV 확정**([07 §8](07-adr-0001-stack.md)) | P0 | 중 | D-6 | ✅ (08-08 — 9크레이트·의존성 역전·CRT정적·크기 프로파일. build/clippy/fmt green. MSRV 1.82 잠정) |
| M0-1b | **★ 횡단 골격 선구축**([13](13-code-design-standards.md)) — `ActionKind`·`ActionCtx`·`Interceptor` 파이프라인 · 포트 스켈레톤(`Clock`/`Rng`/`Meter`/`Tracer`) · **D-21 v1 제약 4건 동시 반영**. **기능보다 먼저** | P0 | 중 | M0-1 | ✅ (08-08 — ActionKind 24종·인터셉터 파이프라인·포트 4종·신원(PeerId/UserId/Recipients)·testkit. 20테스트 green) |
| M0-1c | `ActionKind` ↔ `u16` **안정 코드 매핑표** 초판 · 민감 타입 `Debug` 마스킹 규약 적용 | P0 | 소 | M0-1b | ✅ (08-08 — stable_code 24종·골든/유일성 테스트 · `redact::Redacted/RedactedText` 헬퍼. 29테스트 green) |
| M0-2 | **★ SP-1 예산 검증 스파이크**(D-15) — 빈 창 4타깃 크기(≤3MB)·RSS(≤15MB) · 텍스트 스택 증가분 · **한글 IME** · 500행 60fps · **의존성 라이선스 전수**([07 §6](07-adr-0001-stack.md)). **R-8 해소 지점** | P0 | 대 | M0-1 | 🚧 (08-08 — **크기 차원 해소·P2 확정**. CI가 4타깃 크기 게이트. 잔여 SP-1b/d/e = 실기(디스플레이 필요). ★ **08-12 Windows RSS 관측** — 릴리스 GUI **17.1MB** · 테스트단말 8.5MB(WorkingSet · 발견만 도는 상태)로 **NFR-B-1(30MB) 안쪽 첫 관측** ⇒ ★ **R-8 해소(08-15 mac 재실측)** — 유휴 phys_footprint **18.8MB**(신선)~21MB(8h45m) ≤ 30MB. 기존 85.7은 RSS 지표 오선택(공유 페이지 포함 · 예산 판정 부적합 — 교훈: 지표 정의가 먼저다)) |
| M0-2b | **SP-2 TLS 스택 크기 실측 스파이크**(Q-24-1 · R-14) — 수신자 릴레이(외부 채널)가 **v2로 미뤄져(D-23 확정 08-17) 함께 v2**. 릴레이 착수 시 선행. **개발 제외** | P2 | 소 | M0-2 | ⏸ (릴레이 v2) |
| M0-3 | CI — 4타깃 build/test/lint + **예산 게이트**(크기·RSS·임포트 화이트리스트 · **중립 크레이트 4타깃 테스트** [12 §6](12-asset-reuse.md)) | P0 | 중 | M0-1 | ✅ (08-08 — ci.yml 4잡: lint/test(3-OS)/cross-build(ARM·Intel)/budget(≤10MB). RSS·임포트 게이트는 SP-1 후 발효) |
| M0-4 | [18 빌드&테스트](18-build-and-test.md) SSOT 실내용 · [10 §3 의존성 원장](10-decision-record.md) 개시(건별 라이선스·크기) | P0 | 소 | M0-1 | ✅ (08-08 — 명령 SSOT·CI 표·원장 개시(외부 crate 0)) |

---

## §3. M1 — 발견 (첫 수직 슬라이스)

> **목표: 두 대를 켜면 서로 목록에 뜬다.**(대화는 M2) 게이트 — 발견 ≤3s(P95, NFR-B-8) · **클라이언트 격리 AP에서 S4 성공**(S-3) · 유휴 RSS·CPU 예산(**R-2·R-7 해소**).

| ID | 항목 | 우선 | 규모 | 의존 | 상태 |
|---|---|---|---|---|---|
| M1-1 | `net` 골격 — `Transport` 경계 · **`InMemoryTransport`**(테스트용·[09](09-adr-0003-transport-abstraction.md)) | P0 | 중 | M0-1b · D-9확정 | ✅ (08-08 — Transport/Link/PeerHint/DiscoveryEvent · InMemoryTransport fake(발견·연결·이탈) · core `DisplayName`(RLO 무해화). net 5 + name 6 테스트 green) |
| M1-2 | **L1 링크 상태 구독 3구현**(D-13) — Win `NotifyIpInterfaceChange`/mac `PF_ROUTE`/Linux netlink + 디바운스([06 §2](06-network-stack.md)) | P0 | 중 | M1-1 | ✅ (08-15 — `nbeep-plat::linkwatch` 3-OS(이벤트 **내용 미파싱** — "변했다"만 · raw extern 의존 0) + core `Debouncer`(quiet 1000ms **잠정** — D-8b) 배선 → `Transport::link_changed` 포트 → `UdpDiscovery::kick`(그룹 재조인+즉시 HELLO+S4) · 크로스 win/linux check 0 · ⏸ 실기 = Wi-Fi 토글 3-OS(상태바 "네트워크 변경 감지" + 재발견)) |
| **M1-2b** | **네트워크 전환 세션 연속성 — 상태 유지**(사용자 요청 08-15: 유선↔무선·유선→유선·무선→무선 전환에도 상태 유지) — 있는 조각: L1 감지(M1-2)·재발견(kick)·TCP keepalive 22초 감지·자동 재연결 백오프ⓑ·스레드 대피(재대화 연속 · DR-26 뷰 분리)·핀=키(DR-28 신원은 경로 무관). **설계 잔여**: ⓐ LinkChanged 시 **활성 세션 능동 생사 확인**(keepalive 22초를 기다리지 않고 즉시 판정 — 끊긴 세션은 Closed 처리) ⓑ **직전 활성·핀 상대 우선 즉시 재연결**(백오프 0단부터 — 스케줄 대기 없이 1회) ⓒ 같은 LAN 안 전환은 경로 등급 불변 = **조용히**(DR-28 "경로가 바뀌면 조용히") ⓓ 실기 매트릭스 4종(유선→무선·무선→유선·유선→유선(포트 이동)·무선→무선(SSID/AP 이동)) — 각각 대화 기록·그룹·미승인 전송 상태 유지 확인 | P1 | 중 | M1-2 | 🚧 (★ **08-15 ⓑ 부분 구현** — LinkChanged 시 **재연결 가속**: 걸린 백오프 스케줄 전부 0단·즉시로 리셋 + 직전 대화하다 끊긴 상대(수단 있는 것만) 조용한 연결 1회 — 전환 후 긴 대기 제거 · ★ **08-16 ⓐ 구현** — LinkChanged 시 활성 세션 전부에 기존 `ProfileMsg::Request` 1발(새 와이어 프레임 0 · 죽은 소켓 = write 즉시 실패 → Closed → 리셋된 백오프가 곧바로 재연결 · 산 세션 = 프로필 신선화 부수 효과) · 잔여 = ⓒ 경로 등급 조용히(M5-3c와 함께) · ⓓ 실기 매트릭스 4종) |
| M1-3c | **인터랙티브 헤드리스 채팅**(사람 대 사람 검증 도구) — `--chat-serve`/`--chat-connect`(stdin 타이핑·수신 실시간). GUI와 동일 세션 스택(Noise→TOFU→다중화). **맥↔docker-linux 양방향 대화 실증**(arm64↔amd64) | P2 | 소 | M1-3b | ✅ (08-08) |
| M1-3b | **수동 엔드포인트**(DR-19 · [19](19-adr-0006-manual-endpoint.md)) — `Transport::add_endpoint`(발견 우회 IP/호스트명 직접 연결·신원은 핸드셰이크 확정) · bin `--serve`/`--connect` · **맥↔docker-linux IP 대화 실증**(발견 경계 우회). 잔여 = 원격 신뢰 등급(M4 파일 차단)·인바운드 요청 대기·재연결 백오프 | P1 | 중 | M1-4 | ✅ (08-08 — 핵심+**GUI 수동 추가 UI 완료**: `⌘/Ctrl+K` 주소 입력 오버레이(상태바)→add_endpoint→대화 자동 오픈) · **✅ CLI 실증**(08-09 `--serve`↔`--connect` 지문 확정 · 맥↔Docker) · ⚠️ GUI `⌘K` 오버레이는 육안 미확인 |
| M1-3 | 와이어 포맷 확정([08 §2](08-adr-0002-discovery-transport.md)) · 인터페이스별 바인딩(`IP_MULTICAST_IF`/`IP_PKTINFO`) · **소켓 옵션 크로스플랫폼 검증표**(D-14 · [06 §5-2](06-network-stack.md)) | P0 | 중 | M1-1 | 🚧 (08-08 — **와이어 포맷 확정·구현**: `net/wire.rs` — 고정부 71B(+`instance` 16B D-22)·512B 강제·미지 버전/종류 무시(전방 호환)·이름 무해화+지문 폴백·**골든 레이아웃 테스트 고정** · `CloneWatch`. 잔여 = 소켓 바인딩·옵션 검증표(M1-4와 함께 — socket2 의존 결정 필요)) |
| M1-4 | 발견 **S1~S3**(IPv6/IPv4 멀티캐스트 · 브로드캐스트) **동시 시도** · 자기 패킷 키 필터 + **`LocalDirect` Transport 통합**(발견+TCP 세션) | P0 | 중 | M1-3 | 🚧 (08-08 — **S2+S3 실물 + `LocalDirect` 통합 + 실물 종단 실증** · **S1(IPv6)+S2(멀티캐스트)+S3(브로드캐스트) 동시 발신** — S1은 best-effort(ff02::beb·기본 인터페이스·미지원 환경 조용히 IPv4만) · 수신 소켓 공용/전용 · 잔여 S4(유니캐스트·D-8b): `TcpLink`(길이 접두 프레이밍·폴 타임아웃) · `LocalDirect`(UdpDiscovery+TCP → `Transport` — InMemory 자리에 그대로) · `--live-echo` 헤드리스 종단. **발견→TCP→Noise→암호화 대화 왕복**을 맥 2프로세스 + **Docker 컨테이너 2노드** 양쪽에서 실증. 잔여 = S1(IPv6)·S3(브로드캐스트) 동시 시도·인터페이스별 바인딩 · **GUI LocalDirect 배선 완료(08-08)** — `--window --live`로 실물 발견·대화(창이 `Box<dyn Transport>`라 InMemory↔LocalDirect 한 지점 교체)) — 원 S2 실증 3건: `UdpDiscovery`(socket2·REUSEPORT·자기 패킷 **키** 필터·HELLO/ANNOUNCE 주기·GOODBYE 2회·타이밍 주입) ① 맥 2인스턴스 상호 발견(테스트 `--ignored`) ② 맥 프로브 2프로세스(실 LAN 주소) ③ **Docker 컨테이너 2노드 교차 발견**(`--discover-probe` · 172.18.0.2↔0.3 Hello/Announce 수신 — D-8a-Linux 프로토콜 실증). 잔여 = S1(IPv6)·S3(브로드캐스트) 동시 시도·인터페이스별 바인딩·Transport 통합) |
| **M1-4b** | 🔴 **주소 계열 비대칭 수정 — 발견된 상대에게 연결이 실패한다**(**R-21** · Windows 실측 08-10). 수신 TCP가 `0.0.0.0`(IPv4 전용)인데 광고는 v6까지 3중 발송이고, 주소록이 **마지막 관측 IP로 덮어써서** v6 링크로컬이 기록되면 `Unreachable`. 권장안 = **주소를 후보 목록으로 보관해 순차 시도**(v4 우선·성공 경로 승격 — [06 §4](06-network-stack.md) 폴백 사다리 취지). 듀얼스택 바인딩(`[::]`+`IPV6_V6ONLY`)은 **R-7(OS별 기본값 상이)** 를 함께 검증해야 한다. **회귀 테스트 필수** — 주소록에 v6가 섞여도 연결이 성립하는가 | **P0** | 중 | M1-4 | ✅ (08-10 — **권장안 ⓒ 구현**: `PeerAddrs` 상대별 후보 목록(관측 순·중복 제거·상한 8) + 성공 경로 승격 · 시도 순서 = **성공 경로 → v4 → v6** · `add_endpoint` 다중 해석도 v4 우선 순차(같은 비대칭 재발 방지) · **회귀 6건**(핵심: 죽은 v6 혼입에도 성립 — 루프백 실소켓·CI 가능) · **Windows 실측**: `--live-echo` 2노드 동시·시차(15s) 양방향 왕복 성공·연결 실패 0 · 371 green) |
| M1-5 | `core` 피어 목록 — **PeerId 병합**(FR-D-6 · 다중 경로) · 이탈 판정(goodbye+타임아웃 FR-D-8) · 표시 이름 호스트명 기본값(FR-D-9) | P0 | 중 | M1-4 | ✅ (08-08 — `PeerTable`: 병합 키=오직 PeerId · **goodbye는 경로 단위**(마지막 경로만 이탈) · 타임아웃은 마지막 관측 기준 · **타임아웃 수치는 주입**(D-8 실측 대기 — 하드코딩 금지) · 결정적 정렬 목록. 호스트명 기본값은 힌트 생성 측(M1-4)에서. 순수 로직이라 M1-4보다 먼저 완결 — 실물 발견이 여기 꽂힌다) |
| M1-6 | 최소 UI — 피어 목록 표시(gfx/ui 최소 경로) · **미검증 배지** | P0 | 중 | M1-5 | ✅ (08-08 — gfx `Surface`(클립 보장)+`Font`(ab_glyph·시스템 폰트) · plat `system_ui_font`(3-OS 후보) · ui `peer_list::render`(신뢰 배지 3종 상시·다중 경로 ×N) · bin `--window` = **PeerTable+TrustStore 실물 도메인 경로**의 데모 목록. 3-OS 통합 테스트(한글은 mac/win 단언) · 릴리스 0.53MB. 실물 발견 배선은 M1-4) · **✅ 08-09 육안 실증**(사용자) — 목록 2행 · **배지 전이 `Unverified`→`Pinned` 확인**(FR-S-3) · 타입어헤드 안내. ⚠️ `FingerprintVerified`는 SAS UX(M3-6) 없어 미도달 · ⚠️ **핀이 메모리 전용**(영속은 M2-5) |
| M1-7a′ | **D-8a-Linux Docker 테스트베드**(08-08 실증) — ✅ **검증 완료 2건**: ① Linux 컨테이너 전체 테스트 **146 green**(맥 147 − 한글 래스터 cfg 1건 — CJK 폰트 없는 환경 제외 설계 그대로) ② **컨테이너 2개 간 UDP 멀티캐스트 실도달**(브리지 네트워크 · `239.255.77.77` → RECV 확인) — **M1-4 발견을 실기 없이 컨테이너 2~N개로 테스트 가능**. ⚠️ 가상 스위치 손실≈0 — 타이밍 실측 대체 불가(D-8b 유지). Windows는 Docker 불가(Windows 컨테이너 = Windows 호스트 전용) → VM | P0 | 소 | — | ✅ (08-08) |
| M1-7a | **D-8a 상호운용 검증**(08-08 재정의 — **Windows VM 불요, 사용자 확정**) — ① Linux↔Linux: **Docker 컨테이너 N개** — ✅ **실 프로토콜 교차 발견 완료**(08-08: NXBP 와이어로 컨테이너 2노드 상호 SAW) ⚠️ **실측(08-08)**: **맥 호스트 ↔ Docker Desktop 컨테이너는 멀티캐스트 상호 발견 불가**(`--network host`로도 — 컨테이너의 host = 내부 Linux VM). 맥↔리눅스 실물 검증은 **실기(같은 공유기)** 또는 **브리지 VM(UTM macvlan)** 필요 · 로드맵 **DR-19 수동 엔드포인트**(IP 직접) 구현 시 발견 없이 연결 가능. Linux↔Linux는 컨테이너 2개로 완결. ② Windows 단위·회귀: **CI windows-latest**(매 push 자동) ③ Windows 실행·방화벽·IME: **원격 Windows PC**(망 달라도 무관) ④ **맥↔Win LAN 상호운용만 D-8b 실기 날로 이월**(같은 망에 물리는 날 타이밍 실측과 함께). 타이밍 확정은 여전히 불가(가상 손실≈0) | P0 | 중 | M1-4 | ☐ (M1-4 후 Docker로) |
| M1-7b | **D-8b 실망 타이밍 실측 E-1~E-9**([06 §7](06-network-stack.md)) → 타이밍 6종 확정 → [ADR-0002 §8](08-adr-0002-discovery-transport.md) 정정. 🔴 **실기 2대 + 같은 망**(무선 AP 멀티캐스트 손실·클라이언트 격리·IGMP 스누핑은 VM 재현 불가). 그 전까지 타이밍은 **잠정치 표기** | P0 | 대 | M1-7a | ⏸ **실기 2대 같은 망 필요** |
| M1-8 | **S4**(ARP/NDP 이웃 유니캐스트 프로브 — 클라이언트 격리 대응) · E-3 결과 기반 | P0 | 중 | M1-7 | ✅ (08-15 — `nbeep-net::neigh`(Linux `/proc/net/arp` 완성 항목 · mac `arp -an` 스폰 — pbcopy 선례) · 기동 1회 + 광고 16주기(≈12.8s)마다 서브넷 안 이웃(상한 64) HELLO · ★ **수신측 유니캐스트 Announce 응답**(막힌 망 왕복의 나머지 절반) · `s4_targets` 순수 회귀(서브넷 밖 유출 금지 — FR-S-49) · ★**08-20 WNET-1 ✅ = 3-OS 대칭**(iphlpapi `GetIpNetTable2` 직접 링크 · Unreachable/Incomplete 제외 · **지향 브로드캐스트는 MAC ff 필터**(마스크 무관 — .255 3건 섞이던 실측) · 크로스 검증 = 구현 17개 == Get-NetNeighbor 17개 정확 일치) · ⏸ 잔여 = **E-3 왕복 실기**(멀티캐스트 차단 망 2대 — S-3 판정 · 타이밍 재조정)) |
| M1-9 | 링크로컬 직결(DHCP 없음) 발견(FR-D-7) · 가상/터널 인터페이스 제외 | P1 | 소 | M1-4 | ✅ (08-15 — `nbeep-net::netif`(getifaddrs · unix) 인터페이스별 멀티캐스트 발신(IP_MULTICAST_IF)+서브넷 지향 브로드캐스트+그룹 조인 · **가상·터널 제외**(utun/wg/docker/awdl… 접두사 — VPN 존재 방송 금지 · R-18 결) · 기본 경로 발신은 안전망 유지 · ★**Windows 어댑터 ✅ 08-21**(`101b319` — GetAdaptersAddresses 직접 · offset_of 단언 · 실측 실 IF 4개 열거·마스크/브로드캐스트 정확 · Win 친화 가상 이름 7종 추가 · 한계 = "이더넷 N" 표기 VM host-only는 이름 휴리스틱 밖) · ⏸ 실기 = 2대 직결(169.254) · v6 링크로컬(fe80 스코프별)은 S1 best-effort 지위 유지) |
| **M1-10** | **표시 이름 기본값 정정**(FR-S-50 · **R-19**) — 호스트명 **직접 사용 금지**(macOS 기본은 대개 `{실명}의 MacBook`). 정제/중립 라벨로 교체 · 실명은 **옵트인 + "LAN 전체에 방송된다" 고지** | P0 | 소 | M1-3 | ✅ (08-11 — **Q-29-1 확정 = ⓐ 호스트명 정제 + 지문 라벨 폴백** · 적용 = **즉시 재공지**. `core::neutral_from_host`(장치 단어부터 유지 · 없으면 **fail-closed** → `beep-{지문}`) + `default_display_name` · `plat::host::hostname`(원시 — 정제 필수 주석) · `Transport::set_display_name` 기본 no-op + `LocalDirect`→`UdpDiscovery::set_name`(템플릿 Arc<Mutex> 공유 · **즉시 ANNOUNCE**) · 설정 `profile.display_name`(CatProfile · "auto"/직접 입력 · desc = **LAN 평문 방송 고지** 4어) · 상대 목록은 기존 `PeerTable Renamed`로 갱신. **450 green** · 재공지 실측(60s 주기 전 도달) · ★ **이 PC 실측: 방송 이름 = `beep-6808efd4`** — 사용자명(KIROS33) 미노출. ⏸ 2-PC 상대 목록 rename 실기) |
| **M1-11** | **와이어 평문 자동 회귀**(FR-S-51 · [29 §4-1](29-wire-security-audit.md)) — ① 골든 레이아웃 ② 금칙어 스캔 ③ tap 일반화 ④ Debug 마스킹 · CI 상시 | P0 | 중 | M1-3 | ✅ (**08-15**: ① 기존 골든 + **허용 목록 연접 = 바이트 동일** 구조 강제(전 패킷 종류) ② 발견 금칙어 스캔 ③ tap을 금칙어 세트(이메일·전화·한글 본문·설정 키)로 일반화 ④ Identity Debug 개인키 4byte-hex 전수 부재 단언 — 전부 `cargo test`라 3-OS CI 상시. **SEC-1 잔여 절반 해소** · ★ **08-17 세션 계층 보강** — `tests/wire_plaintext.rs`: 실경로(TcpLink→Noise→Mux) 두 신원을 **투명 TCP 릴레이**로 캡처해 마커 3종(대화·이메일·파일명) 부재+수신 도달 동시 단언 — 기존 ①~④는 인코더 출력 검사, 이것은 **실소켓 종단 바이트** 검사(계층이 다르다)) |
| **M1-12** | **와이어 수동 캡처 절차 상시화**([29 §4-2](29-wire-security-audit.md)) — 멀티캐스트 조인 덤프 스크립트를 저장소에 두고(`sudo` 불필요) **발견 포맷을 바꾼 커밋에서 재실행**. 판정 = ASCII에 사람이 읽을 것이 없는가 · 여러 패킷의 **고정 필드 = 재식별자** 탐색 | P0 | 소 | M1-11 | ✅ (08-15 사용자 승인 — **`tools/wirecap.sh`**(그룹 조인 수신 · hex+ASCII · ★고정 구간 자동 요약 = 판정② 보조) · 29 §4-2를 스크립트 기준으로 개정 + 트리거에 **발신 경로 증가 커밋** 추가 · **첫 실측 통과**: 3신원 실물 — ASCII = NXBP+표시 이름만 · 고정 구간 = 전부 알려진 필드(새 재식별자 0) · 같은 seq 5부 = M1-9 팬아웃 사본으로 설명) |
| **M1-13** | **발견 호환성 표면 — 버전·세대·포트**(사용자 검토 요청 08-11) — 현재 실태: ① 와이어 `ver` 1B는 있으나 **미래 버전 = 조용히 무시** → 신·구버전이 서로 **투명 인간**(왜 안 보이는지 아무도 모름) ② `flags` u16은 예약만 ③ **발견 포트 47100 고정** — 다른 앱이 배타 점유하면 `UdpDiscovery::spawn` 실패 → **GUI `expect` 패닉**(기동 불가). 설계안(검토 08-11): ⓐ **포트·매직은 프로토콜 헌법으로 영구 동결**(포트 변경 = 다른 제품 취급 — "들리는 채널"을 영구 보장) ⓑ **신버전이 구버전 와이어를 병행 발신**(dual-announce · N-1 하위 발신 정책 — 구버전 목록에도 뜬다) ⓒ **flags에 세션 세대(gen) 비트 배정** → 비호환 상대를 목록에서 **제외가 아니라 "버전 불일치 — 대화 불가" 표시**(조용한 비호환 금지) ⓓ 세부 능력 협상은 세션 성립 후 첫 프레임(capability exchange — `Caps` 자리 기존) ⓔ **포트 점유 시 발신 전용 강등**(수신 소켓 실패해도 송신은 임의 포트라 가능 — 상대 목록에 나는 뜨고, 상대가 나를 클릭하면 인바운드로 대화 성립 · 상태바 "발견 수신 불가 — 단방향" 고지 · 패닉 제거) · 포트 사다리는 발신 배수·스캔 오인 비용으로 **보류**. 착수 시 ADR-0002 §2 개정 필요 | P1 | 중 | M1-3 | 🚧 (★ **08-15 ⓔ 완료** — 포트 배타 점유 = 패닉 → **발신 전용 강등** + 상태바 고지(부팅 net_hint·respawn 양쪽) · `UdpDiscovery::recv_degraded` · unix 점유 회귀(Windows는 SO_REUSEADDR 의미가 달라 시뮬레이션 불성립 — R-7 결) · 잔여 = ⓐ~ⓓ(dual-announce·gen 비트·capability)) |
| **M1-14** | **수동 연결 주소 정규화를 CLI에도**(08-13 실기 · [26 §6](26-run-and-manual-test.md)) — GUI 모달은 포트를 생략하면 `:47200`을 붙이는데(`nbeep_ui::addr_prompt::normalize_endpoint`) **CLI `--chat-connect 10.0.0.5`는 `BadAddress`** 로 떨어진다. 같은 입력이 경로에 따라 갈리는 건 규약이 아니라 누락 — 정규화를 **공용(코어/net)으로 내리고** `--chat-connect`·`--connect`에 적용. ⚠️ 비-IP 문자열은 `to_socket_addrs()`가 **DNS로 흘러** 오해를 주는 `Unreachable`을 낸다(실기: 오타 `10.60.218.517`) → 형식 검증을 먼저 태운다 | P1 | 소 | M1-3 | ✅ (08-15 — `nbeep_core::endpoint` 신설(GUI `addr_prompt`는 재수출 = 사용부 무변경) · CLI `--connect`/`--chat-connect` 정규화+형식 오류 즉시 안내 · ★ 숫자·점뿐 호스트 = IPv4 리터럴 강제(옥텟 오타 DNS 유출 차단 · 호스트명/DDNS 통과) · 회귀 4종) |
| **M1-15** | **`--discover-probe`가 실제 세션 포트를 안 찍는다**(08-13 실기 · [26 §6](26-run-and-manual-test.md)) — 출력의 `from=`은 **UDP 발신 주소**지 TCP 세션 포트가 아닌데, 그걸 그대로 `--chat-connect`에 넣어 실패했다(실기에서 실제로 밟음). 패킷은 `tcp_port`를 이미 나르므로 **`tcp=<N>` 한 필드 추가**면 끝 — 발견이 닿지 않는 상대에게 알려줄 값이 프로브 출력에 없다는 게 문제의 핵심 | P1 | 소 | M1-4 | ✅ (08-15 — `tcp=` 필드 추가 · 실측 `from=…:62197 tcp=47200`(UDP 발신 포트 ≠ 광고 세션 포트 실물 확인) · 26 §6 문제 표 미해결 표기 해소) |

---

## §4. M2 — 대화 (신원 확정 + 암호화 기록)

> **목표: 목록에서 고르면 암호화된 1:1 대화가 되고 기록이 남는다.** 게이트 — 경로 변경에도 **대화 스레드 연속**([09](09-adr-0003-transport-abstraction.md) 규칙 3) · InMemory로 상위 계층 테스트 green.

| ID | 항목 | 우선 | 규모 | 의존 | 상태 |
|---|---|---|---|---|---|
| M2-1a | **세션 추상화** — `Link` core 이관 · `Session` 트레이트 · **`PlainSession` 스텁**(crypto testkit) · core `duplex` 링크 fake | P0 | 중 | M1-1 | ✅ (08-08 — Session 경계+스텁으로 상위 계층을 암호 없이 검증. crypto→net 없음) |
| M2-1b | `crypto` — **실물 Noise_XX** 핸드셰이크 · AEAD([08 §4](08-adr-0002-discovery-transport.md)) — **snow 채택** | P0 | 대 | M2-1a | ✅ (08-08 — NoiseSession(snow)·Identity(X25519=PeerId)·평문 미노출 검증. 라이선스 퍼미시브 100%·릴리스 0.40MB 불변) |
| M2-2 | **TOFU** — 핀 저장 · 차단(fail-closed) · 이름 재사용 경고·이력 · SAS 안전번호([08 §4](08-adr-0002-discovery-transport.md)) | P0 | 중 | M2-1 | ✅ (08-08 — `TrustStore`/`MemoryTrustStore`·`TrustedSession` 데코레이터·`safety_number`(BLAKE2s, 새 의존성 0). **"지문 불일치 차단"은 v1에 해당 없음** — PeerId=공개키라 키가 다르면 다른 항목. v1 보호는 **신뢰 불상속** + 이름 재사용 경고이며, 원래 시나리오는 v2 `UserId`에서 실체화 |
| M2-2b | **안전번호 자릿수 상향**([21 §3-1](21-identity-spec.md) · Q-21-5) — Signal/WhatsApp **60자리** 기준 상향 + 다이제스트 전체 사용 | P0 | 소 | M2-2 | ✅ (08-08 — 사용자 승인(추천안 수용). 5자리 12묶음·카운터 도메인 2회 해싱 64B 전체 사용·형식/전폭 사용 회귀 테스트) |
| M2-3 | `Session` 다중화(제어/대화 논리 스트림) · 프레임 상한 · **백프레셔**([08 §3](08-adr-0002-discovery-transport.md)) | P0 | 중 | M2-1 | ✅ (08-08 — `MuxSession`/`StreamId`(Control·Chat) · `[stream 1B][payload]` 봉투 · 미지 스트림 무시(전방 호환) · `MAX_PAYLOAD` 65,518 · 큐 64 초과 시 `Backpressure` fail-closed. 실물 Noise 위 통합 테스트) |
| M2-4 | 1:1 텍스트 송수신 · **논리 시퀀스 · 중복 제거**(발신 경로 = PeerId 집합 일반화, D-21) | P0 | 중 | M2-3 | ✅ (08-08 — `ChatMessage` 봉투(`[ver][sender_device 32B][seq][kind][utf8]` · 골든 테스트) · `Sequencer`(재시작 복원 API) · `DedupIndex`(키=(기기,seq)·창 1024·창 밖 과거=중복 간주 fail-closed) · `fanout`(1:1=그룹=같은 경로·개별 전달 보고 FR-G-4) · **sender_device≠세션 인증 상대 = 거부** · 미지 kind는 Unsupported로 순서 보존. 실물 Noise 종단 통합) |
| M2-4b | **등급·확인 프로토콜 자리**(N-1~N-4 · [24 §7](24-adr-0010-message-priority-notification.md)) — 메시지 봉투 `importance` 1B(미지 값=`Normal`) · **`ack` 제어 메시지**(`Delivered`/`Acknowledged`) · 기록 레코드 상태·시각 필드 · `ActionKind` 5종 + `stable_code` 대역. **M2-4와 같은 슬라이스** — 나중이면 포맷 변경. ★ **`Bye`(정중한 종료) 제어 자리도 함께 검토**. | P0 | 소 | M2-4 · D-23확정 | 🚧 (★ **N-1 등급 자리 ✅ 08-17** — `Importance{Normal/Notice/Urgent}` = ChatMessage kind **상위 니블**(Normal = 바이트 동일·하위 호환 · 미지 = Normal 강등 · kind는 하위 4비트) · 회귀 4종 · ★ **N-2 ack ✅ 08-17**(`ChatAck` Control 태그 10 · 자동 Delivered = 메인 스레드·`chat.send_delivered` 수신자 제어+검증 게이트·프라이버시 무유출) · 잔여 = N-3 기록 상태(M2-5b 위)·N-4 ActionKind 5종+stable_code·Bye 자리·수동 Acknowledged UI(M3-9)) |
| **M2-5a** | **신뢰 핀 영속 최소 슬라이스**(FR-S-47 · R-17) — 데이터 경로 + 키 계층 래핑(FR-S-32) + 핀 세그먼트 · fail-closed | P0 | 중 | D-18 §3 | ✅ (08-11 — **D-18 §3 확정(A 기본+B/C 승격) 즉시 구현**. ① **신원 키 영속 선행**(`crypto::keyfile` — `identity.key` 68B·Unix 0600·손상 시 **덮어쓰지 않고** 임시 신원 강등 — 핀 영속은 내 신원 영속과 한 몸) ② **`nbeep-store::FileTrustStore`** — `MemoryTrustStore` 위임(도메인 변경 = 추가 API `PinRecord`/`export`/`from_records`뿐) · **키 계층**: 마스터 32B 무작위 + 래핑 키(SHA-256(솔트‖기기 키) — 256비트 무작위 원료라 메모리-하드 불요·승격②만 해당) AEAD(ChaCha20Poly1305 — snow 계열·**트리 증가 0**) · **핀 세그먼트 `trust.seg`**(기록과 분리·이름·PeerId 평문 미노출 테스트) · **변경 즉시 저장**(write-through — 핀은 드물어 디바운스 불요·크래시에 마지막 핀 보존) · 원자적 교체 ③ **fail-closed**: 손상·키 불일치 = **잠김**(전부 Unverified + 상태바 고지 + **원본 파일 보존**·영속 중단 — 조용한 재핀 오염 방지) ④ **458 green**(+7) · ★ 실측: 재시작 전후 **같은 PeerId**(`17c470c2`) 방송 확인. ⏸ 2-PC 핀 유지 실기(같은 망) · CLI 도구는 의도적 임시 신원 유지) |
| M2-5b | `store` 나머지 — ~~데이터 경로 결정~~(✅ M5-4e) · ~~**암호화 대화 기록 저장**~~(✅ 08-18 `history-v1` · ~~키 계층~~ ✅ M2-5a 래핑) · ★**08-21 전수 실측 감사 ✅**(실기 GUI 데이터 매직+strings — 기록 1:1/그룹·오프라인 대기·격리물+사이드카·핀/이름 이력·그룹 roster·PII 사이드카·상대 사진 캐시 **전부 암호문**(NBSE/NBTS/NBGS) · 평문 흔적 0 · 평문 = 설정값·identity.key(루트)·아바타 키 캐시(비PII)·로그(봉투 원리) — 전부 의도 경계 · [17 §4-a] 기록) — ~~동기화 폴더 감지 경고~~(**✅ 08-21 `cc8b4bf`** — OneDrive·Dropbox·Google Drive·iCloud·Nextcloud 부팅 1회 경고 · 오탐 회귀) · ~~크립토 셰레딩~~(**✅ 08-21 `b04af4a`** — `keytable.rs` 대화별 데이터 키·삭제=키 폐기·백업 키 동반·회귀 4종) | P0 | 중 | — | ✅ (**08-21 종결** — 저장 축 완전 완료) |
| SEAL-2 | **본인 축 평문 3종 검토**(08-21 감사 관찰) — ③ 클립보드 스테이징 = **부팅 24h 정리**(`c5529ca` · 필터 계약 회귀) · ①② 내 사진 사본·축소본 = **의도 경계 확정**(원본이 사용자 평문 파일 — 사본 봉인은 보호 이득 0 · [17 §4-a] 종결) | P2 | 소 | — | ✅ (08-21) |
| M2-6 | 텍스트 **무해화**(RLO·제어문자 FR-S-13 — `core`/`safe` 경계) · 링크 자동 열기 금지(FR-S-14) | P0 | 소 | M2-4 | ✅ (08-08 — `safetext`: `sanitize_message`(이름과 달리 **개행·탭 보존**·CRLF 정규화·상한 16,384자 잘림 보고) · `find_links`(http/https 화이트리스트·범위만 보고 — **열기 API 부재가 구조 보증**) |
| M2-7 | UI 신뢰 배지 · **비동기 수신 펌프**(실시간 수신 GUI 반영) | P0 | 중 | M2-2 · M1-6 | ✅ (08-08 — **비동기 수신 펌프 완료**: 세션을 액터 스레드로 이전(snow TransportState가 read/write에 &mut 요구 — 한 세션=한 스레드) · `set_recv_timeout` 전 계층 위임(Link→Session→Noise/Plain/Trusted/Mux) · winit `EventLoopProxy`+`AppEvent`로 수신 실시간 반영 · 수신도 `DedupIndex` 통과. 신뢰 배지는 M1-6에서 완료. **인바운드→GUI 대화 자동 생성 완료(08-08)** — 남이 나에게 연결하면 accept(핸드셰이크)→TOFU 판정(메인 스레드)→대화·창 자동 생성(Separate=새 창·Single=목록 화면이면 열고 아니면 알림). **양방향 실시간 대화 성립**) · **✅ 08-09 GUI 육안 실증**(사용자) — 터미널 `--chat-live`↔GUI **실시간 양방향**·한글 표시·`Me:`/`Peer:` 구분 |
| **M2-8** | 🔴 **아웃바운드 연결 수립을 워커 스레드로**(사용자 실기 08-10 — **죽은 상대를 더블클릭하면 GUI 전체가 "응답 없음"**). 원인 = `open_session`(`connect`+Noise 핸드셰이크 블로킹)이 **winit 이벤트 루프 스레드에서 실행** · M1-4b 후보 순차 시도(후보당 3초)가 최악 대기를 키움(강제 종료된 상대는 GOODBYE가 없어 목록에 잔존 — FR-D-8 타임아웃 이탈 미배선과 결합). 설계 = **인바운드와 대칭**: 워커 스레드에서 connect+initiate → `AppEvent::Connected/ConnectFailed`로 복귀(TOFU 판정은 지금처럼 메인) · 목록 행 "연결 중…" 표시 · **중복 클릭 가드** · 후보당 타임아웃 단축(워커라 급하진 않음). **프로세스 분리는 불채택**(IPC·상태 공유 비용 · NFR-B 예산 — 액터 모델이 이미 스레드 기반이라 스레드로 충분) | **P0** | 중 | M2-7 | ✅ (08-10 — **인바운드 대칭 워커 구현**: `start_connect` 워커 스폰 → `AppEvent::Outbound/ConnectFailed` 복귀 · TOFU는 메인 유지 · `connecting` 중복 클릭 가드 · 인바운드 선성립 경합 처리 · `transport = Arc<dyn Transport+Send+Sync>` · 392 green. ✅ **무정지 사용자 실기 확인(08-10)** · **잔여 소진(08-11)** — `LinkState::Connecting`(강조색 점) + `add_endpoint`(⌘K 모달) 워커 이관(성공=`Outbound` 합류·실패=`AddFailed` · 동기 `open_session_addr` 제거 = **블로킹 연결 경로 0**)) |

---

## §5. M3 — 셸 · UI 본체

> **목표: 매일 켜 둘 수 있는 프로그램이 된다.** 게이트 — **S-7(스크린샷으로 OS 구분 불가)** · 60fps · 유휴 RSS(**R-3·R-9 해소**).

| ID | 항목 | 우선 | 규모 | 의존 | 상태 |
|---|---|---|---|---|---|
| M3-1 | **위젯 세트 신규 구현** — `nexa-gui` 인프라 이식(`DrawCtx`/`Widget`/`event`/`geom`/`theme`/**`edit`**(08-08 이식 — 캐럿·선택·삽입/삭제·IME 연결점)/`typeahead`) + `WidgetBase` 컴포지션 + 트레이트 기본 메서드 전파([14 §2](14-control-ux-architecture.md)). ⚠️ `ctl` 코드 재사용 안 함 | P0 | 대 | M2-7 | 🚧 (08-08 — **슬라이스 1 완료**: `geom`/`event`(휠 누적기·now_ms 주입)/`widget`(Invalidations 병합)/`theme`(토큰+danger)/`draw`(DrawCtx — dir2 전용 어휘 제외) 이식 + **`RasterCtx` 신규**(우리 gfx 위 백엔드 — SDF 커버리지 AA 라운드 사각형·타원·폴리라인·클립 텍스트). 슬라이스 2(08-08): **`typeahead` 이식** + **`PeerListWidget`**(첫 실물 Widget — 캐럿 탐색·클릭·휠 분수 노치·타입어헤드 cycle·Enter 활성화 폴링·부분 무효화) · bin 인터랙티브 배선(winit→InputEvent 번역). 잔여 = `edit` 이식(M3-3 IME와 함께)·WidgetBase·버튼/입력 위젯) |
| M3-1b | **OS 동작 어댑터**(`PlatformConventions`) — 수정 키·표준 단축키(`StdAccel`)·스크롤 관성·컨텍스트 메뉴·창 닫기([14 §6](14-control-ux-architecture.md)) | P0 | 중 | M3-1 | 🚧 (08-11 — **클립보드 3-OS 실물 완료**: mac `pbcopy`/`pbpaste` · Linux Wayland `wl-copy` → X11 `xclip` 폴백 · Windows CF_UNICODETEXT(08-10). ★ 그전까지 **비-Windows는 통짜 스텁**이라 ⌘V가 조용히 무동작이었다. **컨텍스트 메뉴 컨트롤 완료**(`ContextMenu` — 대화 창 배선). ★ **08-13 텍스트 에디터 일습**(2-PC 실기 피드백 ①): ⌘/Ctrl+C·X·V **전 뷰 일반화**(그전엔 대화 입력창만 — TextBox copy/cut/paste 신설 → AddrPrompt·Profile·Settings 위임 → 창 역할 라우팅) · TextBox **가로 스크롤**(긴 텍스트 캐럿 상실 해소) · **영역 밖 드래그 자동 진행**. ★ **08-17 StdAccel 계층 ✅** — `nbeep_plat::conventions`(StdAccel 열거·primary_is_super·std_accel — **매핑은 여기, 행동은 앱**(DR-21 결) · 배선 2지점 교체 · 매핑 전수 회귀). 잔여 = 스크롤 관성(**mac은 OS 모멘텀이 휠 이벤트로 이미 온다** — Win/Linux 합성 관성만, 실기 튜닝 필요) · 창 닫기 mac 차등(M3-2d 결정지) · 설정 색상 hex 칸 클립보드 위임 · 드래그 정지 연속 스크롤(타이머). **08-16 착수 지점 실측 박제** — ① StdAccel: 단축키가 `primary_down` 블록 안 물리 KeyCode→리터럴 match 하드코딩 · primary 판정도 `cfg!(macos)` 하드코딩 · ctl 계층은 bool만 받는다 — `nbeep-plat::conventions` 신설(trait PlatformConventions: accelerator/scroll_delta/close_action — [14 §6] 스펙 그대로) 후 그 3지점 교체 ② 관성: `WheelAccum`은 분수 노치 이월기일 뿐 속도·감쇠 상태 없음 · winit TouchPhase 미사용 ③ 창 닫기: CloseRequested가 OS 무관 동일(mac 빨간 버튼 = 숨김 관례 분기 없음 — close_to_tray 스위치뿐)) |
| M3-1c | **macOS 시각 언어 수치표 확정** — 라운드 반경·간격·타이포·상태 6종 시각 규약. 시안 대조([14 §5·§9](14-control-ux-architecture.md)) | P0 | 중 | M3-1 | ☐ |
| M3-1d | **3단계 이벤트 전파**(캡처→타겟→버블) · 포인터 캡처 · hover 합성 · IME 조합 중 단축키 가로채기 금지([14 §3](14-control-ux-architecture.md)) | P0 | 중 | M3-1 | ☐ |
| M3-1e | **입력 스택 단일화**(사용자 요청 08-13 — "문제가 퍼지지 않고 한 군데서 해결") — ① **`TextInput` 공용 코어**: EditState+프리에딧 표시+선택 반전+드래그/자동 스크롤+우클릭 편집 메뉴+⌘단축키를 한 컴포넌트로, 단일 행(TextBox)·다중 행(대화 입력)·콤보 편집 칸이 **위임**해서 쓴다(지금은 TextBox와 ChatView가 각자 구현 — 08-13 전수 검사에서 같은 기능이 창마다 따로 뚫려 있었다) ② **IME 중재 상태기계 추출**: app.rs의 조합 게이트·보류-판정·유출 조합기·잔향 억제를 순수 모듈로 빼고 **08-13 트레이스 실측 순서를 회귀 테스트로 박제**(첫 키 유출·낱개 Commit·이중 배달·조합 중 이동 키) | P1 | 대 | M3-1b | ✅ (② IME 상태기계 ✅(08-14) · **① 공용화 08-17 — 여기서 마무리(사용자 확정)**: EditState(버퍼·캐럿·선택 이미 공용)+**EditMenu**(우클릭 편집 메뉴 두 벌→한 벌)+**프리에딧·H-25·display_text**(EditState로 이동·동작 불변). **선택 하이라이트 페인트는 공용화 안 함 = 미시행 근거**: TextBox(단일행+가로 스크롤 = 사각형 1개)와 ChatView(멀티행+줄바꿈+세로 스크롤 = 줄별 N개)의 **기하가 근본적으로 달라** 강제 공유는 줄기하 추상 비용+H-1~27 실기로 다진 픽셀 페인트 회귀 위험만 키운다 — 위젯별 유지가 정당. 상태·로직 공용화(값진 80%)로 M3-1e 종료) |
| H-26c | ★ **한글 1byte 첫 타 유실 — ✅ 해소(08-14 · 사용자 실타건 확인)** — 절차 = 수집·분석 먼저(원인 실측: *조합 세션 종료 후 첫 1byte keydown 1개, 세션당 1회, winit 경계 소비*) → **ImeGate 상태기계 + 재생 14종 박제**(G3) → **keytap 보충 주입**(G1 — 개정 2차: raw 순서 대조 + 배달 증거 링 · 실타건 2회가 순서 붕괴/이중 입력을 잡아 다듬음) → **G2 저장 트리거 조합 확정 + H-25 선택 대체**. 잔여 없음(Windows 무영향 — keytap cfg 격리) · 상세 [34 §2-8·§4-4](34-hangul-input-issues.md) | P0 | 중 | M3-1e | ✅ |
| M3-2 | **트레이/메뉴바 상주**(FR-U-2 · 사용자 확정 08-15 — 요구 확정: 아이콘 = **사용자 프로필 아바타** · 툴팁/우클릭에 표시 이름 · 우클릭 메뉴에 **종료** · `ui.close_to_tray` 스위치(닫기 = 종료 대신 트레이) · 3-OS 동일 패턴) | P0 | 대 | M3-1b | 📐 **분석 완료·세분화**(08-15 — 종전 🔴 "아이콘 의존"은 해소: 아이콘 = 아바타 RGBA 자체 렌더라 외부 에셋·`tray-icon` 크레이트 불필요. **직접 어댑터 판정** — Win `Shell_NotifyIcon`/mac `NSStatusItem`은 시스템 API 직접(의존 0 · MotW/NSSound 선례 문법) · 트레이 **메뉴는 OS 셸 영역 = 네이티브**(ADR-0014 경계 획정과 같은 논리). 상세 = journal/2026-08-15) |
| **M3-23** | **대화함(Conversations) 툴바 메뉴 — 대화 기록 관리**(사용자 요청 08-17) — 격리함(Quarantine)과 **동일 레벨 툴바 아이콘**(구글 Material **서랍(inventory_2/drawer)** 아이콘 · `mkicons-rgba.sh`로 구움 · 자체 렌더 규약). 기능: ① **대화 내용이 저장된 사용자/그룹 목록 표시**(`data/history/*.seg` 스캔 → 핀 매핑 · 그룹 = `data/history/g-{uid}.seg` — ★ **기록 축은 08-19 구현 완료**(`79bf86f` 기록 3지점·부팅 복원·고아 정리 + `01b2ecd` 발신자 라벨 tag 3) · 이 항목의 잔여 = 관리 UI(목록·삭제·백업·복원)만) · 각 행 아바타·이름·마지막 시각 ② 행 옆 **휴지통 아이콘 = 그 상대/그룹 대화 통째 삭제**(2단계 확인 · sealed 파일 삭제 + 메모리 parked/conv 스레드 비움) ③ **전체 삭제**(하단 · 격리함 Clear All 문법) ④ **백업**(전체 or 선택 → 사용자 지정 폴더에 sealed 그대로 or 복호 내보내기 — 착수 시 형식 결정) ⑤ **복원**: **중복 = 덮어쓰기 · 없으면 추가 · 기존 유지**(사용자 확정 — "깨끗한 복원 = 전체 삭제 후 복원" 순서로 사용자가 처리) · sealed 개봉→병합. 재사용 = M2-5b(`record_history`/`restore_history`·`sealed`·`data/history`)·QuarantineWidget 문법(목록·행·2단계 확인). ⚠ 삭제 시 열린 대화창·목록 즉시 반영 | P1 | 중 | M2-5b | ✅ (**08-20 기본 기능 전체 구현** — 사용자 결정 수집(AskUserQuestion): 백업 = **sealed 그대로·전체만** · 행 = 아바타·이름·시각+**마지막 메시지 1줄·파일 크기** · **행 클릭 = 대화 열기** · **이름 필터** · retention 비채택. 구현 = `ConvboxWidget`(nbeep-ui · 회귀 6종)+서랍 아이콘 자체 작도(`icon-drawer-96.alpha`+SVG)+Role::Convbox 일습+피커 2목적(HistoryBackupDir/RestoreDir — 폴더 백업·폴더/개별 파일 복원·**중복 덮어쓰기**)+메모리 즉시 반영(clear_lines·apply_restored_history — 복원이 이긴다). 711 green · **실기 10항 확인 ✓ 08-20**(잔여 육안 = 전체 삭제·열린 창 반영·그룹 행)) |
| **i18n-1** | **상태바·전송 메시지 다국어 보완**(사용자 요청 08-17 — 기본 언어 En인데 한글 하드코딩이 노출) — ★ **인프라 `tf()` ✅**(인자 있는 번역 · {} 순서 치환) + **파일 전송 스레드 메시지 8종 ✅**(거절/취소/시간초과/받지못함/격리됨/위험/형식불일치/평균 · 대화에 남는 영구 텍스트). **잔여 = 상태바 전환형 ~75개**(`self.status = "한글"`·`format!` 49개)**+ 기타 into() ~73개**(오류·안내) · 하단 "수신 :" 라벨 · 같은 tf 패턴으로 일괄. 각 문자열 = Msg 변형 + 4-lang. 회귀 = 전 키 4언어 비지 않음(기존 테스트) | P1 | 중 | — | 🚧 (인프라+전송 스레드 8 ✅ · **상태바 평문 38+format! 34 ✅ = 상태바 한글 0건** · 잔여 = 기타 into() 오류·안내 ~73) |
| **M3-2a** | **Windows 트레이**(이 PC 실기 가능 — 착수 1순위): `nbeep-plat::tray` **TrayPort**(DR-21 — `set(rgba, tooltip)`·`set_menu`·`TrayEvent{Open,Quit}` → EventLoopProxy) + **Shell_NotifyIcon 어댑터**(NOTIFYICONDATAW · 콜백용 **숨은 메시지 창 전용 스레드**(winit wndproc 불가침 — imgdec 워커와 같은 "복귀는 이벤트" 문법) · 아바타 RGBA→**HICON**(NIM_MODIFY로 프로필 변경 동기 — `refresh_toolbar_avatar` 깔때기에 합류) · 툴팁 = 표시 이름(szTip 128자) · **우클릭 = TrackPopupMenu**(이름 헤더(비활성)·열기·종료 — `SetForegroundWindow` 선행: 안 하면 메뉴가 안 닫히는 고전 버그) · **좌클릭 = 창 복원**) + 설정 **`ui.close_to_tray` Switch**(창 카테고리) + `CloseRequested` 인터셉트(on = main 숨김·off = 종전 종료) | P0 | 중 | M3-2 | ✅ (08-15 — 구현 완주 · 플라이아웃 실측 3신원 아이콘=각자 아바타 · 설정은 CatAdvanced(순서 결합 테스트 회피) · ★ **08-18 실기 완주(사용자)** — 우클릭 메뉴(이름·열기·종료)·좌클릭 복원·close_to_tray on 후 X = 숨김 상주 전부 ✓) |
| **M3-2b** | **macOS 메뉴바**(mac PC 몫): `NSStatusItem` 어댑터(objc 직접 — NSSound 선례) — NSImage(컬러 아바타 · 메뉴바 18~22pt@2x) · `button.toolTip` = 이름 · **좌/우클릭 모두 NSMenu**(맥 관례 — 열기·종료) · **AppKit 메인 스레드 강제**(생성·갱신을 winit 메인 루프에서 — 프록시 이벤트) · ⚠ 맥은 "창 닫기 = 앱 유지"가 이미 OS 관례(빨간 버튼) — 스위치 기본값 차등 여부 = 결정지 | P1 | 중 | M3-2 | ✅ (**08-15 mac 세션**: `tray.rs::mac` — NSStatusItem(가변 길이)+NSMenu(이름 헤더·열기·종료 · 좌/우 공통) · 액션 = 선언 클래스 `NbeepTrayTarget` 셀렉터 → 프록시 · 아이콘 = RGBA→NSBitmapImageRep→NSImage 18pt(32px 원본 = 레티나 2x) · **메인 스레드 강제 = MainThreadMarker**(밖이면 None/무시 fail-soft) · 상태는 thread_local(NSStatusItem 비Send) · **실측 = 메뉴바 diff: 35px 아이콘 3개(신원별 아바타) 등장/소거 확인** · **육안 완주 ✅(08-15)** — 메뉴 동작 + close_to_tray on 숨김(진단 삼중 확인)·**Open 복원(사용자 확인)**·Quit 왕복 — mac 트레이 실기 잔여 0) |
| **M3-2c** | 🔴 **Linux 트레이 범위 결정**: 표준 = StatusNotifierItem(D-Bus — KDE OK·GNOME 확장 필요·파편화) · 구현 = zbus 의존 체인 필요(DR-5 검토) vs **v1 미지원 폴백**(트레이 없음 → Linux에선 `ui.close_to_tray` 숨김·닫기 = 종료 유지) — ~~권고 = 폴백~~ → **사용자 확정(08-15) = 구현**: SNI+dbusmenu를 zbus(MIT · Linux 타깃 한정)로 서빙 · 워처 부재 = None(fail-soft — close_to_tray 가드 유지) | P2 | 소 | M3-2 | 🚧 (08-15 — **구현·크로스 체크 ✅**(x86_64-linux check/clippy 0) · ⚠ **실기 미완**(이 PC에서 불가 — KDE/GNOME+확장 데스크톱 실기 잔여: 아이콘 픽스맵 렌더·메뉴 표시·클릭 왕복)) |
| **M3-2d** | 결정지 2건(🔴 사용자): ① `ui.close_to_tray` **기본값** — 공통 off(예측 가능) vs OS 관례 차등(mac on — 빨간 버튼 관례) ② 숨김 중 수신 시 트레이 아이콘 배지/변형 여부(M3-8 알림과 접점 — 범위 획정) | — | — | M3-2a | ✅ (08-15 사용자 위임 "진행해줘" — 권고안 채택: ① ~~차등 권고~~ → **사용자 확정 = 3-OS 공통 off**(X = 종료 · 예측 가능성 — mac 차등 권고를 뒤집음 · 켜기는 설정 고급 옵트인) + **고급 카테고리 = 사이드바 가장 밑**(첫 등장 순서 규칙이라 close_to_tray 항목을 등록부 끝으로 이동) ② **숨김 중 수신 배지 = M3-8(알림)로 이관** — 알림 강도는 수신자 판정·신뢰 게이트(🔴 D-23)와 얽혀 단독 선행하면 결정을 앞지른다 · 트레이 v1은 아이콘 변형 없음으로 획정) |
| M3-3 | **IME 완성**(FR-U-7 한/중/일) · 고DPI 배율(FR-U-6) · 다크/라이트(FR-U-5) · **영역별 글꼴**(크기·굵기·기울임) | P0 | 대 | M3-1d | 🚧 (08-08 — 배율·조합 프리에딧·다크/라이트(설정)·**영역별 글꼴 설정**(기본/메시지/상태 × 크기·faux 볼드·faux 이탤릭 · 기본 크기 상향 13/15/11→16/18/13) 완료. 잔여 = 글꼴 패밀리 선택(폰트 열거)·중일 검증 · **Windows IME 실기 = M3-3b**) |
| **M3-3b** | **Windows IME 실기 검증 — 상세 체크리스트**(FR-U-7 · [27 §6~7](27-typeahead-hangul-composition.md)) — mac에서 확정한 IME 탈피 설계(목록 = IME off 직접 조합 · 대화 = IME 유지 + 자모 보류-판정)가 **Windows IME 이벤트 순서에서도 성립하는지** 실측. 체크리스트 = 아래 WIME-1~9. 기록엔 실측값 필수(추정 금지) | P1 | 중 | M3-3 | ☐ **Windows 실기(이 PC 가능)** |
| M3-4 | 다국어 한/영(FR-U-3) · 키보드 우선 조작·타입어헤드(FR-U-4) | P0 | 중 | M3-1 | 🚧 (08-08 — **영어 기본 + 한/중/일 언어팩** 도입. ★ **08-13 Windows 목록 한글**([27 §8](27-typeahead-hangul-composition.md)) — 목록 창(IME off)은 Windows에서 라틴만 와 한글 불가였다(실기) → `jamo_from_qwerty`(QWERTY→두벌식)+`hangul_mode` 토글(한/영 키 — 드라이버 수준이라 IME 없이 도달)·상태바 고지·`cfg!(windows)`. 501 green · ✅ **실기 확인(08-13 사용자)**. 잔여 = 키보드 우선 조작 잔여·문자열 전수 이관) |
| M3-5 | 상태(자리비움)·타이핑 표시(FR-M-5) · **블라인드 인덱스 전문 검색**(FR-M-4/S-19 — 한국어 음절 n-gram·후보 복호 대조) | P1 | 대 | M2-5 · M3-1 | ☐ |
| M3-6 | **SAS 지문 대조 UX**(FR-S-4) — 결정적 순간 유도 | P1 | 중 | M2-2 | ✅ (08-15 — **상대 프로필 카드에 안전 번호 출력**: 60자리 = 5자리×12그룹 · **고정폭 6그룹×2줄 중앙**(줄이 흔들리면 눈 대조가 어긋난다 — Mono 슬롯) · 안내 "전화·대면으로 직접 대조" · **"일치 확인 — 대조 완료로 표시" 버튼** 1회성 → `TrustStore::verify`(write-through 영속) → 목록 파란 실 배지·카드 ✓ 표시 즉시 갱신(M3-14b와 한 몸 — 이 버튼이 없어 파란 배지에 도달할 길이 없었다) · 검증 후엔 버튼 대신 완료 표시(표적 소거) · 회귀 = 클릭 1회성·검증 후 무시 · 643 green. ★ **08-18 지문 비교로 전환**(사용자 확정): 카드의 60자리 SAS는 **숨기고** 키 지문만 대조 · **`/fingerprint`**(내·상대 키 지문 출력) → 다른 채널 대조 → **`/verify` 직접 대조 완료**(카드+버튼 없이 · `verify_peer`) · `/unverify` 강등 · 카드 대조 완료(초록)/인증 취소(붉은) 버튼도 공존. ⏸ 2-PC 실기(양쪽 같은 지문 육안)) |
| M3-7 | **프로필**(FR-D-15/16 · [22](22-adr-0008-profile-disclosure.md)) — 필드 설정·**노출 확인 UX**(미리보기) · 세션 경유 조회 + 자동 프리페치(속도 제한) · 목록에 신뢰 배지와 함께 표시 · 이름 재사용 경고 확장 | P1 | 중 | M2-3 · M3-1 | ☐ |
| M3-8 | **알림 강도 UI**(FR-U-15~17 · FR-S-41/42) — 배지/토스트/**최상위 창(포커스 비강탈)** · 미리보기 무해화·이미지/파일명/링크 금지 · 잠금·화면공유 시 내용 숨김 · **소리 포트 3어댑터**(Win winmm · mac NSSound · **Linux D-Bus 알림 데몬**, 없으면 무음 폴백 표시) · 신뢰 등급 강등표 · 24h `Urgent` 횟수 표시 | P0 | 대 | M2-4b · M3-2 | 🚧 (★ **08-15 최소 슬라이스 구현**(사용자 요청 "알림 3-OS" · **아키텍처 문서 = [36](36-notification-architecture.md)**) — `nbeep-plat::notify`: mac `osascript display notification` 스폰(번들 밖 동작 — UserNotifications는 서명·번들 요구라 포터블 불가·M5-4a 후 재검토) · Linux `notify-send` 스폰(fail-soft) · **Windows = 트레이 풍선**(`TrayHandle::notify` — NIF_INFO·기존 아이콘 재사용·WinRT 토스트는 AUMID 요구라 T0 불합) · 배선 = 1:1 수신·그룹 수신(방 이름 제목)·파일 오퍼(**파일명 미표시** — FR-S-41) · 조건 = 설정 on + **앱 전 창 비포커스** + 키별 3초 스로틀 · **신뢰 게이트 = 미검증 무음**(DR-25 — mac 소리 생략·Win NIIF_NOSOUND·Linux suppress-sound 힌트) · 설정 `notify.enabled`(기본 on)/`notify.preview`(기본 **끔** — 내용 비노출·FR-S-42) · AppleScript 이스케이프 회귀(본문 스크립트 탈출 차단) · ★ **mac 실측**: 포커스 억제→디포커스 배너·미리보기 끔·미검증 무음 3중 확인 · 크로스 win/linux 0. **잔여** = 등급 3종·강등표·`Urgent`(D-23 ✅ → M2-4b N-1 ✅ · 알림 강도 판정 배선 잔여) · **수신자 릴레이는 v2·개발 제외** · 소리 포트 정식 3어댑터 · Win/Linux 실기 · ★**알림 클릭 = 앱 열기**(08-15 사용자 실기): **Win ✅ 구현**(NIN_BALLOONUSERCLICK → TrayEvent::Open — 트레이 좌클릭 경로 재사용) / ★**mac 정식 경로 구현 완료(08-15 2차 — 사용자 확정 "정식 알림")**: `objc2-user-notifications`(0.2 — keytap과 같은 objc2 판)로 **번들(.app) 실행 = UNUserNotificationCenter**(권한 요청+클릭 delegate `NbeepNotifyDelegate` → 창 복원) · **비번들 = osascript 폴백 유지**(UN은 번들 신원 필수 — 이원화가 구조적 정답) · ★**Cask 실증 ✅(08-15 사용자 확인 — v0.1.7)**: 무서명이어도 **정식 설치(/Applications)면 권한·배너·아이콘 정상**(임시 경로 직접 실행만 보류되는 개발 함정 — 문서 36 명기) · ★**클릭 = 해당 대화까지(08-15 2차 요청 · 8779080)**: 알림에 불투명 대상 토큰(UN identifier `일련|토큰` · Win 풍선 LAST_TARGET) → 클릭 = `TrayEvent::OpenTarget` → 메인 표시 + **기존 대화 열기 경로 재사용**(단일 = 그 대화로 전환 · 분리 = 대화 창 열어 마지막 스레드 · Peer=activate/Group=open_group_thread · 맵 미스 = 메인만) · 왕복 파서 회귀 · ★ **Windows 실기 ✓(08-18 사용자)** — 풍선 클릭 = 해당 대화 이동(단일·별도 창 모두) · mac은 다음 릴리스 Cask / Linux = `notify-send -A`(0.7.10+) 검토) |
| M3-9 | **수신 확인 UX — 수동 "확인" 버튼**(FR-M-11/12) — Acknowledged 버튼(자동 금지) · `Urgent` 미전달 강조 · 그룹 구성원별(FR-G-4 합류). ※ 전달/읽음 점 마크·양방향 ack는 ✅ 기구현(N-2 08-17~20) — 이 행은 **수동 확인 축만** | P2 | 중 | M2-4b | ⛔ **목표 제외(사용자 확정 08-21)** — 설계([24 §5](24-adr-0010-message-priority-notification.md))만 유지 · 필요 시 요청으로 착수 |
| M3-10 | **수신자 릴레이**(FR-S-43~46 · R-13) — `RelayChannel` 포트(DR-21) + **Webhook 어댑터**(P0)·SMTP(P1) · **다중 어댑터 동시 팬아웃**(병렬·개별 타임아웃·부분 실패·채널별 결과·중복 억제·채널별 묶음·큐 상한) · **규칙 `조건 → 채널 집합`** + 1회성 수동 릴레이 · **채널별 노출 수준 L0~L2**(기본 L0) · 안전한 기본값 + **끌 수 없는 상한** · 자격 증명 암호화 저장([17](17-adr-0005-history-at-rest.md))·로그 마스킹 · 채널별 계측 | P1 | 대 | M3-9 · M0-2b | ☐ (SP-2 결과에 종속) |
| M3-12 | **상대별 별도 대화 창**(DR-26 · FR-U-18 · [14 §11](14-control-ux-architecture.md)) — winit 다중 창(`WindowId`→Conversation 라우팅) · 같은 상대 재활성화 = 기존 창 포커스 · 창 닫기 = 뷰만 · `chat.window_mode` 설정 연동(M3-11 Entry) | P1 | 중 | M3-1 · M3-11 | ✅ (08-08 — **다중 창 구현 완료**: `WinEntry` 맵·역할(`Main`/`Chat(peer)`) 라우팅·창별 배율·재활성화=포커스·창 닫기=뷰만(대화 유지)·동시 대화 N개. **설정 연동 완료(08-08 M3-11 슬라이스 1)** — 설정 창에서 `chat.window_mode` 즉시 변경(새 대화부터). 실행 인자는 초기값 지정용으로 유지 · 잔여 없음 → **완료**(✅ 육안 확인 08-13 — 다중 창 동작·사용자) |
| M1-8x | **정상 종료 경로**(FR-P-7 · R-16) — `plat::shutdown`(SIGINT/SIGTERM 포트·DR-21) + 헤드리스 루프 깨우기 + **GUI 종료 훅**(SIGTERM 0.28s·Drop 체인 GOODBYE·유휴 폴 ~5Hz). docker stop 10.26→0.38s. ★ **08-13 터미널 복원 3중 방어**(kitty `ESC[>1u` 누수 실기 — 대화 루프 폴링+플래그 · 전역 `restore_now()` 멱등 · 패닉 훅(release `panic=abort` 대응) · PTY 실측 pop 확인). 잔여 = zeroize(M2-5)·Windows 콘솔 핸들러 | P0 | 소 | — | 🚧 (핵심+GUI 완료) |
| **T-relaunch** | **relaunch 스크립트 Windows 보강**(08-15 실기 — `tools/relaunch.sh`의 ① 종료 단계가 Windows 프로세스를 못 잡고(0개 감지 → 구 인스턴스가 exe 잠금 → 빌드 os error 5) ④ 창 카운트도 0 고정. PowerShell 분기(`Get-Process nexa-beep \| Stop-Process` · 창 수 = MainWindowTitle 카운트) 추가 · 수용 기준 = Windows에서 relaunch 1회 실행으로 종료→빌드→3신원→발견 확인까지 경고 없이 통과 | P2 | 소 | — | ✅ (08-16 구현 — 빌드 rc 직접 검사(고의 compile_error 주입 검증) + PowerShell 분기 · ★ **08-18 Windows 실기 종결**: 첫 실행이 ④기동 창 0개 — `Start-Process`에 POSIX 경로(`/d/…`)가 넘어가 **조용히 실패**(pw()가 stderr 폐기) → `cygpath -w` 변환 추가 후 **1회 실행으로 종료 3→0 → 빌드 → 3신원 → 발견 3/3 통과** = 수용 기준 충족) |
| M1-8y | ★ **kitty 프로토콜 누수 재발 — 3중 방어를 뚫는 경로 재점검**(08-13 실기 **2회차**: 방어 구현 후에도 Ctrl+C에 이상 문자 → `reset` 수동 복구) — 재현 경로 후보를 하나씩 실측: ① **SIGKILL**(`kill -9`/`pkill -9` — 어떤 훅도 못 돈다 → 방어 불가능한 경로면 **다음 실행 시작 시 선제 `ESC[<u` pop**(잔존 상태 청소)으로 전환) ② `--window --live`(GUI 모드)가 raw 터미널을 만졌는가 — GUI 경로는 RawTerm을 아예 안 켜야 정상 ③ 다중 인스턴스가 같은 TTY에 pop을 중복/경합 ④ nohup·리다이렉트 상태에서 복원 시퀀스가 TTY가 아닌 파일로 새는가(isatty 확인). **수용 기준 = 어떤 종료 방식이든 다음 프롬프트에서 Ctrl+C 정상**(선제 청소 포함) | P0 | 소 | M1-8x | ✅ (**08-17 4경로 완결** — ① SIGKILL(방어 불가) = **진입 직전 kitty pop 선제 청소**(pty 실측 pop<push·2회) ② GUI = RawTerm 무접촉(코드 확정) ③ 다중 인스턴스 경합 = 선제 청소 흡수 ④ 비TTY = is_tty 가드·파이프 ESC 0건 실측+상시 회귀 · 유휴 축은 08-16 fd/스레드 증가 0) |
| ~~M3-13~~ | ⚠️ **M1-8x로 통합**(같은 FR-P-7 · R-16 — 중복 등록이었다). 진행 상태는 M1-8x 참조 | — | — | — | ↩︎ 통합(08-08) |
| M3-11 | **설정 화면**(DR-24 · [14 §10](14-control-ux-architecture.md)) — Entry 레지스트리 · 계층 트리 + AND 토큰 검색(조상 보존·매치 수) · Kind 동적 패널 · 즉시 적용 · `Cmd/Ctrl+,` · 보안 항목(프로필 노출·공유 scope·알림 정책) 확인 훅. ⚠️ 원래 M3-8이었으나 ADR-0010 태스크와 번호 충돌로 재배정(08-08) | P0 | 대 | M3-1 · M3-1b | 🚧 (08-08 — **슬라이스 1**: `registry` 단일 원천(렌더=검색 — 테스트 단언) · AND 토큰 검색 · 카테고리 헤더 · 값 칩 순환(즉시 적용·폴링) · `⌘/Ctrl+,` 별도 창 · **`chat.window_mode` 설정 연동 완료**(M3-12 잔여 해소) + `ui.theme`(다크/라이트 전 창 즉시). 슬라이스 2(08-08 사용자 요청): **좌측 카테고리 트리 사이드바** — 선택 하이라이트·검색 중 매치 카테고리만+매치 수 "(N)"(X-10 ①)·사이드바 클릭=카테고리 이동+검색 해제·단일 원천 불변식 재정의(사이드바 합=레지스트리 전체, 테스트 단언). 슬라이스 3(08-11 사용자 요청): **VS Code식 그룹 구성** — 직속 설정 먼저 → 하위 그룹 순(안정 정렬) · 하위 섹션 제목 · **상단 고정 밴드**(윗줄 상위/아랫줄 현재 하위 · 높이 고정 · 클릭 삼킴) · 제목 위계 +2/+1 굵게(`select_font_sized` 증분) · 스크롤바 자동 숨김 항목 `ui.scrollbar_hide` 추가. **다단 계층 해소** — 잔여 = 확인 훅(값 영속은 **M3-15 ✅ 08-11**)) |
| **M3-14** | **신뢰 배지를 원형 상태 아이콘으로**(FR-U-19 · 사용자 요구 08-09) — 글자 배지 → 이미지 아이콘(**색만으로 구분 금지** · 툴팁으로 등급명). ★ **설계 완료 — 규격 = [14 §13](14-control-ux-architecture.md)**(08-14): ⓐ **표시 가능한 상태 전수 6종**(`Unverified`·`Pinned`·`FingerprintVerified` + **화면에 없던 3종** = `Blocked`·**이름 충돌**(`name_conflict` — v1에서 사칭을 드러내는 **유일한** 가시 신호)·`FirstContact`) ⓑ 아이콘 = **Lucide `badge-*` 한 가족**(`badge-question-mark`/`badge`/`badge-check`/`badge-x`/`badge-alert`/`badge-plus` · ISC · **방패는 쓰지 않는다 — `shield-check`가 이미 격리함**) ⓒ **경로 등급 축**(`house`/`globe`/`waypoints` · DR-28)은 신뢰 오른쪽 별도 자리 ⓓ ★ **기본 상태는 조용히** — `Pinned`는 흐리게·`Local`은 생략(안 그러면 목록 전체가 시각 소음) ⓔ **원본 SVG 9종은 `assets/icons-src/`에 선반입 완료**(08-14) → 굽기는 `tools/mkicons.sh badge badge-check badge-x badge-alert badge-plus badge-question-mark-nb house globe waypoints` 한 줄. ★ **`badge-question-mark`는 파생본 `-nb`를 쓴다** — 원본은 훅이 반원+스템 없음이라 **14px에서 동그라미로 뭉갠다**(사용자 지적 08-14 · 20px 렌더 실측 → 훅 235°+수직 스템으로 다시 그림 · [14 §13-2-1](14-control-ux-architecture.md)). 남은 뭉갬 후보 = `badge-alert`(느낌표 가늚)·`waypoints`(점 4개) — **굽기 전 `rsvg-convert -w 14` 실크기 확인 필수**(추정 금지). ⚠️ **착수 전 [14 §12-6](14-control-ux-architecture.md)·[§13-3](14-control-ux-architecture.md) 필독** — M3-19까지 들어오면 한 행에 표식이 **셋**이 된다(세션 11px 파냄 / 신뢰 14px 윤곽 / 경로 12px). **자리·문법·색 팔레트를 갈라 놓을 것** | P1 | 소 | M1-6 | ✅ (08-15 — 자산 9종 굽기(badge 6 + 경로 축 house/globe/waypoints **자리만** — 경로 축 코드는 M5-3c) · 14px/12px ASCII 실측 후 구움(파생 물음표 생존 ✓ · waypoints 12px 점 뭉침 경향 = v2 전 재판독 주석) · ★ 화면에 없던 3종 가시화: `PeerRow.blocked/conflict`(Blocked=badge-x 덮음 · **이름 충돌**=badge-alert 덧표식 — v1 사칭 유일 신호) · FirstContact=상태바 토스트 기존 문구 · Pinned=흐리게(조용히) · hover 툴팁(등급명+한 줄 · 4개 언어 · 페인트·히트 기하 공유) · ddd5448. ★ **08-15 M3-14b 개편(사용자 실기 지적 "유령 원")** — Lucide badge 알파 마스크 폐기 → **Material Symbols 컬러 RGBA**(Apache-2.0 · 사용자 확정): 미검증=psychology-alt(호박)·대조=verified(SNS 파란 실 관용)·차단=block(🚫)·충돌=warning(⚠)·핀=lock(회색)·첫접촉=person-add · **Pinned 목록 숨김**(문제 상태만 표시 — 유령 원 제거 실기 확인) · 18px · `mkicons-rgba.sh` 신설(컬러 RGBA 파이프라인) · 원장 10 §4) |
| **M3-15** | **설정 영속 구현**(FR-P-8~12 · FR-S-48 · [28](28-adr-0011-settings-persistence.md)) — ~~현재 설정값이 전혀 저장되지 않는다~~(해소 08-11). `Entry` 레지스트리에 저장 태우기 · **`SaveScheduler`**(`Debouncer` 확장 — quiet OR max_delay · 호스트 `tick` 주입) · 미지 키 보존 · 원자적 쓰기(PID temp·fsync·덮어쓰기 rename) · 관용 파싱+`Entry::range` 클램프 · **종료 flush**(M1-8x 훅) · 불변식 T-1~T-9 테스트 | P0 | 중 | D-25 · M3-11 | ✅ (08-11 — **D-25 확정 → 즉시 구현**. **`crates/nexa-conf` 신설**(Q-28-1 확정: 이 저장소 안·분리 가능 상태): 관용 파싱(`_schema` 키)·미지 키 보존·`SaveScheduler`(mark/tick/flush — quiet 1s OR max 10s)·원자적 쓰기(PID temp·sync_all·덮어쓰기 rename·부모 fsync(Unix))·`dir_writable`/`user_config_dir`. 앱 배선: 부팅 로드(`SettingsState::set_by_name` 관용 검증 — 무효 값은 기본값 유지)+`apply_boot_settings`(테마·타입어헤드·툴바·전송 정책) · 변경 = `conf_mark` · `about_to_wait` tick · winit `exiting` flush · 경로 = exe 옆 `data/`(포터블)→사용자 폴더→임시. ★ **`timed` 승인은 저장 시점에 복귀 대상으로 치환**(사용자 확정 08-09 그대로) · 파일에 timed가 있어도 복원 안 함(manual 정규화 — 기간 연장 방지). 테스트 T-1~T-9(+2) · **447 green** · 실측: 부팅 왕복 스냅샷 35키·미지 키 보존·손상 파일 정상 기동) |
| **M3-17** | **프로필 화면 + 프로필 교환**(사용자 요청 08-11 · **DR-22/ADR-0008이 이미 설계** — [22](22-adr-0008-profile-disclosure.md)) — ① **별도 프로필 화면**(Role::Profile): 프로필 이미지·표시 이름·이메일·전화번호 편집(이미지 파일 선택은 탐색형 피커 재사용 · 저장 위치·암호화 여부는 착수 시 결정 — PII라 평문 settings.cfg 부적합 후보) ② **교환 프로토콜**: 연결(세션 성립)된 상대에게만 `StreamId::Profile`(신설)로 **요청-응답**(ADR-0008 — 브로드캐스트 미포함·자동 프리페치) · 수신 이미지는 크기 상한+`imgdec` 경로(R-5) ③ **목록·대화에 상대 이미지·이름 표시**(프로필 이름은 발견 이름과 구분 표기 — 신원은 여전히 키) ④ 공개 게이트 = `profile.share.*` 설정(**08-11 골격 구현됨** — 기본정보/이메일/전화 개별 토글 · 기본 전부 off). ⚠ 응답은 **공개 on인 필드만** · 미공개는 필드 자체 미전송(fail-closed) | P1 | 대 | M2-4 · ADR-0008 | 🚧 (**08-11 — 공개 토글 ✅ + 화면부 ✅ + ★교환 와이어 ✅**: `core::profile::ProfileMsg`(Control 스트림 — Request/Info/ImageChunk · **켠 필드만·미공개는 필드 부재** · 이미지 256KiB 상한·32KiB 청크·순서 어긋남 이미지만 폐기) · **자동 프리페치**(성립 합류점 1회) · 정책 판단 메인 단일 지점 · **프로필 이름 목록·제목 우선 표시**(무해화 통과분·`record_name` 이력 연동) · 이미지는 `data/profiles/` **바이트 캐시만**(픽셀 렌더 = M4-5 imgdec 후 — R-5). 468 green. ★ **08-14 기본 아바타 일습**(다른 세션 WIP 인계 완주): 12간지 내장(NBAV1 160KB)·`AvatarChoice`(`profile.avatar` 단일 문자열·부팅 시드 기본값)·프로필 스와치 14개(사진과 배타)·`draw_builtin` 렌더(프로필·목록)·와이어 Info.avatar(키만·전방 호환·변경 시 능동 재전송). **잔여 = `c:` 관리 복사(사용자 이미지 data/profiles/custom 이관)·연락처 상세 UI·⏸ 실기(스와치·얼굴 왕복)**) |
| **M3-18** | **프로필 화면 적용/취소 버튼**(사용자 확정 08-13 — 절차 변경) — 현재는 필드별 Enter = 즉시 적용(설정 Face 규약 차용)인데, **적용(Apply)/취소(Cancel) 버튼을 두고 저장 전에는 반영되지 않는 절차로 변경**한다: ① 화면 진입 시 스냅샷 → 편집은 로컬 상태만 ② **적용** = 그때 일괄 `apply_settings`(표시 이름 재공지·프로필 재교환 arm도 이 시점 1회) ③ **취소/Esc** = 스냅샷 복원·무반영 ④ 미저장 변경이 있는 채 닫기 = 확인(버리기/적용) ⑤ 공개 토글·이미지 선택도 동일 절차(현재 토글은 클릭 즉시 적용이라 함께 이관). ⚠ 설정 창(M3-11)의 "즉시 적용" 원칙(DR-24)과 **의도적으로 다른** 화면별 규약 — 프로필은 여러 필드가 한 신원 표현을 이루므로 원자적 저장이 맞다(사용자 확정) | P2 | 소 | M3-17 | ✅ (★ **08-16 구현** — 보류 의미론(take_changes = 적용 확정분만 · **호스트 팔·push 10곳+ 무변경**) · 적용 = 일괄+닫기 · 취소 = 폐기+닫기(재열림 = 저장값) · Esc 미저장 2단계(격리함 confirming 문법 · 모달 0) · 피커도 보류 편입('적용 시 반영') · 사진 큰 미리보기는 적용 후(보류 중 캐러셀로 확인 — 한계 명기) · 계약 회귀 3종 · 잔여 = 실기 육안) |
| **M3-24** | **프로필 창 스크롤 구조 — 고정 헤더 + 스크롤 중간영역 + 고정 하단 버튼**(사용자 요청 08-18 · **재설계라 후속으로 미룸**) — 향후 프로필 창이 세로 리사이즈 가능해질 때를 대비: **헤더(시작~Profile image) 고정** · **중간영역(Display name~"Never broadcast" 캡션) 스크롤**(그 안에서 Bio는 hover 시 자체 스크롤 · 다른 컨트롤 위 휠은 중간영역 스크롤) · **Apply/Cancel 하단 고정**. ⚠️ 이 렌더러 `DrawCtx`에 **클립(scissor) API가 없다** → 스크롤 컨트롤이 헤더/푸터를 침범하는 것을 **불투명 배경 덮어그리기**(중간영역 먼저 → 헤더/푸터 배경+컨트롤 위에)로 막고, **클릭 판정은 스크롤 오프셋만큼 좌표 변환 + 뷰포트 밖 무시**로 처리해야 한다. 기존 148 UI 테스트(프로필 클릭)가 좌표 가정에 걸릴 수 있어 테스트 동반 개정 필요. 지금은 **고정 창(770)** 으로 진행하고 Bio만 3줄 고정+내부 스크롤로 확정(08-18) | P2 | 대 | M3-17 · M3-18 | ☐ (08-18 등록 — 우선 고정 창으로 확정) |
| **M3-25** | **시스템 시작 시 자동 실행**(사용자 요청 08-20 — **기본 on** · 옵트아웃) — 3-OS 사용자 수준 등록(T0 무권한 · DR-1·DR-4): Windows HKCU Run(advapi32 직접 링크 · 의존 0) · macOS `~/Library/LaunchAgents` plist(RunAtLoad) · Linux XDG `~/.config/autostart` .desktop · 설정 고급 트레이 항목 위 Toggle(`app.autostart`) · **부팅 재동기화**(포터블 경로 이동 대응 — on = 현재 exe 재등록 · off = 제거 멱등) · 실패 = 상태바 고지. ⚠ [04 T-2] "시작 프로그램 경로 금지"는 수신 파일 축 — 앱 자신 등록과 무관 | P1 | 소 | M3-15 | ✅ (08-20 `5b0c9ef` — 구현+Windows 실측(격리 포터블 — on 등록·off 제거·원상 확인) · 715 green · **잔여 = mac·Linux 실기**(LaunchAgent 로그인 실효·GNOME/KDE autostart 실효)) |
| **M3-26** | **내 지문 표시·복사 UI**(08-22 등재 — 서버 랑데부 시대의 첫 연결 재료): 지금 내 전체 지문(64hex)은 CLI `--whoami`로만 보인다 — 프로필 화면(또는 내 프로필 카드)에 **전체 지문 표시 + 복사 버튼**. 상대는 이 값을 `⌘/Ctrl+K`에 붙여 서버 경유 첫 연결을 연다 | P2 | 소 | — | ✅ (08-22 — 프로필 우측 상단 [전체 64][짧은 8] 복사 버튼 · 클립보드는 호스트 · 상태바 고지) |
| **M3-19** | **연결 상태 배지 = 색 + 실루엣 2중 부호화**(사용자 확정 08-14 · 규격 = [14 §12](14-control-ux-architecture.md)) — 현행 11px **채운 원 1개 + 색만**으로 `LinkState` 4값을 나르고 있어 적록 색각·저대비에서 무너진다. 색 토큰 4종은 **그대로 두고** 디스크 안쪽을 `theme.panel_bg`로 **파내(knockout) 실루엣을 넷으로 가른다**: `Idle`=빈 링 · `Connecting`=갭 링(90°·회전) · `Active`=꽉 찬 원(현행) · `Lost`=가로 막대(통행금지). 기하는 배지 지름 `D` **비율로 고정**([14 §12-3](14-control-ux-architecture.md) — 구멍 0.53D · 막대 0.56D×0.19D · 갭 90°) → 배율 무관. **회전은 캐럿 530ms 틱 재사용**(90°×4스텝 · 새 타이머 0 · `reduce_motion`이면 정지·갭은 유지). 끄기 스위치 `ui.link_badge_shape`(bool·기본 on · Entry 등록·핫 스왑). ⚠️ **M3-14와 한 행에 원형 표식 2개** → [14 §12-6](14-control-ux-architecture.md) 표대로 자리·문법 분리. 테스트 = 상태별 파냄 픽셀 단언 + 배율 2종 비율 불변 + 스위치 off = 현행 그림. **자산·의존·산출물 증가 0**(수식 렌더 — `avatar.rs` 방식) | P1 | 소 | M3-17(아바타 배지 겹침 자리) | ✅ (08-15 — `draw_link_badge`(목록+구성원 모달 공용) · `DrawCtx::fill_pie` 신설(부채꼴 AA — 반평면 2장 교집합) · 갭 링 회전 = 캐럿 530ms 틱 재사용 4스텝 · `ui.link_badge_shape`(기본 on · 부팅+핫 스왑+쌍방 동기) · 픽셀 회귀 6종(상태별 파냄·회전·D=12/22 비율 불변·off=현행) · 3f946c2) |
| **M3-20** | **큰 자리 연결 상태 아이콘**(2층 · [14 §12-7](14-control-ux-architecture.md)) — 20px 이상 자리(대화 창 헤더·피어 정보 카드·툴팁)에 **Lucide 플러그 가족**(ISC · **사용자 확정 08-14 = 한 세트로 통일**): `plug`(Idle) · `plug-zap`(Connecting) · **`cable`**(Active) · `unplug`(Lost). ✅ **자산은 이미 구워 커밋됐다**(08-14 — `icon-{plug,plug-zap,cable,unplug}-96.alpha` 각 9,216B · 상수 `icons::link::*` · 원본 `assets/icons-src/` · 절차 [`tools/mkicons.sh`](../tools/mkicons.sh)) → **잔여 = 배선뿐**(`ToolIcon::Mask` 테마 틴트 · 새 코드 없음). ⚠️ **지금은 붙일 자리가 없다** — `chat_view.rs`·`peer_info.rs` 어디에도 `LinkState`가 넘어오지 않는다. **자리(대화 헤더 상태 줄)를 먼저 만드는 것이 선행**이며 M3-19와 별개 슬라이스. ⚠️ `plug-zap`·`unplug`는 실루엣 60% 공유 — 범례에 나란히 놓을 땐 라벨 동반. 탈락 근거(Wi-Fi 호 = 유선 상대에 거짓말 · 사슬 = Idle/Lost 수렴 · 방송 = 동심원 겹침 · Phosphor = 세트 혼용 비용)는 [14 §12-7](14-control-ux-architecture.md)에 박제 — 재검토 시 되풀이 금지 | P3 | 소 | M3-19 · (대화 헤더 상태 줄) | ✅ (**08-21 `5d255b3`** — 대화 헤더 상태 줄 신설(제목 왼쪽 20px · 상태색 틴트·캐시) · 산출 단일점 `peer_link_state`(세션>시도>끊김>유휴) · 합류점 5곳 배선 · 잔여 = 실기 육안·피어 카드/툴팁 확장은 후속) |
| **M3-21** | **프로필 변경 전파 — 공유 토글이 이미 연결된 상대에게 안 보인다**(사용자 실기 08-14: kiros33이 이메일 공유를 켰는데 상대 프로필 보기는 "(비공개)" — 짐작 그대로 **연결 시점 응답이 캐시된 뒤 재전파가 없다**) — 원인: 능동 재전송 arm이 `profile.avatar`·`avatar_border` **2키에만** 배선(08-14), 상대 프리페치는 **세션 성립 1회**뿐이라 세션이 살아 있는 동안 변경이 영영 안 닿는다. **M3-17ⓘ 부팅 캐시**가 이 낡은 응답을 재시작 후까지 연장한다. 설계 3겹: ① **발신 전파 확대(근본)** — 프로필 응답에 들어가는 키 전부(`profile.share.basic/email/phone` · `profile.email` · `profile.phone` · `profile.display_name` · `profile.image_path` · 기존 avatar 2종)를 **한 arm 깔때기**로 묶어 연결된 전 세션에 `my_profile_frames()` 재송신(변경 즉시 반영 = hot-swap 원칙 · 끄는 변경도 응답에 "필드 부재"로 실려 상대 캐시가 지워진다 — 철회 대칭 그대로) ② **수신 갱신(보완·구버전 발신자 대비)** — **프로필 보기 카드 열기 = 세션이 살아 있으면 `ProfileMsg::Request` 1회**(캐시 먼저 표시 → 응답 도착 시 갱신 · 사용자 행위 트리거라 [13 §12-1] 자동 반복 아님) ③ **카드에 수신 시각 표기(선택)** — "캐시됨" 옆에 마지막 수신 시각(신선도 가시화 — 기존 "이미지 · 캐시됨" 표기와 같은 결). ⚠ M3-18(적용/취소 일괄 저장)이 들어오면 ①의 발화 시점은 "적용 1회"로 옮긴다(충돌 아님 — 같은 깔때기) | P1 | 소 | M3-17 | 🚧 (**08-14 ①②구현 ✅** — push 깔때기(`profile_push_scope` 키 전수 회귀)+`ProfileScope::Full/Info`(image_keep bit5 경량)+철회 대칭+카드 열기 pull·도착 갱신 · push 대상 = 연결된 전 세션(수동 등록 포함 · 사용자 확정) · ★ **08-16 ③ 구현 ✅** — `PeerProfile.received_ms`(수신 insert 기록 · 부팅 복원 = 캐시 mtime 근사) · 카드 '이미지 · 캐시됨 · N분 전 수신' · **①②③ 완결** · 잔여 = 2-PC 실기) |
| **T-macxfer** | ★ **대량 파일 전송 macOS 실기(필수 · 사용자 지정 08-18)** — Windows에서 구축한 발신 스트리밍 일습의 mac 검증: ① 10GB급 DnD = 무정지(해시 진행 % 표시·취소) ② 송신 전 수신 상한 확인(CapAdvert/Request — 즉시 차단 문구) ③ 상한 설정 2종(발신 무제한 기본·수신 512MiB) hot-swap ④ 전송 중 끊김→재연결 = 능동 재-Offer→이어받기 N%(승인 우회) ⑤ 완료 평균 속도·mac 디스크(APFS) 스트리밍 해시 속도 관측(실측값 기록) ⑥ Win↔mac 교차(양방향 대용량) | M4 | P1 | — | ✅ (**08-20 사용자 실기 완료 보고 — ①~⑥ 전항목**. 세부 실측값(⑤ 평균 속도·APFS 해시 속도)은 미제출 — 필요 시 추후 기입) |
| **I18N-2** | **잔여 하드코딩 한국어 스윕**(08-20 등재 — 상태줄 "전송을 취소했습니다"는 처리 완료): 액터 fail 사유(`수신 거부:`·`파일 열기 실패:`·`수신 오류:`·`완료 실패:`·`와이어 오류:` — 풍선 why 텍스트로 노출) · 상태줄 `수락 — {} 수신 시작`·`거절 — {}`·`이어받기 — {} {}%부터` · `파일 선택 — {}` 타이틀 · 로그 상태줄(app.rs:7258). Msg 신설 + tf 치환 일괄 | M3 | P2 | 소 | ✅ (08-20 `a1fb215` — 16곳 Msg 승격(4개국): 액터 fail 5종·이어받기/수락/거절/대기 제안/타임아웃 거절·그룹 오퍼·목록 삭제·로그 없음·피커 타이틀 6종+[폴더] 접두) |
| **M4-2e** | ★ **전송 일시정지 실기 피드백 2건(08-19 Windows 세션 등재 — 구현은 해당 기능 소유 세션(mac 풍선 인라인 제어·수신측 일시정지 = ccfb87d 이후 미푸시 작업)에서)**: ① **일시정지의 수신측 전파(상태 동치화)** — ⓐ 오퍼 전 일시정지: 수신측에 아무 흔적 없음 ⓑ **오퍼 후 일시정지**(재보고 08-19 실기 8 · 스크린샷): 발신측 "Paused 0% ▶✕"인데 수신측은 "승인 대기"로 남는다 → 일시정지를 와이어로 전달해 **양쪽 상태를 동일하게**: 수신측도 일시정지 표시 + **재개·취소 버튼**이 보이게(발신이 한 번도 안 된 파일 포함) ② **수신측 일시정지 토글 즉시 반영** — 수신측이 ⏸를 누르면 송신측은 즉시 ▶로 바뀌는데 **수신측 자신은 두 번 눌러야** ▶가 된다(자기 클릭의 낙관적 반영 누락 — 원격 확인을 기다리지 말고 로컬 상태 즉시 전환) ③ **Cancel All의 일시중지 포함 + 자기 쪽 정리**(08-19 실기 2·스크린샷 2장): 전체 취소 시 **일시중지(Paused) 항목도 함께 취소**돼야 하는데 남는다(발신측 스크린샷: B004 'Paused 0%' 잔존) · 전체 취소를 누른 **자기 쪽 배너·Cancel all 버튼이 안 사라진다**(상대쪽은 정상 정리 — 발신·수신 양방향 대칭 증상: 원격 통지 경로만 정리하고 로컬 상태 정리가 빠진 모양) ④ **배치 종료 판정에 일시중지 포함**(08-19 실기 3): B003 일시중지 상태에서 마지막 파일(B004)이 완료되면 송신·수신 양쪽 **상단 전송 상태·Cancel all이 사라진다** — 마지막 파일 종료 = 배치 종료로 판정하는 듯. **일시중지 대상이 남아 있으면 전송 완료가 아니다**(재개하면 그 1개만으로 배너가 되살아나는 현상이 방증 · 재보고 08-19 실기 7: **시작 전 0% 일시중지**에서도 동일 — 진행 여부 무관). 종료 판정 = 큐 비움 ∧ 활성 없음 ∧ **일시중지 없음** ⑤ **일시중지 상태에서 전체 취소 불발**(08-19 실기 4 · 수신측 스크린샷): 전송이 Paused인 채 Cancel all을 누르면 "No active transfer to cancel"로 거부 — 취소 판정이 **활성 전송만** 보고 일시중지를 제외한다. 일시중지 상태(단일·다수 무관)에서도 전체 취소가 전 파일을 취소하도록(취소 대상 = 활성 ∪ 일시중지 ∪ 큐) ⑥ **수신측 ⏸ 버튼 무동작**(08-19 실기 5): 수신 중 풍선의 일시정지 버튼을 눌러도 전송이 멈추지 않는다 — ②(두 번 클릭 토글)와 별개로 **기능 자체가 발화하지 않는 경우**가 있음(수신→발신 정지 요청 와이어 or 클릭 판정 누락 의심 · 재현 조건 함께 확인 필요) ⑦ **취소 완결 후 발신측 배너 잔존**(08-19 실기 6 · 스크린샷): B003 취소가 쌍방 스레드에 종결(Failed — Canceled/Peer canceled)로 남았는데 **발신측 상단만** "Sending 39% · 187.2MiB (0/1)"+Cancel all이 낡은 진행률로 잔존(수신측은 정상 정리) — ③과 같은 뿌리(자기 쪽 로컬 정리 누락)로 추정 · 배치 잔량 재계산(취소분 차감) 포함 확인 | M4-2d | P1 | 소 | ✅ (08-19 — ①ⓑ~⑦ = 이벤트 신원 태깅·recv_xids·apply_local_cancel_all·CANCEL_ALL 전파로 해소(실기 ✓) · **①ⓐ = QPAUSE 태그 17**(`8911aa8`) — 큐 정지도 이름 기반 통지로 수신 라인 동기화 · 육안 잔여) |
| **M4-12** | **수신 스트리밍(디스크 스풀 + 청크 봉인) — 설계 후 구현**(08-18 등록): 발신은 스트리밍화됐지만(ca66d30) **수신은 메모리 조립 + 전량 봉인**이라 초대용량 수신이 여전히 무겁다. 걸리는 것: ① XferInbox 조립을 sink 추상(core는 I/O 없음 — DR-21 포트)으로 디스크 스풀 ② `.beepq`/NBSE 봉인이 전량 메모리 — **청크 AEAD 형식** 설계 필요(스풀→봉인 재읽기도 스트리밍) ③ 해시 검증 스트리밍(수신 중 증분) ④ `.part` 재개와 스풀 통합(스풀이 곧 부분물) ⑤ imgdec·격리함 읽기 경로의 스트리밍 개봉. 상한 설정(recv_max_mb)이 그동안의 방어선 | M4 | P2 | 중 | ☐ 설계 대기 |
| **M3-22** | **상태바 로깅 + "로그 보기"**(사용자 요청 08-16 — **설계만 등록 · 개발은 후속**) — 상태바에 스쳐 지나가는 상태값들이 유일한 진단 창구인데 휘발된다(실기 디버깅마다 스크린샷 의존). 설계 5겹: ① **선행 리팩터 = status 깔때기** — `self.status = …` 직접 대입이 **108곳 산재**라 로깅 훅 지점이 없다 → `set_status()` 단일 통로로 이관(M3-21 push 깔때기·send_xfer_decision과 같은 교훈: 산재 대입 = 곧 빠뜨린 대입) ② **큐 + 전용 스레드**(사용자 확정) — **bounded 채널 + 가득 차면 드롭**(드롭 카운트만 기록): 로깅이 본 기능(파일 송수신·메시지 발신·UI)을 **절대 블록하지 않는다** — 백프레셔 금지가 1급 요구. 소비 스레드가 순차 기록(BufWriter + 주기 flush + 종료 flush 훅 — shutdown 모듈 합류) · 로거 스레드 사망 = fail-soft(앱 무해·상태바는 그대로) ③ **파일** — `data/logs/beep-YYYYMMDD.log`(**신원 폴더 소속** — 격리함 전역 temp 교훈 08-16) · 시각 스탬프 · 크기 상한/보존 일수 회전 ④ **"로그 보기" 버튼**(하단 상태바) — `.log` 기본 연결 프로그램으로 열기(`nbeep-plat::launch` 재사용 · mac open/Win ShellExecute/Linux xdg-open) ⑤ **봉투 원리 점검** — 로그는 상태만([13 §7] Tracer 규약과 정합): 상태바 문구는 이미 사용자 노출 텍스트라 로컬 기록 수용 가능하되, 키·전문 경로가 문구에 새로 들어오지 않도록 set_status가 관문을 겸한다(redact 재사용 검토). 13 §12-1 점검(flush 타이머 = 중복 가드·종료 조건·소요<간격) ⑥ **설정(사용자 요청 08-16 2차 — ADR-0011/DR-27 규약: Entry 레지스트리 등록·즉시 적용 hot-swap·초기화 짝 키·미지 키 보존)** — `log.enabled`(**Switch · 기본 off — ✅ 사용자 확정 08-18**(진단용 — 상시 디스크 쓰기 회피) · **켜면 로거 스레드 기동·끄면 flush 후 정지** — 재시작 없이) · `log.retain_days`(보존 일수 · 기본 7 — 지나면 날짜 파일 삭제) · `log.max_total_mb`(전체 상한 · 기본 20 — 초과 시 오래된 파일부터) · 설정 트리 위치 = **고급(Advanced) 하위 "로그" 섹션**(고급 맨 밑 규약 08-15) · "로그 보기" 버튼은 `log.enabled` off 또는 파일 부재 시 비활성+안내 · 회전·삭제도 로거 스레드 몫(메인 무영향 불변식 유지) | P2 | 중 | M3-1 | ✅ (★ **08-18 구현 완주**: ① **set_status 깔때기** — `self.status =` 직접 대입 113곳 전량 이관(스캐너 변환+빌림 분리 수동 5곳) ② `statuslog.rs` — bounded 256 큐+전용 스레드(try_send 드롭·드롭 카운트 줄·BufWriter 2s flush·스레드 사망 fail-soft) ③ `data/logs/beep-YYYYMMDD.log`(신원 폴더)+날짜 전환 회전+sweep(retain_days·max_total_mb 오래된 것부터) ④ **로그 보기 = 설정 Action**(오늘 파일→기본 프로그램·없으면 폴더·그것도 없으면 안내 — 설계의 상태바 버튼 대신 설정 행: 로그 설정 옆이 발견성이 낫다 판단) ⑤ set_status = 봉투 관문 ⑥ 설정 4항목(고급›로그 하위 섹션 · `log.enabled` Switch 기본 off — 확정값 · retain 7·cap 20 RadioInput·hot-swap stop→start) + 종료 flush(exiting)+Drop flush. 회귀 2종(기록 왕복·1만 건 논블로킹). **잔여 = 실기**(켜고 로그 쌓임·로그 보기 열림·끄면 정지)) |
| **M3-22** | **실행 파일 직접 실행 시 앱 아이콘**(사용자 스크린샷 08-15 — 포터블·개발 바이너리를 .app 번들 밖에서 실행하면 macOS Dock에 기본 `exec` 아이콘 · 이름 툴팁만 nexa-beep). 설계: ⓐ **macOS = 부팅 1회 `NSApplication.setApplicationIconImage`**(objc2-app-kit 이미 링크 — 트레이 M3-2b와 같은 판 · 자산 = 내장 브랜드 RGBA→NSImage · 번들 실행이면 icns가 이미 이겨서 no-op여도 무해) ⓑ **Windows = exe 리소스 아이콘 확인**(포터블 zip의 exe에 .ico가 박혀 있는가 — 없으면 빌드에 리소스 컴파일(winres류 or 수동 .rc) 검토 · 작업표시줄·탐색기 표시) ⓒ Linux = .desktop 아이콘은 설치본에 이미 있음(포터블은 데스크톱 환경 관례상 한계 명기). 창 아이콘(winit `set_window_icon`)과는 별개 축 — Dock/작업표시줄이 대상 | P2 | 소 | — | ☐ **설계 등록(08-15)** |
| **LIME-1** | **Linux 한글 입력 실기**(08-15 사용자 질문 — "리눅스도 비슷한 문제가?") — mac H-26/H-27 계열(조합 직후 1byte 삼킴·첫 키 유출)은 winit-AppKit 경계 실측이라 **Linux는 미지**. IBus·fcitx × XIM·text-input-v3 조합별로 `NEXA_IME_TRACE` 실측 → 삼킴이 확인되면 keytap에 해당하는 관측 어댑터(X11 XRecord/wayland?) 검토. 게이트 기준값은 이미 설정(`ime.*`)이라 현장 조정 가능 | P2 | 중 | M1 Linux 실기 | ☐ |
| **M3-16** | **주소(ip:port) 입력 모달 창**(사용자 요청 08-10) — 상태바 인라인 입력(⌘/Ctrl+K·툴바 +)을 **별도 모달 입력창**으로 승격: TextBox(Beam 캐럿·클립보드) · host:port/[v6]:port 형식 검증 · Enter 연결·Esc 취소 · (후속) 최근 주소 목록 | P2 | 소 | M3-1b | ✅ (08-11 — `AddrPromptWidget` 신설: 제목+TextBox(Beam·선택·클립보드)+**형식 검증**(host:port·[v6]:port·포트 1~65535)+Connect/Cancel · `Role::AddEndpoint` 모달(⌘K·툴바 + 진입) · **인라인 adding 경로 완전 제거** · 테스트 4 · 436 green. 잔여 = 최근 주소 목록(P3)·연결 워커 이관은 M2-8. ★ **08-13 후속 — 기본 포트 일습**: `DEFAULT_SESSION_PORT=47200` 우선 바인딩(점유 시 임의 폴백) · 설정 `net.session_port`(ⓐ 듣는 포트 = 거는 기본 포트 · 핫 스왑) · 포트 생략 입력(`10.0.0.5`→`:47200`) · 상태바 `수신 :N` 상시 표시 — 포트가 매 실행 바뀌면 수동 등록이 성립하지 않는다(실측). ⚠️ **08-13 육안: 모달리스로 동작** — AlwaysOnTop일 뿐 부모 비활성화 없음(winit 한계 · 진짜 모달은 plat `EnableWindow` 어댑터 필요 · P3) |

**M3-3b 상세 체크리스트 (WIME)** — Windows 실기(이 PC 가능) · 근거 = [27 §6~7](27-typeahead-hangul-composition.md) · 기록엔 **왜 + 실측값** 필수:

- [x] **WIME-1 대화(IME on) 한글 조합 기본** — ✅ **실기 확인(08-13 · 사용자)**: preedit 단계 표시·Commit 삽입·**조합 중 Backspace 자소 단위** 동작
- [ ] **WIME-2 조합 전 첫 키 유출** — 한글 IME 켠 직후 첫 키가 `Character`로 새는지 — mac에서 만든 **자모 보류-판정 ④**([27 §5](27-typeahead-hangul-composition.md))가 Windows 이벤트 순서(KeyboardInput ↔ Ime 도착 순)에서도 걸리는가
- [x] **WIME-3 목록 모드(IME off) 자모 도착** — ★ **실측 완료(08-13 · 사용자 실기): 자모가 오지 않는다**(라틴만 · 한/영 키 무력) — 우려대로 목록 한글 직접 조합이 Windows 무동작이었다. **처방 구현 = [27 §8](27-typeahead-hangul-composition.md)**: `jamo_from_qwerty`(QWERTY→두벌식) + 앱 소유 `hangul_mode` 토글 · 잔여 = 한글 이름 상대 **자소 접두 매칭 실동작**(GUI 실기)
- [ ] **WIME-4 한/영 전환 키** — winit 소스 확인 + ★ **실기 실증(08-13 · 사용자)**: VK_HANGUL → `NamedKey::HangulMode`(물리 `Lang1`)가 **IME 비연결 목록 창에도 도달** — [27 §8](27-typeahead-hangul-composition.md) 앱 토글·상태바 고지·조합·복귀 동작 확인. 잔여 = 대화 창(IME on) 전환 직후 **첫 키 손실**(mac R2 유형) 재현 여부만
- [x] **WIME-5 목록↔대화 전환 IME 토글** — ✅ **실기 확인(08-13 · 사용자)**: 전환 경계 묵은 조합 없음·첫 입력 정상
- [x] **WIME-6 조합 중 Enter — ★ 사용자 확정(08-13): 조합 확정+전송이 한 번에** — Windows IME는 Commit 후 같은 키의 KeyboardInput이 **또** 온다(macOS는 IME가 삼킴). 처음엔 잔향으로 보고 삼켰으나(2단 전송) **사용자 확정 = 통과가 정답**: Commit이 버퍼에 먼저 들어가고 Enter가 전송 → "확정+전송 동시"(Windows 메신저 관례 · DR-16 동작=OS 네이티브 · chat-live 콘솔과 동일). **Esc 잔향만 차단**(조합 취소가 화면 닫기로 새는 것 — `ime_cleared_ms` 1회성 · `cfg!(windows)`). mac은 IME가 삼켜 확정만 되는 2단 — OS별 관례 차이 그대로. 잔여 = 조합 중 Ctrl+K 등 단축키 오동작 여부만(→ WIME-6b로 관찰)
- [ ] **WIME-7 후보창·고DPI** — 한자 변환 후보창이 캐럿 근처에 뜨는가(Preedit cursor 위치 전달) · **배율 125%/150%**(FR-U-6)에서 preedit·후보창 위치가 캐럿에 붙는가
- [ ] **WIME-8 한글 선택·클립보드 왕복** — 드래그 선택 → Ctrl+C → 메모장 붙여넣기 → 다시 Ctrl+V(**CF_UNICODETEXT 어댑터 08-10 구현분의 한글 실증** — M3-1b 연계)
- [ ] **WIME-9 빠른 연속 타이핑** — 자소 유실·순서 뒤바뀜 없는가(이벤트 큐 경합·보류-판정 타이밍)
- [ ] **WIME-10 설정 검색 TextBox** — 아직 레거시 IME 경로([27 §7](27-typeahead-hangul-composition.md)) — stale(묵은 조합) 증상 Windows 재현 여부(재현되면 조합기 재사용 검토) · **한글 AND 토큰 검색** 실동작
- [ ] **WIME-11 신/구 MS IME 양쪽** — Windows 11 신형 IME + "이전 버전의 Microsoft IME" 호환 모드 각 1회전(구현이 달라 이벤트 순서·preedit 통지가 다를 수 있다) · 일/중 IME 첫 키 유출은 X-IME-CJK와 공유


**GUI 세션 판정 실기 (WGUI)** — Windows 실기 · 근거 = `nbeep-plat::gui`([e230e58](../crates/nbeep-plat/src/gui.rs)) ·
맥(콘솔·SSH)과 Linux(컨테이너 2종)는 08-13 실측 완료, **Windows만 실행 검증이 비어 있다**(크로스 컴파일만 통과):

- [ ] **WGUI-1 로컬 콘솔** — 데스크톱에서 무인자 `nexa-beep` = **창이 뜬다**(게이트가 오탐으로 막지 않는가 — 가장 중요)
- [ ] **WGUI-2 원격 PowerShell(WinRM)** — `Enter-PSSession`에서 무인자 실행 = **사유 안내 + exit 3**. 안내에 관측된 윈도우 스테이션 이름이 찍히는가(`Service-0x0-…$` 꼴)
- [ ] **WGUI-3 OpenSSH** — Windows OpenSSH 서버로 접속해 무인자 실행 = 안내 + exit 3
- [ ] **WGUI-4 ★ RDP는 통과해야 한다** — 원격 데스크톱 접속 후 무인자 실행 = **창이 뜬다**. RDP도 `WinSta0`이라 통과가 정답 — 여기서 막히면 **되는 걸 막는 것**이라 판정 기준을 고쳐야 한다
- [ ] **WGUI-5 터미널 모드 비간섭** — 위 어느 세션에서든 `--chat-live`·`--discover-probe`·`--help`는 게이트에 걸리지 않는다(exit 0)
- [ ] **WGUI-6 API 실패 폴백** — 스테이션 이름을 못 얻는 상황이 실제로 있는지(있다면 SSH 아닐 때 통과가 맞는지 재검토)

---

## §6. M4 — 전송 · 무해화

> **목표: 파일을 안전하게 주고받는다.** 게이트 — [18](18-build-and-test.md) **안전 회귀 전항목**(격리물 실행 불가·복원 후 표식 유지·Zip Slip/압축폭탄 거부·데이터 폴더 문자열 스캔 평문 0) · 전송 처리량 링크 70%(NFR-B-10)(**R-5 해소**).

| ID | 항목 | 우선 | 규모 | 의존 | 상태 |
|---|---|---|---|---|---|
| M4-1 | **ADR-0004 확정 반영**([11](11-adr-0004-quarantine.md)) — `.beepq` 컨테이너 포맷 구현·위험 등급 4단계 판정·상태 기계(fail-closed) | P0 | 대 | M2-4 · D-12확정 | 🚧 (08-09 — **도메인 전 구간 완료**: 코덱·위험 등급(합집합 fail-closed)·상태 기계(자동 승인 부재)·이름 정규화(RLO)·격리 저장소(`HashPort`/`MarkPort` 포트·materialize 재검증→원자 rename→표식 후행). safe 37 green. 잔여 = **OS 표식 실물 어댑터**(macOS xattr·Win IAttachmentExecute/ADS — 실기)·검사(§6) 어댑터·[18] 안전 회귀 게이트 편입) |
| M4-2 | 파일 **드래그앤드롭** · **폴더 전송**(FR-X-1/2 구조 유지) · **스트리밍**·진행률·취소(FR-X-4/5) · 협상→수락→전송(FR-X-3) · SHA-256 검증(FR-X-6) | P0 | 대 | M4-1 | 🚧 (08-09 — **정책·UX 완성**: 와이어·수신 조립기 · CLI/GUI 전송 · **전송 자격**(핀+양방향 대화 1회) · **승인 4방식**(자동/기간/수동/거부 · 오퍼당 1승인) · **수신 승인 화면**(정보 4종·즉석 자동승인·타임아웃) · **다중 파일 큐** · **진행률**(목록 막대·대화창·CLI) · **스레드 전송 항목**(08-10 — `ChatBody::Xfer`: 승인 대기→진행 막대→완료/실패가 **대화 기록으로 잔존**·뷰 재열기 복원·FIFO 갱신·자동 거부 미기록) · **대역폭 제어**(자동 50%·쌍방 협상 · 실측 19.9s/이론 20.0s) · TimeoutButton. 잔여 = ~~수락 후 취소 UX~~(★ **08-16 구현 ✅** — 재개형 발신 펌프(ActiveSend · 틱당 4청크 — 종전 블로킹 루프는 전송 중 취소가 구조적으로 불가능했다) + 취소 3경로(내 발신·상대 Cancel 수신 = 펌프 중단·수신측 = inbox 폐기) + active_send/recv 추적 + 진행 배너 취소 버튼 · 실기 = **발신 취소 왕복 ✓(08-16 사용자)** · 수신측 무반응 → 등록 합류점 단일화 수정 + 버튼 식별·피드백 개선(위험색·눌림·MouseUp 발화) · 잔여 = 수신측 취소 재실기. 종전 계획 박제 — 수신측 취소 수단 0(승인 후 표시 전용) · 발신 `cancel_send`는 `awaiting_accept` 가드라 **수락되는 순간 취소 경로 소멸**(`XferAccepted`→`close_send_wait`) · 프로토콜은 양방향 Cancel 준비 완료. 붙일 자리: ① 수신 = `answer_offer`의 xid를 `active_recv` 맵에 보관 + ChatView xfer 배너에 취소 버튼 ② 발신 = 수락 시 창 닫지 말고 라벨 전환 + 가드를 `active_send`로 확장 ③ 액터 송신 루프의 중도 Cancel 수신 처리 확인)·재개(→ **[37](37-transfer-resume.md) 설계 📐 D-31**)·드롭 발신 육안 검증. **폴더 전송은 M4-2c로 분리·기능 제외**) |
| **M4-2c** | **폴더 전송**(FR-X-2) — ⛔ **기능 제외**(사용자 확정 08-17 · ★**08-21 재확정** — "차후 **안정성 충분히 확보된 이후** 재검토"). 폴더 트리 순회 + 각 파일 무해화 게이트 통과 유지 · 파일 단일 전송(M4-2)은 완성이라 이건 순회·묶음 UX만 · 지금 범위 밖(정체성·범위 관리 · [25](25-feature-scope.md) C-2도 ⏸ 정합) | P2 | 중 | M4-2 | ⛔ 제외(재검토 조건 명시) |
| **M4-2e** | ★ **전송 UI 대화창 통합 — 요청 단위 승인·파일별 제외·수신측 제어**(사용자 확정 08-19 · M4-2d의 별도 창을 흡수·대체) — 요구 3: ① **수신 승인 = 요청(배치) 단위**(승인 창에 전체 파일 목록 — 파일마다 묻지 않는다) ② **용량 검사 = 파일 단위** · 한도 초과 파일은 **"전송 제외" 상태로 목록에 잔존**(배치는 계속) ③ **별도 창 제거** — 발신·수신 모두 **대화창 안에서**(수신측도 파일 목록·개별 진행상태·**일시중지** 필요). **설계**: ⓐ 와이어 = **Control 스트림 태그 3종**(manifest=13 `[batch_id 16B‖count‖(name,size,excluded)…]` · **PauseReq=14/ResumeReq=15** `[xid 16B]` 수신→발신) — ★ **File 스트림 신규 kind 금지 근거 실측**: `xfer_step` 디코드 Err 암이 `fail(XferFailed)`라 구버전에 유해(app.rs:1405~) · Control 미지 태그는 무시 = 전방 호환(CAP 11/12 선례) ⓑ 발신 = 드롭 즉시 **배치 전 파일을 스레드 xfer 라인으로 생성**(대기/제외 · 종전엔 오퍼 시점에만 라인) + manifest 발신 · 파일별 크기 검사(발신 상한 + `peer_recv_cap`) = Excluded(오퍼 스킵·목록 잔존) · **라인별 카세트 아이콘 제어**(`icons::xfer` 재사용 · `xfer_cancel_hit` 배너 패턴 확장) · **Role::Sending 창 제거**(`SendBatchWidget`은 라인 렌더러로 전환 또는 폐기) ⓒ 수신 = manifest → 스레드에 전 파일 라인 + **요청 단위 승인/거절 인라인 카드**(Role::Approve 창 대체) · 승인 1회 = 이후 오퍼 자동 수락(**이름+크기 manifest 대조 · 불일치 = 수동 폴백 fail-closed**) · 거절 = 배치 전체(기존 Reject→발신측 큐 정리 경로 재사용) · 라인별 진행 + **일시중지 = Control PauseReq → 발신 액터 `paused_xid` 게이트 재사용** ⓓ 자동 승인 설정(기간/즉시)은 manifest 유무와 무관하게 기존 우선. **붙일 자리** = xfer.rs CAP_TAG(358~) 옆 13~15 · `push_xfer_line`/`set_xfer_line`/`update_xfer_in` · `pending_offers`/`front_offer_info`(3104)/`run_offer_choice`(3189)/`judge_offer`(9770) · 제거 대상 Role::Sending(1644)·Role::Approve(1646)  ★ **일시중지 의미론 확정(사용자 08-19 2차)**: ⓐ **활성 파일 일시중지 = 다음 파일로 진행**(배치가 멈추지 않는다 — 액터는 ActiveSend를 **parked 슬롯**에 보관(열린 파일·해셔·오프셋 유지) 후 다음 오퍼 · 재개 요청은 현재 활성이 끝난 뒤 이어감(스트림은 한 번에 하나 = per-peer 맵 불변식 유지) ⓑ 비정지 파일이 모두 완료되면 **일시중지만 남은 대기 상태**(배치 유지·창/라인 잔존) ⓒ **취소는 어느 쪽에서든 전체 중지**(완료·수신된 파일은 유지 — 격리물 잔존) ⓓ **최종 상태 변화 후 10분 이상 정지 잔존 = 양쪽 자동 전체 취소**(발신 = pause_since 틱 → cancel_send · 수신 = 대칭 틱 → cancel_active_xfer · Cancel 와이어로 상대측도 정리). ★ **최종 UI 확정(사용자 08-19 3차 · 스크린샷)**: 배치 카드 = **SendBatchWidget 행 스타일 그대로**(파일명 / 크기 · 상태색 라벨(Paused=주황·Waiting=흐림 등) / 우측 ▶⏸✕ 아이콘)를 **chat_view 안에** 렌더 — 발신·수신 **양쪽 공통**(별도 창 아님) · **카드 내부 스크롤 없음**(대화창 자체 스크롤 사용 · 사용자 08-19 4차) — 항목(최대 5개 기준)을 행 그대로 펼치고 **각 항목 옆에 제어 버튼**. ⚠ 구현 유의: `update_xfer_in`(FIFO 첫 비종결)이 **Paused 라인을 건너뛰어야** 다음 파일 진행률이 정지 라인에 붙지 않는다(이름 대상 갱신 `update_xfer_named` 신설과 병행). | P0 | 대 | M4-2d | 🚧 (08-19 — 와이어 c9bd077 · 발신 1a d44f3cd · 수신 2a f0322e7 · 승인 창 최상위 f850b20 · Paused 상태 72706f9. 잔여 소진 08-19: 의미론 ⓐ~ⓒ+라인별 아이콘+배치 합산 배너 실기 ✓ · **ⓓ 10분 자동 취소**(`8911aa8` — xfer_pause_since 틱→CANCEL_ALL) · **별도 발신 창 제거**(`f1b9600` — Role::Sending+SendBatchWidget 통째 · 승인 창은 최상위 예외로 유지). 잔여 실기 = ⓓ 10분 관측·큐 정지 수신 동기화 육안) |
| **M4-2d** | **전송 창 재설계 — 배치 목록·파일별 트랜스포트 제어**(사용자 요청 08-19 · ⚠ **M4-2e로 흡수 예정** — 별도 창은 제거 방향, 위젯·상태 전이·pause 게이트는 재사용) — 현재 대기 창은 메시지 1줄 + **파일명이 박힌 TimeoutButton** 하나라 ① 파일명이 선택 하이라이트처럼 잘린다 ② 다중 파일이면 1개만 보인다. **확정 설계(08-19 사용자)**: ⓐ 파일명 = **word-wrap 텍스트**(문자 단위 — 파일명은 공백이 없어 `chat_view::wrap_text` 류 char-level 필요 · ctl `wrap_text`는 공백 기준이라 부적합) ⓑ **맨 위 = 파일 개수 + 총 용량**(고정 헤더 · `send_batch: (u32,u32,u64,u64)` 재사용) ⓒ **아래 = 목록(파일명+용량+상태)** · 높이 초과 시 **스크롤**(nbeep-ctl `ScrollBars` 재사용) ⓓ **제어 = 일반 버튼 대신 아이콘**(카세트 테이프식 — ✅ **자산 완료 08-19**: `nbeep_ui::icons::xfer` PLAY/PAUSE/STOP/CANCEL · [10 §4](10-decision-record.md)) ⓔ **상단 제어 = 전체**(취소=배치 전체 · 일시중지=배치 전체) · **목록 행별 제어 = 그 파일만**(취소=그 파일만 · 일시중지=그 파일만) ⓕ 진행/재개(PLAY)·일시중지(PAUSE)·중지·취소(CANCEL). **아키텍처 = (A) 순차 유지 + 창이 배치 전체 나열**(전송은 한 파일씩 순차 오퍼 — `pump_send_queue`가 `awaiting_accept` 가드 · 상대 파일별 승인 · 대기 창 파일당 열림/`XferAccepted`서 닫힘 app.rs:9504 → **창을 배치 끝까지 유지하도록 생명주기 변경** · 현재 파일 ▶ 표시). 상태 후보 = `send_wait_files: HashMap<PeerId, Vec<(name,size,status)>>`(enqueue 적재·종료/취소 소거). 붙일 자리 = `Role::Sending` paint(app.rs:9100~)·`open/close_send_wait`(2675·2687)·layout(8153)·event(8837)·`cancel_send`. **⚠ 단계 분리** — **P1(무프로토콜)**: 배치 패널 UI(헤더·스크롤 목록·word-wrap·아이콘 제어)+전체/파일별 **취소**(큐 제거·현재 파일 cancel)+**큐 일시중지**(다음 오퍼 보류 = 펌프 게이트, 프로토콜 무변경). **P2(프로토콜)**: **활성 전송 중 일시중지·재개** — 진행 중 파일을 실제로 멈췄다 잇는 건 **M4-10 재개 인프라 위에**(`.part` 보존 + `accept_with_prefix` 재-Offer) 지어야 한다(펌프 중단+.part 유지 = pause · 재-Offer = resume · Cancel과 구분해 부분물 보존). 활성 전송 제어는 대화 스레드 xfer 배너에도 있음(chat_view `xfer_cancel_hit`) — 일관 배선 | P1 | 중 | M4-2 · M4-10 | 🚧 (08-19 — **아이콘 자산 4종**(`icons::xfer` · [10 §4]) · **P1+P2 구현 완료**(`f559b4a`): `send_batch_view::SendBatchWidget`(헤더 개수·총량 · ScrollBars 스크롤 목록 · 문자 word-wrap · 트랜스포트 아이콘·히트) · 창 생명주기 = 배치 끝까지 유지(`XferAccepted`서 안 닫음 · `refresh_send_batch`) · 전체/파일별 취소 · 큐 일시정지(pump pass·순서 보존) · **P2 활성 pause/resume**(`SessionCmd::PauseXfer/ResumeXfer` + 액터 `paused_xid` pump gate · 세션 유지·와이어 무변경) · **상태 전이 다이어그램 = [37 §6](37-transfer-resume.md)**. 703 green. **잔여 = GUI 실기**(2-PC 대용량 전송 중 일시정지·재개·취소 육안 · 활성 pause 중 세션 keepalive 유지 확인) · 대화 배너(chat_view)와 제어 일관성은 후속) |
| M4-3 | **무해화 게이트 4단계**(FR-S-5~10) — 격리 수신·헤더 봉인·검사 · **OS 격리 표식**(MotW/quarantine, 복원 후 유지 FR-S-8) · **AMSI**(Win, FR-S-15) · 승인 UX 등급별 마찰 | P0 | 대 | M4-1 | 🚧 (08-09 — **승인 UX 완료**: 격리함(등급별 마찰·🔴 2단계·키보드 배제) + **수신 승인 화면** · 다운로드 폴더 실체화 + OS 표식(실패도 명시) · 사용자 실기 확인. ★ **08-11 Windows MotW 표식 완료(이 PC 실측)** — `Zone.Identifier` ADS 직접 기록(`ZoneId=3` · IAttachmentExecute 대신 ADS = 의존 0·T0 무권한·내용 결정적) · FAT/exFAT은 오류 명시(macOS xattr 정책과 동일) · `--quarantine-demo` 종단에서 `표식=Ok(Applied)` + `Get-Item -Stream` 실물 확인 · **DR-13 "복원 후에도 MotW 유지"가 3-OS 중 mac·Win 성립**. 잔여 = **검사(§6) 어댑터·AMSI**(Win 실기) · Linux xattr) |
| **M4-10** | **전송 파이프라인 동기·이탈 내성 — 설계 검토**(사용자 요청 08-11 · 방향 결정 대기) — 실태: 전송은 이미 **스트리밍**(송신 청크 → 수신 즉시 소비)이지만 ① **송신 진행률이 소켓 쓰기 기준**이라 로컬·저지연 망에선 버퍼가 즉시 삼켜 "송신 완료 → 그 뒤 수신 진행"처럼 **보인다**(M4-9가 최종 완료 표시는 ack로 고쳤으나 **중간 진행률은 여전히 낙관**) ② 수신측 격리(.beepq 봉인)는 전량 수신 후 수행(마지막 구간 지연). 설계안: **ⓐ 청크 윈도 흐름 제어**(미확인 N청크 상한 — 자연 배압 + 송신 진행률 = 수신 확인 기준 + 메모리 상한 · M4-9 ack의 자연 확장 · 권장) ⓑ 주기 진행 통지만 추가(윈도 없이 표시만 동기 — 트래픽 최소·배압 없음) ⓒ 현행 유지+표기 변경("보냄"/"전달됨" 구분 — 이미 절반 적용). **이탈 내성(fail-over)**: ⓓ **전송 재개**(수신측이 받은 오프셋 영속 → 재연결 시 오프셋부터 재요청 — M4-2 "재개" 실체) ⓔ **지연 ack** — 전량 수신 후 송신자가 떠났으면 격리 완결은 진행하고 ack는 다음 세션에 전달(ack 대기 큐 영속) ⓕ 부분 수신물 보존 상한·만료 정책. 결정 지점 = ⓐvsⓑvsⓒ · 재개 단위 · 보존 기간 | P1 | 중 | M4-9 | 📐 (**08-16 설계 문서 완성 = [37 전송 동기·재개](37-transfer-resume.md)** — `.beepq.part` 부분 격리물(격리함 비표시·완성 시 승격 = 무해화 게이트 불변) · Accept 확장 2필드(resume_offset+prefix_sha · **불일치 = 처음부터 fail-closed**) · 재-Offer 1회+수신 2택 · '동기'는 중단 전송 상태 동기로 한정 · ~~🔴 D-31~~ → **✅ 확정(08-18 — 권고안 그대로: 72h/1GiB·부분 재해시·그룹 재개 보류)** · ★ **08-18 M4-10a+b 구현**: core = Accept 재개 꼬리 2필드(`resume_offset`+`prefix_sha` — 0이면 구버전과 바이트 동일·전방 호환 회귀)+`accept_with_prefix`+**중복 프리픽스 관용**(구버전 발신자가 0부터 재송신해도 바이트 대조 후 흡수·어긋나면 폐기)+`take_partials` · 앱 = 액터 종료 시 부분물 **봉인 보존**(`part.rs` — `parts/{sha}.part` · `xfer-part-v1` 도메인 · 부팅 sweep 72h/1GiB) · 발신 = prefix 해시 대조 후 **중간 송신**(불일치 = 0부터 · 평균 속도는 이번 세션 분량 기준) · 수신 = **수락 3경로 전부 자동 이어받기**(수동 ⌘Y·승인 창·자동 승인 — `.part` 매치 시 상태바 "이어받기 N%부터") · 완료/수동 취소 = `.part` 정리 · 끊김 실패 = 보존(재개 사유). ★ **08-18 M4-10c 완결**: 발신 능동 재-Offer 1회(`current_send`+`resend_offers` — Closed 기억→재성립 flush 재제안·취소 = 소거)+수신 2택 UI(승인 창 [이어받기 N%]/[처음부터] — `resume_pct`·`ApproveFresh`) · ★ **08-18 실기 확인(사용자)** — 수신측 종료→재시작→능동 재-Offer→**승인 우회 이어받기→격리 완료** 전 사슬 육안 완주(35.5MiB · 진행률 단일 항목·평균 속도 표기 동반). 잔여 = Win↔mac 교차 대용량(T-macxfer)) |
| **M4-11** | **자동 대역의 부트스트랩 고착 — 로컬 0.25MB/s**(실기 08-16: localhost 10.5MiB가 42초 · 32초에 7.9MiB = **정확히 `AUTO_FLOOR_BPS`(256KiB/s)**) — 원인 = **관측이 페이싱에 갇힌다**: Auto 목표 = `max(peak×0.5, 256KiB/s)`인데 첫 전송은 peak=0 → 하한으로 조임 → 조인 채 보내니 관측 속도 ≤ 목표 → peak가 하한에 고착 → **영구 256KiB/s**(쌍방 negotiate도 둘 다 하한이라 min=하한 · 링크가 기가비트든 무관). *페이싱 중 관측은 링크 능력이 아니라 자기 목표를 재는 것*. 설계 후보: ⓐ **슬로우 스타트 프로브**(주기적으로 목표 ×2 상향 시도 — 관측이 따라오면 peak 갱신·상승, 못 따라오면 후퇴 · TCP 혼잡 제어와 같은 결) ⓑ **첫 구간 무페이싱 측정**(첫 1~2MiB는 전속 — 진짜 peak 관측 후 50% 적용 · 구현 최소) ⓒ **루프백/자기 호스트 감지 = 무제한**(로컬 실기·같은 PC 다신원엔 페이싱 무의미) ⓓ 명시 설정(PerSec)은 현행 유지. 회귀 = "관측이 목표에 갇히지 않는다"(하한 시작 → N회 후 목표가 실능력의 절반에 수렴) | P1 | 중 | M4-2 | 🚧 (★ **08-16 ⓑ 구현 ✅ + 속도 실기 확인(사용자)** — `feat/m4-11-rate-bootstrap` 병합: `advertised_cap` 신설(공지와 페이싱 목표 분리 — Auto = 상한 무주장 0 · PerSec만 주장) + 발신 첫 2MiB 무페이싱 프로브→경계에서 실측 기반 재산출 · 회귀 = advertised_cap 규칙 · 설계 = [06 §5-1](06-network-stack.md)·[30 §3-4b](30-end-to-end-walkthrough.md) · ★ **08-16 ⓐ ✅ = ⓐⓑ 완결**(페이싱 중 8MiB마다 512KiB 전속 재관측 — 슬로우스타트 과소 보정 · 상향 전용 · 비중 6% · ⓒ 루프백은 프로브로 사실상 해소 — 종결) |
| **M4-3b** | **세션 구간 실소켓 캡처**(W-8·W-9 · [29 §4-3](29-wire-security-audit.md)) — `tcpdump`로 대화·**파일 전송 중** 평문 부재 확인(단위 tap만으로는 실소켓을 증명 못 한다). 파일 바이트가 **별도 평문 경로로 새지 않는가** | P0 | 소 | M4-2 | ⏸ 실기(권한 필요) |
| M4-4 | 아카이브 안전 처리(X-5) — **자동 해제 금지** · Zip Slip · 압축폭탄(FR-S-11) | P1 | 중 | M4-1 | 🚧 (08-09 정책 계층 ✅ · ★**08-21 zip 파서 어댑터 ✅**(`24781b5` — EOCD+중앙 디렉터리만 파싱·압축 데이터 무접촉 · fail-closed(잘림·ZIP64·미끼 EOCD) · 게이트 배선 = 격리 시 점검 → QMeta 꼬리 flags·격리함 "! 아카이브 위반" 라벨·CLI 사유 출력 · 회귀 4종) · ★**08-21 잔여 소진**(`53d151b`) — **zip64 ✅**(로케이터→EOCD64·항목 extra 0x0001 실값·멀티 디스크 거부·항목 상한 100만 — 여전히 목록만 읽는다·총 해제 상한 1GiB 정책 불변) · **내용 목록 UI ✅**(격리함 아카이브 행 재클릭 = 워커 개봉→요약+정책 사유+상위 12항목 경고 모달) · 잔여 = 실체화 시 로컬 헤더 대조(해제 없는 v1엔 소비자 없음 — 해제 도입 시)) |
| M4-5 | **`imgdec` 별도 프로세스**(X-4) — 권한 강등 이미지 격리 디코드 · **재인코딩본만 표시**(FR-S-12, SVG 미지원). **R-5 해소** | P1 | 대 | M4-1 | 🚧 (08-11 — **코어 실물화**: 프로토콜 v1(stdin→`NIMG`+RGBA) · 디코더(`png`·`jpeg-decoder`)는 **격리 bin에만**(본체 미링크 — R-5 격리 성립) · 상한(원본 1MiB·픽셀 2048²·박스 축소 256·**부모 3초 kill**) · 본체 재검증·부재 시 이니셜 폴백 · **아바타 실사진 개통**(목록·카드·편집 미리보기 — 원형 마스크). 실측 PNG/JPEG 왕복·쓰레기 fail-closed. **잔여 = ~~ⓐ 포장 동봉~~ ✅(08-11 · **CI 검증 08-12 v0.1.4** — 포터블 zip에 imgdec 362KB 실재) · ~~ⓑ 수신 파일 미리보기~~ ✅(08-12 — 격리함 36px·스레드 인라인 18px · `.beepq` 재조립→imgdec·1MiB 상한 · ★ **08-18 점진 로딩** `b66edfc` — 512MiB가 목록에 안 뜨던 것: 메타 사이드카 `{hex}.beepq.meta`(봉투만 봉인·`SEAL_QMETA`)로 스캔 즉시 목록 + 행별 검증 잡(`QRow.ready`·`QVerified`) + 미검증 승인 차단 + 수신 재스캔 훅)** ~~ⓒ 권한 강등~~ ✅(**08-15 — R-5 해소**: 입력 전부 읽은 뒤·파싱 전 자기 잠금 — mac Seatbelt `pure-computation`(로컬 실측 ✅) · Linux seccomp-bpf 허용 목록+no_new_privs(libc 상수 — 손 번호 금지) · Win 완화 정책(동적 코드·원격 이미지 금지 — win32k 락아웃은 WGUI 실기 후 강화) · 강등 실패 = fail-closed(exit 4) · **통합 테스트가 실제 바이너리를 3-OS CI에서 스폰**(`tests/lockdown.rs` — 추정 금지 실측 경로) · 크로스 3타깃 check ✅) · 잔여 = ~~확대 미리보기~~(★ **08-16 스레드 축 구현 ✅** — 썸네일 클릭 = Role::ImageView 단일 창 · imgdec 1024px·1MiB 상한·R-5 불변 · ★ 08-16 격리함 재클릭 진입 ✅(상태 기반 — 진입점 ② 완결) · 잔여 = 실기 육안. 종전 계획 박제 — 썸네일 3종 전부 그리기 전용·클릭 핸들러 0·이미지 뷰어 Role 부재. 붙일 자리: `Role::ImageView(beepq path)` 신설 + chat_view 썸네일 rect 별도 hit + `thumb_raw_from_beepq` 큰 치수(1024 상한) 재호출 · 격리함은 행 더블클릭)) |
| M4-6 | 오프라인 큐(FR-M-3) — 부재 시 **송신자 로컬 보관** 후 재접속 전달 · 보관 상한(그룹 `pending_group_sends` 문법의 1:1판) — ★ 사용자 재확인(08-14): **서버리스 한계 = 송신자도 꺼져 있으면 못 받는다** → 완전한 미수신 해소는 서버 모드 ②([32 §5·GS-2](32-adr-0013-server-modes.md))가 담당(v1 아님) | P1 | 중 | M2-4 | ✅ (**08-21 구현+실기 ✓** — 상한 200·SEAL_PENDING 재시작 유지·성립 합류점 flush·대기 막대·공지 편입 · 08-21 행 정정) |
| **M4-9** | 🔴 **전송 완료의 종단 확인(ack) 부재 — 발신 "전송 완료"가 거짓일 수 있다**(사용자 점검 요청 08-10). 현재 `XferSendDone`은 마지막 Chunk+Done을 **소켓에 쓴 시점**에 발화 — 수신측 SHA 재검증·격리 성공과 무관("보냈다≠닿았다" 위반 상태). 와이어에 수신 완료 메시지가 없다(`XferMsg` = Offer/Accept/Reject/Chunk/Done/Cancel — 6종 전부 확인). 정상 종료 직후엔 OS가 송신 버퍼를 마저 흘려보내 대체로 도달하지만(페이싱 덕에 잔량이 소켓 버퍼 수준) **커널 동작에 기댄 우연한 안전**이고, 강제 종료·크래시면 수신측 부분 수신→폐기(fail-closed·안전)인데 발신 UI는 이미 "완료"로 남는다 · 수신측 해시 불일치·저장 실패도 발신자는 영원히 모른다. 처방: ① **`XferMsg::Received{id}`/`Failed{id,why}` ack 신설**(전방 호환 — 미지 종류 무시 규약 유지) — 발신 스레드 항목은 ack 후에만 "완료", 그 전엔 "전달 중 · 확인 대기" ② **종료 가드** — 미확인 전송이 있으면 종료 시 확인·짧은 flush 대기. ★ DR-25의 수신확인(사람·수동 버튼)과 별개인 **전송 계층의 기술적 확인** — 봉투 원리 준수(완료 사실만) | **P0** | 중 | M4-2 | ✅ (08-11 — ① **`XferMsg::Received{id}`(kind 7)·`Failed{id}`(kind 8)** 신설 · 수신측 격리 성공→Received/실패→Failed 반송(actor·CLI) · 발신 UI `XferLineState::AwaitingAck`("전달됨·확인 대기")→ack로 완료/실패 종결 · ★ `update_xfer_in`이 AwaitingAck 건너뛰고 `update_xfer_ack`가 확인 대기만 닫아 **다중 파일에서 다음 진행이 앞 확인 대기를 안 덮음** ② **종료 가드** — 미확인 전송 시 첫 닫기 경고·둘째 확정(2단계) · ack 완료 시 자동 해제 · 432 green. ⏸ 2노드 실물 왕복 GUI 실기 확인 대기) |
| M4-7 | **공유 목록 + 가상경로 서버**(FR-X-8/9/11 · [23](23-adr-0009-shared-folders.md)) — 등록·범위 확인 UX · `ShareList`/`ShareGet` · **fail-closed 경로 해석기**(traversal·심링크 안전 회귀 [18](18-build-and-test.md) 추가) · 계측 4종 | P1 | 대 | M2-3 · M4-2 | ☐ |
| M4-8 | **pull 다운로드 수신 경로**(FR-X-10) — 상대 공유 브라우즈 UI · 다운로드→**동일 4단계 게이트** 합류 | P1 | 중 | M4-3 · M4-7 | ☐ |

---

## §7. M5 — 그룹 · 마감 · 배포 (v1 출시)

> **목표: v1 출시.** 게이트 — [00 §5](00-vision.md) **성공 기준 S-1~S-8 전항목** · [05 NFR-B](05-requirements.md) **예산 전항목**.

| ID | 항목 | 우선 | 규모 | 의존 | 상태 |
|---|---|---|---|---|---|
| M5-1 | **그룹 관리 + 그룹 전송**(FR-G-1~4) — 로컬 그룹 생성/편입/제외·재시작 유지 · 팬아웃 + 단일 스레드 UI · 개별 전달 상태 | P0 | 대 | M4-2 | 🚧 (08-13 — **기반 일습 커밋**: `groups.seg` 암호화 영속(FR-G-1 ✅) · 목록 상단 그룹 섹션 + ⌘클릭 다중 선택 + 우클릭 생성/개명/편입/제외/삭제 · `TextPromptWidget` 이름 모달 · **동보 스레드 + 팬아웃 발신**(세션 즉시·미연결 자동 연결 후 이어 전달·백오프 소진 실패 라인 — FR-G-2·G-6 ✅ · FR-G-4 초판) · 527 green. ★ **방향 확정(사용자 08-13) = 진짜 그룹(한 방 대화)** → [31 ADR-0012](31-adr-0012-shared-group-chat.md) 설계 → 잔여 = 🔴 **D-28 확정** 후 G1~G4 구현 · ⏸ 동보 기반 2-PC 실기 · ★ **08-19 초대 유실 연쇄 해소**(`8d37247`·`9924a01` — 3신원 실기): 첫 초대가 세션 미성립·churn으로 유실(백오프 소진 = 폐기) + Invited 방 행 미표시 = 재시작 후 수락 불가 → ① 소유자 구성원 목록 **"초대 대기"**(`group_accepts` 수락 추적 — 비소유자 가시화는 roster에 수락 상태 실어야 하는 후속) ② 재초대 수신 시 Accept 재통지(자가 치유) ③ 구성원 목록 열기 = 미수락·미연결 재연결 넛지 ④ **Invited 방 목록 표시("초대됨 · 클릭하여 수락") + 클릭 = 수락 모달** · 실기 ✓ 새 방+대화 경유 수락 전 사슬 · 잔여 = Invited 행 클릭 수락 육안) |
| **M5-1g** | **공유 그룹 채팅 G1~G4**([31 ADR-0012](31-adr-0012-shared-group-chat.md) — 한 방 대화·발신자 라벨·roster 서명·초대 수락·구성원 관리 모달·재동기) · ⚠️ **[32 §8 P-9~P-13](32-adr-0013-server-modes.md) 선행 제약을 함께 지킨다**(콘텐츠 키 봉투 수용 포맷 · 중복 제거 키 고정 · roster 정책 필드 자리 · **`blob_id=H(암호문)` 내용 주소 자리** · **오퍼/수락 UX를 경로 무관하게**) — 나중에 넣으면 **와이어·서명 포맷 변경** | P0 | 대 | ~~D-28~~ ✅ | 🚧 (08-13 — **✅ D-28 완결 · G1~G3 구현**: `sgroup.rs`(Roster 수용 규칙·SGroupMsg 6종·P-11 확장 영역)+`StreamId::Group`+groups.seg v2 · 그룹 만들기=전원 초대(미연결 대기+자동 연결)·수락/거절 카드 · 방 대화(Msg 팬아웃·발신자 라벨·unread 배지·명부 운영·제외 수신=방 닫힘) · 서명은 세션 인증 대체(DR-20에서 얹음) · 532 green. ★ **G4 완료(08-15)** — 구성원 편집 모달(제외·초대) + **재동기 확장**(세션 성립마다 소유 방 Invite 재송 — 인바운드 포함 · 수신측은 기존 방이면 명부 갱신으로만 · 유실 초대/낡은 명부/최신 세 경우를 멱등으로 닫음 · a58c188) + **로컬 그룹 승격**(부팅 1회 — 동보 UI 숨김 후 유령이던 구버전 로컬 그룹 → 내 소유 공유 그룹 · 초대는 재동기 push가 세션 성립 시 전달 · 재오픈 생존 회귀) · ~~Q-32-7~~ ✅ **확정(08-21 — 자리 예약)**: kind 프레이밍 = 추가 시 와이어 무변경 확인 · P-9 전용 **kind 8 예약**(`K_CMSG_RESERVED` — blob_id(P-12) 포함 형태 명문화) · 실구현 = 서버 축(X-1/X-3) 때 · 잔여 = ⏸ 2-PC 실기(초대 왕복·방 대화)) |
| M5-2 | 전체 **브로드캐스트 공지**(FR-M-6) | P1 | 소 | M2-4 | ☐ |
| M5-3 | **스팸·플러딩 방어**(FR-S-16 · X-3) — 발신자별 속도 제한·차단 목록·미확인 발신자 격리. **R-4 해소** | P1 | 중 | M2-4 | ☐ |
| M5-3b | **수동 엔드포인트 등록**([19](19-adr-0006-manual-endpoint.md)) — IPv4/IPv6/DDNS 등록 · 원격 신뢰 등급(대조 전 파일 차단 FR-S-24) · **인바운드 요청 대기**(FR-S-25) · 백오프 재연결 · 병합(FR-D-13) | P1 | 중 | M5-3 · D-19확정 | 🚧 **08-21 1차** — 등록·백오프 기구현 + **FR-S-24 ✅**(발신 모달·수신 즉시 거절 — 판정 단일 지점 `file_allowed` 곱 행렬) · **FR-S-25 ✅ 완결(2차)** — 요청 대기 모달(`pending_remote` 슬롯+2택 · 수락 = `accept_inbound` 합류점 · 거절/X = 소켓 닫힘) · **CLI 축 ✅(3차 — 같은 곱 함수·양방향 차단)** · 잔여 = 공인망 실기 |
| **M5-3c** | **경로 축 분리 + 경로 수명**(FR-D-17~19 · FR-S-52 · **R-20**) — **신원 신뢰 / 경로 등급 2축 모델**과 정책 매트릭스 · **경로 등급은 성립한 세션이 정한다**(가짜 LAN 광고로 원격 제약 우회 차단) · 경로 수명(실패 상한·**다른 키 성립 시 즉시 무효화**·보존 상한) · 경로 전환 시 **조용한 표시 변경**(신원 변화만 시끄럽게) | P0 | 중 | M1-3b · M5-3b | 🚧 **08-21 1차** — `PathClass` 2축 ✅(성립 실소켓 주소 판정 `class_of_ip` — 광고 불신) · **P-3 ✅**(수동 주소가 다른 키로 성립 = 즉시 삭제+고지) · 원격 성립 = 스레드 1줄 고지(조용한 표시) · P-1/P-2 실패 상한 = 기존 백오프 중단이 자동 시도 축을 이미 충족(수동 클릭은 의도적 행위라 유지 — v1 `manual_addrs` 비영속이라 P-6 보존 축 해당 없음) · ★**헤더 상시 배지 ✅(08-21 후속 `4c60011`)** — 대화 헤더 지구본 16px+"인터넷 경유" 라벨(warn색·4개 언어·Local 비표시) — **M5-3c 코드 축 소진** |
| M5-4 | **배포 2채널**(DR-4) — 포터블 + 설치본, 4타깃 · 데이터 경로 폴백 | P0 | 대 | M0-3 | 🚧 (08-11 — **포장 파이프라인 완료**: `release.yml`이 5타깃(win x64·arm64 / mac arm64·x64 / linux x64) × 2채널을 만든다. 설치본 = NSIS `.exe`(**사용자 단위·무권한** · `/S` 무인) · `.dmg`(.app+icns) · `.deb`(desktop+아이콘). **설치본도 zip 동봉**(실행 확장자 차단 대비 · 사용자 요청) · `SHA256SUMS.txt` · 릴리스 산출물에도 ≤10MB 게이트. **winget·choco 매니페스트 4종**(설치본/포터블 × 2) 자동 생성 — 실제 산출물 SHA로 채우고 자리표시자 잔존 시 실패. **Homebrew 추가**(08-11 · 참고: `sosomlab-tauri-test1` 탭 운영 기록) — Cask `nexa-beep`(.app) + Formula `nexa-beep-portable`, `kiros33/homebrew-tap`. ★ **트리거 정책**(사용자 확정 08-11): 기본 배포 = `v*` 태그 push **자동 공개** · Homebrew = 릴리스 직후 자동(`TAP_TOKEN` 유무만) · **winget·choco만 저장소 변수로 잠금**(`WINGET_PUBLISH`/`CHOCO_PUSH` + 시크릿 — `nexa-memkeeper`(Windows 전용) 검증 패턴 차용 · 중앙 검수라 되돌리기 어려워서 · mac/Linux 경로에는 영향 없음). 치환은 `render-manifests.sh` **단일 지점**. **★ 08-11 첫 배포 실행 — v0.1.0 → v0.1.1 공개**(5타깃 자산 · Homebrew 탭 반영). 배포 중 실패 4건 수정(bash 안 PowerShell 변수 문법 · 한글 조사가 변수명에 붙어 `set -u` 사망 · `brew audit` 경로 인자 폐지 · winget 폴더 깊이 추측). ★ **macOS 격리 실측** — 서명 없는 앱 + quarantine = **SIGKILL + 앱 삭제**, 애드혹 서명도 못 넘는다 → Cask `postflight` 제거 + 애드혹 서명. 포장 = 플랫폼 관례(Win zip·mac/Linux tar.gz). 잔여 = ⓐ **서명·공증**(mac notarize·win 인증서 — 별도 결정 · **격리 제거가 임시방편임을 잊지 말 것**) ⓑ **데이터 경로 폴백**(FR-P-3 — 포터블/설치본 동작 차이는 **설정 영속 M3-15와 같은 슬라이스**, 지금은 저장하는 것이 없어 채널이 설치 방식만 다르다) ⓒ winget-pkgs PR·choco push 실제 제출 ⓓ Linux arm64) |
| **M5-4a** | **코드 서명·공증**(macOS notarize + Windows 인증서) — ★ **현재 Cask `postflight`의 격리 표식 제거는 임시방편이다**. 실측(08-11): 서명 없는 앱 + quarantine = **SIGKILL + 앱 삭제** · 애드혹 서명으로도 못 넘는다 → 공증만이 정답. 인증서 구매·팀 등록이 선행 | P1 | 중 | M5-4 | ⏸ **차후 보완(사용자 확정 08-21 — 인증서 이슈로 지금 불가 · 요청 시 진행)** |
| **M5-4b** | **winget·Chocolatey 실제 게시** — 매니페스트 생성·검증은 통과, **게시만 변수로 잠겨 있다**. ⓐ ~~Windows 실기 확인 선행~~ **✅ 실기 완료(08-11 · 이 PC)** — SHA-256 3/3 일치 · 무인 설치(`/S`)·업그레이드(0.1.1→0.1.2)·완전 제거 통과 · winget validate 통과 · 매니페스트 해시 실측 일치. ~~단 🔴 M5-4d(더블클릭 결함) 선행~~ **✅ 해소**(v0.1.3에 실려 배포 실물로 재검증) ⓑ 시크릿(`WINGET_TOKEN`·`CHOCO_API_KEY`) + 변수(`WINGET_PUBLISH`·`CHOCO_PUSH`) 설정 ⓒ `publish-windows-packages` 실행 ⓓ 검수 통과 확인(둘 다 중앙 저장소라 즉시 노출 아님) | P1 | 소 | M5-4d | 🚧 **첫 게시 검수 대기**(08-21 스위치 ON → 첫 제출 · ★**08-22 스위치 재-OFF**: winget PR #421961/#421967 = OPEN+`Validation-Executable-Error` 라벨(수동 검수 대기) · choco = 모더레이션 미통과 — **첫 검수 종료 전 새 버전 제출 금지** 판정으로 `WINGET_PUBLISH`·`CHOCO_PUSH` false. 검수 통과 후 다시 켠다 + `Validation-Executable-Error` 원인 확인(무서명 exe 추정) |
| **M5-4d** | 🔴 **Windows 더블클릭 무반응 — macOS 번들 버그의 미수정 쌍둥이**(실기 발견 08-11). 포터블 exe = 콘솔 서브시스템 + 인자 없음 → 스캐폴드 출력 후 종료 = **더블클릭 시 콘솔 번쩍**. NSIS **시작 메뉴 바로가기·완료 실행도 인자 없이** 호출 → 설치본 정식 실행 경로도 동일하게 깨짐. `launched_from_app_bundle()`([main.rs:116](../crates/nexa-beep/src/main.rs))이 `.app/Contents/MacOS/` **전용**이라 Windows 대응 없음. 처방 = ⓐ NSIS 바로가기·FINISHPAGE에 `--window --live`(설치본만) ⓑ **Explorer 실행 감지(부모 콘솔 없음·`GetConsoleProcessList==1`) → 창 모드**(포터블+설치본 · macOS와 대칭 · 권장) → v0.1.3 | **P0** | 소 | — | ✅ (08-11 — **ⓑ 채택**: `plat::launch::from_gui_shell()` GetConsoleProcessList 휴리스틱(반환 1 = 탐색기 새 콘솔) → 창 모드 · `hide_gui_console()`(FreeConsole)로 잔존 콘솔 제거 · macOS 번들 판정과 대칭 · NSIS 바로가기·완료에 `--window --live` 두 번째 방어선. 실측: 터미널 무인자=CLI 보존 · Start-Process=GUI 전환·콘솔 없음 · 431 green. **v0.1.3에 실려 공개 완료(08-11) — 배포 실물로 재검증 통과**. ★ 08-13 후속: **무인자 기본 = 창+라이브로 승격**(사용자 확정 — 어디서 부르든 무인자면 창 · `from_gui_shell` 휴리스틱은 **콘솔 분리에만** 잔존 · 사용법은 `--help` 신설·`--version` 명시 분리) |
| **M5-4c** | **Linux 배포 확장** — arm64(`aarch64-unknown-linux-gnu` · X11/Wayland 크로스 링크 부담) · `.rpm`·AppImage·Flatpak 수요 확인 후 결정 | P3 | 중 | M5-4 | ☐ |
| **M5-4e** | **데이터 경로 폴백 종점 정정**(FR-P-3 · 08-19 실기 진단) — `data_dir()`는 ① 실행 파일 옆 `data/` → ② 사용자 설정 디렉터리 → **③ 임시 폴더** 순인데, ③은 macOS `com.apple.tmp_cleaner`(매일 자정 · 3일 미접근 삭제)가 `identity.key`를 지우는 자리다 → **신원이 조용히 바뀌고**(지문 변경) 이전 키에 sealed된 격리물·핀을 **영구히 못 연다**(크립토 셰레딩 — 실기로 `.beepq` 8개 소실). 실기 폴더는 `$HOME/.nexa-beep-multi`로 옮겼으나(08-19) **제품 폴백은 그대로**라 절반만 막힌 상태. 후보 = ⓐ ③ 제거 + **fail-loud**(쓸 곳이 없으면 안내 후 종료 — 조용한 신원 상실보다 낫다 · 권장) ⓑ ③을 OS별 durable 경로로 교체 ⓒ ③ 유지 + 첫 기동 경고 배너. 관련 [26 §3-4](26-run-and-manual-test.md) · [28 ⓔ](28-adr-0011-settings-persistence.md) · DR-4 | P2 | 소 | — | ✅ (08-20 `a1fb215` — **ⓑ 채택(사용자 확정 "임시폴더가 아닌 곳으로 이동")**: ③ = 홈 숨김 폴더 `~/.nexa-beep`(Win `%USERPROFILE%\.nexa-beep`) · temp는 홈조차 없는 극단만 잔존) |
| M5-5 | **24h 상주 누수 실측**(E-9 · NFR-B-6) · 100대 규모(E-8) | P0 | 중 | M5-1 | ☐ |
| M5-6 | 문서 마감 — 사용 안내 · **알려진 한계**(TOFU가 못 막는 것·기기 이전 시 기록 미이전 V-7·저장 위협 모델 H-5) | P0 | 소 | — | ☐ |

---

## §8. v2 · 포스트 v1 (보류)

| ID | 항목 | 우선 | 규모 | 의존 | 상태 |
|---|---|---|---|---|---|
| X-1 | **`nexa-beepd`** — 릴레이 서버 MVP([32 §12-6](32-adr-0013-server-modes.md)) — ★**✅ MVP 구현(08-21 · 사용자 명시 요청 — `feat/udp-relay-server`)**: `crates/nexa-beepd` 신설(별도 bin · 배포 = `beepd-v*` 태그 · release-server.yml · W-1~W-4) — 프레즌스(회전 RID)·랑데부(TCP/UDP 관측 엔드포인트 교환)·**blind 릴레이**(Accept 양방향 성립 후만·연결당 토큰 버킷·저장 0)·서버 자기 핀(기동 시 키 출력) · 외부 의존 0·0.35MB · e2e 6종(종단 Noise 관통·핀 불일치·ChClosed 전파). **비목표 불변 = 계정·오프라인 저장·팬아웃·웹 가입**. 잔여 = 공인망 실기·미수락 채널 소멸 타이머(개선 여지) | P2 | 대 | ~~X-UDP~~ | ✅ MVP (08-21 · 실기 잔여) |
| X-UDP | ★ **UDP 홀펀칭 준비 — 상위 묶음**([32 §12-2](32-adr-0013-server-modes.md) · Q-32-15) — 홀펀칭·릴레이 전체의 선행 관문. ★**착수 선언(사용자 08-21 — "이후 UDP 구현과 서버 개발 진행" = Q-32-15 방향 확정)** — 진입 = X-UDP-a 결정. 원칙 = **TCP는 유지**(LAN 직접 · S-1) · UDP는 **`Link` trait 뒤 형제 구현 하나 추가**(위층 Noise·mux·채팅·파일·프로필 **무변경** — 이미 `NoiseSession<L: Link>` 제네릭). 하위 X-UDP-a~e 순서대로. ★**08-21 a·b ✅ + c·d 축 개통**(`feat/udp-relay-server` — Q-32-15 확정 = 착수 그 자체) — `Link` 형제 구현 원칙 그대로 실증(위층 무변경) | P2 | 대 | ~~Q-32-15~~ ✅ | 🚧 (a·b ✅ · e 잔여) |
| ~~X-UDP-a~~ | **✅ 확정(08-21 · 착수 지시로 권고안 채택)** — **ⓐ 경량 자작 ARQ**. ⓑ QUIC(quinn)은 자체 TLS가 Noise와 중복(암호화 두 겹)+의존 수 MB(DR-5·DR-12 손해). 실물 = `nbeep-net::arq`(순수 상태기계 · SACK+누적 ACK·고정 창 64·RTO 백오프·동시 열기·죽은 경로 15s) | P2 | 소 | — | ✅ (08-21) |
| ~~X-UDP-b~~ | **✅ 구현(08-21)** — `nbeep-net::UdpLink`(`Link` 계약 만족 · arq 셸): 분할 1152B/재조립·SACK 재전송·배압(512KiB)·`punch`(소켓 지정 동시 열기 — 펀칭용). **회귀 = TcpLink 계약 테스트 재사용 ✓** + 결정적 주입 10종(15%손실+중복+재정렬 순서·완전성·60KB 비트 동일). 성공 기준 실증 = **종단 Noise가 무변경으로 UdpLink 위 성립**(beepd e2e). 개선 여지 = 혼잡 제어(지금은 고정 창+RTO 백오프) | P2 | 대 | — | ✅ (08-21) |
| X-UDP-c | **STUN 랑데부 + 홀펀칭 코디네이션** — ★**배관+조율 ✅(08-21~22)**: 서버 UDP 관측(STUN-lite)·엔드포인트 교환·**지연 수락으로 타이밍 조율**(프로브가 수락보다 먼저 = 관측이 반드시 신선 · 펀치 소켓 = 관측 소켓·시도마다 신선) · 루프백 동시 punch→종단 Noise ✓. 잔여 = **실 NAT 실기** · ★**08-22 절반 실측**(OCI 상시 서버 — 같은 NAT 2단말: 등록·관측·랑데부 ✓ · 헤어핀 펀치 실패 → **릴레이 폴백 성립·왕복 확인**(체크리스트 1·4·6)) · 남은 것 = **다른 NAT 펀치**(맥↔회사 Windows — Full/Restricted/Port-restricted/Symmetric 유형별 · 추정 금지) + 관찰 1건(재기동 직후 1차 랑데부 비대칭 실패 — 재현 시 추적) | P2 | 대 | — | 🚧 (코드 ✅ · 릴레이 폴백 실측 ✅ · 펀치 실기만) |
| ~~X-UDP-d~~ | **✅ 완결(08-22)** — beepd blind 중계(양방향 성립만·토큰 버킷·S-3) + `connect_via` **펀치 실패 → 릴레이 자동 폴백**(각 단 독립 핸드셰이크) · 경로 등급 = 성립 링크 실주소(신원 신뢰 불변 §2-5) | P2 | 중 | — | ✅ (08-22) |
| X-UDP-e | **경로 선택 통합** — ★**원격 구간 ✅(08-22)**: `connect_via`/`accept_via` 사다리(홀펀칭 UDP → 릴레이 폴백 · 받는 쪽 = **첫 프레임 링크 추종**) · WrongPeer = 암호학적 근거 fail-closed · DR-28 2축 정합(등급 = 성립 실주소). ★**LAN 통합 절반 ✅(08-22 · X-2b)** — GUI `start_connect` 3단 사다리(발견→수동→서버 랑데부 · 자동 재연결도 서버를 수단으로 인정) · **단일 목록 프레즌스는 프로토콜상 없음**(회전 RID = 서버가 명부를 못 준다 — 봉투 원리 · 서버 상대는 "아는 키"를 사다리로 두드려 성립할 때 목록에 뜬다) · 잔여 = 다중 경로 병합(FR-D-6) | P2 | 중 | — | 🚧 (원격 ✅ · GUI 사다리 ✅ · FR-D-6 잔여) |
| X-2 | 릴레이 모드 클라이언트(`RelayTransport`, DR-8 ②) — ★**어댑터 층 ✅(08-21)**: `crates/nbeep-relay` 신설(서버와 **같은 크레이트로 같은 와이어** — Q-32-13 §9-1) — `RelayClient`(서버 Noise 세션 액터·핀 검증 `PinMismatch`)·`RelayLink`(=`Link` · 32KiB 조각 · 종단 Noise 무변경). 잔여 = `Transport` 구현체로 승격(발견 증강·`connect` 사다리 통합 — X-UDP-e)·오프라인 큐 서버 보관(Content 모드 — 별도) | P2 | 대 | ~~X-UDP · X-1~~ | 🚧 (어댑터 ✅ · Transport 승격 잔여) |
| ~~T-flake~~ | **✅ 해소(08-21)** — 정체 = **nbeep-plat 스위트가 힙 오염(0xc0000374·간헐 AV)으로 중도 사망**(60개 중 31개 시점 abort = "정확히 29개 모자람"과 일치). 근본 원인 = **`OpenClipboard(NULL)`의 거짓 성공**: 내부 오픈 상태를 "연 창 핸들"로 대조하는데 NULL==NULL이라 둘째 호출자(스레드·프로세스 불문)도 성공 → 한쪽 `EmptyClipboard`가 상대가 `GlobalLock` 중인 HGLOBAL을 해제 = use-after-free. **제품 실버그였다**(이미지 워커 × UI 복사 · 3-신원 다중 인스턴스). 수정 = 호출마다 메시지 전용 창(진짜 HWND)으로 열기 + 프로세스 내 뮤텍스 + `GlobalSize` 상한 스캔. 실측 = cargo 루프 수정 전 4/20·2/25 크래시 → 수정 후 0/70. 부수 해소 2건 = ① `dir_writable` PID 프로브 공유(같은 PID 두 스레드 → Windows delete-pending 순간 create 실패 = false 오판 → `data_dir()` 폴백 튐 — 08-21 CI part 테스트 사망 원인 · 시퀀스 접미로 유일화) ② 클립보드 왕복 테스트 환경 플레이크(set 직후 히스토리 서비스 잠금·타 프로세스 덮어씀 = skip) | P2 | 소 | — | ✅ (08-21 · 726 green) |
| NETMON-1 | **netmon 후속**(본체 ✅ 08-21 — [06 §7-1](06-network-stack.md)·[26 §3-5](26-run-and-manual-test.md)) — ~~ⓐ CLI 배선~~(**✅ 08-21** — `NEXA_NETMON` 환경변수 진단 스위치 · 전 모드 공통) ⓑ 상대별·주소별 분해 여부 🔴(봉투 원리와 상충 — **[38] BL-4 상대별 계수기가 사실상 이 축을 연다** · 필요 시 결정) ⓒ 임계값 설정 승격 여부(지금은 코드 상수 25pps/100pps/30conn·분) ~~ⓓ 기준선 수집~~(**✅ 08-21** — 2-인스턴스 10분 자동 시나리오 · journal 4차: 발견 3.7pps·메시지 85B·전송 오버헤드 0.25%·경고 0) | P3 | 소 | — | 🚧 (ⓑⓒ 잔여) |
| BL-1~5 | **수신 제한(차단) 목록 4축 구현**(M5-3 스팸 방어 1차 — **설계 ✅ 확정 08-21** = [38](38-receive-restriction-design.md) · 사용자 4문항: 그림자 차단·IP accept 즉시·자동 임시 차단 포함·기간부+정확 일치) — BL-1 모델+봉인 영속+blocked 이관 · BL-2 판정 4지점 배선 · BL-3 UI(우클릭 차단+설정 차단 목록) · BL-4 자동 임시 차단(상대별 계수기) · BL-5 회귀+3-신원 실기 | P1 | 대 | — | ⏸ (**개발은 요청 시** — 사용자 08-21) |
| X-3 | **그룹 파일 × 컨텐츠 서버 경로 + 보관함 온라인 통합**(사용자 확정 08-20 — 설계 = [32 §5-11](32-adr-0013-server-modes.md)) — **릴레이까지 = 현행 pairwise 팬아웃 유지** · 컨텐츠 모드 = **업로드 1회**(기존 파일 와이어 재사용 · 서버 = 자동승인 수신 단말 · ⚠본문 = P-9 콘텐츠 키 암호문 — S-3) → **방 종속 보관** → 멤버 다운로드 링크 통지 → **능동 pull**(무해화 동일 통과) · **보관함 통합** = 로컬+온라인 한 목록 · 온라인 클릭 = 다운로드 · 완료 = "다운로드됨" 상태(위치는 온라인 유지) · 저장하기 = 기존 실체화 경로 | P2 | 대 | X-1(컨텐츠 모드) · Q-32-7 | ⏸ (**설계만 — 개발은 요청 시**·서버 후) |
| X-2c | **CLI Managed 접속**([32 §13](32-adr-0013-server-modes.md)) — ★**P0~P2 ✅(08-22)**: P0 `--identity`(기본 data/identity.key · 서버 사용 시 영속 강제 · 내 지문 64hex 출력) · P1 `--chat-live --server`(핀 `server.pin` TOFU·불일치 = 중단·실패 = LAN 계속) + `--chat-connect-via <지문>`·`/connect <64hex>` · P2 = 사다리에 UDP 펀치 포함(connect_via) — 전부 `nbeep-relay` 공유(CLI 전용 네트워킹 0 · C-1 ✓) · 3프로세스 실물 스모크 ✓([26 §3-7](26-run-and-manual-test.md)). 잔여 = **P3 `--enroll`**(모드 ③ Registered 축 후) · `net.server.*` 설정 SSOT 연동(지금은 인자만 — X-2b와 함께) | P2 | 중 | — | ✅ P0~P2 (08-22 · P3 잔여) |
| X-2b | **Managed 모드 클라 배선**([32 §12-1](32-adr-0013-server-modes.md)) — ★**핵심 배선 ✅(08-22 · mac)**: ① 접속 정책 공용화(`nbeep_relay::attach` — 주소 해석·핀 TOFU·등록 = CLI와 한 벌 · `is_alive`) ② `server_tick`(설정 `net.server.*` SSOT 수렴 단일 경로 · 2s 페이스 · 백오프 5s→300s 상한 반복 · **핀 불일치 = 경고 모달+자동 재시도 정지** · hot-swap = gen 가드) ③ 인바운드 = accept_via 사다리 → 기존 `Inbound` 깔때기(TOFU·FR-S-25·경로 등급 그대로 — 새 정책 0) ④ 아웃바운드 3단 사다리(발견→수동→서버 랑데부)+**주소 모달 64자리 지문 연결** · ★로컬 3프로세스 실측 = 설정만으로 접속→핀→홀펀칭 직결→GUI 메시지 도달 · 잔여 = **타입 협상(auto)**(서버 타입 통지 와이어 필요 — MVP는 relay 단일이라 실질 auto=relay)·**Registered LAN 억제**(타입 축과 함께 · [32 §12-5])·GUI 발신 육안 실기 | P2 | 대 | X-UDP · X-1 | 🚧 (핵심 ✅ 08-22 · 타입 축 잔여) |
| X-2d | **아는 상대 서버 프레즌스 표시** — ★**검토로 흡수·축소(08-22 · [32 §12-7](32-adr-0013-server-modes.md))**: roster(X-2e)가 Announce 사용자를 덮으므로 이 항목은 **비공개(Announce 안 켠) 상대 한정 보완**으로만 남는다(경량 질의 kind — 후순위) | P2 | 소 | X-2e | ⏸ (X-2e 뒤 재평가) |
| **X-2e** | ★ **같은 서버 사용자 자동 목록 표시(roster)** — 검토 완료([32 §12-7](32-adr-0013-server-modes.md) · 08-22 사용자 질문): §12-5 확정("LAN ∪ 같은 서버 사용자")의 실물화 · **Announce 옵트인**(§2-3 비기록 원칙과 화해 — 켠 연결만 서버가 PeerId 보관·배포, 저장 0 불변) · 와이어 `Announce`/`PeerUp`/`PeerDown`(전방 호환) · 클라 = 서버 상대 행(지문 이름 · 클릭 = 사다리 · 프로필은 성립 후 P2P) · 서버 재배포 필요(beepd-v0.2.5) | P2 | 중 | ~~X-1 · Q-32-16~~ ✅ | ✅ (08-22 — **Q-32-16 확정 ⓐ 기본 on**(사용자 "지금 구현") · 와이어 3종+서버 listed 맵+RelayClient poll_roster+GUI 목록 병합·설정 토글·hot-swap · e2e(스냅숏·델타·상호성·이탈) · ★**실서버 프로브 = 상호 표시 검증 ✓**(beepd-v0.2.5 OCI 배포) · 잔여 = 양쪽 GUI 새 빌드 실기) |
| X-6 | **다중 기기 신원 구현**(DR-20 · [20](20-adr-0007-multi-device-identity.md)) — 기기 목록 서명·검증 · 페어링 SAS · 폐기/롤백 방지 · sender copy · 기기 관리 화면 · **주 기기 + 복구 시드** — FR-D-14·FR-M-8·FR-S-28~40 | P1 | 대 | D-20 · D-21 | ⏸ |
| X-7 | **E2E 백업·기기 이전**([17 §8](17-adr-0005-history-at-rest.md)) — 새 기기가 **과거 기록**을 보게 하는 별개 설계(X-6로 안 풀림) | P2 | 대 | X-6 | ⏸ |
| X-8 | S5 서브넷 스캔 폴백 · 서브넷 너머 발견(FR-D-10) — 사용자 동의 UX | P2 | 중 | M1-8 | ⏸ |
| X-9 | QR·초대 링크 노드 등록([19 §8](19-adr-0006-manual-endpoint.md)) | P2 | 중 | M5-3b | ⏸ |
| X-10 | 진짜 그룹 채팅(FR-G-5) · 화면 캡처·주석 전송(FR-M-7) · 접근성(FR-U-8, R-9) · 전송 재개(FR-X-7) · 자동 업데이트(FR-P-6) · mDNS 광고 · 모바일 | P2 | 대 | v1 | ⏸ |

- [ ] **X-IME-CJK**: 일/중 입력 실측 — 대화(IME on) 조합 전 첫 키 유출 여부 · 목록 일/중 매칭 UX(인라인 검색 필드) 검토 → [27 §7-1](27-typeahead-hangul-composition.md)

---

## §9. 🔐 보안 상시 관찰 (SEC) — **끝나지 않는 항목**

> **다른 섹션과 성격이 다르다.** 마일스톤이 끝나면 닫히는 일이 아니라 **제품이 사는 동안 계속 돌아야 하는 일**이다.
> 기준 = [29 와이어 보안 점검 기준](29-wire-security-audit.md) W-1~W-12 · 회귀 = [18 §2](18-build-and-test.md) · 리스크 SSOT = [05 §4](05-requirements.md).

### 9-1. 점검 트리거 — **달력이 아니라 사건에 건다**

주기적 점검은 잊힌다. **무엇을 건드리면 무엇을 다시 보는지**로 묶는다.

| ID | 트리거(이걸 하면) | 반드시 다시 하는 것 | 근거 |
|---|---|---|---|
| **SEC-T1** | **발견 와이어 포맷을 바꿨다** | **W-1~W-7 재실행** — 인코더 골든 + **수동 캡처로 ASCII 눈으로 확인**(M1-12) | 평문 구간이라 한 필드가 곧 유출 |
| **SEC-T2** | **브로드캐스트에 필드를 추가하려 한다** | **허용 목록 심사**(FR-S-49) — *"이게 평문으로 LAN 전체에 방송돼도 되는가"* 를 먼저 답한다. 기본 답은 **아니오** | R-19가 이 심사 부재로 생겼다 |
| **SEC-T3** | **새 기능·ADR을 설계한다** | [10 §0](10-decision-record.md) 질문 — ***"이 계층의 봉투는 무엇이고 내용은 무엇인가"***. 답이 안 나오면 그 계층은 과하게 보고 있다 | 관통 원리 |
| **SEC-T4** | **외부로 나가는 경로를 추가한다** | **FR-S-20 예외 심사** — 지금 예외는 수신자 릴레이 **1건뿐**. 두 번째를 만들려면 ADR이 필요하다 | 인터넷 경로는 되돌리기 어렵다 |
| **SEC-T5** | **외부 crate를 추가한다** | 라이선스 퍼미시브 확인(DR-12) + **서브트리 전수** + 크기 실측 → [10 §3 원장](10-decision-record.md) 등재 | 공급망 |
| **SEC-T6** | **신뢰·신원 판정 코드를 고쳤다** | TOFU 전이(`Unverified`→`Pinned`→`FingerprintVerified`) **육안 재확인** + 신뢰 불상속 테스트 | 배지가 유일한 사용자 신호 |
| **SEC-T7** | **파일 수신 경로를 고쳤다** | [18 §2](18-build-and-test.md) **안전 회귀 전항목**(격리물 실행 불가·복원 후 표식 유지·Zip Slip·압축폭탄·traversal·심링크) | fail-closed 붕괴는 조용하다 |
| **SEC-T8** | **릴리스 직전** | **W-1~W-12 전체** + 안전 회귀 전항목 + at-rest 문자열 스캔 + 아래 9-2 트래커 **전 행 재평가** | 출시 게이트 |
| **SEC-T9** | **마일스톤 종료 시** | 9-2 트래커에서 **상태가 3주 이상 안 바뀐 행**을 다시 판단(방치 탐지) | 리스크는 잊혀서 커진다 |

### 9-2. 열린 보안 항목 트래커 — **닫힐 때까지 남긴다**

| 리스크 | 무엇이 위험한가 | 담당 | 상태 |
|---|---|---|:--:|
| ~~R-19~~ | **표시 이름 기본값이 실명을 평문 방송**(실측 확인) | **M1-10** · Q-29-1 | ✅ **해소(08-11)** — 정제 호스트명+지문 폴백 · 실측 `beep-{지문}` · 실명은 옵트인+고지 |
| **R-18** | **고정 공개키 = 영구 재식별자**(실측 확인) · 완화가 TOFU와 충돌 | Q-29-2 | 🔴 **판단 대기**(감수+고지 유력) |
| ~~R-17~~ | **TOFU 핀이 재시작하면 사라진다** → 매번 "최초 접촉" = MITM 창 상시 개방 | **M2-5a** · D-18 §3 | ✅ **해소(08-11)** — 신원 키+핀 세그먼트 영속(암호화·fail-closed) · 재오픈 `Known(Pinned)` 테스트 고정 |
| **R-1** | 최초 접촉 MITM — **원천 해소 불가** | M3-6(SAS UX) | 🚧 **완화 · 영구 관찰** |
| **R-12** | 키 파일 복제 — U-P1·U-P2 구현됨, **실증 미완**(재현 환경 필요) | 실증 시나리오 | 🚧 **구현 · 미실증** |
| **R-16** | 종료 경로 — 핵심 완료, **Windows 콘솔 핸들러·zeroize** 잔여 | M1-8x · M2-5 | 🚧 |
| **R-5** | 이미지 파서 취약점 = RCE | M4-5(`imgdec` 분리) | ✅ (08-15 — 격리 프로세스+**강등 3-OS**: mac Seatbelt·Linux seccomp·Win 완화 정책 · fail-closed · CI 실측 `tests/lockdown.rs` · 08-21 행 정정) |
| **R-4** | 무인증 발신 → 스팸 폭탄 | M5-3 | ☐ |
| **R-13** | 수신자 릴레이 남용 — "내가 켠 규칙이 나를 때린다" | M3-10 | ☐ |
| **R-10 · R-11** | `UserId` 단일 실패점 · 팬아웃 중복 전송 | X-6 | ⏸ v2 |
| **W-8** | **세션 평문 부재를 실소켓으로 증명 못 했다**(단위 tap만) | **M4-3b** | ⏸ 실기 |
| **W-11** | `--chat-*` 도구가 **본문을 stdout에 찍는다** | Q-29-4 | ⚠️ 검증 도구 한정 |
| **W-12** | at-rest 평문 부재 미검증 | M2-5b | ☐ |

### 9-3. 상시 과제

| ID | 항목 | 상태 |
|---|---|:--:|
| **SEC-1** | **와이어 평문 자동 회귀를 CI에 상시**(FR-S-51) — 이게 있어야 SEC-T1이 사람 기억에 의존하지 않는다 | 🚧 (08-09 — **안전 송수신 4항목은 CI 상시화 완료**([safety_regression](../crates/nbeep-safe/tests/safety_regression.rs) · 이름 있는 단계 · 변이 검사로 유효성 확인). 잔여 = **와이어 평문 회귀**(W-1~W-7) 자동화) |
| **SEC-2** | **수동 캡처 스크립트를 저장소에** 두고 포맷 변경 커밋에서 재실행 | ☐ **M1-12** |
| **SEC-3** | 릴리스 노트에 **"막지 못하는 것"** 을 항상 싣는다([29 §5](29-wire-security-audit.md) — 존재·연결 상대·길이·타이밍) | ☐ M5-6 |
| **SEC-4** | 리스크 등록부([05 §4](05-requirements.md))와 이 트래커의 **불일치 점검** — 새 리스크가 여기 안 올라오면 관찰에서 빠진다 | 상시 |

---

## §10. 반복·멱등 로직 점검 (RL) — 08-17 전수 점검분

> **배경**: 프로필 반복 갱신 버그(08-16 3겹 수정)와 같은 부류 — 상태이상·반복 로직에 의한 불필요 실행 — 를 [13 §12-1](13-code-design-standards.md) 여섯 질문 잣대로 전수 점검한 결과(4축 병렬: 네트워크 반복 · UI 틱/redraw · 저장/영속 · 이벤트 재진입). **무한 루프(에코/핑퐁)는 0건** — 잔여는 전부 **증폭(N배)·상시 낭비·잠재 재발** 계열. 상세·깨끗 확인 목록 = [journal/2026-08-17](journal/2026-08-17.md).
> **뿌리 2가지**: ⓐ **"값이 같으면 안 한다" 멱등 가드가 디스크(S-3)에는 있는데 와이어·수신·일부 저장 경로에 없다** ⓑ **08-16 처방(쿨다운·필터)이 형제 경로 중 한 곳에만 적용됐다**.
> **착수 권장 순서** = RL-1(증폭기 차단 — 이거 하나로 하류 피해 대부분 소멸) → RL-3(트리거 견고화) → RL-6(최대 CPU 낭비) → RL-7(저장 O(n²)) → RL-11·12(조용한 실패·성능 회귀).

| ID | 항목 | 소속 | 우선 | 규모 | 상태 |
|---|---|---|---|---|---|
| **RL-1** | **PeerProfile 수신 멱등 가드** — 동일 내용 재수신에도 매번 사진 재봉인+`.img`/`.meta` 재기록+**imgdec 자식 프로세스**+trust.seg 전체 재암호화+목록 재조립([app.rs:8505~](../crates/nexa-beep/src/app.rs)). 기존 `peer_profiles`와 **값 비교 한 겹**이면 모든 반복 경로의 하류 비용이 차단된다(원조 증상의 비용 측 — 08-16 수정은 트리거 빈도만 낮췄다) | M3-17 | P1 | 소 | ✅ (08-18 — 수신 원문 지문 `peer_profile_fp` 조기 반환: 사진 재봉인·imgdec 자식·trust 재암호화·목록 재조립 전부 차단 · 수신 시각만 메모리 갱신) |
| **RL-2** | **프로필 push 발신 멱등** — ⓐ 무변경 Enter·이미 선택된 스와치 재클릭도 push([profile.rs](../crates/nbeep-ui/src/profile.rs) on_event — `harvest_texts`에는 있는 값 비교가 없는 비대칭) ⓑ `apply_settings`가 **키 단위 발화** = 적용 1회에 N벌(Full 키면 256KiB 사진 N회 — 배치 1회로 coalesce) ⓒ `do_restore_settings` 동일값 필터 부재(`do_reset_settings`의 `get(k) != d` 필터와 비대칭 — 복원 1회 = 무변경이어도 프로필 9벌 push) | M3-17/18 | P1 | 소 | ✅ (08-18 — ⓐ 무변경 Enter·같은 스와치 재클릭 push 제거(원값 비교 · 회귀 개정 2종) ⓑ apply_settings 프로필 push 배치 1회(Full 우선 병합) ⓒ 복원 동일값 생략(reset과 대칭)) |
| **RL-3** | **재연결·LinkChanged 잔여 처방** — 08-16 쿨다운이 형제 3경로 중 프로브 1줄에만 적용됐다: ⓐ LinkChanged가 백오프 **전면 0단 리셋** + `closed_peers`(단조 증가 집합) 전원 재연결 — 쿨다운 0([app.rs:8650~](../crates/nexa-beep/src/app.rs)) → 플랩 시 4단 상한 영구 미소진(§12-1 종료 조건 위반) ⓑ `Closed`가 무조건 stage=0 → 성립 즉시 끊김 사이클 = **5초 무한 재연결**(상한은 `ConnectFailed` 경로에만) ⓒ 생사 프로브 응답이 Full(사진 전량) — 목적 대비 과중(경량 응답 검토) | M1-2b | P1 | 중 | ☐ |
| **RL-4** | **linkwatch 자가유발 필터 플랫폼 대칭** — mac만 타입 필터([linkwatch.rs](../crates/nbeep-plat/src/linkwatch.rs)) · Linux netlink는 무엇이 읽히든 무조건 발화. mac에서 실증된 감지→kick→감지 1초 루프의 실패 양식이 두 플랫폼에 잔존(현 구독 그룹 구성상 지금은 안 돈다 — 잠재. 08-16 교훈 "디바운스만으론 못 막는다") | M1-2 | P2 | 소 | ✅ (08-18 — Linux netlink `nlmsg_type` 필터(NEWLINK/DELLINK/NEWADDR/DELADDR · mac PF_ROUTE와 동형 · 내용 불독) · Windows는 NotifyIpInterfaceChange가 이미 인터페이스 한정 · 크로스 check 0) |
| **RL-5** | **`PeerTable::sweep` 미배선** — 무응답 만료 판정이 정의만 있고 프로덕션 호출 0([peers.rs:124](../crates/nbeep-core/src/peers.rs)). 죽은 상대가 영원히 "발견됨" → `ConnectFailed`의 `reachable` 영구 참 → 무의미 재연결 사다리 완주 반복("무의미 트래픽 금지" 주석 의도 무력화) | M1 | P2 | 소 | ✅ (08-18 — 발견 폴에서 sweep 배선(60초 무관측 = 이탈) · **아는 상대(핀·대화·수동 주소)는 extra_peers 오프라인 행으로 강등 유지**("비발견 목록 유지" 08-13 불변) · 모르는 상대만 행 소멸) |
| **RL-6** | **XferProgress 목록 재조립 스로틀 우회** — 32KiB 청크마다 `apply_xfer_view`→`refresh_rows` 직행 = 발견 경로가 일부러 건 `list_refresh_ms` 1500ms 스로틀을 우회([app.rs:2470~](../crates/nexa-beep/src/app.rs)). 100MB 파일 = 3,200회 전 피어 순회+2중 정렬. **점검 중 최대 CPU 낭비** — 전송 중 목록 스크롤 튐(08-08 회귀)이 재현될 소지 | M4-2 | P1 | 소 | ✅ (08-18 — `apply_xfer_view_throttled`: 대화 위젯(배너·진행 바)은 즉시 · 목록 재조립은 dirty+`ui.list_refresh_ms` 주기(발견과 같은 깔때기)) |
| **RL-7** | **`record_history` 증분화·디바운스** — 메시지 1건마다 스레드 **전량** 재직렬화+재봉인+전량 쓰기 = 세션 O(n²) · 파일 전송 종결 순간 같은 파일 연속 3회([app.rs record_history](../crates/nexa-beep/src/app.rs)). 유일하게 SaveScheduler를 안 타고 `sync_all`도 없는 저장 경로(내구성 규약 불일치). M2-5b(08-17 구현)의 후속 견고화 | M2-5b | P1 | 중 | ☐ |
| **RL-8** | **저장 멱등 가드 일습** — ⓐ `save_pii_sidecar`가 매 저장 사이클 무조건 재봉인(cfg는 S-3로 걸러져도 `profile.sec`는 항상 씀 · salt/nonce 신선이라 dedup 불가 · 비원자 `fs::write`) ⓑ `record_name`([trustfile.rs:171](../crates/nbeep-store/src/trustfile.rs))·`set_mine`([groupfile.rs](../crates/nbeep-store/src/groupfile.rs))만 형제 API(`set_fav`·`note_seen` 등)와 달리 무조건 persist — core가 변경 여부(bool)를 안 돌려주는 구조 ⓒ Resized/Moved·`apply_settings`가 동일값도 dirty(부팅 시 무조작 저장 1회) ⓓ `last_saved` 부팅 미시드 = 매 실행 무의미 쓰기 1회 ⓔ `do_backup_settings`가 `flush_now` 미소비 = 저장 2회 | M3-15 | P2 | 중 | ☐ |
| **RL-9** | **캐럿 틱 정밀화** — 포커스 가드는 지켜지나 "그 창에 캐럿을 그리는 컨트롤이 있는가"는 안 봄 → 포커스 텍스트박스 없는 창(설정·목록 등)이 530ms마다 **완전히 동일한 프레임**을 전창 재도색. + `WindowEvent::Occluded` 전면 미처리 · 트레이 숨김 시 `os_focused` 명시 정리 없음(OS Focused(false) 암묵 의존) | M3 | P2 | 소 | ☐ |
| **RL-10** | **유휴 바닥 만들기** — `ControlFlow` 무조건 200ms(5Hz) 재설정 = 트레이 상주·유휴에도 0으로 내려갈 바닥이 없다. + shutdown 브릿지 50ms(20Hz — 최다 상시 웨이크업)·udp 광고 루프 100ms 쪼갬 = 필요 주기의 8~20배 웨이크업. **M5-5(24h 상주 누수·CPU 예산) 선행 정지 작업** | M5-5 | P2 | 중 | ☐ |
| **RL-11** | **`wire_pending` 래치 watchdog** — 해제가 워커 완료 이벤트 단일 경로. 유실(워커 패닉·프록시 사망 — 둘 다 무음) 시 이후 모든 `push_profile`이 조용히 return = **프로필 전파 영구 침묵**(M3-21 증상의 조용한 재발 자리 · §12-1 "실패 경로 복귀" 위반). 타임아웃 또는 실패 복귀 이벤트 필요 | M3-21 | P1 | 소 | ✅ (08-18 — 15초 watchdog: 래치 해제 + 사진 없이 Full 전파(텍스트·키는 닿는다 · 실패 가지와 같은 의미론) + 상태바 경고 — 전파 영구 침묵 자리 폐쇄) |
| **RL-12** | **`TcpLink::recv` 타임아웃 덮어쓰기** — 본문 수신 후 무조건 200ms 재설정([tcp.rs:109~](../crates/nbeep-net/src/tcp.rs)) → 액터가 건 1ms(08-16 페이싱 최적화)가 첫 프레임 수신 즉시 무효화, 액터는 값 동일로 오인해 재설정 안 함 = **영구 200ms 고착**(종전보다 나쁨 · 성능 회귀) | M4-11 | P1 | 소 | ✅ (08-18 — `recv_timeout` 필드 기억 → 본문 복원을 호출자 값으로(고정 200ms 폐지) · set_poll_mode도 동기 · **타이밍 회귀 테스트**(20ms 설정이 150ms 안에 TimedOut)) |
| **RL-13** | **자원 고갈 3종** — ⓐ 인바운드 accept = 연결 1건당 블로킹 스레드·상한/핸드셰이크 타임아웃 0([app.rs spawn_inbound_accept](../crates/nexa-beep/src/app.rs) — 스캐너 반복 시 단조 증가 · CLI는 08-13 수정, GUI 잔여) ⓑ `serve_manual` accept `Err` 무휴식 스핀([manual.rs:19~](../crates/nexa-beep/src/cli/manual.rs) — fd 고갈 시 CPU 100%) ⓒ `--chat-live` 재대기 사이클마다 `shutdown::install`이 20Hz 폴링 스레드 신규 스폰·영구 잔존(1회성 가드 없음 — N사이클 = 좀비 2N개) | M2/M5-3 | P1 | 중 | ☐ |
| **RL-14** | **고착·누락 2종** — ⓐ `awaiting_ack`가 `Closed`에서 미정리 → ack 대기 중 상대 이탈 시 종료 가드가 영구 "확인 대기 N건"(M4-9 잔여) ⓑ `about_to_wait`가 `gchats` tick 미호출 → 그룹 대화 창 스크롤바 자동 숨김 미발화(과다가 아닌 **미실행**) | M4-9/M5-1g | P2 | 소 | ✅ (08-18 — ⓐ Closed에서 `awaiting_ack` 정리(영구 "확인 대기 N건" 방지) ⓑ gchats 페이드 틱 배선(그룹 방·단일 창 그룹 모두)) |
| **RL-15** | **UI 반복 소소 묶음** — ⓐ 갤러리 데모 TimeoutButton 만료 시 자기 재시작 = 창 열린 동안 1Hz 영구 재도색·종료/가시성 가드 0([gallery.rs:486~](../crates/nbeep-ui/src/gallery.rs)) ~~ⓑ QThumb 재스캔~~(**✅ 08-18 — 워커 스캔+원시 캐시로 해소**: 썸네일 도착 재렌더 = 캐시 · 격리함 로드 자체도 워커) ⓒ 기간 승인 1초 갱신이 `inv` 무관 무조건 redraw ⓓ 중복 redraw 요청 3건(Focused(false)×2 · XferProgress×3) ⓔ 프로필 적용 배치에서 `refresh_toolbar_avatar`→트레이 아이콘 재생성 최대 3회 | M3 | P2 | 소 | ☐ |
| **RL-16** | **발견 광고 중복 송신 + 무변화 재조립** — ⓐ `send_all`이 같은 링크에 동일 패킷 3~4부(기본경로 멀티캐스트+글로벌 브로드캐스트+IF별 멀티캐스트+IF별 브로드캐스트 · [udp.rs:251~](../crates/nbeep-net/src/udp.rs)) — HELLO 1발당 상대마다 유니캐스트 응답 3~4개 증폭 ⓑ `poll_discovery`가 `observe()` 반환값(None=무변화)을 버리고 무조건 dirty → 상대가 광고하는 한 무변화에도 1.5초마다 목록 재조립·재도색 영구 | M1 | P2 | 소 | ✅/🚧 (08-18 — **ⓑ 완료**: observe 무변화(None) = dirty 생략(광고 중 1.5초 영구 재조립 제거) + 60초 심장박동(상대 시각 라벨 신선) · **ⓐ 중복 송신은 잔여** — 발견 도달 사다리와 얽혀 실기 검증 필요) |

> **깨끗 확인**(재점검 불요 — 근거는 journal): `ConnectLatch`(양 경로 finish) · 재연결 펌프 `due=MAX` 래치 · `SaveScheduler` 코어(6질문 전부 통과) · `recv_any` · 그룹 roster 엄격 부등호 멱등 · HELLO 비유도 응답 · `wire_gen` 세대 판정 · 스크롤바 페이드 · 트레이 3-백엔드(폴링 0) · 설정 위젯 역반영(루프 구조적 불가) · 그룹 파일 팬아웃 대기(상한+양방향 종결) · 격리함 썸네일 요청 래치.

---
