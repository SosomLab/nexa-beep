# 10 · 통합 결정 기록 (Decision Record)

> 이 프로젝트의 **개발 방향·기술 요소 확정 기록**. 변경 시 **과거 항목을 지우지 말고** 새 항목/개정으로 정정하며 "정정"임을 명기한다([16 §2-5](16-doc-git-conventions.md#2-문서-작성--필수-규칙-8)).
> 대안 비교가 필요한 큰 결정은 별도 **ADR 문서**로 남기고 §2 색인에 건다.

## 1. 확정된 핵심 결정

| # | 영역 | 결정 | 근거 | 확정일 |
| --- | --- | --- | --- | --- |
| **DR-1** | 정체성 | **제로 컨피그 LAN 메신저** — 서버 주소 입력·계정 생성·상대 등록 **없이** 실행만 하면 같은 로컬 네트워크의 사용자가 자동으로 목록에 뜨고 즉시 대화. "실행 = 참여" | 사용자 정의(08-08). 이를 훼손하는 기능은 채택하지 않는다 | 2026-08-08 |
| **DR-2** | 문서·git 규약 | `nexa-dir2` [16 문서·커밋/푸시 규약](16-doc-git-conventions.md) **전면 차용** — 4층 문서 체계·작성 규칙 8·커밋/브랜치/푸시 규칙 | 검증된 이식용 표준 | 2026-08-08 |
| **DR-3** | 대상 플랫폼 | **크로스플랫폼 4종** — Windows(x64·**ARM64**) · macOS · Linux. 플랫폼별 기능 차등 없음 | 사용자 확정(08-08). 경쟁 제품 중 ARM64까지 가는 건 Squiggle 하나뿐 = 빈 자리([03 §6-1](03-competitive-landscape.md)) | 2026-08-08 |
| **DR-4** | 배포 | **설치본 + 포터블 2채널 병행** — 포터블은 압축 해제 후 즉시 실행, 영속물은 실행 파일 옆(쓰기 불가 위치는 사용자 데이터 폴더 폴백) | 사용자 확정(08-08). BeeBEEP·LocalSend가 포터블로 실사용 검증 | 2026-08-08 |
| **DR-5** | 예산 게이트(NFR) | **최소 설치 크기 · 최소 메모리 · 장기 실행 누수 0** — 마일스톤마다 실측하고 초과 시 main 병합 금지. **수치 확정(08-08 · [05 §2-1](05-requirements.md) NFR-B-1~12)**: 유휴 RSS ≤30MB · 활성 ≤80MB · 산출물 ≤10MB/타깃 · **런타임 의존 0** · 24h 누수 RSS ≤2MB·핸들 증가 0 · 발견 ≤3s(P95). ⚠️ B-1/B-3은 **잠정** — ADR-0001 확정 후 M0 게이트에서 재확인(R-8) | 사용자 확정(08-08). 경쟁 제품이 전부 런타임(.NET/Flutter/Qt/JVM) 의존이라 **이 축이 비어 있다**([03 §6-4](03-competitive-landscape.md)) | 2026-08-08 |
| **DR-6** | UI·렌더링 | **모든 렌더링을 직접 구현한 커스텀 컨트롤**로 UI를 짓는다 — 플랫폼 간 **동일 UI** 보장. OS 위젯·UI 프레임워크(Qt/Avalonia/Flutter/WebView) **금지** | 사용자 확정(08-08). 프레임워크 UI는 플랫폼마다 테마를 타서 동일성이 깨진다(BeeBEEP 사례) | 2026-08-08 |
| **DR-7** | 보안 | **단말 간 종단 암호화(E2E) 필수** — 전송 경로(로컬 직접/릴레이 경유)와 **무관하게** 종단 간. 릴레이 서버는 평문에 접근할 수 없다 | 사용자 확정(08-08 — "회사 내부에서 쓰려면 P2P 사이 보안이 지켜져야 한다"). 키 **인증** 방식은 ADR-0002 — 잠정 권고 TOFU 기본 + 지문 확인 선택([03 §6-2](03-competitive-landscape.md)) | 2026-08-08 |
| **DR-8** | 아키텍처 — 전송 2모드 | **서버리스가 기본**이되, 설계는 처음부터 **전송 계층을 추상화**해 2모드를 전제한다:<br>① **로컬 직접 모드**(기본) — LAN 자동 발견 + P2P 직접 연결. 서버 없음<br>② **릴레이 모드**(선택) — 사용자가 특정 서버를 추가하면 그 서버를 경유해 IP/로컬에 매이지 않고 일반 메신저처럼 통신<br>**v1 출시 범위는 ①만.** 단 ②를 나중에 얹을 수 있도록 인터페이스·주소 체계·세션 모델을 1일차부터 2모드 전제로 설계한다 | 사용자 확정(08-08). 나중에 전송을 끼워 넣으려면 신원·주소·세션 모델을 전부 뒤집어야 하므로 **설계 시점에만 싸게 살 수 있는 결정** | 2026-08-08 |
| **DR-9** | 저장소 분리 | 릴레이 서버는 **별도 프로젝트 `nexa-beepd`** 로 개발한다(본 저장소는 클라이언트). 착수 시점은 v1 이후 | 사용자 확정(08-08) | 2026-08-08 |
| **DR-10** | 기술 스택 | **Rust** 정적 링크 **단일 바이너리** + **자체 CPU 래스터라이저 · 자체 텍스트 스택**(GPU·OS 위젯·UI 툴킷 미사용) + **얇은 플랫폼 계층(P2 — 라이브러리로 시작, 실측 후 재평가)**. **Linux는 Wayland·X11 둘 다**. **폰트는 시스템 폰트 + 통일 폴백 체인**(임베드 안 함) | 사용자 확정(08-08) → **[ADR-0001](07-adr-0001-stack.md) Accepted**. ⚠️ 예산 달성 여부는 미검증 — **SP-1 실측**(빈 창 ≤3MB·RSS ≤15MB·한글 IME·60fps·의존성 라이선스 전수)이 M0 최우선 | 2026-08-08 |
| **DR-11** | 디스커버리·전송 프로토콜 | **자체 컴팩트 UDP**(S1~S5 단일 와이어 포맷, ≤512B, mDNS 미채택) + **TCP 세션**(다중화·백프레셔) + **Noise_XX**(X25519 / ChaCha20-Poly1305 / BLAKE2s) + **TOFU**(지문 변경 시 기본 차단) + **SAS 지문 대조 선택**. **발견 패킷은 인증하지 않고 "미검증 힌트"로 취급** — 신원은 핸드셰이크로만 확정 | 사용자 확정(08-08) → **[ADR-0002](08-adr-0002-discovery-transport.md) Accepted**. ⚠️ 타이밍 파라미터 6종은 **D-8 실측 후** 정정 항목으로 추가 | 2026-08-08 |
| **DR-12** | 라이선스 | **`nexa-dir2`와 동일 구성** — **PolyForm Noncommercial 1.0.0**([LICENSE.md](../LICENSE.md) · 한글 [LICENSE.ko.md](../LICENSE.ko.md)), 개인·비상업 무료 / 상업 사용은 별도 유료 라이선스(kiros33@sosomlab.com). **의존성은 퍼미시브 온리 — GPL/LGPL 계열 금지** | 사용자 확정(08-08). ⚠️ [03](03-competitive-landscape.md)의 선행 제품 다수가 GPL(LAN Messenger·Technitium Mesh)·LGPL(KouChat)이다 — **코드 차용 금지**, 참고 가능한 것은 MIT(Squiggle)·Apache-2.0(LocalSend)·공개 표준(XEP-0174)뿐 | 2026-08-08 |
| **DR-13** | 메시지 페이로드·수신 안전 | 페이로드 **3종(Text·Image·File)**. 수신 파일은 **4단계 게이트**(협상 → **격리 수신** → 검사 → **승인·실체화**)를 반드시 거친다. 격리물은 `.beepq` 컨테이너(**헤더 봉인** · 콘텐츠 주소 파일명 · 실행권한 없음 · **OS 격리 표식**)로만 존재하며, **앱은 수신 파일을 실행하지 않는다**. 복원 후에도 MotW/`com.apple.quarantine`을 **유지**한다 | 사용자 확정(08-08). 설계 상세 [04 안전 송수신](04-safe-transfer.md). 업계 표준(MotW·Gatekeeper 격리·AMSI·CDR·디팽잉)에 맞춰 구성 — **헤더 변조 단독으로는 불충분**(Windows는 확장자로 실행을 결정하고 `.bat`/`.ps1`/`.lnk`는 매직바이트가 없다)이라 다층으로 건다 | 2026-08-08 |
| **DR-14** | 네트워크 계층 | **L1~L4를 직접 다룬다** — L1 링크 상태 구독(폴링 금지) · L2 이웃 테이블 조회 · L3 인터페이스별 멀티캐스트/브로드캐스트·링크로컬·IPv6 · L4 UDP(발견)+TCP(세션·파일)·소켓 옵션·백프레셔. 발견은 **6단계 폴백 사다리**(S1~S6).<br>**단 능력 등급 분리** — **T0(무권한)이 제품의 완전한 형태**이고, L2 원시 프레임(T2)은 권한·드라이버가 필요해 **선택 기능으로만** 둔다 | 사용자 확정(08-08). 설계 [06 네트워크 스택](06-network-stack.md). ⚠️ L2 원시 소켓은 Linux `CAP_NET_RAW`·macOS `/dev/bpf`·**Windows는 Npcap류 드라이버 설치**가 필요 → **DR-1(실행=참여)·DR-4(포터블)와 정면 충돌**하므로 필수 경로에 둘 수 없다 | 2026-08-08 |

| **DR-15** | 코드 설계 표준 | **모듈화·추상화·횡단 구조를 규약으로 고정**한다 — ① 포트&어댑터(도메인은 포트만 안다·`Clock`/`Rng`도 포트) ② **모든 유의미한 행위를 `ActionKind` 단일 통로**로 표현하고 **Trace→Policy→Validate→Meter→Handler 파이프라인** 통과 ③ **인터셉터**(`before`/`after` 기본 메서드)로 확장 ④ **사용 계측 레저**(append-only·해시 체인·5W 모델) — **로컬 전용·자동 전송 없음·내용 미포함·상대는 솔티드 해시·끌 수 있음** ⑤ 정적 디스패치 기본, 계측은 **feature로 분리** | 사용자 확정(08-08). 설계 [13 코드 설계 표준](13-code-design-standards.md). ⚠️ **충돌 2건을 명시적으로 절충** — 추상화↔DR-5(크기), 계측↔NFR-O-5(외부 전송 금지)·DR-1. 계측의 정당성은 **DR-12 상업 라이선스** 근거 제시 | 2026-08-08 |

### DR-8 보충 — 2모드가 설계에 강제하는 것

| 항목 | 로컬 직접 모드 | 릴레이 모드 | ⇒ 공통 설계 요구 |
|---|---|---|---|
| 신원 | 기기 키쌍(자체 생성) | 동일 | **키 지문이 유일한 신원** — 계정 개념을 도입하지 않는다 |
| 주소 | LAN IP·포트(수시 변동) | 서버상의 논리 주소 | 상위 계층은 IP를 모른다. **신원 → 전송 해석은 전송 계층 안에서만** |
| 발견 | 멀티캐스트/브로드캐스트 | 서버 디렉터리 | 발견 결과는 동일한 `Peer` 표현으로 수렴 |
| 암호화 | 단말 간 E2E | 단말 간 E2E(**서버는 봉투만 본다**) | 암호화는 전송 계층 **위**에 둔다 — 전송이 바뀌어도 그대로 |
| 오프라인 | 상대 부재 시 로컬 큐 | 서버 보관 | 큐는 인터페이스로, 구현은 모드별 |

## 2. ADR 색인

| ADR | 주제 | 상태 |
| --- | --- | --- |
| **[ADR-0001](07-adr-0001-stack.md)** | 기술 스택(언어·렌더링 백엔드·플랫폼 계층) | ✅ **Accepted**(08-08) — Rust · 자체 CPU 래스터라이저 · 플랫폼 계층 P2 · Wayland+X11 · 시스템 폰트 |
| **[ADR-0002](08-adr-0002-discovery-transport.md)** | 디스커버리·전송 프로토콜 + 키 교환/인증 | ✅ **Accepted**(08-08) — 자체 컴팩트 UDP · TCP 세션 · **Noise_XX** + TOFU. 타이밍은 D-8 실측 후 |
| **[ADR-0003](09-adr-0003-transport-abstraction.md)** | 전송 계층 추상화 — 로컬 직접/릴레이 2모드 경계 | 📐 **Proposed**(08-08) |
| **[ADR-0004](11-adr-0004-quarantine.md)** | 수신 파일 무해화 — `.beepq` 컨테이너·위험 등급·승인 UX | 📐 **Proposed**(08-08) — 컨테이너 레이아웃 · 등급 4단계 확장자표 · 상태 기계(fail-closed) · 플랫폼 표식 API |

> ADR 파일명은 `NN-adr-000N-주제.md`. 번호(NN)는 부여 후 **불변**.

## 3. 의존성 원장

> 외부 의존성은 **추가할 때 건별로** 여기에 적는다(무엇을·왜·대체 불가 사유). DR-5(최소 크기·메모리)의 직접 통제 수단이다.

| 의존성 | 용도 | 사유 | 추가일 |
| --- | --- | --- | --- |
| — | — | _(아직 없음)_ | — |

## 4. 차용 자산 원장

| 자산 | 출처 | 용도 | 상태 |
| --- | --- | --- | --- |
| 문서·git 규약 | `nexa-dir2` `docs/16-doc-git-conventions.md` | DR-2 | ✅ 차용 완료(08-08) |
| ~~**Nexa Controls**(커스텀 컨트롤 14종)~~ | `nexa-dir2` `docs/ctl/` · `crates/nexa-app` | ~~DR-6 자체 렌더링 UI의 설계·계약 원본~~ | ❌ **폐기(정정 08-08)** — 아래 정정 행 참조 |
| 크로스플랫폼 타당성 검토 | `nexa-dir2` `docs/23-cross-platform-feasibility.md` | DR-3 판단 근거 | ✅ 검토 완료(08-08) → [12](12-asset-reuse.md) |
| **정정(08-08) — `ctl` 코드 이식 불가** | `nexa-dir2` `crates/nexa-app/src/ctl` (7,139 LOC) | — | ❌ **재사용 불가.** `ctl`은 **컨트롤마다 Win32 창(HWND)** 인 모델이라 [ADR-0001](07-adr-0001-stack.md) B안("창 1개 + 픽셀 버퍼")과 근본적으로 다르다. 히트테스트·포커스·Z순서·팝업을 OS에 위임하는 구조라 그대로는 성립하지 않는다. **가져올 코드 사실상 0** |
| **정정(08-08) — 실제 자산은 `nexa-gui`** | `nexa-dir2` `crates/nexa-gui/src` (8,488 LOC · `windows::` 참조 0 · 맥 147 테스트 green) | DR-6 자체 렌더링의 **경계 설계 원본** | ✅ **인프라 1,187 LOC 이식 확정** — `DrawCtx`(백엔드 교체 전제 추상화)·`Widget`·`event`·`geom`·`theme`·`edit`(IME 연결 지점)·`typeahead`. ⚠️ `DrawCtx`의 `draw_image(hint:&str)`는 **FR-S-12와 충돌하므로 재설계 필수**. 상세 [12](12-asset-reuse.md) |
| **`ctl` 계약·시각 규약**(코드 아님) | `nexa-dir2` `docs/ctl/overview.md` | 시각 일관성 | ✅ 계승 — `Style` 팔레트 모델(색 하드코딩 금지)·공통 자동 높이·`behind` 배후색 블렌드·라벨 실측 정렬·자원 소유 규율 |
