# CLAUDE.md — Nexa Beep 프로젝트 컨텍스트 (이식용 메모리)

> 이 파일은 **다른 PC에서 clone 시 즉시 컨텍스트를 복원**하기 위한 휴대용 프로젝트 메모리다.
> **먼저 읽기:** [docs/STATUS.md](docs/STATUS.md)(현황) → [docs/10-decision-record.md](docs/10-decision-record.md)(결정) → [docs/03](docs/03-competitive-landscape.md)(경쟁 지형).

## 1. 이 프로젝트는

**Nexa Beep** = **제로 컨피그 로컬 네트워크 메신저**.
서버 주소 입력·계정 생성·상대 등록이 **전부 없다**. 실행하면 같은 로컬 네트워크에서 실행 중인
사용자가 **자동으로 목록에 뜨고**, 고르면 **즉시 대화**한다. 발견된 사용자는 **그룹으로 묶어** 그룹 단위 전송도 한다.
메시지는 **Text·Image·File** 3종이며, 수신 파일은 **무해화 게이트**를 거쳐야 실체화된다.

- 조직: **SosomLab** · 개발자: Sangyong Bae · kiros33@gmail.com
- 저장소: `git@github.com:SosomLab/nexa-beep.git` · 릴레이 서버 **`nexa-beepd`** = ★**같은 저장소의 별도 크레이트·별도 배포**(`beepd-v*` 태그 — DR-9 개정 08-21 · MVP 구현 ✅)
- 현 단계: **설계 문서 45종(00~44 — 41 beepd 운영 가이드 · 42 릴레이 랑데부 설명서 · 43 모바일 메신저 조사 · 44 nexa-clip 연계) · ADR 14종(✅13 / 📐1 = 0013 서버 모드) · DR-1~28 · FR 119건+ · 리스크 R-1~21.** ★ **공개 = v0.2.13**(08-29 — 탑재 = 자동 실행 **인스턴스별 슬롯**(exe 경로 해시 · 3-OS · 옛 이름 1회 이관) · v0.2.12 = 트레이 열기 진짜 포커스(GNOME 셸 토큰 → xdg_activation) · v0.2.11 = Linux 실기 5건(시스템 테마 추종 기본 `ui.theme=system`·창 app_id·트레이 Wayland 대응·종료 join 상한) · v0.2.10 = Linux .desktop 런처 데모 결함 정정 · v0.2.9 = M5-1h 그룹창 송수신 통합+자동 실행 외부 삭제 존중(M3-25 후속) · 채널 = brew·Releases — **winget/choco 검수 잔존 = 제외 규칙**(18 §5 · 첫 검수 v0.2.2 통과 후 재개) · 배포 3채널 골격은 v0.2.2에 완성) · **794 green**(+ignored 3 = 실 멀티캐스트 2·포털 신호 1). 코드는 **M0 기반 🚧(크기 축 ✅ · ★**RSS 축 ✅ = R-8 해소**(08-17 — Windows 17.1MB · mac **phys_footprint 18~20MB** · ps RSS 120~270MB는 reclaimable 허수라 판정 부적합 — 3-OS 전부 ≤30MB · ★Linux 17.5MB 08-29 실측)) → M1 발견 🚧(S1~S3 실물·와이어 확정 · ★R-21 ✅=M1-4b · ★R-19 ✅=M1-10 표시 이름 정제 · ★**M1-8 S4 유니캐스트 = 3-OS 대칭 ✅**(08-20 WNET-1 — Win GetIpNetTable2 직접 링크·지향 브로드캐스트 MAC ff 필터·Get-NetNeighbor 17개 정확 일치 · 잔여 = E-3 차단 망 왕복 실기) · 잔여 M1-13·실기(ⓓ 매트릭스·E-3) — M1-2·M1-11 ✅ 확인·★M1-9 Win netif 실물 ✅(08-21 GetAdaptersAddresses)) → M2 대화 🚧(실물 Noise·M2-8 연결 워커 ✅ · ★**M2-5a 신원·핀 영속 ✅ = R-17 해소**(D-18 §3 확정 · 백업/복원 핫 로딩) · 잔여 M2-4b — ★M2-5b ✅ 소진(08-21 — 동기화 폴더 경고+**크립토 셰레딩** `keytable.rs` 대화별 데이터 키·삭제=키 폐기)) → M3 셸·UI 🚧(말풍선 대화 화면 · 커스텀 컨트롤 **19종**(+Switch·Carousel·IconDropdown·크기 배율) · **★M3-15 설정 영속 ✅**(D-25 · `crates/nexa-conf`) · ★**M3-1e ② IME 상태기계 ✅**(`ime_gate.rs` 순수 상태기계 + **트레이스 재생 회귀** — 08-14 · 34번이 지목한 근본 처방) · **★M3-17 프로필 ✅ 일습** — 화면·공개 토글·백업/복원·**교환 와이어**(세션 경유·프리페치)·**목록 아바타 2줄 행·프로필 보기 카드**·아바타 실사진 · ★**Windows 한/영 직접 토글 ✅**(27 §8 — IME 없이 앱이 모드 소유 · 조합 Enter=확정+전송 동시 확정) · ★**M3-21 프로필 변경 전파 ✅**(push 깔때기+카드 pull — 공유 토글이 연결된 상대에게 안 보이던 구멍) · ★**프로필 캐시 부팅 복원 ✅** · ★**H-25·H-26·H-27 해소**(실기 확인 — IME 3대 이슈 완주) · ★**상태 표시 시각 규약 📐**([14 §12·§13](docs/14-control-ux-architecture.md) — 연결 배지 파냄 M3-19 · 큰 자리 아이콘 M3-20 · 인증 6상태 M3-14 설계 완료) · ★**M3-6 ✅**(08-15 카드 대조 → 08-18 **키 지문 대조**로 전환 — `/fingerprint`→다른 채널→`/verify`) · ★**M3-2 트레이 = 3-OS ✅**(Linux 실기 08-29 — Wayland 한계 [40 §6] · 셸 토큰 활성화) · ★**`ui.theme` 시스템 추종 기본**(08-29 · `nbeep_plat::theme`)) → M4 전송·무해화 🚧(파일 송수신 ✅ · ★M4-9 종단 ack ✅ · **Win MotW ✅**(mac·Win 2/3-OS) · **★M4-5 imgdec ✅ 코어+포장 동봉+수신 미리보기+★디코드 워커화**(UI 무정지 — 08-13) · ★**대량 전송 Backpressure 해소 ✅**(08-13 — 스트림별 폴링이 2MiB에서 세션을 끊던 것 · `MuxSession::recv_any`) · ★**ⓒ 권한 강등 ✅ = R-5 해소**(08-15 — mac Seatbelt·Linux seccomp·Win 완화 정책 · fail-closed · CI `tests/lockdown.rs` 실측 — ⚠08-21 확인: 요약들이 "잔여"로 낡아 있었음) · ★**M4-10 전송 재개 ✅**(08-18 D-31 — `.part` 봉인 보존·프리픽스 해시 대조·재-Offer 2택 · 실기 완주) · ★**M4-4 아카이브 ✅ 소진**(08-21 zip64·격리 내용 목록)) → M5 배포(★**v0.2.2 공개**(08-21 — **배포 3채널 완성**: 5타깃×2채널·자산 14종 · brew 자동 추종 ✓ · **winget PR·choco push 첫 게시**(검수·모더레이션 대기) · 이후 릴리스 = 태그 하나로 전 채널 · ⚠교훈 = **로컬 게이트에 fmt·반대편 타깃 check 포함**(1차 태그가 unused_mut·fmt로 CI 사망) · ~~M5-4b~~ ✅(08-21 스위치 ON) · ★**그룹 M5-1 🚧**(ADR-0012 ✅ — **한 방 대화 G1~G3 개통**·Suggest 초대+방별 정책·**그룹 파일 팬아웃**(미왕래 = 수동 승인 강등 확정)·3-신원 실기 · ★**M5-1h 그룹창 송수신 통합 ✅**(08-24 — 발신 카운터·수신 그룹 스레드+합산 배너·Control 18 표식 **종결 소비**·자동 취소 일습·목록 그룹 행 막대 · 실기 확인·main 병합)))**(**777 green** · 크레이트 14 · 산출물 약 1.7MB/10MB · ★2-PC 실증: Win↔Mac IP 수동 등록·메시지·파일 왕복 · ★**무인자 = 창+실물 기본**·`--help`(+★**GUI 실행 가능 세션 판정** — 못 여는 자리면 안내 후 exit 3 · 3-OS) · dedup 세션 리셋+스레드 대피(재대화 연속성) · ★**IME 경합 실측 확정**(`NEXA_IME_TRACE` — 첫 키 유출·낱개 Commit·이중 배달 → 유출 조합기)+편집 전수 3차).
  **ADR**: 0001·0002·**0003**·**0004**·**0005**·**0006**·0007·0008·0009·**0010**·**0011**·**0012**·**0014** ✅ Accepted / **0013 📐 Proposed**(서버 모드 — 방향성만·구현 v1 이후).
  **다음 관문** — ~~D-23~~·~~D-24~~·~~D-18 §4~~·~~D-19~~ **전부 ✅ 확정**(D-23 08-17 ADR-0010 / D-24·D-18 §4~·D-19 08-21 — 커트라인 승인 + 스팸 방어 M5-3 = v1.0 필수). ★ ~~X-UDP-a~~ **✅ 확정·개통**(08-21~22 — ⓐ 자작 ARQ 채택 · Q-32-15/Q-32-13 확정(DR-9 개정) · `UdpLink`+`nexa-beepd` MVP+원격 사다리+CLI Managed P0~P2 · ★**X-2b GUI Managed 핵심 ✅**(08-22 mac — 설정 SSOT `server_tick`·attach 공용화·인바운드 깔때기 합류·아웃바운드 3단 사다리·주소 모달 지문 연결 · 로컬 3프로세스 실측 관통) · **잔여 = 실 NAT 홀펀칭 실기**(tools/beepd-cloud — musl 정적 바이너리 업로드 · gcloud 인증 자리에서 사용자 실행)·서버 타입 협상 축). ~~D-22~~·~~D-25~~·~~Q-29-1~~·~~D-18 §3~~은 **✅ 확정·구현 완료**(D-22 08-08 · D-25 08-11 `nexa-conf`+M3-15 · Q-29-1 08-11 M1-10 R-19 해소 · D-18 §3 08-11 M2-5a — 신원·핀 영속+백업/복원 핫 로딩).

## 2. 확정 결정 ([docs/10](docs/10-decision-record.md) **DR-1~28**, 변경 시 새 ADR/journal)

| # | 결정 |
| --- | --- |
| DR-1 | **제로 컨피그가 정체성** — "실행 = 참여". 사전 등록·계정·서버 설정을 요구하는 설계는 채택하지 않는다 |
| DR-2 | 문서·git 규약은 `nexa-dir2` [docs/16](docs/16-doc-git-conventions.md) **전면 차용** |
| DR-3 | **크로스플랫폼 4타깃** — Windows(x64·**ARM64**)·macOS·Linux. 기능 차등 없음 |
| DR-4 | **설치본 + 포터블 2채널** — 포터블은 압축 해제 즉시 실행, 영속물은 실행 파일 옆(폴백 있음). ⚠️ **폴백 종점을 임시 폴더로 두지 않는다**(08-19 실기 — macOS `tmp_cleaner`가 `/tmp` 아래 `identity.key`를 지워 신원이 조용히 바뀌고, 이전 키에 sealed된 격리물·핀을 영구히 못 연다). 3신원 실기 폴더 = **`$HOME/.nexa-beep-multi`**. ⚠️ **"실행 파일 옆 쓰기 가능"만으로 포터블 판정 금지**(08-24 실기 — mac `.app` 번들·brew keg는 사용자 소유라 쓰기가 되지만 `brew upgrade`가 폴더째 교체해 신원·설정 소실 → `nexa_conf::is_replaced_on_upgrade`로 제외 · Win NSIS는 `data/` 보존 설계) |
| DR-5 | **예산 게이트** — 유휴 RSS **≤30MB** · 산출물 **≤10MB/타깃** · **런타임 의존 0** · 24h 누수 RSS ≤2MB·핸들 증가 0. 초과 시 main 병합 금지 ([05 NFR-B](docs/05-requirements.md)) |
| DR-6 | **모든 렌더링 자체 구현 커스텀 컨트롤** — 플랫폼 간 동일 UI. OS 위젯·UI 프레임워크(Qt/Avalonia/Flutter/WebView) **금지** |
| DR-7 | **단말 간 E2E 암호화 필수** — 전송 경로 무관. 릴레이 서버도 평문 접근 불가. 키 인증(TOFU/지문)은 ADR-0002 |
| DR-8 | **전송 2모드 추상화** — ① 로컬 직접(기본·서버리스) ② 릴레이 경유(선택). **v1은 ①만 출시**하되 인터페이스·신원·주소·세션은 1일차부터 2모드 전제 |
| DR-9 | ⚠️**개정(08-21 · Q-32-13)** — 릴레이 서버 `nexa-beepd`는 **같은 저장소 워크스페이스 멤버 + 배포만 분리**(`beepd-v*`·release-server.yml · 가드레일 W-1~W-4 — 클라 의존 불침투·예산 게이트는 클라만) |
| DR-10~11 | 스택 · 디스커버리/전송 프로토콜 **미정** — ADR-0001/0002 |
| DR-12 | **PolyForm Noncommercial 1.0.0**(nexa-dir2 동일) + **의존성 퍼미시브 온리 — GPL/LGPL 금지** |
| DR-13 | 페이로드 **Text·Image·File**. 수신 파일은 **4단계 게이트**(협상→`.beepq` 격리→검사→승인 실체화). **앱은 수신 파일을 실행하지 않는다**. 복원 후에도 MotW/quarantine 유지 → [docs/04](docs/04-safe-transfer.md) |
| DR-15~17 | **코드 설계 표준**([docs/13](docs/13-code-design-standards.md) — `ActionKind` 단일 통로·인터셉터·계측) · **컨트롤/UX**([docs/14](docs/14-control-ux-architecture.md) — 시각=macOS/동작=OS 네이티브·3단계 이벤트) · **기록 저장 암호화**([docs/17](docs/17-adr-0005-history-at-rest.md) — 블라인드 인덱스·크립토 셰레딩) |
| DR-18 | **PC 1대 = 노드 1개**(기본). 기기 이전 시 기록이 따라가지 않는다(v1 한계·UI에 명시). ⚠️ **개정(DR-20)** — "사용자 개념 없음"은 **기본값**이지 금지가 아니다 |
| DR-19 | **수동 엔드포인트 등록** — 직접 IP/DDNS로 노드 추가(S6 + 공인 IP). **LAN 밖은 위협 모델이 다르다** → 원격 신뢰 등급·SAS 전 파일 차단·인바운드 요청 대기 → [docs/19](docs/19-adr-0006-manual-endpoint.md) |
| DR-20 | **다중 기기 신원(선택 계층)** — `UserId` 1:M `PeerId`. `UserId` 키가 **기기 목록을 서명**하고 상대가 검증(양방향 소유 증명) · 발신은 **그룹 팬아웃 재사용** · TOFU/SAS 대상이 `UserId`로 상승 · **`UserId` 개인키는 주 기기 1대 + 오프라인 복구 시드**(전 기기 복제는 폐기 경쟁에서 회복 불가라 탈락) · **`UserId` 키는 서명 전용**(저장 래핑 키로 쓰지 않는다) · **구현은 v2, v1 제약 4건은 M0에 반영** → [docs/20](docs/20-adr-0007-multi-device-identity.md) |
| DR-14 | **L1~L4 직접 제어** — L1 링크 상태 구독 · L2 이웃 테이블 · L3 인터페이스별 멀티캐스트/브로드캐스트/링크로컬 · L4 UDP+TCP. 발견 폴백 **S1~S6**. **T0(무권한)이 완전한 제품** — L2 원시 소켓은 선택 → [docs/06](docs/06-network-stack.md) |
| DR-21 | **외부 기술은 이음새(포트) 뒤에** — 독자 기술이 아닌 모든 외부 기법은 trait 뒤에 숨겨 교체 가능하게. 외부 크레이트 타입이 포트 시그니처에 나오면 위반 → [docs/13 §2-4 규칙 7](docs/13-code-design-standards.md) |
| DR-22 | **프로필 옵트인 노출** — 본인이 설정·노출 확인한 식별 정보를 발견 상대에게 표시. 단 **브로드캐스트에는 미포함**(평문 수확·사칭) — 세션 경유 + 자동 프리페치. 기본 전부 비노출 → [docs/22](docs/22-adr-0008-profile-disclosure.md) |
| DR-23 | **공유 폴더(pull)** — 공유 허용 목록을 **가상경로**로 묶어 직접 다운로드. 실경로 비노출 · 읽기 전용 · fail-closed 경로 해석 · **pull도 무해화 게이트 동일 통과** → [docs/23](docs/23-adr-0009-shared-folders.md) |
| DR-25 | **메시지 등급·알림·수신확인·수신자 릴레이** — 등급 3종(`Normal`/`Notice`/`Urgent`)은 **발신자의 요청**, 알림 강도는 **수신자 판정**(신뢰 게이트 — **미검증은 소리 없음**) · 수신확인은 **수동 버튼만**("읽음" 없음) · **수신자 릴레이**(내 규칙으로 내 채널에 · 외부에도 **봉투만** · **다중 어댑터 동시 팬아웃**) · Webhook 우선 → [docs/24](docs/24-adr-0010-message-priority-notification.md) 📐 |
| DR-26 | **대화 창 모델** — 대화 상태(세션·스레드)는 뷰와 분리·상대별 유지(뷰 닫아도 대화 유지). 창 모드 옵션 `chat.window_mode`: v1 기본 단일 창 / 별도 창(M3-12) — 설정에서 변경 → [docs/14 §11](docs/14-control-ux-architecture.md) |
| DR-24 | **설정 화면 = VS Code 방식** — `nexa-dir2` `prefs.rs` 설계 차용(코드 미이식): Entry 레지스트리 단일 원천 · 트리+AND 토큰 검색 · 즉시 적용 · Kind 동적 패널 → [docs/14 §10](docs/14-control-ux-architecture.md) |
| DR-27 | **설정 직렬화·영속 표준** — Entry 레지스트리에 저장 태우기 · `SaveScheduler`(quiet OR max_delay) · 미지 키 보존 · 원자적 쓰기 · 종료 flush → [docs/28](docs/28-adr-0011-settings-persistence.md) ✅(D-25 확정·M3-15 구현 08-11 — `crates/nexa-conf` · 부팅 반영 포함 · `timed` 승인은 복원 안 함) |
| DR-28 | **경로 이동·신원 연속성 — 신뢰 2축 분리** — **신원 신뢰는 키에**(이동해도 따라간다) / **경로 등급은 경로에**(통로가 바뀌면 바뀐다) · 정책은 두 축의 곱 · **경로 등급은 광고가 아니라 성립한 세션이 정한다** · 경로 수명(실패 상한·다른 키 성립 시 즉시 무효화 — R-20) · *신원이 바뀌면 시끄럽게, 경로가 바뀌면 조용히* → [docs/19 §5-1](docs/19-adr-0006-manual-endpoint.md) |

### ★ 관통 원리 — "봉투만 본다"

> **어떤 계층도 자기 일에 필요한 만큼만 보고, 내용은 보지 않는다.**
> 릴레이는 목적지만 · 발견 패킷은 존재만 · 격리물은 메타만 · 저장 세그먼트는 헤더만 · 계측은 횟수만 · 로그는 상태만.
> 계층별 대조표 [docs/10 §0](docs/10-decision-record.md). **새 기능 설계의 첫 질문 = "이 계층의 봉투는 무엇인가?"**

### 설계 시 절대 놓치면 안 되는 것

- **신원 = 기기 키 지문. PC 1대 = 노드 1개**(DR-18 기본). **연결은 `PeerId`에, 대화·기록·차단은 `UserId`에** 붙인다(DR-20 V1-1 — v1은 둘이 1:1).
- **병합의 근거는 언제나 암호학적 증거** — 같은 노드의 여러 경로는 병합(FR-D-6), 다른 PC는 별개. **서명된 같은 `UserId`면 접어 표시**(v2). 이름·IP·사용자 주장은 근거가 아니다.
- **`UserId`는 그룹과 다르다** — 그룹은 **로컬 개념**(FR-G-3)이지만 `UserId`는 **상대가 믿어야 하는 주장**이라 서명·검증·폐기(롤백 방지)가 필수([docs/20 §2](docs/20-adr-0007-multi-device-identity.md)).
- **다중 기기는 릴레이로 풀리지 않는다** — 릴레이는 "닿는가"만 푼다. 기여는 오프라인 큐 하나.
- **차단은 기기가 아니라 사람(`UserId`) 단위** — 기기 단위 차단은 상대가 새 기기를 추가하면 곧바로 우회된다.
- **권한 설계의 판단 기준 = "잘못됐을 때 되돌릴 수 있는가"**(Q-1이 이걸로 갈렸다). 예방만 보면 회복 불가 설계를 고르게 된다.
- **암호화는 전송 계층 "위"** 에 둔다 — 릴레이는 봉투만 본다.
- **발견은 다단 폴백 필수** — 기업 무선망 클라이언트 격리·VLAN이 멀티캐스트를 차단한다([06 §4](docs/06-network-stack.md)).
- **T0(무권한)에서 전 기능이 돌아야 한다** — 관리자 권한·드라이버를 요구하는 순간 DR-1·DR-4가 깨진다.
- **수신 파일은 보낸 사람이 누구든 믿지 않는다** — 무해화는 신원과 무관하게 항상 적용([04 T-7](docs/04-safe-transfer.md)).
- **스팸 방어는 1차 범위** — 무인증 즉시 발신 UX의 필연적 공격면(국내 소프트메신저 서비스 종료 실사례).
- **GPL 코드 차용 금지** — 참고 가능한 선행 제품은 MIT(Squiggle)·Apache-2.0(LocalSend)·표준(XEP-0174)뿐.
- **네트워크는 추정 금지, 실측 필수** — 소켓 옵션 의미는 OS마다 다르고, "보냈다"는 "닿았다"가 아니다.

## 3. 작업 규약

- **문서·커밋/푸시 규약 SSOT = [docs/16](docs/16-doc-git-conventions.md)** — 4층 문서 체계·작성 규칙 8·커밋/브랜치/푸시 필수 규칙.
- 개발 규율([docs/15](docs/15-dev-methodology.md)): **수직 슬라이스 · 단위=커밋 1개 · 초안→확장 · main 항상 green · Conventional Commits**.
- **스테이징은 `git add <파일>`로 내가 고친 것만** — `git add -A`·`git add .` 금지([16 §3](docs/16-doc-git-conventions.md)). 이 저장소는 **여러 세션이 동시에 같은 작업 트리**를 만져서, 전부 담으면 남의 미완성 변경이 섞이고 **그쪽 커밋 사유가 유실된다**(08-14 두 번 발생). 커밋 전 `git status --short`로 확인.
- **큰 단위=브랜치, 세부 기능=커밋. push는 사용자 명시 요청 시에만.** 파괴적 작업(삭제·reset·force push·덮어쓰기)은 실행 전 확인. 그 외 일상 작업·상태 md 갱신은 묻지 않고 자동 진행.
- 기록: **한 작업 = 한 트랜잭션 갱신** — 커밋 → [journal/YYYY-MM-DD](docs/journal/) 상세 → [DEVLOG](docs/DEVLOG.md) 한 줄 → **[STATUS](docs/STATUS.md) 한 장(N차)** → [MILESTONES](docs/MILESTONES.md)/[TODO](docs/TODO.md) 상태 → (브랜치면) [BRANCHES](docs/BRANCHES.md).
- 기록에는 **"왜 + 실측값"**. 네트워크 기능은 **추정 금지 · 실측 필수**(도달률·지연·패킷 크기).
- **설계 전 기존 문서·선행 사례부터 확인**(재발명 금지) — [docs/03](docs/03-competitive-landscape.md) · `nexa-dir2` `docs/ctl`(Nexa Controls 14종)·`docs/23`(크로스플랫폼 검토서). 차용 시 출처 경로를 커밋 본문에 명기.
- 문서 번호(`NN-`)는 **불변**. 재번호 금지, 신규는 뒤에 append.
- `.claude/settings.json`(권한)은 **덮어쓰기 금지, 병합만**.
- **모델 방침(사용자 확정 08-08)**: **코드 구현·저수준 개발은 Fable로 진행**한다. `.claude/settings.json`의 `"model": "fable"` 로 세션 기본을 Fable에 고정했다(하네스는 세션 중 자동 전환하지 않는다). 분석·설계·문서 작업은 Opus로 충분하며, 그럴 때만 사용자가 `/model opus` 로 그 세션을 전환한다. **코드 관련 작업 중에는 모델을 바꾸지 않는다.**
- 빌드/테스트 SSOT = [docs/18](docs/18-build-and-test.md) — 절차 변경 시 같은 커밋에서 갱신.

## 4. 새 세션 오리엔테이션

1. 이 CLAUDE.md + [docs/STATUS.md](docs/STATUS.md) → 2. [DEVLOG](docs/DEVLOG.md) 최상단 + 최신 journal → 3. 할 일 = [docs/TODO.md](docs/TODO.md) 순차.

## 5. 다음 단계 (2026-08-29 · STATUS 08-29 10차 기준 — Linux 첫 실기 완주 · v0.2.13)

> **08-29 요약(Linux · Ubuntu GNOME 50 Wayland 첫 실기 — v0.2.10→0.2.12 3연속 릴리스)** — ① 게이트 전량·3신원 상호 발견 ✓ · `relaunch.sh` cargo PATH 폴백 ② ★**.deb 런처가 InMemory 데모를 띄우던 결함**(`Exec=nexa-beep --window` — 08-13 "무인자 = 실물" 확정 때 `packaging/`을 못 따라감 · v0.2.2~0.2.9 Linux 전부) → v0.2.10 ③ ★**Linux 실기 5건**(v0.2.11): `ui.theme` **시스템/다크/라이트 · 기본 시스템**(`nbeep_plat::theme` — Linux 포털 `SettingChanged` 실시간·Win 레지스트리·mac `AppleInterfaceStyle`·창 장식 동기) · 창 **`app_id=nexa-beep`**(Dock 톱니바퀴 해소 · `win_attrs()` 단일 지점) · 트레이 열기 = Wayland 한계(되살리기 불가) · 종료 액터 join **2s 상한**(강제 종료 창 원인) ④ ★**트레이 열기 진짜 포커스**(v0.2.12): GNOME appindicator 확장이 클릭 전 넘기는 **`ProvideXdgActivationToken`** 수신 + `nbeep-plat::wlactivate`(winit wl_display 차용 → `xdg_activation.activate` · 같은 판 의존·lock 신규 0) · 폴백 = 주의 요청 · **[40 §6] 등재**. 교훈 3 = **인자 의미 변경 시 `packaging/` 런처도 호출부** · **게이트와 커밋을 한 셸 체인에 섞지 않는다**(fmt 누락 push 1회) · **`pkill -f "nexa-beep( |$)"`는 감싼 셸도 죽인다**(relaunch.sh 종료 단계 · 스크립트로 감쌀 때 주의). ★**사용자 실기 전부 ✓ + Linux↔mac·Win 통신 OK(2-PC 실증 3-OS 전 조합)** · 추가 검토 = Linux 유휴 RSS 17.5MB(R-8 3-OS 완성) · **🔴 L-1 클립보드 도구 부재**(GNOME 기본에 wl-copy/xclip 없음 → 자체 구현 필요 · TODO §L) · winget PR 2건 OPEN(6회차 제외) · ★**9차 = 자동 실행 인스턴스별 슬롯**(`313344b` — 재부팅 실기: OS 슬롯 1개를 설치본·개발 빌드가 줄다리기 → 등록 삭제·설정 off 도미노 · 3-OS 전부 이름 고정이었음 → exe 경로 해시 `instance_tag` · 옛 이름은 내 경로일 때만 이관 · **교훈 = 사용자당 1개인 OS 자원은 인스턴스 키를 박는다**) · ★**11차 = L-1 클립보드 자체 구현**(`eb7429b` — Wayland 상주 워커(winit wl_display 차용 · ext_data_control→wl_data_device · 두 번째 wl_keyboard 시리얼)+X11 셀렉션(x11rb) · 도구 스폰 폴백 · 의존 신규 0 · **배포판 무관 = 프로토콜 층** · Ctrl+V 실기 잔여). **796 green**. ★**08-30(mac) = L-1 mac 판 `macclip`**(`NSPasteboard` objc2 직접 · 스폰·임시 파일 폐지 · **동시 접근 SIGABRT → 뮤텍스** · PNG/TIFF 실측 ✓ · **3-OS 자체 경로 완결**) + 자동 실행 mac 슬롯 회귀 2종(디렉터리 주입형) + `install-local.sh` mac md5 순서 결함 정정. **799 green**.

> **직전(08-22 3차 기준 — X-2b GUI 배선 완결 · v0.2.3 준비)**

> **08-22 3차(mac) 요약** — ★**X-2b GUI Managed 배선 완결**(접속 정책 공용화 `nbeep_relay::attach`+`is_alive` · `server_tick` 설정 SSOT 수렴(백오프 300s 상한 반복·핀 불일치 = 모달+정지) · 인바운드 accept_via → 기존 Inbound 깔때기(새 정책 0) · 아웃바운드 3단 사다리 + 주소 모달 **64자리 지문 연결**) · ★스모크 발각 2건 수정(**자유 입력형 설정 재시작 증발** — RadioInput 무옵션 = 키 미등록 영속 구멍(08-17 잠복 · "키 등록과 값 저장은 쌍" 세 번째 발병) · `--whoami` full 지문) · ★**로컬 3프로세스 실측** — GUI가 설정만으로 beepd 접속→핀 TOFU→CLI 지문 발신→**홀펀칭 UDP 직결**→GUI 메시지 도달 전 구간 · ★**musl 정적 전환**(사용자 확정 — beepd Linux 자산 = static-pie 651KB · `.cargo/config.toml` linker 한 곳 · beepd-cloud = 바이너리 업로드 기본) · ★**winget/choco 승인 대기 판정 → 스위치 false**(첫 검수 종료 전 새 버전 제출 금지 — M5-4b 🚧). **781 green**.

> **08-22 서버 축 2차(★병합 완료 — `feat/udp-relay-server` 9커밋 → main `4562bff` · push 08-22 + GCP 단발 실측 킷 `tools/beepd-cloud`)** — ★**X-UDP-e 원격 사다리 ✅**(`nbeep_relay::connect_via/accept_via` — 랑데부→**홀펀칭**→실패 시 **릴레이 자동 폴백** · 받는 쪽 = 펀치 병행 + **첫 프레임 링크 추종** · 인증 키 ≠ 지문 = WrongPeer fail-closed · 구조 = **지연 수락**(수락 전 프로브 = 관측 순서 보장 · 펀치 소켓 = 관측 소켓·시도마다 신선 — recv 경합 차단)) · ★**X-2c CLI Managed P0~P2 ✅**(`--identity` 영속(기본 data/identity.key·지문 64hex 출력) · `--chat-live --server`(핀 `server.pin` TOFU — 불일치 = 중단·실패 = LAN 계속 S-2 · 인바운드 랑데부 = 수신 전용 채널 합류) · `--chat-connect-via <지문>`·`/connect <64hex>` → `run_interactive` 무변경) · ★**3프로세스 실물 스모크 완주 = UDP 직결(홀펀칭) 성립 + 메시지·수신확인·프로필 전 스택 무변경**(서버 로그 = 봉투만 · 절차 = [26 §3-7](docs/26-run-and-manual-test.md)) · **777 green**(+3) · X-UDP-d ✅ 완결·X-2c P3(`--enroll`)·X-2b GUI 배선·실 NAT 실기 잔여.
>
> **08-21 UDP+릴레이 서버 축 개통(→ 08-22 main 병합 완료)** — 사용자 지시("별도 실행 파일·별도 배포·UDP 선행") 실행: ★**X-UDP-a 확정 = ⓐ 경량 자작 ARQ**(quinn은 Noise와 TLS 중복·의존 수 MB) · ★**X-UDP-b ✅ `UdpLink`**(`nbeep-net::arq` 순수 상태기계 — SACK+누적 ACK·고정 창 64·RTO 백오프·**동시 열기(펀칭 전제)**·죽은 경로 15s · **결정적 주입 회귀 10종** + 셸 4종(TcpLink 계약 재사용) · send는 자기 프레임 1차 송신까지 구동(창 밖 큐 방치 구멍 실측)) · ★**X-1 ✅ `nexa-beepd` MVP**(신설 bin+lib — 프레즌스(**회전 RID** = SHA-256(키‖epoch_day)[..16]·어제/오늘/내일 3개 = 시계 오차 흡수 · R-18 방지)·랑데부(TCP/UDP 관측 엔드포인트 교환 = STUN-lite)·**blind 릴레이**(★Accept 양방향 성립 전 0바이트 = 증폭 방지·연결당 토큰 버킷 1MiB/s·**저장 0 = 파이프**)·서버 자기 핀(keyfile 재사용·기동 시 키 출력·불일치 = `PinMismatch` 시끄럽게) · **외부 의존 0·0.35MB**) · ★**`nbeep-relay` 공유 크레이트**(서버·클라 같은 와이어 = 어긋남은 컴파일 에러(Q-32-13 §9-1) · `RelayClient` 세션 액터+`RelayLink`(=`Link`) — **종단 Noise가 코드 무변경으로 관통 = DR-21 재증명·S-3 실증**(서버는 종단 암호문만) · remote_ip = 서버 주소 = 경로 등급 fail-closed) · ★**루프백 홀펀칭 배관 ✅**(X-UDP-c 절반 — 관측→교환→같은 소켓 동시 punch→UdpLink 위 종단 Noise · **실 NAT 실기 잔여**) · **배포 2축 분리**(release-server.yml · `beepd-v*` · W-1~W-4) · **결정 2건 확정 처리 = Q-32-15·Q-32-13(DR-9 개정)** · 함정 실측 2(Win accept 논블로킹 상속 = 핸드셰이크 즉사 · 클라 drop 후 액터 잔존 = Shutdown 배선) · **774 green**(+24) · 잔여 = **X-UDP-e 경로 사다리(LAN→펀치→릴레이 자동)** · X-2b/2c GUI·CLI Managed 배선(`net.server.*` 소비·서버 핀 저장) · 실 NAT 홀펀칭 실기(공유기 2대 · 대행 불가).
>
> **08-21 심야 요약(완전성 마감 완주 + 결정 3건 + 경로 2축 1차)** — ★**완전성 마감 4회전 완주**(코드 슬라이스 소진 선언 — ① **AMSI 실물**(`3fb34e0` 수신 게이트 4단계 완성 · 동적 로드 · **실측 EICAR Detected** · 포트 뒤라 게이트 무변경 = DR-21 증명) ② **zip 파서 어댑터**(`24781b5` M4-4 — 중앙 디렉터리만·inflate 부재 = 폭탄 구조 차단·fail-closed 전면) ③ SEAL-2 종결 ④ 동기화 폴더 경고(M2-5b ①) ⑤ **M3-20 헤더 연결 아이콘 배선 ✅** ⑥ ★**M1-9 Win netif 실물**(`101b319` — Windows도 인터페이스별 발신·S3/S4 판정 실물 · 실측 IF 4개) ⑦ **imgdec win32k 락아웃**(실측 성립) · **유령 정정 7건**(D-12·R-5(M4-5ⓒ 권한 강등 = 08-15 완료·사용자 지적)·M4-6·M1-11·M3-19·M3-14·M3-1e ① — 전부 "상세 행 ✅·상위 표만 낡음" 유형 → 처방 = 완료 커밋에 상위 표·CLAUDE.md 동기) · ★**[40 알려진 한계](docs/40-known-limitations.md) 신설**(M5-6 골격 — "무엇을 막지 못하는가" 6축 SSOT)) · ★**결정 3건 확정**(전부 권고안 — **D-24 커트라인 승인+스팸 방어(M5-3) = v1.0 필수** · **D-18 §4~ 전체 Accepted**(ADR-0005) · **D-19 Accepted**(ADR-0006) — ADR 집계 **✅13/📐1**) · ★**크립토 셰레딩**(`b04af4a` D-18 §7 — `keys.seg` 대화별 무작위 데이터 키(신원 키 봉인) · **삭제 = 키 폐기** = 잔존 바이트 난수 · 백업 키 동반·복원 병합 · 레거시 폴백) · **기본 아바타 12간지 무작위**(`0a47c6b` CSPRNG 배정 후 저장) · ★**M5-3b/c 1차 — 경로 2축 실물**(`fea0355` D-19 후속 · DR-28 실물 절반: **`PathClass`**(core — 등급은 **성립 세션의 실소켓 주소**가 정한다·`Link::remote_ip` 판정 전용·fail-closed) · **FR-S-24 ✅** 원격 파일 게이트(`file_allowed` 곱 행렬 단일 지점 — 발신 모달·수신 즉시 거절) · **미등록 원격 인바운드 드롭**(§6 절반 — 요청 대기 UX 잔여) · **P-3 ✅**(R-20 — 다른 키 성립 = 수동 주소 즉시 무효화) · 원격 성립 = 스레드 1줄 고지) · ⚠**날짜 오기 정정**(오후 세션 전체 08-22 오기 → 시계 실측 08-21: 저널 병합·20파일 치환·차수 재부여) · ★**후속(같은 날 저녁)**: **M5-3b 2·3차**(요청 대기 모달 FR-S-25 완결·CLI 축 게이트 — 경로 2축 코드 완결)+**헤더 "인터넷 경유" 배지**+**M4-4 완결**(zip64·격리 내용 목록)+**G4 유령 8호 정정**+**비활성 창 더블클릭 수정**(드레인 키보드 한정)+**Q-32-7 ✅**(kind 8 예약) · ★**v0.2.2 공개 — winget·choco 첫 게시 = 배포 3채널 완성**(M5-4b 스위치 ON · brew 0.2.2 추종 ✓·choco 2종 push ✓ 모더레이션 큐·winget PR #421961/#421967 검수 대기 · 이후 릴리스 = 태그 하나로 전 채널) · ★**다음 축 = UDP+Relay 서버**(사용자 선언 = Q-32-15 방향 확정 · 진입 = 🔴 X-UDP-a 방식 결정) · **750 green** · 문서 = 00~**42**(43종 · 41 운영 가이드 · 42 릴레이 랑데부).
>
> **08-21 후반 요약(v0.2.1 + 안정성 종결 + 계측·제동·차단 설계)** — ★**v0.2.1 공개**(`33a0260`+태그 — 자산 14종 · **brew cask 0.2.1 추종 ✓** · 탑재 = 3종+CLI 축+WNET-1+안정성 2건) · ★**T-flake 정체 규명·종결**(`35a6160` — **클립보드 `OpenClipboard(NULL)` 거짓 성공**(NULL==NULL 대조라 둘째 호출자도 성공) → 한쪽 `EmptyClipboard`가 상대 `GlobalLock` 중 HGLOBAL 해제 = **use-after-free**(nbeep-plat 힙 오염 중도 사망 = "정확히 29개 모자람"의 정체 · 제품 실버그 — 이미지 워커×UI 복사·3-신원 인스턴스 간) · 수정 = **메시지 전용 창(진짜 HWND)로 열기**+프로세스 내 뮤텍스+GlobalSize 상한 · cargo 루프 4/20→0/70 실측 · 부수 = **`dir_writable` PID 프로브 공유 경합**(`ea96094` — CI part 테스트 red의 범인: delete-pending 순간 false 오판 → `data_dir()` 순간 폴백 튐 · 시퀀스 접미 유일화)) · ★**netmon 계측 모드**(`d363473` — 과도 송수신 관측: 카운터 상시(이음새 4곳 · **횟수·바이트만** = 봉투 원리)/기록 옵트인(`netmon.enabled` 기본 off · `netmon-*.log` · 과다 = warn 태그+상태바) + **`NEXA_NETMON` 환경변수 진단 스위치**(전 모드 · NETMON-1 ⓐ) · 설계 [06 §7-1]·절차 [26 §3-5]) · ★**10분 2-인스턴스 자동 실측 = 과다 송수신 없음**(발견 3.7pps·자기 에코 50%(same-PC)·메시지 85B+ack 62B·유휴 세션 앱 프레임 0·64MiB 12초 = 32KiB 청크 그대로 **오버헤드 0.25%**·/quit 후 재연결 0 · **전 구간 warn 0** — 기준선 = 26 §3-5) · ★**공지 제동 2건**(`8da3bce` 사용자 확정 — **발신 3초 1회 제한**(연타 = 상태바 안내·제목 명시)+**받지 않기**(`notify.broadcast_mute` 기본 off · 와이어 공지 표식 = kind 상위 니블 bit2 전방 호환 · 그림자 폐기 · 대기 편입도 표식 유지 · CLI `[공지]` 접두 · 검증 = **[26 §3-6]**)) · ★**[38 수신 제한 4축](docs/38-receive-restriction-design.md) 설계 확정 📐**(M5-3 스팸 방어 1차 — 사용자 4문항: **그림자 차단·IP accept 즉시·자동 임시 차단(10분)·기간부+정확 일치** · 축 층위 = 신원(PeerId/UserId — DR-20 "차단은 사람 단위")/경로(IP — DR-28)/표시(이름) · 판정 = 비용 순 · 저장 = trust.seg 봉인 확장 · **BL-1~5 = 개발은 요청 시**) · ★**[39 네트워크 점검 기록부](docs/39-netmon-records.md) 신설**(살아 있는 기록 — §1 판독법·§2 기준선 SSOT·§3 회차 append · 1회차 = 08-21 실측) · ★**공지 UX 후속 4연**(실기 연쇄 — ① **공지 후 대화창 진입 종결**: 목록 Enter = 캐럿 행 활성화 × 프롬프트 Enter 제출 잔향 → **방어 3겹**(모달 닫힘 시점 가드+포커스 복귀 가드+**활성화 드레인 최종 폐기** — 이벤트 순서 무관) ② **프롬프트 창 = 메인 중앙 배치**(경고 모달 문법) ③ **등급 배지·프롬프트 i18n**(일반/알림/긴급 4개 국어 · 배지 폭 = paint 실측 · 공지 창 제목 "Group"→공지·플레이스홀더) ④ **제목 word-wrap+창 높이 자동**(420 논리 폭 · 실측 단어 접기·`desired_height` 1회 보정) · 실기 확인 ✓ = 3초 제한 안내·중앙 배치 · 검증 = [26 §3-6] 1~6항) · **731 green** · 문서 = 00~**39**(40종).
>
> **08-18~20 요약(대용량 전송 축 완성 + 전송 UI 인챗 통합 + 요청 단위 승인)** — ★**M4-10a~c 전송 재개 ✅**(D-31 확정 08-18 — `.part` 봉인 보존 72h/1GiB · Accept 재개 꼬리 2필드(전방 호환) · 프리픽스 해시 대조(불일치 = 처음부터 fail-closed) · 능동 재-Offer+수신 2택 · **실기 완주**: 재시작→이어받기→격리 완료) · ★**발신 스트리밍 일습**(10GB DnD 무정지 — 파일을 메모리에 안 올림 · **지연 해시**(Done 꼬리 선언 — 승인 즉시) · **송수신 상한 설정**(발신 무제한·수신 512MiB 기본)+**송신 전 상대 상한 확인**(CapAdvert/Request — 읽기 전 차단)) · ★**M4-2e 인챗 전송 제어 ✅**(08-19 mac 마라톤 — 별도 창 제거 · 풍선 안 ⏸▶✕ 양방향 동치 · 액터 parked 슬롯 · 배치 합산 배너+**전체취소 와이어 전파**(양쪽 대칭) · 정지 방치 자동 취소(카운트다운·기본 2분·설정)) · ★**요청 단위 승인·거절 ✅**(08-20 Win — 승인 1회 = 배치 전체(파일 수·이름 목록·총합·**제외됨 행**) · 거절 1회 = 쌍방 전체 Declined · **요청당 최대 파일 수**(`xfer.batch_max` 1~5 기본 5) · 초과 드롭 = **시도 0건+안내 모달**(DnD 묶음 선판정) · 경고 모달 = 진원 창 소유·중앙(메인 부상 z역전 해소)) · ★**그룹 영속 3종**(08-19 — 잠긴 세그먼트 부팅 자가 복구(`.locked` 보관 이동) · **그룹 기록 영속**(`g-{uid}.seg`) · 발신자 라벨 tag 3(전방 호환) · 초대 가시화+Invited 자가 수락) · ★**신원 durable 일습**(08-19 실기 사고 — mac tmp_cleaner가 identity.key 삭제 = 신원 조용히 교체: relaunch = `$HOME/.nexa-beep-multi` · **M5-4e ✅** 제품 `data_dir()` 폴백 종점 = `~/.nexa-beep`(임시 폴더 폐지) · `--whoami` 진단) · **M3-22 상태 로깅 ✅**(bounded 큐·일자 파일·기본 off) · **검증 원복 규약**(`ControlBase.last_value` 전 컨트롤 상속 · [14 §14]) · **I18N-2 ✅**(한국어 16곳 승격) · **T-macxfer ①~⑥ 완료 보고**(08-20) · 서버 설정 영역(기본 Unmanaged·저장만) · 폰트 절대 프리셋(Small 14/Normal 16) · 격리함 워커 스캔(1.5GB 무응답 해소) · ★**M3-25 자동 실행 ✅**(08-20 사용자 확정 — 3-OS 사용자 수준 등록(Win HKCU Run advapi32 직접·mac LaunchAgent·Linux XDG autostart) · **기본 on** 옵트아웃 · `app.autostart` 고급 트레이 위 · 부팅 재동기화(포터블 경로 이동) · Win 실측 ✓ · 잔여 = mac/Linux 실기) · **대화함 풀다운 메뉴 항목 추가**(M3-23 후속) · ★**Windows 콘솔 창 근절 ✅**(08-20 실기 — 콘솔 서브시스템+FreeConsole이 **Windows Terminal에서 무력**(ConPTY 창 = 루트 종료에 묶임) → **windows 서브시스템 전환**+`attach_parent_console`(CLI 출력 보전·리다이렉트 핸들 복원)+콘솔 자식 CREATE_NO_WINDOW(imgdec·open_path) · ★후속 = **대화형 CLI 입력 경합 해소**(pwsh chat-live 키 증발 실기 — 셸이 GUI 앱을 안 기다려 콘솔 입력을 나눠 읽음 → `own_console_for_interactive`: 콘솔 stdin+cmd/pwsh 부모(Toolhelp 판별)면 **자기 콘솔 창 분리** · bash/파이프 no-op · 26 §3-2·§7)) · ★**CLI 단말 축 완결 ✅**(08-20 연쇄 · 실기 전항 확인 — ① GUI→CLI 파일 전멸 수정(지연 해시 Done 꼬리 보정이 GUI에만 — "배선 = 빠뜨린 호출부" 재발) ② **요청 단위 승인+다중 /send**(manifest 태그 13 정식 배선 — 그간 구버전 강등 경로) ③ **수신/읽음 확인**(ChatAck 양방향 — 터미널 = 출력 즉시 읽음) ④ **/peers·/connect <번호|주소>·대화 종료 = 목록 복귀** ⑤ ★**수신 전용 채널 구조**(사용자 확정 "수신은 전 채널·상호 채팅은 명시 /connect 1명" — 인바운드 전부 수락·유지(표시+ack·파일 거절·대화 미진입)로 **GUI 백오프 [무시] 홍수 근본 해소**(거절 = 무한 재시도 유발)) · **CLI Managed 접속 설계 = 32 §13 신설**(RelayLink 공유 · P0 신원 영속 무의존 · 구현 = X-1 서버 후 · TODO X-2c) · ⚠T-flake 등재(전량 테스트 간헐 687+1 · 2회 관측·미재현)) · ★**08-20 후반 3건** — **WNET-1 ✅**(위 M1-8) · **X-3 설계 등재**(그룹 파일 × 컨텐츠 서버 + **보관함 로컬/온라인 통합** — [32 §5-11](docs/32-adr-0013-server-modes.md) · 릴레이까지 pairwise 유지·컨텐츠 = 업로드 1회(P-9 암호문)→방 종속 보관→링크→pull · 개발 = 요청 시) · **docs/25 상태 열 현행화**(🅐 5/6·🅑 6/8 ✅ — D-24 확정 자료) · ★**08-21 클라이언트 공백 3종 ✅**(경쟁 대조 2·3·4순위 — 결정 4건 질문 후 무중단 순차 · **실기 3종 전부 확인**): ① **오프라인 큐 M4-6·C-3**(세션 없는 발신 = **조용한 유실**이던 구멍 발각·해소 — 보관 상한 200·**재시작 유지**(SEAL_PENDING)·성립 합류점 flush·풍선 대기 막대·"내 PC 켜져 있고 상대 나타나면" 한계 명시) ② **클립보드 이미지 3-OS**(Ctrl+V = PNG 파일 전송 — Win CF_DIB 순수 파서+imgdec `--encode-raw`(R-5 유지)·mac AppleScript·Linux wl/xclip · 실물 왕복 비트 동일 실측 · mac/Linux 실기 잔여) ③ **등급+공지 = D-1 잔여+FR-M-6**(입력줄 배지 1회 적용+`/notice`·`/urgent` · 풍선 등급 링 · 미검증 무음 유지·핀/검증 Urgent = 전면 알림 · **공지 = 발견 전체 Notice 팬아웃**+미연결 = ①큐 편입) · 실기 절차 = [26 §3-4](docs/26-run-and-manual-test.md) — **v1.0 커트라인 실질 공백 = 스팸 방어(M5-3)로 압축**(★폴더 전송 C-2 = **사용자 확정 08-21 제외** — 안정성 확보 후 재검토 · TODO M4-2c 08-17 제외의 재확정). **725 green** · 확정 3건 = D-30(시스템 다이얼로그)·D-31(재개)·`log.enabled` off.
>
> **08-17 요약(보안 봉인 + 입력 단일화 + 서버 설계 검토)** — ★**평문 3면 봉인**(사용자 승인 · 전수 조사 보고분): 공용 봉투 **`nbeep_store::sealed`**(ChaCha20-Poly1305 · `NBSE` · **도메인 분리 파생 키** · fail-closed) → 격리물 `.beepq`·PII 사이드카 `profile.sec`·프로필 img 캐시 · 실증 3면 NBSE·평문 0(문서 = [17 §4-a]) · ★**저장 암호화 A(기기 키 파생) 단일 확정**(승격 B=OS키체인·C=암호 둘 다 **실격** — DR-4 포터블·DR-1 제로컨피그 충돌 · 재검토는 Managed Server 모드 · [17 §3-1·§3-2]·DR-17). ★**M3-1b StdAccel 계층**(conventions — 매핑 단일화)·**M3-1e ① EditMenu**(편집 우클릭 메뉴 두 벌 → 한 벌)·**M1-11 세션 계층 회귀**(투명 릴레이 실소켓 캡처 4종)·**M1-8y 완결**(SIGKILL 선제 pop·비TTY)·**R-8 해소**(mac RSS = ps 허수 · footprint 18~20MB). ★**서버 설계 검토**(개발 제외 · TODO만): 클라 **서버 설정 영역**만 개발(기본 Unmanaged) · **홀펀칭 = 가능**(선행 관문 = UDP 세션 전송 = `Link` trait 뒤 UdpLink 형제 추가·위층 무변경 · X-UDP-a~e·🔴 Q-32-15) · [32 §12]. **669 green**.
>
> **08-16 8~10차 요약(Windows 왕복 + 1·2순위 배치)** — **8차(Win)**: 빌드 파손 수정(신설 scan.rs의 nbeep-core가 plat에서 **unix 한정 의존** — "조건부 컴파일은 반대편을 의심한다"의 의존 선언판 · 전 타깃 승격) · **9차(Win)**: 설정 Auto 행에 **실측 속도 표시**(`XferSendDone.peak_bps`·`note_peak` · 노트 채널 = 비용 0) · **10차(mac · 1·2순위 배치 5슬라이스)**: ★**T-relaunch**(빌드 실패 오보 종결 — rc 검사·**고의 오류 주입 검증** · Win PowerShell 분기) · **승인 창 강등 사유 1줄**(자동 중 미왕래 오인 재발 방지) · **격리함 재클릭 = 확대 미리보기**(진입점 ② 완결) · ★**M4-11 ⓐ 완결**(페이싱 중 8MiB마다 512KiB 전속 재관측 — 슬로우스타트 과소 보정 · M4-11 종결) · ★**M3-18 적용/취소 ✅**(보류 의미론 — take_changes = 적용 확정분만: 호스트 팔·push 10곳+ 무변경 · Esc 미저장 2단계 · 계약 회귀 3종). **660 green** · 로깅 M3-22 = 최하위(요청 시 착수 — 사용자 확정). 잔여 실기 = M3-18·강등 사유·재클릭 육안 · Win T-relaunch 1회 통과.
>
> **08-16 5~7차 요약(실기 연쇄 — 평균 속도가 버그를 연달아 발각)** — ★**확대 미리보기**(썸네일 클릭 = Role::ImageView 단일 창 · imgdec 1024px · **디코드 상한 16MiB 상향**(1MiB는 폰 사진에서 침묵 — 방어선 불변)) · ★**격리함 신원 폴더 이관**(전역 temp 공유 발각 — `data/quarantine/` + lazy 1회 이관) · ★**자가 루프+쓰기-쓰기 교착 종결**(PF_ROUTE 무파싱이 kick의 RTM_NEWMADDR를 되읽는 1초 루프 → 프로브 편승 → 양쪽 write 동시 블록·전송 동결 — 수정 3겹: 타입 필터·프로브 쿨다운 10s·**TCP 쓰기 타임아웃 30s**) · ★**M4-11 대역 일습**(0.25MB/s 고착 = 부트스트랩 되먹임 → `advertised_cap`(Auto = 상한 무주장)+첫 2MiB 무페이싱 프로브+`note_burst`(관측 창 대기 없이 1차부터 전속 적응) — 설계 = **06 §5-1**·**30 §3-4b** · 잔여 ⓐ 주기 상향 프로브) · **완료 항목 평균 송수신 속도 표기** · 설계 등록 2건 = **docs/37**(M4-10 전송 재개 📐 🔴 D-31) · **M3-22**(상태바 로깅+로그 보기 — status 깔때기 선행(108곳)·bounded 큐+드롭·`log.enabled` Switch 등 설정 · 개발 후속). **657 green** · ★Windows 실기 예정: 알림 클릭→대화·취소 UX·확대 미리보기·평균 속도·첫 전송 속도.
>
> **08-16 4차 요약(취소 UX 완결)** — ★**수락 후 취소 UX**(M4-2 잔여): 구조 발견 = 발신 청크 펌프가 Accept 처리 안 **블로킹 루프**(펌프 중 취소 명령·상대 Cancel 불가시) → **재개형 펌프**(`ActiveSend` 틱당 4청크 · 시계 `xfer_now_ms` 단일화) → 취소 3경로(내 발신·**상대 Cancel이 발신자를 멈춤**·수신측 inbox 폐기) + `active_send/recv` 추적 + 배너 취소 버튼. 실기 3연: 발신 취소 ✓ · 수신 무반응 = 수락 경로 2곳 중 등록 1곳 → **`send_xfer_decision` 합류점 단일화**(*호출부 배선 = 곧 빠뜨린 호출부*) · 버튼 식별(위험색+눌림+MouseUp 발화). 부수 판정 2건 = 설계 범위(TooLarge 256MiB 자동 거절 · 자동승인 중 미왕래 Ask 강등 — 개선 여지: 승인 창 강등 사유 표시). **656 green**.
>
> **08-16 3차 요약(백로그 일괄)** — 탐색 검토 후 6슬라이스: **docs/37**(M4-10 전송 재개 설계 📐 · 🔴 **D-31**) · **M3-21 완결**(③ 수신 시각) · **M1-2b ⓐ**(활성 세션 능동 생사 촉진 — 기존 Request 1발 재사용) · **FileScanner 포트**(M4 §6 — AMSI는 `platform_scanner()` 하나만 갈면 됨) · **사진 관리 복사**(`data/profiles/custom/` — 원본 불의존) · 미착수 4건(M3-18·취소 UX·확대 미리보기·M3-1b) TODO 계획 박제. 발견 = 연락처 상세 UI 기구현. 실측 = M1-8y 유휴 핸들 증가 0. 판단 = objc2 정렬 보류(실기 unsafe 3곳 재작성 대비 이득 없음). 문서 = 00~**37**(38종).
>
> **08-16 2차 요약** — ★**프로필 사진 256KiB 초과 "조용한 생략" 종결**(실기: 본인 실사진·상대는 옛 내장 아바타 — 포맷 무관·크기가 범인(스티커 PNG 66KB 통과·사진 JPG 574KB 초과 실측)): ① **와이어 축소본**(imgdec `--encode-png` 신설 — 인코더도 본체 미링크 · 워커가 256px PNG `me.wire.png`를 굽고 프레임이 싣는다 · **실측 574KB→102,922B** · 부팅 보장 · 실패 = 상태바 경고 — 조용한 생략 금지) ② **두 필드 동시 송수신**(내장 키 `!image_shared` 조건 제거 — 사진과 키 항상 동반 · 수신은 **사진 우선·키 폴백**(keep 승계 구멍도 수정)) ③ ★**한 번에 push**(후속 실기 "원숭이→실사진 2단계" — `wire_pending` 동안 push 보류 → 완료 이벤트가 Full을 한 번에 · **세대 번호 `wire_gen`**로 사진 교체 경합 차단: 워커는 바이트 복귀·쓰기는 메인 단일 지점). ★**명령 2차 확정** — **첫 글자 `/` = 무조건 비전송**(escape `//`·한 줄 한정·`/` 단독 예외 **전부 폐지** · 판정 = trim 전 원본 첫 글자 하나 · 미지 이름 = fail-closed 안내). **655 green** · 다음 릴리스(v0.1.8 후보) 탑재 대상 = 이 둘 + 알림 클릭→대화.
>
> **08-15 후반~08-16 1차 요약** — ★**v0.1.7 공개**(알림 3-OS(정식 mac UNUserNotificationCenter — brew Cask 실증 ✅ 사용자 확인)·신뢰 배지 개편·SAS 대조·**트레이 완주**·M1 발견 견고화 · 알림 클릭 = 해당 대화(단일=전환·분리=대화 창)) · ★**대화창 명령 `/…` GUI 도입**(판정 단일 지점 `core::command` — 규칙은 08-16 2차로 개정: 첫 글자 `/` = 무조건 비전송 · GUI 4종 `/help`·`/trust`·**`/verify`(SAS 카드 — M3-6 출력 지점)**·`/close`) · M1-14 endpoint 정규화 코어 단일화 · linkwatch(M1-2 계열) 진행.
>
> **08-15 추가 2(트레이 상주 M3-2)** — ★**Windows 트레이 ✅**(`nbeep-plat::tray` — Shell_NotifyIcon 직접(의존 0) · **아이콘 = 내 아바타 32px 합성**(시드 원+그림+보더) · 툴팁/메뉴 헤더 = 표시 이름 · 우클릭 = 이름·열기·**종료**(네이티브 TrackPopupMenu) · 좌클릭 = 복원 · **`ui.close_to_tray`**(고급 · 기본 off) = X 눌러도 숨김·상주 · 플라이아웃 실측 3신원 아이콘 각자 아바타) · ★**Linux SNI 구현 🚧**(사용자 확정 "여기서 구현" — zbus 5(Linux 한정)로 StatusNotifierItem+dbusmenu 서빙 · 워처 부재 fail-soft · **크로스 check/clippy 0 — 실기는 Linux 환경 잔여**) · **mac = M3-2b 잔여**(mac PC 몫 — NSStatusItem·좌우 모두 메뉴·AppKit 메인 스레드 · 스텁 준비됨) · 결정지 M3-2d(스위치 기본값 공통/차등 · 숨김 중 수신 배지).
>
> **08-15 추가(모달 창 규약)** — ★**모달 = 마우스 위치 + 메인 창 소유**(`modal_attrs` — 프로필은 커서 좌상단에 열림 · Windows `with_owner_window`로 모달 8종이 메인에 종속: **모달 클릭 = 그 앱 창 묶음이 함께 부상**(2-앱 z순서 실기 해소) · AlwaysOnTop 금지 표준과 정합 · 대화 창은 종속 금지 명문). **617 green**. ⚠ relaunch 스크립트 종료·창 카운트가 Windows 미동작(TODO 등록).
>
> **08-15 요약(IME 완주 + 상태 표시 규약)** — ★**H-27 해소**(같은 문자 반복 "ㅇㅇㅇ222→ㅇㅇ2ㅇ22"): 원인 3중 — ① 유출 보류 자모가 **다음 키의 같은 자모 preedit**에 `starts_with` 오폐기(→ `SAME_KEY_MS` 40ms — *같은 keypress 판정은 시각이 가른다*) ② 삼켜진 키가 같은 문자 keydown의 정상 도달로 오인 소진 + **배달 증거 링이 진짜 주입까지 차단**(→ 링 폐기·**선배달 장부(owed)** · 불변식 "관측 수 = 도달 수 + 삼켜진 수") ③ 조합 중 stale 틱 주입이 Commit보다 앞에 박힘(→ 조합 중 대기 + leak 선확정). 재생 회귀 **17종** 박제. **[34](docs/34-hangul-input-issues.md) H-1~27 · 교훈 8**로 확장 — *한글 3대 이슈 완주*.
> ★**상태 표시 시각 규약 신설**([14 §12·§13](docs/14-control-ux-architecture.md)) — 목록 표시가 **11px 색 점 1개 + 색만**이라 적록 색각·저대비에서 무너진다(실측) → 그 자리는 아이콘이 아니라 **디스크 파냄**(빈 링/갭 링/찬 원/가로 막대 · Discord 색맹 모드 선례 · 기하는 배지 지름 `D` 비율 고정 · 회전은 캐럿 530ms 틱 재사용 · **자산 증가 0**) = **M3-19**(⏸ 목록 프로필 만지는 세션 작업 후 착수). 큰 자리는 **Lucide 단일 세트**(ISC · 사용자 확정) `plug`/`plug-zap`/`cable`/`unplug` **자산 4종 구워 커밋** = **M3-20**(배선만 잔여). 인증 축 전수에서 ★**화면에 없던 3종 발견**(`Blocked`·**이름 충돌**(v1에서 사칭을 드러내는 유일한 통로)·`FirstContact`) → Lucide `badge-*` 가족(**방패는 이미 격리함이라 못 쓴다**)+경로 축 `house`/`globe`/`waypoints` · *기본 상태는 조용히*(Pinned 흐리게·Local 생략) = **M3-14 설계 완료**. ★**파생본 규약**(`<name>-nb.svg`) — 원본 물음표가 14px에서 동그라미로 뭉개 다시 그렸다(20px 렌더 실측). 굽는 절차는 **[`tools/mkicons.sh`](tools/mkicons.sh)** 로 승격(그동안 journal 한 줄로만 있던 `rsvg-convert` 파이프라인 · **런타임 SVG 파서 0 유지**).
> **08-14 후속** — **M3-21 프로필 변경 전파 ✅**(공유 토글이 연결된 상대에게 안 보이던 구멍 — push 깔때기로 응답 키 전수 재송신 + 카드 열기 pull) · **프로필 캐시 부팅 복원**(재시작해도 아는 상대가 이름·아바타로 뜬다 · 부팅 자동 연결은 **비채택** — 핀 N개 전원 아웃바운드 = 부팅 풀메시) · **목록 아바타 클릭 = 프로필 보기** · **그룹 구성원 모달**(두 진입점 한 모달) · **비연결 상대 카드 = 연결 시도** · **갱신 스크롤 3택** · **캐러셀 스크롤 방향 옵션**.

> **08-14 요약(아바타 일습 + 컨트롤 라이브러리화)** — ★**기본 아바타 완주**(다른 세션 WIP 인계): 12간지 내장 자산(NBAV1 무손실 RLE · **원형 클립 굽기** · 160px/163KB — "필요 수준 축소" 확정) · `AvatarChoice` 단일 문자열 모델(`profile.avatar` — 부팅 기본값 = **키 지문 시드 안정 배정**) · 프로필 **스와치 14개 선택 UI**(사진과 상호 배타) · **아바타 보더**(`profile.avatar_border` — 기본 r/g/b 채널별 시드 유도 · ColorPicker 직접 지정 · **큰 3px/작은 2px** 확정) · **와이어 = 키만**(`Info` 꼬리 확장 bit3/bit4 — 구버전 전방 호환 · 변경 시 능동 재전송) · ★**툴바 프로필 버튼 = 내 얼굴**(우측 끝 `align_right` + `ToolIcon::Avatar`). ★**컨트롤 라이브러리화 R1~R4(기능 불변)**: **`nbeep-ctl` 크레이트 신설**(12번째 — 기반 9모듈+컨트롤 17종 · **앱 도메인 의존 0 불변식** · Interface=`Widget`/`DrawCtx` · Abstract=`Control`+`ControlBase` · 상속=조합+위임 명문화 · 기존 경로 재수출=사용부 무변경) · i18n 라벨 주입 · **문자 경계 측정 O(n²)→O(n)**(`text_prefix_widths` — 비트 동일 계약) · 조건 정리 실측(이미 깔끔). **564 green** · 산출물 1.67MB.
>
> **08-14 후반(재기동 규칙 · 캐러셀 · 모달 표준 · 영속 구멍)** — ★**`tools/relaunch.sh` + `relaunch` 스킬**(종료→재빌드→3신원→**상호 발견 확인**까지 한 번에 · **기본은 신원 보존** — 지운 적이 있어 뒤집었다) · **Carousel 컨트롤(18종째)** + 프로필 아바타 스와치·**최근 이미지 캐러셀**(트랙패드 가로 스크롤·× 삭제·3초 파일명 툴팁) · **ContextMenu 폭 실측 자체 보정**(자당 근사가 글꼴 상향 뒤 메뉴를 잘랐다 — 고칠 곳을 한 곳으로) · ★**앱 모달 표준 재정리**(AlwaysOnTop 제거 = 남의 앱 위에 뜨지 않는다 · `modal_id()` 단일 판정 · 입력 흡수·재부상) · ★**영속 구멍 2겹**(최근 이미지 목록 증발 — 저장이 위젯 변경을 버렸고, 로드는 `HIDDEN_KEYS` 미등록이라 무시했다. **둘 다 고쳐야 살아난다** · 창 위치 기억도 같은 구멍) · **H-25·H-26 등록**(H-26 = `commit` 직후 키다운이 **앱에 아예 미도달** — winit-IME 경계 소비 실측). **573 green**.
>
> **08-14 마감(IME 상태기계 · winit 삼킴 보충)** — ★**M3-1e ② 완수**: `crates/nexa-beep/src/ime_gate.rs` **순수 상태기계**(827줄)로 app.rs의 조합 게이트·보류-판정·유출 조합기·잔향 억제를 전부 흡수(app.rs −519줄) + **트레이스 재생 회귀 테스트**로 실측 순서를 박제 — [34 §4-3](docs/34-hangul-input-issues.md)이 지목한 근본 처방이 닫혔다. ★**G1**(H-26): winit이 삼켜 **앱에 아예 도달하지 않던 1byte**를 `nbeep-plat::keytap`(mac `NSEvent` 탭)으로 보충 주입 — 1차의 시간 창 방식이 순서를 못 지켜(가나다1223 회귀) **raw 스트림 순서 대조 + 배달 증거 링**으로 개정. **586 green**.
> ⚠️ 새 의존 4종(mac 전용 `objc2`·`objc2-app-kit`·`objc2-foundation`·`block2`) — 라이선스 전부 퍼미시브(DR-12 ✅). 다만 **`objc2`가 0.5.2(keytap)와 0.6.4(softbuffer/winit)로 둘 다 들어간다** — 커밋 본문의 "같은 판으로 중복 트리 방지"와 어긋난다. 산출물 1.69MB로 예산(≤10MB)엔 여유가 크지만 정렬 여지는 남는다.
>
> **08-13 심야~08-14 요약(Windows 실기 연쇄 — 보고 즉시 수정·전부 실기 확정)** — TextBox 선택 계열 일습(클릭 연속 판정 · 이동 키 무효화 · **선택 하이라이트 뷰포트 클립**(글자·반전 클립 통일 — 픽셀 포렌식 검증)) · 갤러리 클립보드 배선 · **우클릭 메뉴 z순서(`paint_popup` 6컨테이너) + 키보드 탐색**(↑/↓/Enter · 컨테이너 Esc/Enter `popup_open` 가드) · ★**"나다다" 이중 입력 종결**(`ime_selfcommit` — 포커스 이탈 수동 확정의 늦은 Commit 잔향 · "Enter 후엔 정상"이 단서) · **캐럿 깜빡임 전 컨트롤**(`DrawCtx::caret_on` — 530ms·포커스 창만·입력 리셋) · 프로필 배타 포커스(Choose… 묵은 포커스 = Enter 이중 처리) · **UI 자동 실기 방법 확립**(SendKeys/SendInput+스크린샷·픽셀 판독 — 단 합성 입력은 IME 조합·긴 문자열 불가 실측, 긴 텍스트는 클립보드로) · ★**빌드 조용한 실패 함정**(실행 중 exe 잠금 → 링커 os error 5 → `| tail`이 가림 — **표준 사이클 확립**: 빌드→전체 종료→메인(mtime 확인)→서브 A/B). 설계: **[35 ADR-0014](docs/35-adr-0014-native-file-dialog.md) 📐 신설**(🔴 D-30 파일 다이얼로그) · **M3-18**(프로필 적용/취소 — 확정 방향) · M3-1e 근거 5건+.
>
> **08-13 하루 요약(전반)** — ★**v0.1.5→v0.1.6 공개** → **2-PC 실증 성공**(Win↔Mac IP 수동 등록·메시지·파일 왕복) → 실기 피드백 일습: 비발견 상대 목록 유지 · 인바운드 자동 열림 제거 · 읽지 않음 배지 · `--port` · 텍스트 에디터 · kitty 누수 3중 방어 · TCP keepalive(22초 감지 실측) · 경고 모달 · **ⓑ 자동 재연결 백오프** · **[13 §12-1](docs/13-code-design-standards.md) 자동·반복 동작 점검 규칙** · mux 백프레셔 2MiB 수정.
> **08-13 후반(Windows 실기 주도)** — ★**Windows 목록 한/영 직접 토글**([27 §8](docs/27-typeahead-hangul-composition.md) — IME 없이 앱이 모드 소유 · WIME-3/4 실측 닫음) · **조합 Enter = 확정+전송 동시**(사용자 확정 · Esc 잔향만 차단) · ★**무인자 = 창+실물 기본** + `--help`/`--version`/미지 인자 안내 · **chat-live 대기 중 /quit**(공유 stdin 채널 · 세션 끊김 자동 복귀) · ★**dedup 세션 리셋 + 스레드 대피**(재대화 메시지 증발·끊김 기록 소실 해소 — DR-26) · ★**imgdec 디코드 워커화**(대화창 열림 1~2초 얼음 해소 — 프로세스 스폰+Defender 비용을 워커로). **502 green** · WIME-1·3·4·5·6 닫힘.
>
> **08-13 마감(대량 전송·세션 내성·GUI 진입)** — ★**파일 전송이 2MiB에서 매번 끊기던 Backpressure**(스트림별 폴링 × `MAX_QUEUED` 64 × 32KiB 청크 — M2-3의 "한 스트림 몰림 = 프로토콜 위반" 전제가 M4에서 깨졌다 → **`MuxSession::recv_any`** 로 큐가 생기는 구조 자체를 제거 · CLI·GUI 두 펌프) · **연결 래치 구멍**(넣은 키≠뺀 키 — 주소 재사용으로 상대 신원이 바뀌면 그 행이 영영 안 열림 → `ConnectLatch`) · **`--chat-live`가 떠돌이 연결 하나(`nc -z`)에 죽던 것**(대기 루프 + `AliveGuard`) · ★**GUI 실행 가능 세션 판정**([`nbeep-plat::gui`](crates/nbeep-plat/src/gui.rs) — Windows `WinSta0`(RDP 통과) · macOS `CGSession…` NULL · Linux `$DISPLAY`(`ssh -X` 자동 통과) · 폰트 축 별도 · 맥 SSH 실기 확인 · **Windows 실행 검증 = [TODO WGUI-1~6](docs/TODO.md)**). **534 green**.
>
> **08-13 마감 2(그룹·서버 방향)** — ★**ADR-0012 공유 그룹 채팅 ✅ Accepted + G1~G3 개통**(한 방 대화 · 소유자 roster · 초대 수락제 · `StreamId::Group`) · ★**[32 ADR-0013 서버 모드](docs/32-adr-0013-server-modes.md) 📐**(릴레이·컨텐츠·관리 — *세 모드 = 서버가 **아는 것**의 세 단계* · 불변식 S-0~S-3 · **§5 그룹 결합**에서 **pairwise 팬아웃 × 릴레이 = N배 업링크** 발견 → 모드 ②의 "1회 업로드 + N개 키 봉투"가 답 · 선행 제약 **P-9~P-11**을 그룹 슬라이스에 부착) · **`recv_any`가 새 `Group` 스트림을 안 보던 구멍 수정**(`StreamId::ALL`로 구조화 — 스트림 추가 시 빠뜨릴 수 없게).
>
> **08-13 마감 3(그룹 실기·입력 전수·문서)** — ★**3-신원 그룹 실기 마라톤**(한 PC에서 신원 3개 · IME 트레이스 실측으로 가설 뒤집기 · 그룹 파일 팬아웃 · 텍스트 편집 전수 3차) · **문서 4종 추가**: **31**(ADR-0012 ✅) · **32**(ADR-0013 서버 모드 📐 — 모드② **동기 저장소 확정** · 기록 등급 L0~L2 · **저장소는 한 곳 + 배포 분리 권고**(DR-9 개정 제안)) · **33**(그룹 다자 실기 안내서 — OS별·Docker 빌드 전처리 + 신원 3개 + T-1~8 + bash/pwsh) · **34**(한글 입력 이슈 총람 — H-1~21 · `NEXA_IME_TRACE` · **교훈: 입력도 추정 금지·실측 필수**). **538 green**.
> 🔴 새 확정 대기 — **Q-32-7**(콘텐츠 키 봉투 P-9를 v1.5 그룹 와이어에 지금 넣을 것인가 · G4 진행 중이라 시점이 겹친다) · **Q-32-13**(DR-9 개정 = 서버를 같은 저장소에) · Q-32-4/14(에스크로 허용 여부).
>
> **08-13 심야(3-신원 그룹 실기 마라톤 · STATUS 21차)** — 같은 맥 3인스턴스(포터블 `data/` 분리)로 그룹 실기 · **Suggest 구성원 초대+방별 `member_invite` 정책**(P-11 첫 사용 · 부분 가시성 분기 없음 검토 완결) · ★**그룹 파일 팬아웃**(`offer_file_group` — 미연결 대기 상한 16+자동 연결) + **정책 개정: 상호 미왕래 = 하드 거절 → 수동 승인 강등**(`judge_offer` · 발신 1줄 경고 · 사용자 확정) · ★**IME 경합 실측 확정**(`NEXA_IME_TRACE` 신설 — **첫 키는 keydown으로 유출되고 IME는 둘째 키부터**(`key="ㄴ"`→`preedit="ㅏ"`) · 같은 키 이중 배달(`commit=" "`→`key=Space`) → 유출 조합기 `leak`(전 IME 창)·보류-판정 개정·`ime_commit_echo`) · **TextBox 프리에딧 표시** · **편집 전수 검사 1~3차**(그룹 방·NamePrompt 클립보드 구멍 · 선택 반전 페인트 · **한글 자판 ⌘단축키 물리 키 판정**("ㅁ" 입력 버그) · 우클릭 편집 메뉴 내장 · 조합 중 이동 키 재생 · 목록 ⌘A/⇧방향키/드래그 범위+자동 스크롤 · 팝업 z순서) · **M3-1e 입력 스택 단일화 등록**(TextInput 공용 코어+IME 상태기계+트레이스 회귀 박제) · **M1-8y kitty 누수 재발 재점검 등록**. **538 green** · Windows 크로스 체크 통과 → **다음 = Windows 실기**.

1. 🔴 **사용자 확정이 다음 작업을 잠그고 있다**
   - **D-23 ADR-0010**([24](docs/24-adr-0010-message-priority-notification.md)) — 확정되면 **N-1~N-4 프로토콜 자리를 M2-4b**에 넣는다 → M3-8(알림)·M3-9(수신확인)·M3-10(릴레이)이 연쇄로 풀린다.
   - **D-24 기능 범위**([25](docs/25-feature-scope.md)) — v1.0 커트라인(v1 FR 94 → 약 65건) 승인 여부.
   - ~~D-25 ADR-0011 설정 영속~~ — **✅ 확정·구현 완료(08-11)**: `crates/nexa-conf` + M3-15(설정값 영속·부팅 반영).
   - ~~D-18 §3 기록 암호화 키 관리~~ — **✅ 확정·구현 완료(08-11 · M2-5a)**: 신원 키(`identity.key`)+핀 세그먼트(`trust.seg` — 래핑 키 계층·AEAD·fail-closed) 영속 → **R-17 해소**. 잔여 = ADR-0005 §4~ 전체 확정(M2-5b 전).
   - ~~Q-29-1 표시 이름 기본값~~ — **✅ 확정·구현 완료(08-11 · M1-10)**: 호스트명 정제+지문 폴백 · **R-19 해소**(실측: 방송 이름 `beep-{지문}` — 실명 미노출).
   - **D-12 ADR-0004**([11](docs/11-adr-0004-quarantine.md)) — M4-1 도메인은 구현 완료, ADR 공식 확정만 남음.
   - **D-30 ADR-0014**([35](docs/35-adr-0014-native-file-dialog.md) 📐 08-13 신설) — **파일 선택을 시스템 기본 다이얼로그로**(DR-6 경계 획정 · `FileDialogPort` · 권고: Win/mac 직접 어댑터 + Linux 자체 피커 유지 + P-1~3 이관).
2. **결정 없이 진행 가능한 코드**(열린 P0 없음 — 08-15 기준) — ~~M4-5ⓒ 권한 강등~~(**✅ 08-15 = R-5 해소** — 3-OS lockdown · 08-21 정정) · **M1 잔여**(M1-2 L1 링크 구독 3-OS · M1-8 S4 유니캐스트 · M1-9 링크로컬 · **M1-13 발견 호환성** 설계 등록분) · **M1-11 와이어 평문 자동 회귀**(CI 상시 — SEC-1의 잔여 절반) · **M4 잔여**(검사(§6) 어댑터 — 지금은 `Unavailable` 고정 · ~~수락 후 취소 UX~~ ✅ · 폴더 전송 = 제외 확정 · ~~M4-4~~ ✅ 08-21 zip64·내용 목록까지 소진) · **M3 잔여**(M3-1b — 클립보드·컨텍스트 메뉴 ✅, 잔여는 `StdAccel`·스크롤 관성·창 닫기 관례 · ~~**M3-1e ② IME 상태기계**~~(**✅ 08-14** — `ime_gate.rs` 순수 상태기계 + 재생 회귀 17종) · **M3-1e ①**(TextInput 공용 코어 — 잔여) · **M3-19 연결 배지 파냄**(⏸ 목록 프로필 만지는 세션 작업 후 · 규격 [14 §12](docs/14-control-ux-architecture.md)) · **M3-14 인증 배지**(설계 완료 · [14 §13](docs/14-control-ux-architecture.md)) · **M3-20 큰 자리 아이콘**(자산 ✅ · 배선만) · **M3-18 프로필 적용/취소** ·  **M3-2 트레이 = 🔴 트레이 아이콘 의존 판단 대기**(아이콘 없이 창만 숨기면 앱을 되찾을 수 없다) · **M3-6 SAS 대조 UX — 60자리는 구현됐는데 출력 지점이 없다**) · **M5-1 그룹**(G1~**G4 ✅** — 구성원 편집(추가·제외·비소유자 Suggest)은 **08-15 완료(유령 8호 — 08-21 확인)** · ~~Q-32-7~~ ✅ 08-21 = kind 8 예약 · 잔여 = 2-PC 실기) · **M1-8y kitty 누수 재발 재점검**(08-13 2회차 실기).
3. ★ **이 저장소는 Windows 실기 항목이 열려 있다**(Windows PC에서 작업 시 우선 후보) — ~~Win MotW 표식 어댑터~~(**✅ 08-11 완료**) · **AMSI 검사**(FR-S-15) · **Windows 콘솔 핸들러**(R-16 잔여 — Windows raw 터미널도 같은 슬라이스) · Windows IME 실기 = **WIME-1·3·4·5·6 ✅(08-13)** · 잔여 WIME-2·7~11 · 방화벽 실기 검증.
4. ⏸ **실기 필요(대행 불가)** — D-8b 실망 타이밍 실측(**실기 2대 같은 망**) · SP-1b/d/e · **mac 유휴 RSS 재실측**(85.7MB vs 30MB — R-8 잔여 축. ★ **Windows는 08-12 관측 17.1MB로 예산 안쪽**이라 초과는 mac 축 문제일 가능성이 크다) · M4-3b 실소켓 캡처(W-8) · 2-PC 실기 — ★**08-13 상당 부분 확인**(IP 수동 등록·메시지·파일 왕복·프로필 왕복) · 잔여 = 핀 유지·rename 전파 · 08-13 신규분 육안(읽지 않음 배지·자동 재연결·경고 모달).
5. 🔐 **보안은 상시** — 트리거·트래커 = [TODO §9](docs/TODO.md) SEC-T1~T9 · 기준 = [29](docs/29-wire-security-audit.md) W-1~W-12. **해소: R-5·R-12·R-15·R-17·R-19·R-21** / **최우선 열린 항목 = R-18**(고정 공개키 = 영구 재식별자 · Q-29-2 판단 대기)·R-20(경로 수명)·R-4(스팸).

> ADR 상태: **0001·0002·0003·0004·0007·0008·0009·0010·0011·0012·0014 ✅ Accepted**(0004 = D-12 · 0010 = D-23 확정 08-17 · **0014 = D-30 확정 08-18**(Win/mac 시스템 다이얼로그 + Linux 자체 유지 — 구현 후속) · 릴레이는 v2) / **0005·0006 📐 Proposed — 사용자 확정 대기** / **0013 📐 Proposed(D-29 · 서버 모드 — 방향성만·구현은 v1 이후 별도 저장소)**. **D-31(docs/37 전송 재개) ✅ 확정 08-18**(권고안 그대로 · 그룹 재개만 보류) — M4-10a~c 착수 · **`log.enabled` 기본 off 확정 08-18** · D-24는 별도 세션 검토.
> **설계 문서 = 00~44(45종)** — 최신 = 44(nexa-clip 연계 — 공유 서버·와이어 규약 · 자매 프로젝트 요청 08-26) · 43(모바일 메신저 조사 · 기술 검토 자료 08-25) · 41(beepd 운영 · 3-OS) · 42(릴레이 랑데부 종단 설명서). 종전 기록: 00~37(38종) — 37(전송 동기·재개 📐 D-31 · 08-16) — 색인은 [docs/README](docs/README.md). 08-13에 넷이 추가됐다: **31**(ADR-0012 공유 그룹 채팅 ✅) · **32**(ADR-0013 서버 모드 📐 — 릴레이·컨텐츠·관리 + §5 그룹 결합) · **33**(그룹 다자 실기 안내서 — 빌드 전처리·신원 3개·T-1~8) · **34**(한글 입력 이슈 총람 — H-1~21·`NEXA_IME_TRACE`·교훈 5). 26(실행·수동 테스트)·27(한글 직접 조합)·28(ADR-0011 설정 영속)·29(와이어 보안 점검)·30(종단 동작 설명서)이 08-08~09에 추가됐다 · **35**(ADR-0014 파일 선택 = 시스템 기본 다이얼로그 📐 D-30 · 08-14).
> **예산 게이트 수치 = [05 §2-1](docs/05-requirements.md) NFR-B-1~12** — 유휴 RSS ≤30MB · 산출물 ≤10MB/타깃 · 런타임 의존 0 · 24h 누수 RSS ≤2MB·핸들 증가 0.
> 문서 번호 배정 계획은 [docs/README](docs/README.md) — **번호는 불변**이므로 새 문서는 반드시 그 표를 보고 붙인다.
