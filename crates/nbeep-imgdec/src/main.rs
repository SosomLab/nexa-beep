//! `nbeep-imgdec` — 이미지 격리 디코드 전용 프로세스.
//!
//! 본체가 이 실행 파일을 자식 프로세스로 띄워 신뢰 못 할 이미지를 디코드시킨다(FR-S-12).
//! 권한 강등·픽셀/메모리/시간 상한·재인코딩본만 반환. 크래시해도 본체는 무사(R-5).
//! 프로토콜·격리 실구현은 M4-5. M0에서는 자리만.

fn main() {
    eprintln!(
        "nbeep-imgdec {} — scaffold (격리 디코드는 M4-5)",
        env!("CARGO_PKG_VERSION")
    );
}
